# ศรีวรรณ อะไหล่แอร์ - Active Plan

## Purpose
- ไฟล์นี้เป็น entrypoint สำหรับ AI และคนที่เข้ามาทำงานต่อใน repo
- เก็บเฉพาะภาพรวมระบบ, งานที่กำลังทำ, งานที่ต้องตัดสินใจ, และลิงก์ไป source of truth
- รายละเอียดเก่าทั้งหมดถูก archive ไว้ที่ [docs/archive/PLAN-legacy-2026-05-21.md](/D:/autoparts/docs/archive/PLAN-legacy-2026-05-21.md)

## Summary
- โปรเจกต์นี้คือระบบร้านอะไหล่แอร์รถยนต์ มี public storefront และ admin backoffice
- Tech หลัก: Next.js App Router, TypeScript strict, Prisma, PostgreSQL, NextAuth, Tailwind, Shadcn UI
- ระบบ stock ใช้ `StockCard` เป็น source of truth สำหรับ movement และ moving average cost
- กฎ cross-module สำคัญอยู่ใน [AGENTS.md](/D:/autoparts/AGENTS.md)
- Cash/Bank Lite (Phase 6.6) implement เสร็จและใช้งานจริงแล้ว — เหลือเฉพาะงานติดตามหลัง rollout บางส่วน

## Current Focus
- **Knowledge RAG Quality (2026-07-31)** — Round A–D เสร็จ: ล็อก admin-only ทุกชั้น, เพิ่ม inventory owner/review/expiry/evidence, quality gate duplicate/conflict/stale ก่อนอนุมัติและ publish, เพิ่ม FAQ ที่มีหลักฐาน 3 เรื่อง, backfill production 38 sources สำเร็จ; Round C เพิ่ม offline weighting/chunk experiments, paraphrase/hard-negative และ threshold/latency gate โดย production ผ่าน retrieval `21/21`, admin-only `24/24`, hard-negative `8/8` และยังคง baseline เดิม; Round D เพิ่ม PII-safe operations dashboard แยก LINE/Messenger, admin feedback, gap backlog ที่ต้อง review ก่อนสร้าง draft, failure notification และ rollback runbook; embedding แยกจากสินค้า (`gemini-embedding-2:768` vs `gemini-embedding-001:768`). รายละเอียดอยู่ใน [docs/specs/knowledge-rag-roadmap.md](/D:/autoparts/docs/specs/knowledge-rag-roadmap.md)
- `Phase 6.6` โมดูลบัญชีธนาคาร/เงินสด Lite — **เสร็จและใช้งานจริงแล้ว** (schema 4 model + 5-step permission + nav + posting wire ครบทุกเอกสารเงิน + ledger/transfer/adjustment report). รายละเอียดด้านล่าง Active Workstreams #1
- ติดตาม manual/ongoing work ของ `Phase 7` ด้าน SEO, verification, และ content expansion
- Product Search Quality phase ถัดไป: Review Outcome Tracking, Fitment/Year remediation, Closed-Loop Measurement, และ Guarded Auto-Apply
- **Warranty Manual Mode + Cancel Flow (2026-05-28)** — เพิ่ม 2-mode บน `/admin/warranties/new` (WITH_SALE / NO_SALE) + ฟังก์ชันยกเลิกประกันที่สร้างเอง (เฉพาะ `createdVia = MANUAL`, hard delete, block ถ้ามี active claim) รายละเอียดอยู่ใน [docs/roadmap/active.md](/D:/autoparts/docs/roadmap/active.md)
- **Purchase Invoice OCR (2026-06-16)** — ตัวช่วย AI (Gemini) อ่านรูปใบส่งของซัพพลายเออร์แล้วเติม draft รายการสินค้าในหน้า `/admin/purchases/new` เท่านั้น (จับคู่สินค้าด้วย semantic search — part no. ก่อน แล้ว fallback ข้อความ) เป็น read-only ไม่แตะ `createPurchase`/StockCard/MAVG; สเปก+checklist อยู่ใน [docs/purchase-invoice-ocr-plan.md](/D:/autoparts/docs/purchase-invoice-ocr-plan.md). เหลือทดสอบกับใบจริงเพื่อวัด match rate
- **Product AI Research (2026-06-23)** — ปุ่ม "AI Research" บนฟอร์มสินค้า (`/admin/products/new` + `[id]/edit`) เรียก Gemini (เปิด Google Search grounding) เตรียม draft: ชื่อสินค้า/คำอธิบาย/หมวด/แบรนด์/alias 8 หมวด/รุ่นรถ VERIFIED+POSSIBLE แล้วเปิด modal ให้ admin ตรวจ/แก้ก่อนกรอกลงฟอร์ม (ยังไม่บันทึกสินค้า). Read-only ต่อ business logic — ไม่แตะ รูป/ราคา/สต็อก/หน่วย/lot/ประกัน; permission `products.create`/`products.update`; กรอง source ต้องห้าม (sriwanparts + เว็บต่างประเทศ); ปีรถบังคับ ค.ศ. (auto-แปลง พ.ศ.); key rotation วนได้ถึง 10 key (`maxDuration=300`). สเปก/prompt อยู่ใน [docs/specs/product-research-agent-prompt.md](/D:/autoparts/docs/specs/product-research-agent-prompt.md). เหลือทดสอบกับสินค้าจริงเพื่อวัดคุณภาพ draft + ตัดสินใจว่าจะเก็บ draft history/confidence ลง DB หรือไม่
- **Keyword-First Search + LINE-Grade Submit (2026-06-27)** — autocomplete หน้าบ้านเปลี่ยนเป็น keyword-first (ตาราง `SearchKeyword` + endpoint `/api/search/keywords`, prefix lookup ~40ms แทน trigram 139–475ms ต่อ keystroke) แสดงคำล้วนแบบ Shopee; ตอน submit ใช้ pipeline เดียวกับ LINE OA (`resolveStorefrontSearchIntent`: classify→guard→fitment hard-filter + year-drop retry) คุมให้ผลตรงหมวด/ยี่ห้อ/ปี ไม่กว้าง. cache classify 1 วัน + degrade ปลอดภัยเมื่อไม่มี Gemini. admin search ไม่เปลี่ยน. สเปก: [docs/specs/keyword-first-search.md](/D:/autoparts/docs/specs/keyword-first-search.md)
  - [x] (2026-08-05 / prod log `[search-keyword] background refresh failed` + `Client has encountered a connection error and is not queryable`) ยืนยันจาก Vercel log ว่า error ไปโผล่บน request ที่ไม่เกี่ยว (`GET /admin/products/[id]` RSC prefetch, status 200) เพราะ **rerun ของ coalescer หลุดออกนอก `after()`** — `void runRefreshCoalesced()` ใน `.finally()` เป็น floating promise ที่ waitUntil มองไม่เห็น → Vercel freeze instance ทันทีที่ตอบ response แล้ว rebuild ไปทำงานต่อตอนถูกปลุกด้วย request ถัดไป ซึ่ง socket ใน pool ตายไปแล้ว. แก้ 2 ชั้นใน [lib/search-keyword-index.ts](lib/search-keyword-index.ts): (A) `createSearchKeywordRefreshCoalescer()` วน rebuild ที่ค้างทั้งหมด **ภายใน promise เดียวที่ caller await** (`do…while (rerunPending)`) → ทุกรอบอยู่ในช่วง keep-alive ของ request เดิม; (B) `runSearchKeywordRefreshWithRetry()` retry ทั้ง rebuild **1 ครั้ง** เมื่อเจอ connection-level error (rebuild เป็น idempotent: upsert ทุกแถว + delete-stale ด้วย `runStartedAt` ใหม่) — error จริงเช่น SQL/lock timeout โยนทันทีไม่ replay. เพิ่ม `is not queryable` เข้า `TRANSIENT_DB_ERROR_PATTERN` + export `isTransientDbError()` จาก [lib/db.ts](lib/db.ts). ไม่แตะ business logic / cron / advisory lock. tsc + eslint + build + tests 6 เคส (เพิ่ม 3: coalescer รอ rerun, coalescer กลืน error แล้ว rerun ต่อ, retry เฉพาะ transient) ผ่าน
- **Document Mutation Guard — UI Lock บนหน้าแก้ไข (2026-06-30)** — ต่อยอดจาก transaction mutation guard (server) ให้หน้า `/edit` ของ 5 เอกสาร (sales, purchases, credit-notes, purchase-returns, supplier-advances) เรียก `checkDocumentMutation()` แล้วแสดง `DocumentMutationBlockedNotice` (พร้อมลิงก์เอกสารปลายทาง) + ส่ง prop `submitLocked` เข้าฟอร์มเพื่อ disable ปุ่มบันทึก + กัน submit เมื่อเอกสารถูกดึงไปใช้ที่เอกสาร ACTIVE ปลายทาง. เพิ่ม helper `buildMutationBlockReferenceLinks()` (รวม mapping entityType→route) ใน `lib/document-mutation-guard.ts`. การเช็ก reference-chain เดิมในแต่ละ Server Action ยังคงไว้ครบ (defense-in-depth). tsc + test guard ผ่าน
- **ส่วนลดระดับรายการ — SaleItem list price + line discount (2026-07-02)** — เพิ่ม 2 ฟิลด์ใน `SaleItem`: `unitListPrice` (ราคาตั้งก่อนลด/หน่วย) + `lineDiscount` (ส่วนลดรวมของบรรทัด, บาท) แบบ **additive ล้วน** — `salePrice` ยังเป็นราคาสุทธิต่อหน่วยและเป็น source of truth ของทุกการคำนวณ (กำไร/VAT/ยอด/ใบลดหนี้ไม่แตะ). Invariant: `unitListPrice ≥ salePrice`, `lineDiscount = (unitListPrice − salePrice) × qty` บังคับซ้ำใน Zod `.transform()` ฝั่ง server. UI หน้าขาย (`SaleForm.tsx`) แยก 3 คอลัมน์ "ราคาขาย/หน่วย" (ก่อนลด)/ส่วนลดต่อหน่วย/ราคาสุทธิ คำนวณสองทางอัตโนมัติ + สรุป "ราคาขายรวม" และ "ส่วนลดระดับรายการ" ท้ายบิล (แยกจาก "ส่วนลดท้ายบิล" = `Sale.discount`). Print (`SharedSalesDeliveryPrintDocument` + delivery/LIFF selects) โชว์ราคาตั้งขีดฆ่าเมื่อมีส่วนลด. รายงาน: `salesSummary.lineDiscountTotal` (memo อ้างอิง ไม่เข้าการคำนวณกำไรซ้ำ) + แถบใน `ReportsContent` + แถว CSV. Backfill `unitListPrice = salePrice` ให้ 99 row เดิมแล้ว (`prisma/scripts/backfill-saleitem-listprice.ts`). Audit snapshot เก็บ 2 ฟิลด์เพิ่ม. `db push` + `tsc --noEmit` ผ่าน
- **ซ่อนราคาหน้าบ้าน + ประเภทลูกค้าคุมราคาบน LINE (2026-07-02)** — (1) เว็บ storefront ซ่อนราคาทั้งหมด แสดง "สอบถามราคา" แทน ผ่านธงกลาง `HIDE_STOREFRONT_PRICE` ใน [lib/storefront-pricing.ts](/D:/autoparts/lib/storefront-pricing.ts) (ครอบ ProductCard, หน้ารายละเอียดสินค้า, autocomplete `mode="storefront"`, ตัด price ออกจาก `ProductJsonLd`, ซ่อนฟิลเตอร์ช่วงราคา — logic query คงไว้). (2) เพิ่มโมเดล master ใหม่ `CustomerType { name, showPrice, isActive, sortOrder, isSystem }` + `Customer.customerTypeId` (nullable → ถือเป็นทั่วไป/ซ่อนราคา). Seed 2 ค่าเริ่มต้น: `ลูกค้าทั่วไป` (showPrice=false, isSystem) + `อู่ซ่อมรถ` (showPrice=true) ผ่าน [scripts/seed-customer-types.ts](/D:/autoparts/scripts/seed-customer-types.ts). เมนูใหม่ `/admin/master/customer-types` (ใช้สิทธิ์ `master.*` เดิม + AuditLog `CustomerType` + กันแก้/ปิด `isSystem`) + nav item + ฟอร์มลูกค้าเลือกประเภทด้วย SearchableSelect + badge ในหน้า list/detail. (3) LINE OA gate ราคา: `resolveLineShowPrice(lineUserId)` ใน [lib/line-conversation-repository.ts](/D:/autoparts/lib/line-conversation-repository.ts) (linked + customerType.showPrice → เห็นราคา, ที่เหลือ/unlinked → ซ่อน) + helper `applyLinePriceVisibility()` เซ็ต salePrice=0 (sentinel เดิม → Flex การ์ด + ข้อความ AI fallback เป็น "สอบถามราคา" อัตโนมัติ ไม่ต้องแก้ formatter/prompt) เสียบใน `line-webhook-processor` 2 จุดสร้าง products. รายงานเจ้าของร้าน (daily summary) + เอกสารขาย/ใบเสร็จ + logic ราคา/สต็อก ไม่แตะ. `db push` + `tsc` + เทสต์ (flex 8, processor 53) ผ่าน. แถมแก้บั๊กเดิม `getLatestDate` ใน [lib/storefront-sitemap.ts](/D:/autoparts/lib/storefront-sitemap.ts) (unstable_cache deserialize Date→string ทำ `.getTime()` พังตอน build) ให้ coerce เป็น Date
- **ราคาที่ 2 (ราคาขายปลีก) ตามประเภทลูกค้า (2026-07-09)** — เพิ่มระดับราคาที่สองแบบ Customer Price Group (SAP/Dynamics style). Schema: `Product.retailPrice @db.Decimal(10,2) @default(0)` + enum `PriceTier { WHOLESALE, RETAIL }` + `CustomerType.priceTier @default(RETAIL)`. ตรรกะเลือกราคาในหน้าขาย (`SaleForm.tsx`) ผูกกับ **ประเภทลูกค้าที่เลือก** ผ่าน helper `priceForTier()`: กลุ่ม `WHOLESALE` (เช่น อู่ซ่อมรถ) ใช้ `salePrice` (ราคาขายส่งเดิม), กลุ่ม `RETAIL`/ลูกค้าไม่มีประเภท ใช้ `retailPrice` และ fallback ไป `salePrice` เมื่อ retailPrice=0 (ไม่มีราคาเป็น 0). เปลี่ยนลูกค้ากลางบิลแล้วระดับราคาต่างเดิม + มีสินค้าในบิล → `window.confirm` ถามยืนยันก่อน `repriceItemsToTier()` (ไม่ทับราคาที่แก้มือถ้าไม่ยืนยัน). Admin: ฟอร์มสินค้าเพิ่มช่อง "ราคาขายส่ง/ราคาขายปลีก" (`ProductForm` + Zod + create/update + audit snapshot ผ่าน `include`); หน้า `master/customer-types` เพิ่ม dropdown เลือกระดับราคา + badge + column (`priceTier` ใน schema/parse/create/update/audit). `getTransactionCustomers()` ดึง `customerType.priceTier` มาด้วย; sales new+edit page ดึง `retailPrice` + flatten `priceTier` เข้าฟอร์ม. **Additive ล้วน** — `SaleItem.salePrice` ยัง snapshot ราคาสุทธิเป็น source of truth, ไม่แตะ createSale/updateSale/รายงานกำไร/CN/ใบพิมพ์/Shopee; storefront + LINE คงซ่อนราคาลูกค้าทั่วไปเหมือนเดิม (ยังไม่โชว์ retailPrice หน้าบ้าน). ข้อมูลเก่า retailPrice=0 → ราคาเท่าเดิมทุกบิลจนกว่าจะกรอก. `db push` + `tsc --noEmit` + eslint ผ่าน. **Backfill (2026-07-09, รันแล้ว)** ผ่าน [prisma/scripts/backfill-retail-price.ts](/D:/autoparts/prisma/scripts/backfill-retail-price.ts): +20% หมวด `คอมแอร์ (Compressor)`; +30% หมวด `คอยล์เย็น/คอยล์ร้อน(=แผงแอร์)/หม้อน้ำ/โบเวอร์ พัดลมแอร์/มอเตอร์พัดลมหน้าเครื่อง`; หมวดที่เหลือ retailPrice=salePrice เป๊ะ. ราคาที่บวก% ปัดขึ้นทีละ 50 (`CEIL(ROUND(salePrice×mult,2)/50)*50`), จับเฉพาะหมวดหลัก (ไม่รวมหมวดย่อยที่มีคำเดียวกัน), salePrice≤0 → retailPrice 0. อัปเดต 858 แถว (มี dry-run โหมด default + `--apply` รันจริงในทรานแซกชันเดียว timeout 60s)
- **LINE: ลูกค้าซ่อนราคาถามราคา → ส่งเรื่องแอดมิน (2026-07-02)** — ต่อยอดจากข้อบน. ใน [lib/line-webhook-processor.ts](/D:/autoparts/lib/line-webhook-processor.ts) เมื่อ `showPrice===false` (ลูกค้าทั่วไป/unlinked) และลูกค้าถามราคา (`route.intent===PRICE_NEGOTIATION` หรือข้อความมีคำว่า "ราคา/กี่บาท/เท่าไร/เท่าไหร่" — ตั้งใจไม่รวม PURCHASE_INTENT ล้วนอย่าง "เอาตัวนี้") จะส่งเรื่องให้แอดมิน โดยแยก 2 เคสด้วย **`guardedSearchIntent` (evidence-gated intent ของข้อความเทิร์นนี้ ไม่รวม history)**: (A) ข้อความ**ระบุสินค้าเอง**ในเทิร์นนี้ (มี part/car เช่น "คอยเย็นวีโก้ เท่าไรครับ") + เจอสินค้า → โชว์การ์ด ("สอบถามราคา") + note "ส่งเรื่องให้แอดมิน" แล้ว freeze `WAITING_ADMIN` + แจ้งแอดมิน (ปลด purchase-intent เพื่อไม่ข้ามการ์ด); (B) **ถามราคาล้วนไม่ระบุสินค้า** (เช่น "ราคาเท่าไร" ต่อจากที่โชว์ไปแล้ว) → forcedResponse handoff ตรง (`AI_PRICE_HIDDEN_HANDOFF`) ไม่โชว์การ์ดซ้ำ. อู่ (showPrice=true) เห็นราคาปกติ ไม่ handoff. reuse กลไก handoff เดิม (bell+Telegram). เพิ่มเทสต์ 4 เคส (processor รวม 56 ผ่าน). ปรับ 2 รอบหลังทดสอบจริงจาก log DB: (1) regex เดิมไม่จับ "เท่าไร" → เพิ่ม, (2) เดิมใช้ topicShift แต่ topicShift=false ตอน first-turn/ไม่ได้สลับ part → เปลี่ยนมาใช้ `guardedSearchIntent` แทน
- **ไฟล์แนบหลักฐานในโมดูลค่าใช้จ่าย (2026-07-27)** — เพิ่มการแนบสลิปโอนเงิน/ใบเสร็จเป็นหลักฐานของบันทึกค่าใช้จ่าย แบบ **ไม่บังคับและ additive ล้วน** — ไม่แตะ VAT, cash/bank posting, profit fact, หรือ flow ยกเลิกเอกสาร. Schema: model ใหม่ `ExpenseAttachment { expenseId, url, fileName, contentType, fileSize, uploadedById, createdAt @db.Timestamptz(3) }` (`onDelete: Cascade` จาก Expense, `@@index([expenseId, createdAt])`). Storage: [lib/expense-attachment-storage.ts](/D:/autoparts/lib/expense-attachment-storage.ts) ตรวจชนิดไฟล์จริงจาก magic bytes เสมอ (ไม่เชื่อ MIME จาก client) แล้วรูปภาพผ่าน `sharp` → rotate + **grayscale** + resize 1000px + WebP q60 (pipeline เดียวกับสลิป LINE, ~20–50KB), ส่วน **PDF เก็บต้นฉบับไม่แปลง** (sharp decode PDF ไม่ได้ และ rasterize จะทั้งบวม bundle และลดคุณค่าเชิงหลักฐาน) เก็บลง Vercel Blob public ใต้ `expense-attachments/<expenseId>/` (path content-addressed) + `isOwnedExpenseAttachmentUrl()` gate การลบ. ค่าคงที่แยกไว้ที่ [lib/expense-attachment-constants.ts](/D:/autoparts/lib/expense-attachment-constants.ts) เพื่อไม่ให้ `sharp`/`@vercel/blob` หลุดเข้า client bundle. Server Actions ใหม่ใน [attachment-actions.ts](/D:/autoparts/app/admin/(protected)/expenses/attachment-actions.ts): `uploadExpenseAttachments` / `deleteExpenseAttachment` — ทั้งคู่ใช้สิทธิ์ `expenses.update` (ไม่เพิ่ม permission key ใหม่), block เมื่อเอกสาร `CANCELLED`, และเขียน `AuditLog` action=UPDATE entityType=Expense พร้อม meta `attachmentsAdded`/`attachmentRemoved`. UI: `ExpenseAttachmentPicker` (shared) เสียบในฟอร์มสร้าง/แก้ไข (`createExpense` คืน `expenseId` เพิ่มเพื่ออัปโหลดต่อหลังบันทึก — ถ้าอัปโหลดพลาดยังโชว์ docNo ที่บันทึกสำเร็จเสมอ) + `ExpenseAttachmentsPanel` บนหน้า `/admin/expenses/[id]` (thumbnail ผ่าน `next/image`, PDF เป็นไอคอน, เพิ่ม/ลบได้, light+dark). **ข้อจำกัด: 3MB/ไฟล์ สูงสุด 5 ไฟล์/เอกสาร** — เพดาน 3MB มาจาก `serverActions.bodySizeLimit` ใน [next.config.ts](/D:/autoparts/next.config.ts) และเพดาน request body 4.5MB ของ Vercel; ถ้าต้องรับไฟล์ใหญ่กว่านี้ต้องย้ายไป Vercel Blob client upload ซึ่งจะทำให้ server ไม่เห็นไบต์ก่อนเก็บ (grayscale ต้องย้ายไปฝั่ง client). `db push` + `npm run build` ผ่าน
- **พิมพ์ต้นฉบับ / ต้นฉบับ+สำเนา บนเอกสารพิมพ์ฝั่งแอดมิน (2026-08-16)** — เพิ่ม segmented toggle "ต้นฉบับ | ต้นฉบับ+สำเนา" ข้างปุ่มพิมพ์ใน 3 หน้า: `/admin/sales/[id]` (ทั้งขายสด+ขายเชื่อ), `/admin/delivery/print`, `/admin/receipts/[id]` โดยมีผลกับ**ลายน้ำ**บนแบบฟอร์ม. **Presentation ล้วน — ไม่มี Server Action ใหม่, ไม่มี query เพิ่ม, ไม่มี permission key ใหม่, ไม่ต้องเขียน AuditLog** และค่า default คือต้นฉบับอย่างเดียวจึงไม่เปลี่ยนพฤติกรรมพิมพ์เดิม.
  - [x] **แยกลายน้ำออกจากระบบ verify** — ย้ายบล็อกลายน้ำจาก [PrintDocumentVerifyMark.tsx](/D:/autoparts/app/admin/_components/print/PrintDocumentVerifyMark.tsx) ออกเป็น primitive ใหม่ [PrintDocumentCopyWatermark.tsx](/D:/autoparts/app/admin/_components/print/PrintDocumentCopyWatermark.tsx). เดิมลายน้ำผูกกับ `verify != null` ทำให้**หายทั้งใบเงียบๆ ถ้า `DOC_VERIFY_SECRET` ไม่ถูกตั้ง**; ตอนนี้ลายน้ำแสดงเสมอ ส่วน QR ตรวจสอบเอกสารยังผูกกับ env เหมือนเดิม. ถอด field `variant` ออกจาก `PrintDocumentVerifyBadge` + `buildPrintDocumentVerifyBadge()` ใน [lib/verify-token.ts](/D:/autoparts/lib/verify-token.ts) เพราะหมดหน้าที่แล้ว
  - [x] เพิ่ม prop `copyLabel` ใน shared print document **ทั้ง 2 ตัว** (`SharedSalesDeliveryPrintDocument`, `SharedReceiptSettlementPrintDocument`) + ค่าคงที่กลาง `PRINT_COPY_LABEL_ORIGINAL` / `_DUPLICATE` / `_LIFF` และ `PRINT_COPY_VISIBILITY_CSS` ใน [print/shared.ts](/D:/autoparts/app/admin/_components/print/shared.ts) — อัปเดต consumer ครบ 9 จุดรอบเดียวตาม [AGENTS.md](/D:/autoparts/AGENTS.md); LIFF 4 หน้าส่ง `"สำเนา (LIFF)"` เพื่อคงข้อความเดิม
  - [x] **รื้อ print CSS ให้รองรับหลายใบ** — `#receipt` ใน sales/receipts เปลี่ยนจาก "ตัวเอกสาร" เป็น "wrapper ที่ครอบต้นฉบับ+สำเนา" (ยัง `position:absolute` เหมือนเดิม) แล้วย้าย `min-height:100vh` + flex column ลงไปที่ `.print-slip` แต่ละใบ. ใบสำเนา render ไว้ล่วงหน้าฝั่ง server (ใช้ QR object เดิม ไม่ gen ใหม่) แต่ซ่อนบนจอเสมอ → preview เห็นใบเดียว ไม่เพิ่ม CLS และไม่ต้อง navigate/query ซ้ำ
  - [x] `PrintCopyModeToggle` (client) เขียน `data-print-copies` ที่ `<html>` แล้ว print stylesheet เป็นคนตัดสินใจ — ถ้า attribute ไม่ถูกตั้งเลย (กด Ctrl+P ก่อน hydrate) จะ fallback เป็นต้นฉบับอย่างเดียว. ทำ light + dark
  - [x] เลี่ยงหน้าว่างท้ายเอกสาร: ใช้ `break-before: page` บนใบสำเนาแทน `page-break-after: always`. ที่ `/admin/delivery/print` เปลี่ยน `.slip:last-child { page-break-after: avoid }` → `.slip:not(.print-slip-lead) { break-before: page }` เพราะใบสำเนาที่ถูก `display:none` ยังนับเป็น `:last-child` อยู่ ทำให้กติกาเดิมพัง. ลำดับหน้าเป็นแบบสลับคู่ต่อบิล (A-ต้นฉบับ, A-สำเนา, B-ต้นฉบับ, B-สำเนา)
  - [x] **รอบ 2 — toggle อยู่คู่กับปุ่มพิมพ์ทุกจุด**: ปุ่มพิมพ์ที่เหลือ**ทุกตัวเป็นการนำทาง**ไปหน้าพิมพ์ด้วย `?print=1` ทำให้ `data-print-copies` บน `<html>` ของหน้าเดิมหายตอนเปลี่ยนหน้า — ใส่ toggle เฉยๆ จะกดแล้วไม่มีผล. แก้โดยส่งค่าผ่าน query `copies=2` ([print-copies.ts](/D:/autoparts/app/admin/_components/print/print-copies.ts)) แล้วปลายทางอ่านกลับ 2 ชั้น: `AutoPrint` ตั้งค่าก่อนสั่งพิมพ์ + `PrintCopyModeToggle` sync ปุ่มให้ตรง (ลิงก์จึงทำงานถูกแม้ปลายทางไม่มี toggle)
  - [x] แยก component เป็น 3 ชั้น: `PrintCopyModeSwitch` (สวิตช์ controlled ล้วน ไม่มี side effect) → `PrintCopyModeToggle` (หน้าพิมพ์ เขียน `data-print-copies`, ครอบ Suspense ในตัวเองเพราะใช้ `useSearchParams`) + `PrintCopyModeLink` (หน้าที่ต้องนำทาง ต่อ `copies` เข้า href) — ปุ่มพิมพ์เดิมคง className เดิมทุกตัว หน้าตาไม่เปลี่ยน
  - [x] เพิ่ม toggle 6 จุด: SaleForm (แบนเนอร์บันทึกสำเร็จ + ท้ายฟอร์มตอนแก้ไข), ReceiptForm (แบนเนอร์บันทึกสำเร็จ + ท้ายฟอร์มตอนแก้ไข), `/admin/delivery` (พิมพ์ทั้งหมด), `/admin/warranty-claims/[id]` (พิมพ์)
  - [x] **ขยายไปใบเคลมสินค้า** — `WarrantyClaimPrintDocument` รับ `copyLabel` + หน้า `/admin/warranty-claims/[id]/print` render ต้นฉบับ+สำเนา + toggle (ถ้าใส่ toggle โดยเอกสารไม่รองรับสำเนา กดแล้วจะไม่เกิดอะไรขึ้น)
  - [x] **ตัดสินใจไม่ใส่ toggle** ที่ `PrintFromListButton` (ปุ่ม "พิมพ์" เล็กต่อแถวใน 4 หน้ารายการ: sales, receipts, delivery, warranty-claims) — จะทำให้คอลัมน์ action รกทุกหน้ารายการ. ปุ่มนี้พิมพ์ผ่าน hidden iframe และยังได้ต้นฉบับอย่างเดียวตาม default ถูกต้อง (ถ้า hydrate ไม่ทันก่อน `print()` attribute จะไม่ถูกตั้ง ซึ่ง CSS `html:not([data-print-copies="2"])` ก็ fallback เป็นต้นฉบับอยู่แล้ว)
  - [x] `tsc --noEmit` + `eslint` (0 error) + `npm run build` + `npm test` (802 pass / 2 skip) ผ่านทั้ง 2 รอบ
  - [ ] เหลือทดสอบพิมพ์จริงกับเครื่องพิมพ์: ยืนยันโหมดต้นฉบับได้ 1 หน้าเป๊ะไม่มีหน้าว่างท้าย, `/admin/delivery/print` จำนวนหน้าโหมดต้นฉบับเท่าเดิมกับก่อนแก้ และปุ่มพิมพ์จากฟอร์ม/คิวจัดส่งส่ง `copies=2` ไปถึงปลายทางจริง
- รักษาเอกสารให้ AI อ่านง่าย: ใช้ไฟล์นี้เป็น index และย้ายรายละเอียดลงเอกสารเฉพาะเรื่อง

## Current Priorities
- [x] `Phase 6.6` Cash/Bank Lite — implement เสร็จและใช้งานจริงแล้ว (schema/posting/transfer/adjustment/report ครบ)
- [ ] ทยอยย้าย decision สำคัญจาก archive ไป `docs/decisions/`
- [ ] ทยอยย้าย detailed module specs จาก archive ไป `docs/specs/`

## Active Workstreams
### 1. Cash/Bank Lite
- สถานะ: **เสร็จและใช้งานจริงแล้ว** (Phase 6.6)
- เอกสาร spec: [docs/specs/cash-bank-lite.md](/D:/autoparts/docs/specs/cash-bank-lite.md)
- สิ่งที่ทำเสร็จ:
  - schema: `CashBankAccount` / `CashBankMovement` / `CashBankTransfer` / `CashBankAdjustment` (+opening balance/date, primary transfer account, low-balance threshold)
  - posting: `lib/cash-bank.ts` (`replaceCashBankSourceMovements` / `clearCashBankSourceMovements` / `recalculateCashBankAccount`) wire เข้า sales, receipts, purchases, purchase-returns, credit-notes, expenses, supplier-payments, supplier-advances, delivery-commissions, และ Shopee create-sale/escrow
  - UI: `/admin/cash-bank` (account manager) + `/ledger` + `/transfers` + `/adjustments` ครบ loading.tsx
  - permission ครบ 5-step (`cash_bank.*` ใน access-control + route rules + requirePermission ใน page/action + nav `admin-navigation.ts`)
  - report: `lib/cash-bank-report-queries.ts` → `/admin/reports/cash-bank-ledger` / `cash-bank-transfers` / `cash-bank-adjustments` + Excel/CSV export
- Out of scope (ตาม spec, ยังไม่ทำตามตั้งใจ): full bank reconcile, bank statement import, payment run/clearing, slip attachment

### 1.1 Multi-Channel Payment (Split Payment)
- สถานะ: **Phase 1 นำร่อง Receipt เสร็จ** — ขยายเอกสารอื่นต่อ
- เอกสาร spec: [docs/specs/multi-channel-payment.md](/D:/autoparts/docs/specs/multi-channel-payment.md)
- สิ่งที่ทำเสร็จ:
  - schema: `DocumentPayment` (polymorphic docType+docId, no FK) + enum `DocumentPaymentDocType`
  - helper กลาง `lib/document-payments.ts` (parse/validate/replace/clear/toCashBankEntries) — reuse ได้ทุกเอกสาร
  - Receipt: form split rows (บัญชี+จำนวนเงิน+เพิ่ม/ลบแถว), actions create/update/cancel, detail breakdown, audit snapshot มี payments[]
  - shared UI `components/shared/PaymentChannelsInput.tsx` (reuse ทุกเอกสาร)
  - back-compat: `<doc>.cashBankAccountId` = บัญชีแถวแรก (primary) — ข้อมูลเก่า/print/report เดิมไม่พัง; flow programmatic (Shopee/LINE) ยัง post movement บัญชีเดียวโดยไม่สร้าง DocumentPayment (detail fallback รองรับ)
- [x] Phase 2: **Expense** (target=netAmount), **Purchase** (เฉพาะ CASH_PURCHASE, draft เก็บ payments), **Sale** (เฉพาะ CASH_SALE, draft เก็บ payments, `resolveSalePaymentMethodFromAccounts`) — form/actions/detail/edit ครบทุกตัว
- [x] Phase 3: **SupplierPayment** (target=totalCashPaid), **SupplierAdvance** (target=totalAmount), **CreditNote** (เฉพาะ CASH_REFUND, OUT), **PurchaseReturn** (เฉพาะ CASH_REFUND, IN) — form/actions/detail/edit + audit payments[] ครบทุกตัว
- [x] Phase 4 (print ใบเสร็จ): แสดง "รายละเอียดการรับชำระ" ทุกช่องทาง + ยอดรวม ในกล่อง "ชำระโดย" ครบทุกฟอร์ม (admin detail + LIFF + liff-print ผ่าน prop `payments`)
- [x] Phase 4b (print Sale): `SharedSalesDeliveryPrintDocument` แสดง "รายละเอียดการรับชำระ" หลายช่องทางแล้ว — ครบทั้ง 6 caller (admin sale detail, delivery batch print, LIFF/liff-print receipt+invoice)
- [x] Phase 5: report-queries account filter เป็น **multi-channel-aware** — helper `resolveAccountDocIds` + `accountWhereFilter` (OR ระหว่าง `cashBankAccountId` primary กับ docId ที่มี DocumentPayment บนบัญชีนั้น). Additive-only → ผลลัพธ์ข้อมูลเดิม (single-channel/legacy) ไม่เปลี่ยน; Cash/Bank Ledger (`cash-bank-report-queries.ts`) อ่าน `CashBankMovement` อยู่แล้วจึงถูกต้อง per-account เต็มรูปแบบ
- **Multi-channel payment ครบทุก phase** ✅ (10 เอกสาร: Receipt, Sale, Purchase, Expense, CreditNote, PurchaseReturn, SupplierAdvance, SupplierPayment + print + reports)

### 2. SEO / AEO / AIO Follow-up
- สถานะ: baseline หลักเสร็จแล้ว แต่ยังมี external/manual work ต่อเนื่อง
- เอกสารอ้างอิง:
  - [docs/seo/phase-7-final-status-2026-04-03.md](/D:/autoparts/docs/seo/phase-7-final-status-2026-04-03.md)
  - [docs/seo/phase-7-follow-up-2026-04-03.md](/D:/autoparts/docs/seo/phase-7-follow-up-2026-04-03.md)
  - [docs/seo/phase-7-external-verification-2026-04-03.md](/D:/autoparts/docs/seo/phase-7-external-verification-2026-04-03.md)

### 3. Product Search Quality Follow-up
- สถานะ: Phase E + F1 + F2 เสร็จแล้ว, phase ถัดไปถูกเพิ่มใน roadmap
- เอกสาร active: [docs/specs/product-search-log-analysis.md](/D:/autoparts/docs/specs/product-search-log-analysis.md)
- สิ่งที่ต้องทำต่อ:
  - [x] Phase F3 - Review Outcome Tracking
  - [x] Phase F4 - Fitment/Year Remediation Flow
  - [x] Phase F5 - Closed-Loop Measurement
  - [x] Phase F6 - Guarded Auto-Apply
  - [x] Phase F7 - Search-quality hardening (2026-06-03, จาก production diagnostic)
    - [x] แก้ year-fragment bug: เลขชิ้นส่วน 4 หลัก (เช่น `1950`, `446610-1950`) ไม่ถูกตีความเป็นปีรถอีกต่อไป — `extractYearFromQuery` รับเฉพาะ token ปีเดี่ยว และ year-only query ใช้ UNION (text/code/oem ∪ fitment-year) แทน strict-only (`lib/product-search.ts`)
    - [x] กรอง bot/keyboard-mash/foreign-spam ออกจาก telemetry + จำแนกเป็น `review-noise` (`lib/search-noise.ts` wired เข้า `product-search-telemetry.ts` + `product-search-log-analysis.ts`)
    - [x] เติม synonym EN↔TH รุ่นรถยอดนิยมเชิงรุก 51 cluster (idempotent + audited): `prisma/scripts/seed-model-synonyms.ts` — SearchSynonym 134→143 terms
    - [x] (Phase F / concept-synonym) เติม synonym กลุ่มเคมีภัณฑ์/หมวดที่ขาด 4 cluster — น้ำยาล้างคอยล์ (P0482), น้ำยาล้างระบบแอร์ F-11 (P0458, แยกกลุ่ม), ใบพัดลม, คอนโทรลวาล์วคอมแอร์ — ทุกคำ grounded จาก ProductAlias จริง (idempotent + audited): `prisma/scripts/seed-concept-synonyms.ts` — SearchSynonym 143→147 terms
    - [x] (Brand-synonym) เติม synonym ชื่อแบรนด์อะไหล่ที่เพิ่งเพิ่มใน production 6 cluster — Aeroflex/AEROTAPE, Fujikoki, COCO, ATC, K.AIR, Mazda Genuine Parts (ตัวสะกด/ทับศัพท์/เว้นวรรคของชื่อแบรนด์เท่านั้น ไม่ใส่คำหมวดสินค้าทั่วไป, idempotent + audited): `prisma/scripts/seed-brand-synonyms.ts` — SearchSynonym 200→206 terms
    - [x] (LINE skip rule) แก้ต้นเหตุจริงของอาการ "LINE ตอบคอยล์ทั้งที่ถามน้ำยาล้าง": `matchPartTypeToCategoryHint` ใช้ `.includes()` substring → ชื่อเคมีภัณฑ์/อุปกรณ์ (น้ำยาล้าง, ฟองน้ำ, น็อต, ฝาปิดวาล์ว ฯลฯ) ถูก map เข้าหมวดอะไหล่ผิดเป็น **hard category filter** (พบ 17/45 ตัวในหมวด "อะไหล่อื่นๆ" เสี่ยง). เพิ่ม `isAccessoryOrChemicalIntent()` + skip rule ใน `lib/line-fitment-resolve.ts` (เช็คทั้ง partType และ query text เต็ม ผ่าน `queryText` ที่ wire จาก `line-webhook-processor.ts`) → ข้าม category filter เฉพาะกลุ่มนี้ คง brand/model ไว้. ยืนยันจาก production `LineAiAuditLog` ว่า AI ไม่ได้ตัดคำ — `consolidatedQuery` ครบ แต่ `categoryName="คอยล์เย็น (Evaporator)"` คือตัวตัดทิ้ง
    - [x] (A-light / Thai segmentation) เพิ่ม `lib/thai-segment.ts` (`segmentThaiQueryTokens` ใช้ ICU `Intl.Segmenter`) ตัดคำไทยที่เขียนติดกัน → ป้อนเข้า required-token (LIKE-contains AND) ผ่าน `runStorefrontProductSearchWithRequiredTokenFallback` เฉพาะเมื่อตัดได้ ≥2 คำ (compound จริง) + อาศัย strict→fallback เดิมกัน regression. ไม่แตะ DB/index/trigger. ฝั่งเว็บได้ AND-precision วลีไทย; **note:** data issue 3 ตัว "น้ำมันคอมแอร์ EMKARATE/SUNOCO" ไฟล์ผิดหมวด (อยู่ "อะไหล่อื่นๆ" ควรอยู่ Compressor Oil) — ยังไม่แตะ รอยืนยัน
    - [ ] (พิจารณาภายหลัง) A-full: ตัดคำฝั่ง index (search_document) เพื่อ FTS เต็มรูป — งานใหญ่ ต้องย้าย doc-building มา app layer + re-index ทั้งหมด; ทำเฉพาะถ้า telemetry ชี้ว่า ranking ยังอ่อนหลัง A-light
    - [x] (LINE year-range shorthand) แก้ต้นเหตุ "LINE ตอบไม่พบ คอยเย็น Avanza 12-15" ทั้งที่มีสินค้าจริง: เลขช่วงปีย่อ `12-15`/`2012-2015` ถูกดันเป็น **required-token (hard anchor)** ใน `extractProductSearchRequiredTokens` (มีตัวเลข ≥3 ตัว ไม่ใช่ปี 4 หลัก) → ไม่มีชื่อสินค้าตัวไหนมีสตริง `12-15` → ค้นเจอ 0 เสมอ + ไม่เคยถูกแปลงเป็น `fitmentYear`. ยืนยันจาก production `LineAiAuditLog` (conv `cmqexstoe…`): เทิร์นที่พลาด `requiredTokens:["12-15"], fitmentYear:null, total:0`; เทิร์นถัดมาที่ระบบ consolidate เป็น `2012-2015`+`fitmentYear:2012` เจอสินค้า 1 รายการ (พิสูจน์ว่าสินค้ามีจริง). **A**: `lib/car-year-shorthand.ts` (`isCarYearRangeToken`) + skip ใน `product-search-required-tokens.ts` → range ไม่เป็น anchor. **B**: `parseCarYearRangeStart()` ดึงปีเริ่มต้นจากข้อความลูกค้าตรงๆ เป็น fallback ของ `fitmentYear` ใน `line-webhook-processor.ts` (grounded ในข้อความลูกค้า จึงผ่าน year guard). เพิ่ม unit test `car-year-shorthand.test.ts` + regression ใน `product-search-required-tokens.test.ts` (STB-2116 ยังเป็น anchor). ไม่แตะ DB/index/business logic
    - [x] (Option A / correlated model+year) แก้ต้นเหตุ "LINE ตอบวาล์ว/คอยล์ Vios 2013 ปนของผิดรุ่นปี (Vios 07-12 / Vigo / Fortuner) + ตัวที่ตรงเป๊ะ (P0368) กลับหลุด top-5": เดิม `carModelClause`/`carBrandClause` กับ `yearFilterClause`+`yearBoostExpr` เป็น `EXISTS` แยกกัน ไม่ผูก fitment row เดียวกัน → สินค้าที่มีแถว "Vios 07-12" **และ** แถว "Vigo 06-13" ผ่านทั้งตัวกรองรุ่น (Vios) และปี (2013 via Vigo) ทั้งที่ไม่มี fitment Vios-2013 จริง; และ +700 yearBoost แจกให้ทุกตัวที่มีแถวใดคลุมปี → ของถูก/ของผิดคะแนนเท่ากัน แล้วแพ้ tiebreak. ยืนยันจาก production `LineAiAuditLog` (conv `cmra797i1…`): valve turn `categoryName="วาล์ว (Expansion Valve)", carModelName=Vios, fitmentYear=2013, total=9` แต่ 5 ตัวที่โชว์มีของผิดรุ่นปี 3 ตัว (P0092/P0358/P0371) และตกของถูก 4 ตัว. **แก้**: correlate รุ่น/แบรนด์ (by name) + ปี ให้อยู่ fitment row เดียวกัน — ทั้ง V2 raw SQL path (`buildVehicleYearExists()` ใช้ทั้ง `yearFilterClause` และ `yearBoostExpr`) และ Prisma fallback path (`buildCorrelatedVehicleYearSome()` + `mergeAndCondition()`) ใน `lib/product-search.ts`. ขอบเขต: มีผลเฉพาะเทิร์นที่ระบุ **รุ่น หรือ แบรนด์ (ชื่อ) + ปี** พร้อมกัน (`hasNameVehicleScope`); year-only / ไม่มีรุ่น-แบรนด์ / id-based UI scope (`carModelId`/`carBrandId`) + range filter (`yearMin`/`yearMax`) คงพฤติกรรมเดิม. ยืนยันกับข้อมูลจริง: หลังแก้ correlated filter คืน 6 ตัวที่ fit Vios-2013 จริง (รวม P0368) ตัด P0092/P0358/P0371 ออกครบ. ไม่แตะ DB/index/schema
    - [x] (Generation marker / required-token) แก้ความเสี่ยงแฝง: คำบอกเจเนอเรชันรถ ("gen3", "เจน3", "จีเอ็น8") เข้าเกณฑ์ `extractProductSearchRequiredTokens` (ยาว ≥3 + มีเลข) → ถ้า AI consolidation ล่ม (Gemini off/error) แล้ว raw text "Vios gen3…" ถูกส่งเข้า search ตรงๆ, "gen3" จะกลายเป็น **hard required-token** ทั้งที่สินค้าที่ระบุเจนผ่านช่วงปี (เช่น Evaporator alias "Vios13-19") ไม่มีสตริง "gen3" → ยุบผล/0. Research prod data (2026-07-07): **ไม่มี CarModel/Product code ใดใช้ "gen" เป็น identifier** (Volkswagen เป็นแบรนด์ substring, "Genuine" ไม่มีเลขจึงไม่เข้าเกณฑ์อยู่แล้ว); "Gen1/Gen3/Gen4/Gen8" ปรากฏเฉพาะเป็นตัวบอกเจเนอเรชันใน ProductAlias/ชื่อ. แก้: เพิ่ม `GENERATION_MARKER_RE = /^(?:gen|เจน|จีเอ็น)\d+$/` skip ใน [product-search-required-tokens.ts](lib/product-search-required-tokens.ts) (pattern เดียวกับ year-range) → ไม่เป็น hard anchor แต่ยังอยู่ในสตริง query สำหรับ FTS ranking (soft). unit test เพิ่มใน `product-search-required-tokens.test.ts` (STA-7065 ยัง anchor). ไม่แตะ DB/business logic
    - [x] (Option A / partType grounding) แก้ต้นเหตุ "ลูกค้าส่งรูปหน้าเว็บ **วาล์ว** P0368 → AI ตอบ **คอยล์เย็น**": รูปถูก classify ถูกต้อง (`IMAGE_CLASSIFIED` HIGH → frame `partType:"วาล์วแอร์"`, category "วาล์ว (Expansion Valve)", ถามรุ่นรถ) แต่พอลูกค้าตอบแค่ "Vios gen3 ปี2013" (ไม่มีคำอะไหล่) AI classifier กลับ**เดา `partType="คอยล์เย็น"`** ขึ้นมาเอง → `reconcileInquiryFrame` ตี `topicShift=true` เขียนทับวาล์วที่รูปตั้งไว้ → ค้นผิดหมวด. ยืนยันจาก production `LineAiAuditLog` (conv `cmra797i1…`): 05:21 frame=วาล์วแอร์ → 05:22 (text "Vios gen3 ปี2013") frame flip เป็นคอยล์เย็น topicShift=true. **ต้นเหตุ**: `partType` **ไม่ถูก evidence-gate** ต่างจาก brand/model/year ที่ `guardChatSearchIntent` กรองด้วย `lineValueHasCustomerEvidence` (ทั้งที่ comment ใน `reconcileInquiryFrame` ระบุว่า `latest` ควร gated มาแล้ว). **แก้** ใน [line-webhook-processor.ts](lib/line-webhook-processor.ts): ก่อนสร้าง `latest` frame — เชื่อ partType จาก classifier เฉพาะเมื่อมาจาก **รูปใหม่** (`imageFields.partType`) หรือ **มีร่องรอยในข้อความลูกค้า** (`lineValueHasCustomerEvidence`); ถ้าไม่ และมี partType เดิม stored อยู่ → drop เป็น null ให้ frame carry over ของเดิม (gate เฉพาะเมื่อมี stored part จึงไม่กระทบการ classify ครั้งแรก). unit test เพิ่มใน `line-webhook-processor.test.ts` (รูปวาล์ว → follow-up "Vios gen3 2013" → frame ยังเป็นวาล์วแอร์; และ topic-shift จริงที่ลูกค้าพิมพ์ชื่ออะไหล่ใหม่ยังทำงาน). ไม่แตะ DB/business logic
    - [x] (Option A / product-code fast-path) รองรับเคสลูกค้าดูสินค้าจากเว็บ/แอปแล้วส่ง**รหัสสินค้า** ("สอบถามราคา P0368") หรือ**แคปรูปหน้าเว็บ**ที่มีรหัส → AI ตอบสินค้านั้นตรงๆ. เดิม gate (`decideChatSearchGate`) ไม่รู้จักรหัสสินค้า → รูปที่ vision ดึง `partNumber` ได้ (เช่น DI261411-0300 = P0368) ยังโดนถาม "แจ้งรุ่นรถ" เพราะ code-anchor bypass เช็คแค่ `requiredTokens` (text) + `searchHints` แต่**โค้ดอยู่ใน `partNumber`** (ไม่ใช่ searchHints). ยืนยันจาก production `LineAiAuditLog` (conv `cmra797i1…`): image classification เก็บ `partNumber:"DI261411-0300"`, `searchHints` ไม่มีโค้ด → gate `PART_ONLY` ask need_car. **แก้** ใน [line-webhook-processor.ts](lib/line-webhook-processor.ts): เพิ่ม product-code fast-path — รวม candidate โค้ดจาก customer text + image `partNumber` + code-like searchHints → validate กับ catalog ผ่าน `resolveCatalogCodes` (export ใหม่จาก [product-search-bridge.ts](lib/chat-core/product-search-bridge.ts)) → ถ้า resolve ได้ให้ค้นแบบ exact-code (ส่ง `extractedPartNumber` ให้ `buildSearchQuery` ชนะ query), **bypass gate + low-confidence guard + ไม่ใส่ fitment hard filter** (โค้ดระบุตัวสินค้าเองแล้ว). ข้าม hardGuard (payment/claim) + payment-slip image. audit `PRODUCT_CODE_DIRECT`. เพิ่ม unit test 3 เคส (รูปมีโค้ด resolve ได้ → ค้นด้วยโค้ด, โค้ดไม่มีในระบบ → fallback ถามรุ่น, text มีโค้ด → ค้น exact). ไม่แตะ DB/business logic; ราคายัง defer ให้แอดมินตามนโยบายซ่อนราคา
    - [x] (Messenger parity / product-code fast-path) นำ Option A ไปใช้กับ Facebook Messenger ด้วย ([messenger-webhook-processor.ts](lib/messenger/messenger-webhook-processor.ts)): เพิ่ม `resolveDirectProductCode()` + `MESSENGER_PRODUCT_ROUTE` — (ก) **text path**: ก่อน `route.requiresAdmin` escalation ถ้าข้อความมีรหัสที่ resolve กับ catalog ได้ ("สอบถามราคา P0368", "P0368 ราคาเท่าไหร่") → ค้น exact-code ตอบเลย (กันเคส price keyword → escalate แอดมินโดยไม่ค้น); (ข) **image path**: ถ้า OCR `partNumber`/hint resolve ได้ → ค้น exact-code แบบ **ไม่ใส่ fitment filter** (กัน partType/car ที่ classifier เดา มากรองโค้ดทิ้ง). อีก 3 fix ครอบ Messenger อยู่แล้ว: correlated model+year + gen-marker อยู่ใน `product-search.ts`/`product-search-required-tokens.ts` ที่ใช้ `searchChatProductInquiry` ร่วมกัน; partType grounding เป็นของ LINE inquiry-frame (Messenger ไม่มี frame → N/A). tsc + LINE 62 tests + build clean (messenger test runner ใช้ `node:test mock.module` ที่ runtime นี้ไม่รองรับ — fail เหมือนกันบนโค้ดเดิม ไม่เกี่ยวกับการแก้)
    - [x] (2026-07-10 / 3 บั๊ก LINE จับหมวดผิด — ยืนยันจาก production `LineAiAuditLog` ทุก event) (1) **alias `คอม`/`com` ตะกละ** (CONTAINS) ไปจับสตริงในชื่อรุ่นรถ "commuter"/"คอมมิวเตอร์" → hard filter หมวด = คอมแอร์ ทำให้ "หม้อน้ำ commuter" คืนแต่คอมแอร์ → ปิด 2 alias นี้ใน DB (data fix, live). (2) **partType หลุดในพาธ rule dictionary**: `resolveKnownQueryIntent` ตั้ง partType เป็นชื่อหมวดเต็ม "หม้อน้ำ (Radiator)" แล้ว evidence gate ที่ line ~1408 ปัดทิ้ง (ลูกค้าไม่พิมพ์ "(Radiator)") → gate ถาม CAR_ONLY → ข้าม evidence gate เมื่อ `usedRuleIntent` (พาธ rule มโนไม่ได้). (3) **คำสะกดผิด "วาว์ล"** ไม่มีใน dictionary → เพิ่ม CategoryAlias + SearchKeyword synonym → Expansion Valve (data fix, live)
    - [x] (2026-07-10 / LLM category fallback + auto-stage) เมื่อ resolver จับหมวดไม่ได้ (`categoryName=null`) เฉพาะเทิร์น text ที่จะค้นจริง → [category-llm-fallback.ts](lib/chat-core/category-llm-fallback.ts) ให้ Gemini **แก้คำผิดอย่างเดียว** (`correctPartSpelling`, inject ได้ผ่าน dependencies) แล้ว **re-map คำที่แก้ผ่าน resolver เดิม** — ใช้เป็น hard filter เฉพาะเมื่อได้หมวดเดียว (95% เชิงโครงสร้าง ไม่เชื่อ self-confidence). สำเร็จ → stage คำผิดเป็น CategoryAlias **`matchMode=EXACT, isActive=false, source=AI_AUTO, reviewStatus=PENDING`** (fire-and-forget หลังตอบลูกค้า, guardrails + collision check ใน [category-alias-guardrails.ts](lib/chat-core/category-alias-guardrails.ts), กันซ้ำ/กัน rejected เสนอซ้ำ). Schema: enum `CategoryAliasSource`/`CategoryAliasReviewStatus` + field `source/reviewStatus/aiCorrectedTerm` + index. แอดมินอนุมัติ/ปฏิเสธผ่านแท็บ "AI เสนอ (รออนุมัติ)" ในหน้า `/master/categories` (route เดียว, สิทธิ์ `master.update`); อนุมัติ = เปิดใช้ + สร้าง SearchKeyword synonym + AuditLog, ปฏิเสธ = `REJECTED` คง row กันเสนอซ้ำ. `db push` + `tsc` + tests (webhook 65 / staging guardrails 4 / fitment / resolver) + build ผ่าน; ยืนยัน e2e กับ Gemini จริง (typo "คอมมแอร์" → "คอมเพรสเซอร์แอร์" → Compressor) แล้วลบ row ทดสอบ
    - [x] (2026-07-10 / Messenger parity — category resolution) ยกระดับ Facebook Messenger text path ([messenger-webhook-processor.ts](lib/messenger/messenger-webhook-processor.ts)) ให้ resolve หมวด/รุ่น/แบรนด์/ปี เป็น **hard filter** เหมือน LINE (เดิมเส้นทาง text ส่งแค่ `{route,text}` เข้า bridge = free-text ล้วน ไม่จับหมวด). เพิ่ม `resolveMessengerFitmentHints()`: classify (`extractChatSearchIntent`) → evidence-gate (`guardChatSearchIntent`) → `resolveChatFitmentFilters` → **LLM fallback** (`correctPartSpelling` → re-map → apply เมื่อได้หมวดเดียว) + **auto-stage** คำผิดผ่าน `stageAiCategoryAlias` (ใช้ helper ร่วมกับ LINE ทั้งหมด). Messenger ไม่มี inquiry-frame → resolve จากข้อความเทิร์นปัจจุบัน (history ใช้แค่ให้ classifier). Safety-first: ทุก step catch → ถ้าล้มเหลว/ไม่ resolve = ไม่ใส่ filter (free-text เดิม). เพิ่ม Gemini classifier call ต่อเทิร์น product text (ต้นทุนของ parity, เหมือน LINE). ยืนยัน e2e จริง: "คอมแอร์ vigo"→Compressor+Vigo, "วาว์ลอัลติสแท้03"→Expansion Valve+Altis+2003. `tsc` + build ผ่าน. อนุมัติ alias ที่ Messenger stage ใช้แท็บเดียวกับ LINE
    - [x] (2026-07-11 / A+F — LINE+Messenger ตอบรุ่นรถผิด "สายแอร์ใหญ่สตาด้า2500" → D-Max/Revo/Colorado) ยืนยันจาก production `LineAiAuditLog` (conv `cmq4ziq6l…`): classifier ได้ carModel "Strada" แต่ **evidence-grounding ทิ้ง** เพราะ "สตาด้า"(ไทย)≠"Strada"(อังกฤษ) — transliteration ไม่อยู่ใน evidence data; เหลือ hard filter แค่หมวด A/C Hose + requiredToken "2500"(ซีซี) → คืนสายแอร์รถรุ่นอื่นที่มีรุ่น 2500cc. **มาสเตอร์มี CarModel Strada + SearchSynonym "สตาด้า"→Strada ครบ** — พังที่ grounding ต้นน้ำ ไม่ใช่ data ขาด. **F (model synonym grounding)**: ใหม่ [car-model-alias-cache.ts](lib/car-model-alias-cache.ts) + [car-model-alias-loader.ts](lib/car-model-alias-loader.ts) โหลด `SearchSynonym` แบบ cache (TTL 60s, mirror brand-alias) → ส่ง `modelLookup` เข้า `guardChatSearchIntent` ([search-guards.ts](lib/chat-core/search-guards.ts) `lineModelHasCustomerSynonymEvidence`) ให้ "สตาด้า" ground "Strada" ได้; wire ทั้ง LINE + Messenger. **A (vehicle-unresolved gate)**: carModel grounded แต่ resolve เป็น hard filter ไม่ได้ (carModelName/carBrandName ว่าง) + มีผลจะโชว์ → ไม่โชว์การ์ด, ตอบ `CHAT_VEHICLE_UNRESOLVED_HANDOFF_REPLY` (ยืนยันรุ่นรถ) + ส่งแอดมิน (audit `AI_VEHICLE_UNRESOLVED_HANDOFF`), ทำทั้ง 2 processor. **ไม่แตะ engine `product-search.ts`** → ไม่กระทบ storefront. Test: `line-search-guards.test.ts` (Strada grounding + proof drop-without-lookup), `car-model-alias-cache.test.ts`, `line-webhook-processor.test.ts` (Strada gate e2e). ดูรายละเอียด [docs/line-messenger-ai-product-mismatch-audit.md](docs/line-messenger-ai-product-mismatch-audit.md) §9. เหลือ C/D/E ในเอกสารเดียวกัน
    - [x] (2026-07-11 / G1+G2 — LINE ตอบรุ่นรถผิดจาก frame carryover "อะไหล่แอร์ สิบล้อ HINO ISUZU" → สายแอร์ D-Max กระบะ) ยืนยันจาก production `LineAiAuditLog` (conv `cmq4ziq6l…`, เทิร์นถัดจาก Strada): classifier เทิร์นนี้คืน `partType=null` → frame **carry "สายแอร์" + หมวด A/C Hose** จากเทิร์นก่อน → completeness gate ตัดสินจาก frame partType (ไม่ใช่ข้อความจริง) → `isBroadChatPartType("สายแอร์")=false` → ค้น + hard-filter หมวดเดิม → คืนสายแอร์ D-Max (กระบะ) ให้คำถามรถสิบล้อ. **G1 (broad-gate อ่านข้อความจริง)**: gate เช็ค `isBroadChatPartType(consolidatedQuery ‖ processText)` เพิ่ม → ถ้าเทิร์นนี้กว้าง ("อะไหล่แอร์/สิบล้อ/ระบบแอร์") บังคับ BROAD_PART_TYPE handoff แม้ frame carry part เฉพาะ (parity: Messenger เช็ค `isBroadChatPartType(processText)` อยู่แล้ว → G1 เป็น LINE-only). **G2 เล็ก (ตัดสินโดยเจ้าของ: ความกว้างมาก่อน + connective คง frame)**: เมื่อเทิร์นใหม่ระบุ**คลาสรถ** (สิบล้อ/รถบรรทุก/เทรลเลอร์ — ไม่ใช่รุ่นจริง) + classifier ไม่มี part ใหม่ + **ไม่มีคำเชื่อมถามต่อ** (แล้ว/และ/หรือ/…ล่ะ) → drop partType เดิมไม่ให้หมวดเดิม hard-filter → gate ถามชนิดอะไหล่ (helper `namesVehicleClassTerm`/`hasFollowUpConnective` ใน [inquiry-frame.ts](lib/chat-core/inquiry-frame.ts), audit flag `droppedCarriedPartOnVehicleClassSwitch`; LINE-only เพราะ Messenger ไม่มี frame). ไม่แตะ engine. Test: `line-inquiry-frame.test.ts` (helper), `line-webhook-processor.test.ts` (G1 broad-after-specific, G2 drop-on-class-switch, G2 keep-on-continuation). ดู [docs/line-messenger-ai-product-mismatch-audit.md](docs/line-messenger-ai-product-mismatch-audit.md) §9
    - [x] (2026-07-11 / Fix1+2 — LINE ถามชนิดอะไหล่ซ้ำทั้งที่ลูกค้าพิมพ์มาแล้ว "คอล์ยเย็นนิสสันมาร์ค") ยืนยันจาก production `LineAiAuditLog` (conv `cmr2xbf16…`): classifier **แก้คำผิดสำเร็จแล้ว** (`classifierPartType="คอยล์เย็น"`) แต่ **partType-grounding gate** ([line-webhook-processor.ts](lib/line-webhook-processor.ts) `groundedLatestPartType`) โยนทิ้งเป็น null → gate เห็น `hasCar && !hasPart` → CAR_ONLY ถามชนิดอะไหล่ (ที่ลูกค้าบอกแล้ว) + search ถูกบล็อกก่อน LLM category fallback จะทำงาน. **ต้นเหตุ 2 ชั้น:** (1) gate เช็ค `stored?.partType` โดยไม่ดู `sessionStale` → frame เก่า 9 วัน (session ใหม่ `sessionStale=true`) ยังเปิด gate; (2) evidence check เอาคำที่แก้ถูกแล้ว ("คอยล์เย็น") ไปเทียบ substring กับข้อความพิมพ์ผิด ("คอล์ย" สลับ ล/ย) → ไม่เจอ → drop. **หลักการที่ยึด (เจ้าของสั่ง): ก่อนตัดต้องผ่าน resolve/LLM ก่อน หาก map สำเร็จต้องโชว์สินค้า.** **Fix 1:** เพิ่ม `!sessionStale` — session ใหม่ไม่มี part เก่าให้ปกป้อง → เชื่อ classifier. **Fix 2:** เพิ่ม `lineValueHasCustomerTypoEvidence` ([search-guards.ts](lib/chat-core/search-guards.ts), Damerau-OSA distance + sliding window บน glued Thai) — คำพิมพ์ผิดที่เป็น typo ของ partType (เช่น "คอล์ย"↔"คอยล์") นับเป็น evidence → ไม่ตัด → ปล่อยไป resolve หมวด + ค้น + โชว์. hallucination guard เดิม (follow-up รถล้วน ไม่มีคำอะไหล่) ยังทำงาน (ไม่มีทั้ง literal + typo evidence → drop). ไม่แตะ engine. Test: `line-search-guards.test.ts` (typo evidence), `line-webhook-processor.test.ts` (Fix1 stale-session keep+search, Fix2 live typo keep, + hallucination guard เดิมยังผ่าน). ดู [docs/line-messenger-ai-product-mismatch-audit.md](docs/line-messenger-ai-product-mismatch-audit.md) §9
    - [x] (2026-07-14 / A+B — LINE ยืนยันซื้อ "เอาตัว900 / 1อันคัฟ" แต่ AI ถามรุ่นรถซ้ำแทนที่จะส่งแอดมิน) ยืนยันจาก production `LineAiAuditLog` (conv `cmr9w83lp…` "ช่าง ชาร์ป"): 2 ข้อความถูก coalesce เป็นเทิร์นเดียว → keyword router คืน `PRODUCT_INQUIRY_TEXT` (regex `PURCHASE_INTENT_RE` ไม่จับ "เอาตัว+เลข"/"เลข+อัน") **แต่ AI group classifier จับถูกเป็น `group=purchase` → route→PURCHASE_INTENT → `isPurchaseIntent=true` อยู่แล้ว**. ต้นเหตุจริงคือ **ลำดับ branch ใน `forcedResponse`**: เพราะ purchase เป็น non-product turn → gate ของ `faqAnswer` (`isNonProductTurn && route.intent !== SHOP_INFO`) ปล่อยให้ FAQ ตอบ `answered=true` (คำ generic "ขอทราบรุ่นรถ ปีรถ") และ **FAQ branch อยู่เหนือ purchase-handoff branch** → FAQ ชนะ (`AI_SEND_DECISION reason=FAQ` ทั้งที่ `routedIntent=PURCHASE_INTENT`). **A**: ย้าย branch `isPurchaseIntent` ให้อยู่**เหนือ** `faqAnswer.answered` ใน forcedResponse (แพตเทิร์นเดียวกับ escalation ที่ comment ระบุ "MUST win over FAQ"). **B**: เพิ่ม `!isPurchaseIntent` ที่ gate ของ `faqAnswer` → purchase turn ไม่วิ่งเข้า FAQ ตั้งแต่แรก (กัน pre-empt + ประหยัด Gemini call). ผล: ทุก turn ที่ classify เป็น purchase → `PURCHASE_HANDOFF_MESSAGE` + freeze + notify (bell+Telegram). **ไม่แตะ engine/regex/DB** (regex เป็นแค่ชั้นแรก group classifier แก้ให้แล้ว — เก็บขยาย regex เป็น defense-in-depth ภายหลังถ้าต้องการ). Test: `line-webhook-processor.test.ts` เพิ่ม 1 เคส (group=purchase + FAQ answerable → ต้องได้ `AI_PURCHASE_HANDOFF` ไม่ใช่ FAQ; ยืนยัน fail-on-baseline/pass-after-fix). tsc + 74/79 pass (5 fail เดิม environmental — ต้องการ Gemini/network, ไม่เกี่ยวการแก้)
    - [x] (2026-07-24 / Option A+B — LINE ส่งรูปคอมแอร์เปล่า "คอมแอร์รุ่นนี้มีไหม" ไม่บอกรุ่นรถ → AI ค้น+เสนอคอมมั่วแทนที่จะถามรุ่นรถก่อน) ยืนยันจาก production `LineAiAuditLog` (conv `cmryljcnx…`): เทิร์น**ข้อความ**ถามถูก gate `GATE_ASK:PART_ONLY` → ถามรุ่นรถ (`searched=false`) ✓ แต่เทิร์น**รูป** (partType=คอมแอร์ HIGH, ไม่มีรถ/รุ่น/ปี) กลับ `searched=true`, `appliedConstraintKeys:[]`, `droppedImageCodes:["072060"]` → โชว์คอมเพรสเซอร์ 5/58 ตัวที่ไม่ผูกรถ. **ต้นเหตุ**: override `specific_latest_turn_without_car` ใน [line-webhook-processor.ts](lib/line-webhook-processor.ts) ที่ตั้งใจไว้ให้ "ลูกค้าให้รหัสเจาะจงแล้วไม่ต้องถามรถ" ใช้ `extractChatRequiredSearchTokens(trustedImageSearchHints)` ซึ่งจับเลข "072060" ที่ปั๊มบนตัวคอม (ไม่ใช่รหัสแคตตาล็อกจริง — ถูก drop ทีหลัง) เป็น required token → **ข้าม gate ถามรุ่นรถ**. **แก้ (Option A+B)**: ย้ายบล็อก resolve product-code (`codeCandidates`/`resolveCatalogCodes`/`directProductCodeCandidate`) ขึ้นมาก่อน gate + เพิ่ม `imageHasValidatedCatalogCode` (โค้ดจากรูปที่ผ่าน `isDirectProductCodeToken` **และ** resolve ในแคตตาล็อกจริง) → override เทิร์นรูปยิงเฉพาะเมื่อมีรหัส SKU จริง (เทิร์นข้อความยังใช้ `guardedSearch.requiredTokens` เดิม ไม่แตะ). ผล: รูปอะไหล่ fitment ที่ไม่มีรถ + ไม่มีรหัสจริง → ถามรุ่นรถเหมือนเทิร์นข้อความ; รูปที่มีรหัส SKU จริง → ค้นตรงได้ (Option A คงไว้). **ไม่แตะ engine/DB/business logic**. Test: `line-webhook-processor.test.ts` เพิ่ม 2 เคส (รูปคอมแอร์+เลขปั๊มไม่ใช่ SKU → `AI_SEARCH_GATE_ASK` ไม่ค้น; รูป+รหัส SKU จริง → ค้น) + อัปเดตเคสเดิม "part-image OCR hints drop stale vehicle hard filters" ให้ใช้ SKU จริง (z0016525a); tsc ผ่าน. **Messenger parity (รอบเดียวกัน)**: พาธรูปใน [messenger-webhook-processor.ts](lib/messenger/messenger-webhook-processor.ts) เดิม**ไม่เรียก `decideChatSearchGate` เลย** (gate แค่ confidence) → เพิ่ม gate อ่านจาก image classification + `imageValidatedCode` (`resolveDirectProductCode`) → เทิร์นรูปเดี่ยว (ไม่มี text) conf≠LOW + gate ask + ไม่มี SKU จริง → ตอบ `buildChatSearchAskReply(need_car)`; มี SKU จริง → ค้น. Messenger ไม่มี inquiry-frame จึง gate จาก classification ตรงๆ. Test แยกไฟล์ `lib/messenger/__tests__/messenger-image-gate.test.ts` (2 เคส, isolation ของ module-mock) เพิ่มใน `npm run test:messenger-webhook` — ผ่านทั้ง 3
    - [x] (2026-07-24 / Option A — LINE+Messenger ลูกค้าพิมพ์รุ่นที่ไม่มีในระบบ "ฮอนด้าออดิซี่" → AI เสนอกรองอากาศ Honda รุ่นอื่น (City/Jazz/Accord/CR-V) เป็น "ใกล้เคียง") ยืนยันจาก production `LineAiAuditLog` (conv `cmq6o1g1u…`): master ไม่มี CarModel Odyssey/synonym/product แต่ **แบรนด์ Honda resolve ได้** → `SEARCH_QUERY_CONSOLIDATED carModelName:null, carBrandName:"Honda", categoryName:"กรองอากาศ"` → `PRODUCT_SEARCH_SUMMARY searched:true total:10 returnedCount:5 appliedConstraintKeys:[]` (ค้น brand+category ล้วน ไม่มี filter รุ่น). **ต้นเหตุ**: vehicle-unresolved guard (Option A) เดิมที่ [line-webhook-processor.ts](lib/line-webhook-processor.ts) `Boolean(carModel) && !carModelName && !carBrandName` — เงื่อนไข `!carBrandName` ทำให้ **แบรนด์ที่ resolve ได้ปลด guard** → รุ่นที่ระบุแต่ unknown ถูกทิ้งเงียบ ๆ แล้ว fallback เป็น brand scope โชว์ของรุ่นอื่น (comment เดิมเขียนว่า "brand-only scope acceptable"). **แก้ (Option A)**: ตัดเงื่อนไข `!carBrandName` ออก — เมื่อลูกค้า**ระบุรุ่นเจาะจง** (grounded) แต่ resolve เป็น carModelName ไม่ได้ → ยิง handoff `CHAT_VEHICLE_UNRESOLVED_HANDOFF_REPLY` (ขอยืนยันรุ่น + ส่งแอดมิน) แม้แบรนด์จะ resolve; เคสระบุ**แค่แบรนด์** (ไม่มีรุ่น) → `carModel` null → guard ไม่ยิง brand scope ทำงานปกติ. แก้ทั้ง single-path (`vehicleUnresolvedGuard`) + multi-subject (`vehicleUnresolvedSubject`) ใน LINE + `vehicleNamedButUnresolved` ใน [messenger-webhook-processor.ts](lib/messenger/messenger-webhook-processor.ts) (parity). **ไม่แตะ engine/DB/business logic อื่น**. Test: `line-webhook-processor.test.ts` +2 (Odyssey known-brand → handoff; bare brand → ยังค้น) + `messenger-vehicle-unresolved.test.ts` (ไฟล์ใหม่ เพิ่มใน `npm run test:messenger-webhook`); tsc + 18 LINE tests + 4 Messenger tests ผ่าน
    - [x] (2026-07-25 / Fix A+B — LINE ลูกค้าถาม "แผงแอร์รถตู้ commuter หน้าสั้นปี 2013" → AI ส่งคอยล์เย็น 4 รายการแทนแผงแอร์/คอยล์ร้อน) ยืนยันจาก production `LineAiAuditLog` (conv `cms07g5ew…`): 2 เทิร์นแรกค้นถูก (categoryName "คอยล์ร้อน (Condenser)") แต่เทิร์นราคา ("ราคาประมาณเท่าไหร่ครับ") classifier **rewrite query+partType เป็น "ตู้แอร์"** ซึ่งลูกค้าไม่เคยพิมพ์ — frame grounding เดิมตัด partType ปลอมทิ้งถูกต้อง (frame คง "แผงแอร์") แต่ `consolidatedQuery` ไม่ถูก ground → "ตู้แอร์" ไหลเข้า `matchDbCategoryAlias` ซึ่งเจอทั้ง "แผงแอร์"(Condenser,240) จาก partType และ "ตู้แอร์"(Evaporator,240) จาก query → priority+ความยาวเท่ากัน → ตัดสินด้วยลำดับ row จาก DB → Evaporator ชนะ. **Fix A** ([line-webhook-processor.ts](lib/line-webhook-processor.ts)): เมื่อ classifier partType ถูก drop เป็น ungrounded rewrite (ต่างจาก frame part — carryover ที่ซ้ำ part เดิมไม่นับ) → `consolidatedQuery` fallback เป็น `frameQuery` (ยกเว้น forceLiteralQuery); audit field ใหม่ `droppedUngroundedClassifierQuery`. **Fix B** ([category-alias-resolver.ts](lib/category-alias-resolver.ts)): `matchCategoryAliasRows` เลือก best alias ต่อ text แล้วตัดสินด้วย priority/ความยาว แต่เสมอกันให้ text ลำดับแรก (partType→query→rawText) ชนะแทน DB row order — alias priority สูงกว่าใน rawText ยังชนะเหมือนเดิม (เคส "พัดลมโบ"). Messenger ได้ Fix B อัตโนมัติ (ส่ง processText เป็น queryText ไม่มี LLM query). ไม่แตะ engine. Test: `category-alias-resolver.test.ts` +1, `line-webhook-processor.test.ts` +1 (103 pass), ชุดข้างเคียง 115 pass, build ผ่าน. commit `670c835`. **Retest 18:15 ยังไม่หาย → Fix C (commit `942dfe5`): ต้นตอจริงคือ `extractPriceProductSubjectsFromText` ([price-product-subjects.ts](lib/chat-core/price-product-subjects.ts)) — regex cooling-unit เดิม `ตู้\s*(?:แอร์)?` ("แอร์" optional) จับ "ตู้" ใน "รถตู้" → subject "ตู้แอร์" (universal) ทับ LLM classifier ทั้งเทิร์นผ่าน `priceSubjectIntent` (audit `source=price_subject_rule`; Fix A ไม่ยิงเพราะแหล่งไม่ใช่ LLM + sessionStale). แก้เป็น `(?<!รถ)ตู้\s*(?:แอร์|เย็น)` + ล้าง regex mojibake ในไฟล์เดียวกัน + test ใหม่ `price-product-subjects.test.ts` 7 เคส (รวมข้อความ production จริง). Messenger ใช้ extractor เดียวกัน = parity.** ดู [docs/line-messenger-ai-product-mismatch-audit.md](docs/line-messenger-ai-product-mismatch-audit.md) §9
    - [x] (2026-07-25 / A+B — ปิด scenario คำใกล้เคียงชนกันทั้งระบบ หลัง audit จำลองพบ 7 เคสผิดหมวดยืนยันได้จาก CategoryAlias production 253 ตัว) **A1**: pattern ตู้แอร์/คอยเย็น ใน [price-product-subjects.ts](lib/chat-core/price-product-subjects.ts) ต้องเป็น head noun (lookbehind มอเตอร์/พัดลม/วาล์ว/แผง/สวิตช์/ล้าง) — "มอเตอร์ตู้แอร์ vigo ราคา" ไม่ถูกยุบเป็น "ตู้แอร์" อีก. **A2**: [line-webhook-processor.ts](lib/line-webhook-processor.ts) เชื่อ LLM classifier ก่อน price-subject rule เมื่อให้ product subject มา (ยืนยัน live LLM 20 calls: "ตู้แอร์วีโก้"→Vigo 5/5, multi-subject 3/3) — rule เหลือเป็น fallback ตอน LLM ล่ม. **B**: [category-alias-resolver.ts](lib/category-alias-resolver.ts) longest-span ใน text เดียวกดทับ alias ซ้อนใน ("พัดลมคอนเดนเซอร์" ชนะ "คอนเดนเซอร์" ที่ priority สูงกว่า) — ปิดเคส โบเวอร์/รีซิสเตอร์/Condenser Fan/กรองแอร์ห้องโดยสาร พร้อมกัน. Messenger ได้ A1+B อัตโนมัติ. Test extractor 8 + resolver 7 + LINE 105 + ข้างเคียง 97 ผ่าน, build ผ่าน. commit `3b939bb`
    - [x] (2026-07-27 / audit ย้อนหลัง LINE OA — ตรวจ `LineAiAuditLog` production 3,979 rows (8 มิ.ย.–27 ก.ค., 602 inbound events, 50 ห้อง) + รัน resolver/engine จริงกับ catalog + เรียก Gemini จริง เพื่อพิสูจน์สมมติฐานจุดอ่อน 10 ข้อ ผลคือยืนยัน 2 ข้อ ถอน 4 ข้อ) แก้ 3 อย่าง:
        - **(1) observability — `usedBroadFallback` ไม่เคยถูกบันทึก (0/508 rows)**: reply path แตกกิ่งด้วยค่านี้เพื่อแนบ `BROAD_FALLBACK_NEAR_MATCH_NOTE` มาตลอด แต่ payload `PRODUCT_SEARCH_SUMMARY` ไม่มีฟิลด์ → ไม่มีทางวัดว่าลูกค้าเห็นผล "ใกล้เคียง" บ่อยแค่ไหน (และทำให้พิสูจน์ข้อสงสัยเรื่อง relevance gate ไม่ได้). เพิ่ม `usedBroadFallback` + `accessoryVehicleDropped` เข้า payload ใน [line-webhook-processor.ts](lib/line-webhook-processor.ts). Messenger ไม่มีระบบ audit จึงไม่มี parity ให้ทำ
        - **(2) universal/accessory สืบทอดรถจาก inquiry frame → ตอบ "ไม่มีของ" ผิด**: frame ตั้งใจคงรถไว้ข้าม topic shift และ `droppedVehicleCarryover` ยิงเฉพาะเทิร์นที่มี token ตัวเลข → คำถามคำล้วน ("มีน้ำยาล้างคอยล์ไหม") ต่อจาก "หม้อน้ำ vios 2010" ค้นด้วย filter Vios+2010 บน SKU ที่**ไม่มี fitment row เลย** → 0 ผลเสมอ → `SEARCH_NO_MATCH_HANDOFF`. วัดกับ catalog จริง: น้ำยาล้างคอยล์ 1→0, ฟองน้ำ 4→0, โอริง 68→1. ยืนยันด้วย Gemini จริงว่า **classifier ไม่ผิด** (คืน `partKind=universal, car=null` ถูกต้อง) — ต้นเหตุอยู่ที่ frame ล้วน. **แก้แบบ retry-on-empty ใน [product-search-bridge.ts](lib/chat-core/product-search-bridge.ts)** (ไม่ใช่ strip filter ตั้งแต่ต้น): เมื่อ `accessoryHeadNoun` ถูกตั้ง + มี vehicle filter + ค้นได้ 0 → ค้นซ้ำโดยตัด brand/model/year ทิ้ง คง head-noun anchor ไว้ → reason `SEARCHED_ACCESSORY_HEAD_ANCHORED_NO_VEHICLE`, `appliedFilters` สะท้อนการค้นที่สำเร็จจริง (ลิงก์ "ดูทั้งหมดบนเว็บ" จึงตรงกับที่ลูกค้าเห็น). เหตุผลที่เลือก retry: เทิร์นที่**ค้นเจอแล้ว**ไม่ถูกแตะเลย → อะไหล่ fitment ที่ LLM ติดป้าย `universal` ผิดจะไม่หลุด vehicle scope ไปโชว์ของรถคันอื่น (วิธี strip ล่วงหน้าทำ 6 เทสเดิมพัง จึงถูกทิ้ง). ยืนยันหลังแก้: 1/4/68 กลับมาครบ, fitment part (คอยล์เย็น/หม้อน้ำ/สายน้ำยา) ยังผูก Vios เหมือนเดิม `dropped=false`
        - **(3) master data — "Hilux Tiger D4D" resolve เป็นรุ่นไม่ได้**: cluster `Tiger` มีแต่คำเดี่ยว และ prefix-match ของ `resolveCanonicalCarModelHint` รับ suffix เฉพาะ gen/ขนาดเครื่อง (รหัสเครื่อง "D4D" ไม่เข้าเกณฑ์) → `carModelName=NULL` → ยิง vehicle-unresolved guard เด้งแอดมิน 2 ครั้งจริงเมื่อ 2026-07-24. เพิ่ม `["Tiger","Hilux Tiger","Hilux Tiger D4D","Tiger D4D","ไฮลักซ์ไทเกอร์"]` ใน [seed-model-synonyms.ts](prisma/scripts/seed-model-synonyms.ts) แล้วรันจริง (updated=1, additive, มี `AuditLog` action=UPDATE entityType=SearchSynonym) → ทั้ง 4 สะกดคืน `model=Tiger` แล้ว
        - Test: `line-product-search-bridge.test.ts` +3 (retry ตัดรถ / ไม่ retry เมื่อเจอของแล้ว / ไม่แตะเทิร์นที่ไม่ใช่ accessory) = 33 ผ่าน, LINE 105 ผ่าน, ชุด line-*/chat-* 334 ผ่าน, Messenger 4 ผ่าน, `tsc` ไม่มี error ใหม่, `npm run lint` 0 errors
        - **ยังไม่ได้แก้ (บันทึกไว้ตัดสินใจต่อ)**: ห้องที่ AI freeze แล้วลูกค้าพิมพ์ต่อ — 60 handoff → **76 เทิร์นที่ AI เงียบสนิท** (15% ของ inbound), median รอแอดมิน 8.3 นาที แต่ **p90 = 541 นาที** และมีห้องหนึ่งเงียบ 27 ข้อความติดต่อกัน 22 ชม. (ข้อความที่ตกหล่นมีทั้ง "ถ้ามีตรงรุ่นก็รับเลยครับ"). Telegram ยังเด้งทุกครั้ง (`sendTelegramWhenDeduped`) จึงไม่ใช่ปัญหาการแจ้งเตือน แต่เป็นเรื่องความเร็วรับสาย — ทางเลือก: auto-resume เมื่อลูกค้าเปิดเรื่องใหม่ / ส่งข้อความ "แอดมินกำลังมา" แบบ throttle / escalate Telegram เมื่อเกิน 30 นาที
    - [ ] (Ops) รีวิว no-result report รายสัปดาห์ → เติม SearchSynonym/ProductAlias สำหรับคำที่ miss จริง (ใช้หน้า `reports/product-search-no-result` ที่ตอนนี้กรอง noise แล้ว)

### 4. Shopee Open Platform Integration (โมดูลแยกอิสระ)
- สถานะ: **Phase A–F เสร็จ** (schema + sale-core + create-sale service + UI ครบ: dashboard `/admin/marketplace/shopee`, orders list/detail, products, stock, channel filter ในรายงาน) · คงเหลือเฉพาะงานที่ต้อง verify กับ Shopee credentials จริง (live OAuth/item fetch/order pull/stock push/logistics/cancel-refund/escrow) — ดู [docs/shopee/PLAN-shopee.md](/D:/autoparts/docs/shopee/PLAN-shopee.md)
- หลักการ: แยกโมดูลออกจากระบบเดิมทั้งหมด (`lib/shopee/*`, `app/admin/(protected)/marketplace/*`, `app/api/shopee/*`) ไม่กระทบ logic หน้าร้าน/backoffice เดิม
- เอกสาร active:
  - [docs/shopee/README.md](/D:/autoparts/docs/shopee/README.md) — ภาพรวม + isolation principles
  - [docs/shopee/PLAN-shopee.md](/D:/autoparts/docs/shopee/PLAN-shopee.md) — checklist เต็มทุก phase
  - [docs/shopee/USER-TASKS.md](/D:/autoparts/docs/shopee/USER-TASKS.md) — งานที่เจ้าของร้านต้องทำเอง

### 5. LINE OA + AI Chat Hardening (2026-06-09)
- สถานะ: **review-driven hardening รอบแรกเสร็จสมบูรณ์** (13 ข้อจาก code review + `prisma db push` รัน index ใหม่แล้ว 2026-06-09)
- หลักการ: ปรับ reliability/performance ของ webhook + AI pipeline โดยไม่เปลี่ยน business logic / พฤติกรรมที่ลูกค้ารู้สึกได้
- ไฟล์หลัก: `app/api/line/webhook/route.ts`, `lib/line-webhook-processor.ts`, `lib/line-conversation-repository.ts`, `lib/line-ai-job-worker.ts`, `lib/line-intent-router.ts`, `app/api/line/ai-jobs/reconcile/route.ts`
- งานที่ทำเสร็จ:
  - [x] (1+3) แยก webhook เป็น ACK-ทันที + รัน profile lookup/AI pipeline ใน `after()` background → LINE ไม่ re-deliver, reply token ยังสดเมื่อ pipeline ถึงขั้นตอบ
  - [x] (2) Idempotency: catch P2002 บน `LineMessage.lineEventId` → `DuplicateLineEventError` กัน double-process จาก LINE re-delivery race
  - [x] (4) Atomic job claim ใน cron worker ด้วย `UPDATE ... FOR UPDATE SKIP LOCKED` กัน cron 2 รอบ pick job ซ้ำ
  - [x] (5) Reconciler cron `/api/line/ai-jobs/reconcile` (ทุก 10 นาที) mark OUTBOUND ที่ค้าง PENDING > 5 นาที เป็น FAILED
  - [x] (6) Per-event try/catch ใน processor loop → 1 event พังไม่ทำให้ทั้ง batch หยุด
  - [x] (7) แก้ comment ที่ระบุผิดว่า handoff notify ไม่ส่ง Telegram (จริงๆ ส่งทั้ง bell+Telegram ตาม Iron Rule)
  - [x] (8) ลบ `PRODUCT_HINT_RE` dead branch (default route ทำงานเหมือนกันอยู่แล้ว)
  - [x] (9) เปลี่ยน audit writes 7 จุด (debug/metric) เป็น fire-and-forget ตัด round-trip ออกจาก reply latency
  - [x] (10) Dedupe LINE profile fetch ต่อ userId ใน webhook payload เดียวกัน
  - [x] (11) เก็บ `pipelineDurationMs` ใน AI_SEND_DECISION audit เพื่อวัด p95
  - [x] (12) เพิ่ม composite index `LineAiAuditLog [conversationId, action, createdAt desc]` (schema แก้แล้ว + `prisma db push` แล้ว 2026-06-09)
  - [x] (13) Cap history แต่ละ turn ที่ 400 ตัวอักษร กัน Gemini prompt budget ล้น/ตอบถูกตัด
  - [x] (14) Sticker handler: สติกเกอร์ไม่เข้า search/handoff/notify pipeline อีกต่อไป — เดิมตก UNKNOWN → handoff → ตอบ "รับทราบค่ะ เดี๋ยวแอดมินมาดูแล" ซ้ำ + freeze AI + ping แอดมินทุกใบ. ใหม่: ทักทายครั้งเดียวเมื่อเป็น contact ใหม่/ห่าง > 6 ชม. (`lastCustomerMessageAt` snapshot), นอกนั้นนิ่งสนิท ไม่ตั้ง waiting_admin ไม่แจ้งเตือน (`handleStickerEvent` ใน `lib/line-webhook-processor.ts`) + เพิ่ม audit `STICKER_HANDLED` + 2 unit tests
  - [x] (15) AI-consolidated search query (แก้ปัญหา drip-feed): เดิมลูกค้าทยอยพิมพ์ ("คอยเย็น d max" → "ปี 06") แล้ว search ใช้แค่ข้อความล่าสุด "ปี 06" → เจอ 516 รายการมั่ว (ทั้งการ์ดสินค้า + ลิงก์ `?q=ปี 06`). ใหม่: `extractLineSearchIntent()` ใน `lib/line-ai-service.ts` (เดิม `consolidateLineSearchQuery`) ให้ Gemini รวมหัวข้อที่ลูกค้าตามหาจากบทสนทนาเป็นคำค้นเดียว (ชนิดอะไหล่+รุ่น+ปี, แปลงปีย่อ 2 หลัก) แล้ว `processLineAiReply` ป้อน query นั้นเข้า search แทน raw text (เรียกเฉพาะเทิร์น follow-up ที่มี history → เทิร์นแรกไม่เพิ่ม call). Fallback ปลอดภัย: Gemini off/error/ตอบ NONE → ใช้ logic เดิม (latest text + fitment carryover). แถมปิดรูรั่ว regex ใน `lib/line-fitment-extract.ts`: รองรับ "d max" เว้นวรรค + ขยาย "ปี NN" 2 หลัก (00–35) → ค.ศ. + audit `SEARCH_QUERY_CONSOLIDATED` + unit tests
  - [x] (16) จำกัดขอบเขตการค้นหา (option C — เคส "หม้อน้ำ mazda 2" เจอ 516):
    - **A. core search** (`lib/product-search.ts` + `lib/search-synonyms.ts`): เดิม FTS แตก token แล้ว **OR** กันทั้งหมด + เลขสั้น "2" กลายเป็น `2:*` → match ทุกปี 20xx → ระเบิด. ใหม่: `expandQueryTokenGroups()` แยก query เป็น "คอนเซ็ปต์" แล้ว `buildTsQueryExpression()` ทำ **AND ข้ามคอนเซ็ปต์ / OR ภายใน synonym** + เลข 1-2 หลักเป็น exact lexeme (ไม่ prefix). มี **OR-fallback**: ถ้า AND (หลายคอนเซ็ปต์) ได้ 0 แถว → รันซ้ำด้วย OR กันเคสเจอ 0. กระทบทั้งเว็บ+LINE
    - **B. LINE structured filters**: `extractLineSearchIntent()` คืน {query, partType, carBrand, carModel, year}; `resolveLineFitmentFilters()` (`lib/line-fitment-resolve.ts`) map ชื่อ → master data จริง (CarBrand/CarModel/Category, case-insensitive) แล้วส่งเป็น **hard filter** เข้า search ผ่าน bridge `fitmentHints` (+เพิ่ม `categoryName`). ปลอดภัย: hint ที่ resolve ไม่ได้จะถูก drop (ไม่ทำให้เจอ 0)
    - unit tests: tsquery builder 6 + parseLineSearchIntent 4 + processor (resolved fitment hints) 1
  - [x] (17) Hybrid semantic search (Phase 1 — ยกระดับคุณภาพ retrieval) — **เปิดใช้งานจริงแล้ว** (`PRODUCT_SEARCH_SEMANTIC=on` ใน Vercel):
    - **Gated ทั้งหมด** ด้วย env `PRODUCT_SEARCH_SEMANTIC=on` (default off) → ถ้าไม่เปิด/ไม่มี embedding/ล้มเหลว = lexical เดิมเป๊ะ ไม่กระทบ logic อื่น
    - embedding client: `generateGeminiEmbedding()` ใน `lib/google-ai-client.ts` (**gemini-embedding-001** ผ่าน `embedContent` + `outputDimensionality=768` — text-embedding-004 ไม่รองรับบน key นี้) ใช้ key rotation เดียวกับ generate (400/404 ไม่ cooldown key); helper `lib/embeddings.ts` (`embedQuery`, `embedTexts`, `buildProductEmbeddingText`, `toPgVectorLiteral`, `isSemanticSearchEnabled`)
    - schema (อนุมัติแล้ว, รันผ่าน `npm run db:setup-search-v2`): `CREATE EXTENSION vector` + `product_search_documents.embedding vector(768)` + HNSW cosine index. คอลัมน์ embedding ถูกกันออกจาก `refresh_product_search_document` upsert (เหมือน sales_count) → text refresh ไม่ลบ vector
    - backfill: `npm run backfill:embeddings` (`prisma/scripts/backfill-embeddings.ts`, batch 50, idempotent, `--all` เพื่อ re-embed)
    - sync ตอนแก้สินค้า: `reembedProductSearchDocument()` (`lib/product-embedding-sync.ts`) เรียกผ่าน `after()` ใน createProduct/updateProduct (fire-and-forget, ไม่ block response)
    - hybrid ใน `searchProductIdsV2`: vector recall (cosine, reuse exactScope filters เดิม) → inject เป็น candidate (`OR v.product_id IS NOT NULL`) + score boost (sim × 500, ต่ำกว่า exact/oem/contains) ใน ranked query เดียว → pagination/total semantics คงเดิม
    - unit tests: embeddings helpers 4
    - resolver part-type→category: `matchPartTypeToCategoryHint()` ใน `lib/line-fitment-resolve.ts` map คำที่ลูกค้า/AI พูด ("วาล์วแอร์", "คอยเย็น", "แผงแอร์"...) → หมวดจริงในระบบ (19 หมวด, alias เรียง specific→generic กันชน "วาล์ว"/"หม้อน้ำ"/"คอม") ก่อน fallback equals/contains → category hard filter ทำงานแม้คำพูดไม่ตรงชื่อหมวด + unit tests 3
    - **ขั้นตอนเปิดใช้:** 1) `npm run db:setup-search-v2` ✅ 2) `npm run backfill:embeddings` ✅ (embed 598/598) 3) ตั้ง `PRODUCT_SEARCH_SEMANTIC=on` ใน Vercel env ✅ **(เปิดใช้งานจริงแล้ว)**
    - ทดสอบจริงแล้ว: "หม้อน้ำ mazda 2" → หม้อน้ำ Mazda 2 ขึ้นอันดับ 1 (sim 0.84), "ตัวทำความเย็นแอร์วีออส" → เจอคอยล์เย็น/แผงแอร์ Vios (semantic เข้าใจคำบรรยาย)
    - [x] **Golden test A/B (2026-07-30)** — `npm run test:semantic-golden` (`scripts/test-semantic-search-golden.ts`): ยิงทุกเคส 2 รอบด้วย data จริง (semantic on vs `disableSemantic:true`) แล้วเทียบ retrievalMode / cosine / vector-only / หน้าผลลัพธ์ → พิสูจน์ 3 อย่างในไฟล์เดียว: (1) vector path รันจริง (mode `hybrid`), (2) ช่วย recall/ranking บนคำบรรยาย, (3) additive — code/lexical แรง ๆ ได้หน้าเดิมเป๊ะ และคำถามไม่ใช่สินค้ายังได้ 0 แถว. ผล: **12/12 ผ่าน** (semantic ยิง 6 เคส, vector-only rescue 5 เคส) — `check:search-v2-health` เขียว (941 docs, embedding ครบ, HNSW ok)
    - [x] **Harness วัดคุณภาพ retrieval (ถาวรใน repo, read-only ทั้งหมด)** — สูตรคะแนนที่ mirror มาจาก `runRankedQuery` อยู่ที่ `scripts/semantic-eval-core.ts` ที่เดียว (import น้ำหนัก vector จาก `lib/product-search.ts` ไม่ hardcode ซ้ำ) **ถ้าน้ำหนักใน production เปลี่ยน ต้องอัปเดตไฟล์นี้รอบเดียวกัน**:
      - `npm run evaluate:semantic-retrieval` — eval set 11 คำค้นที่มี ground-truth หมวด+รุ่นรถ, re-rank candidate ชุดเดิมด้วยทุกสูตรที่เสนอ + validate ว่า mirror ตรงกับหน้าจริง (`mirrorValid`)
      - `npm run diagnose:semantic-query -- "<คำค้น>"` — เจาะคำค้นเดียว: A/B semantic on/off, การกระจาย similarity เทียบ threshold, เพื่อนบ้านใกล้สุดตาม cosine ดิบ, และการแยกองค์ประกอบคะแนนต่อแถว
      - `npm run experiment:semantic-embedding-text` — ทดลองสูตร embedding text ด้วยการ re-embed ใน memory (ไม่เขียน DB) ต้องรันก่อนคิดจะ `backfill:embeddings --all` ทุกครั้ง
      - `npm run experiment:semantic-or-merge -- "<คำค้น>"` — เทียบ candidate ชุดปัจจุบัน (AND+vec) กับชุดที่ผสม OR-fallback ด้วยสูตรคะแนนเต็มรวม `ts_rank_cd`
    - [x] **ผลวัด 4 ข้อเสนอปรับปรุง (2026-07-30) — วัดบน production data 941 docs, eval set 11 คำค้น paraphrase ที่มี ground-truth หมวด+รุ่นรถ (mirror ตรงกับหน้าจริง 10/11). สรุป: ไม่ทำทั้ง 4 ข้อ (ดูเหตุผล) — baseline ดีอยู่แล้ว 10/11 หมวดถูกใน top-5, 9/11 ถูกที่อันดับ 1:**
      - **(G1) แก้ embedding text ให้เน้นชนิดอะไหล่ + re-embed → ตกไป.** ทดลอง re-embed จริงบน subset 10 ชิ้น × 4 สูตร (ปัจจุบัน / หมวดขึ้นก่อน / หมวดซ้ำ 2 ครั้ง / ใส่ป้าย "ชนิดอะไหล่:") margin ระหว่างของถูกกับตัวสับสนยัง **ติดลบทุกสูตร** (-0.024 → ดีสุด -0.015) ตัวสับสนยังอันดับ 1 ทุกสูตร คือย้าย/ซ้ำหมวดไม่ได้แก้เรื่องนี้ — ไม่คุ้มค่า re-embed ทั้งชุด
      - **(G2) ลดน้ำหนัก tie-breaker ในหน้า vector-only → ตกไป และเกือบพลาด** (aggregate: damp x0.1 = 10/11 เท่าเดิม, sim เดี่ยว = 11/11 แต่ทำอันดับ 1 พังในเคส Vios)**.** ข้อเท็จจริงจาก DB: `sales_count` = 0 ที่ 810/941 ชิ้น, max 30 (ไม่ใช่ 100), p95 = 1 → พจน์ sales จริง ๆ ให้ ≤ 1.2 แต้มเกือบทุกแถว; `stock ≤ 0` แค่ 30/941 → พจน์ stock เป็น +60 คงที่ 96.8% ของแถว. ที่เหลือคือ trigram ซึ่ง **ไม่ใช่ noise แต่เป็นสัญญาณ lexical อ่อน ๆ ของคำชนิดอะไหล่ไทย** — พอ damp x0.1 แล้วเคส "ตัวทำความเย็นแอร์วีออส" แย่ลง (คอยล์เย็นตกจากอันดับ 1 → 2 แผงแอร์ขึ้นแทน) ส่วน aggregate ไม่ขยับ (9/10 → 9/10)
      - **(G3) ให้หน้า hybrid ผสมผล OR-fallback → ตกไป (regression 2 จาก 3 เคส).** จำลองด้วยสูตรคะแนน production เต็ม: "รังผึ้ง ซิตี้" ดีขึ้น (หม้อน้ำขึ้นอันดับ 2) แต่ "ตัวกรองฝุ่น ซีวิค" พัง (โบลเวอร์ Civic ขึ้นอันดับ 1, กรองแอร์ตกไปอันดับ 6) และ "ถังน้ำหล่อเย็น ซิตี้" พัง (ท่อยางหม้อน้ำยึด 3 อันดับแรก) — เพราะ `ts_rank_cd × 220` ของ OR-tsquery ให้ ~600 แต้มกับแถวที่แมตช์แค่ "ชื่อรุ่นรถ" ซึ่งใหญ่กว่าช่วง `sim × 500` ทั้งช่วง. ต้นทุนเวลาไม่ใช่ปัญหา (+30–50ms) แต่ต้องทำ score normalization ก่อนถึงจะใช้ได้
      - **(G4) บังคับ car-scope กับแถว vector-only → เก็บเป็นตัวเลือก ไม่ใช่ของฟรี.** ช่วยเคส "แผงระบายความร้อนน้ำยาแอร์ ดีแมค" (หมวด+รถถูก อันดับ 3 → 1) และตัด noise รถผิดรุ่นได้จริง แต่ aggregate ไม่ขยับ (9/10) และ **ตัด recall แรง**: candidate เหลือ 2–17 จาก 30 เช่น "ชุดหน้าคลัชคอมเพรสเซอร์ วีโก้" total 72 → 2. ต้องมี model resolution ที่เชื่อถือได้ก่อน (มี `CarModel` 148 รุ่น / `CarBrandAlias` แต่ path ค้นหาเว็บไม่ได้ resolve ชื่อรุ่นจาก free text) และ 92/916 ชิ้นไม่มี `car_model_text` เลย → จะถูกตัดทิ้งทั้งหมด
      - **เคส "รังผึ้งระบายความร้อน ซิตี้" ที่เคยขึ้น FAIL = expectation ผิด ไม่ใช่ระบบผิด** — ข้อมูลร้านเองแมป `รังผึ้งหม้อน้ำ`→หม้อน้ำ, `รังผึ้งแอร์`→คอยล์ร้อน (`SearchSynonym`) และ `รังผึ้งแอร์ใน` อยู่ใน alias ของคอยล์เย็น 40 ชิ้น → คำว่า "รังผึ้ง" เดี่ยว ๆ ไม่ระบุชนิดอะไหล่. เปลี่ยน golden case เป็น "ยอมรับ core ระบายความร้อนของรถรุ่นที่ถูก" + เพิ่มเคสที่ไม่กำกวม "ถังน้ำหล่อเย็นหน้ารถ ซิตี้" (ผ่าน: หม้อน้ำ Honda City อันดับ 1–2, lexical-only ให้สายกลางแอร์/ท่อยาง) → ชุดทดสอบเป็น **12/12**
      - **หมายเหตุความปลอดภัยของ threshold:** คำถามไม่ใช่สินค้าได้ maxSim 0.577 ("ร้านอยู่ที่ไหน") / 0.520 ("เปิดกี่โมง โอนเงินยังไง") → floor `SEARCH_V2_VECTOR_MIN_SIMILARITY = 0.62` กันไว้ได้ แต่เหลือ margin แค่ 0.043 — ห้ามลด floor โดยไม่วัดซ้ำ. และ `LIMIT 30` เป็นตัวกรองความเกี่ยวข้องจริง (127/941 ชิ้นผ่าน floor ที่คำค้น "ตัวทำความเย็นแอร์วีออส") ไม่ใช่ตัว threshold

  - [x] (18) Intent-gated retrieval (กันสินค้าหลุดมาตอบคำถามทั่วไป — เคส "ร้านอยู่ที่ไหน" ดันรายการคอมแอร์ Vigo + การ์ดมาด้วย):
    - **A.** ขยาย `SHOP_INFO_RE` ใน `lib/line-intent-router.ts` ครอบ "ร้านอยู่ที่ไหน/อยู่ที่ไหน/พิกัด/แผนที่/ไปยังไง/เปิดไหม/สาขา..." → route SHOP_INFO (ตอบ canned ไม่ค้น ไม่แปะการ์ด)
    - **B.** `extractLineSearchIntent` คืน `isProductQuery` — ถ้าข้อความล่าสุดไม่ใช่การหาสินค้า (ถามข้อมูลร้าน/ทักทาย/ขอบคุณ/คุยเล่น) → `isProductQuery=false`, query=null. `processLineAiReply` gate: non-product turn → **ข้าม search + ไม่แปะการ์ด + ไม่ carryover** (audit `SEARCH_SKIPPED_NON_PRODUCT`) ไม่ให้บริบทสินค้าเก่าหลุดเข้าคำตอบ
    - unit tests: processor non-product 1 + parse isProductQuery 2

  - [x] (19) Latency: ตอบให้ทันใน reply-token window (ฟรี ไม่ต้องใช้ push) — ข้อมูลจริงพบ tail หลุด 60-118 วิ จาก Gemini call ค้างชน timeout 30 วิ แล้ว rotate key สะสม:
    - **(1)** เพิ่ม `timeoutMs`+`maxKeyAttempts` ใน `generateGeminiContent`; chat calls (generate/extract/purchase-intent/faq) ใช้ `CHAT_CALL_TIMEOUT_MS=15s` + `CHAT_MAX_KEY_ATTEMPTS=3` → key ที่ค้าง fail over เร็ว ไม่เผา 30 วิ (งาน embedding/backfill ใช้ default เดิม)
    - **(2)** รัน `classifyPurchaseIntent` + `generateLineSuggestion` แบบขนาน (เริ่ม generate ล่วงหน้าใน product path) → ตัด 1 call ออกจาก critical path; ผลลัพธ์เหมือนเดิม
    - **(3)** Deadline guard: race reply gen กับเวลาที่เหลือใน token window (margin 5s); ถ้าไม่ทัน → `buildJuneDeadlineReply()` ตอบสไตล์จูน + แสดงสินค้า/การ์ดชุดเดิมครบ (ต่างแค่ถ้อยคำ) ส่งทัน reply token เสมอ + audit `AI_DEADLINE_FALLBACK`
    - unit tests: deadline fallback 1; รวม LINE suite 43+ ผ่าน
    - **(4)** 2026-06-20 — แก้เคส log จริง (ส่งรูปคอมแอร์ + พิมพ์ "24v"): vision OCR ใช้ ~31 วิ เผางบ reply-token จน `shouldUseReplyTokenDeadlineFallback` เด้ง → ข้าม search ทั้งที่ frame รวม `partType=คอมแอร์`+`24v` ได้แล้ว เลยตอบถามรถ/ปีแทน (audit `DEADLINE_FALLBACK_BEFORE_SEARCH`). แก้:
      - turn ที่มีรูป OCR (`isImageOcrTurn`) **ไม่ตัดจบก่อน search** อีกต่อไป — ค้นเสมอ แล้วส่งทาง reply token ถ้าทัน ไม่ทันจริง → PUSH ตามหลัง (กลไกเดิม `allowPushFallback`)
      - ขยาย reply-token window `replyTokenMaxAgeMs` 45s → **55s** (margin 5s ใต้เพดาน serverless 60s) ให้รูปที่ vision ช้ายังตอบทัน free token
      - "ห้ามเดา": รูปล้วนที่ OCR `confidence=LOW` → ข้าม search + ถามยืนยันสไตล์จูน (`buildJuneAskDetailsReply`, audit `AI_IMAGE_LOW_CONFIDENCE_ASK`); รูป LOW ไม่ feed fields เข้า inquiry frame (กัน frame เพี้ยน)
      - unit tests: ปรับ deadline-image test เป็น "ค้นต่อ ไม่ bail" + เพิ่ม low-confidence ask 1; processor suite 51 ผ่าน, coalesce 12 ผ่าน
    - **(4b)** 2026-06-23 — แก้เคส log จริง (ห้อง sunantha: พิมพ์ "อีซูซุเดก้า360" + ส่งรูปสายน้ำยาแอร์): ยืนยันจาก production `LineAiAuditLog` ว่า **OCR ทำงานถูก** (`IMAGE_CLASSIFIED kind=part_image confidence=HIGH`) และ search ทำงานแต่ `total=0` (ของไม่มีในสต็อกจริง). ปัญหาคือ branch `productSearch.total===0` ตก **FAQ** → ตอบ generic "ส่งรูปอะไหล่เดิมมาให้จูนช่วยเช็ก" ทั้งที่ลูกค้าเพิ่งส่งรูป → ดูเหมือน "ไม่ได้อ่านรูป". แก้:
      - เพิ่ม flag `partImageNoMatch` (รูป part_image, confidence≠LOW, search แล้ว total=0) → **ข้าม FAQ branch** + เพิ่ม forced-response branch ใหม่ใช้ `buildJunePartImageNoMatchReply()` (ใน `lib/line-ai-service.ts`) ที่ยอมรับว่าเห็นรูป[ชนิดอะไหล่] + บอกว่ายังไม่เจอในระบบ + handoff แอดมิน (audit `AI_PART_IMAGE_NO_MATCH`, set `waiting_admin`, notify bell+Telegram)
      - unit test: "part image recognized but search empty → acknowledges the part + hands off" (processor suite 52 ผ่าน)
    - **(4c)** 2026-06-23 (B1 — bound เวลาต่อ step กัน pipeline ทะลุ 60s): root ความช้าจาก log จริงคือ product turn ยิง Gemini chat call **เรียงต่อกันหลายครั้ง** (extract-intent → purchase-intent → faq/suggest) ช่วง Gemini latency spike แต่ละ call คืนที่ ~14s (ชิด timeout เดิม 15s) → ทบกันทะลุเพดาน serverless 60s → reply token หมด (PUSH ช้า) + coalesce lease หมด → job ถูกฆ่าเป็น `STALE_PROCESSING_TIMEOUT` (~126s). แก้: ลด `CHAT_CALL_TIMEOUT_MS` 15s→**8s** และ `CHAT_MAX_KEY_ATTEMPTS` 3→**2** ใน `lib/line-ai-service.ts` (กระทบ extract-intent/purchase-intent/faq/suggest/scoped-reply ทุกตัวที่ใช้ค่านี้)
      - 8s ยังพอสำหรับ latency ปกติ (~1–4s); เฉพาะช่วง spike เท่านั้นที่ call จะ abort → ตก **deterministic fallback สไตล์จูน** (สินค้า/การ์ดชุดเดิม เปลี่ยนแค่ถ้อยคำ) ที่ทันใน free reply-token window; spike กระทบทุก key พร้อมกัน → key ที่ 3 เดิมแค่เสียเวลา (2 พอสำหรับเคส 429/rotate)
      - **ไม่แตะ** vision OCR (`VISION_CALL_TIMEOUT_MS=20s/3` ใน `lib/line-image-service.ts`) — คนละ knob, การลดจะกระทบความแม่นของการอ่านรูป; เก็บไว้ประเมินแยก
      - tests: processor + faq + ai + coalesce 78 ผ่าน; ติดตามผลจริงด้วย `pipelineDurationMs` (AI_SEND_DECISION) + จำนวน `STALE_PROCESSING_TIMEOUT` 3–5 วัน
      - **(4d)** 2026-06-23 (B2a+B2b+B2c) — แก้ root ความช้า/งานซ้ำของ coalescing + เพิ่ม multi-subject (ยืนยันแผนกับเจ้าของร้านครบ):
        - **B2a — persist classification, ไม่ re-OCR:** ingest เก็บ classification เต็ม (kind/confidence/hints/partType/carBrand/carModel/year/partKind ฯลฯ ยกเว้น content/ocr) ลง job payload (`serializeClassificationForReuse`); repo `getStoredImageClassificationsByMessageRowIds()` ดึงกลับ; `buildMergedTurnInput` ใช้ in-memory cache → stored → ค่อย vision → owner re-run/cron recovery ไม่ re-OCR (vision รันครั้งเดียวต่อรูป) — ไม่แก้ schema
        - **B2b — bound owner loop, invariant OCR→search→reply:** `OWNER_LOOP_FINAL_PASS_AFTER_MS=40s` วัดจาก `receivedAt`; พ้นงบ → ข้าม debounce + force-send 1 ครั้ง (`shouldAbortBeforeSend`→false) ก่อนเพดาน 60s (กัน Vercel kill → กัน STALE 169s + cron-recovery 2 นาที); การันตีรัน OCR+search ครบก่อนเสมอ (fallback อิงผลจริง) + audit `AI_OWNER_BUDGET_FORCED_SEND`
        - **B2c — multi-subject split:** classifier (`extractLineSearchIntent`) คืน `subjects[]` เมื่อมี ≥2 ชนิดอะไหล่ต่างกัน (เกณฑ์ 1=ก: แยกตามหมวด ไม่แยกตามรุ่นรถ); `respondMultiSubject()` resolve แต่ละ subject → group ตามหมวด (distinct<2 → คืน single path), cap **3 หัวข้อ** (เกิน → N2 note), ค้น+บล็อกต่อหมวด, deterministic (ไม่ยิง AI gen ต่อหัวข้อ), pack reply ≤5 + push ส่วนเกิน; หัวข้อไม่เจอ → N1 line + notify แอดมิน **ไม่ freeze ห้อง** (เกณฑ์ จ); C1: คำว่า "แทน/เปลี่ยน" → เอาเฉพาะหัวข้อล่าสุด; กำกวม (back-ref/partType=null) ใช้ gate-ask เดิม; audit `AI_MULTI_SUBJECT`
        - เคสหัวข้อเดียว/รูปเดียวคงพฤติกรรมเดิมทุกอย่าง (รวม Option A handoff). tests: +B2a/B2b/B2c (coalesce) + subjects parser → line suite **187 ผ่าน**, build ผ่าน
      - **(4e)** 2026-06-24 (Bot DB-load guard — ดับ P2028 ที่ root) — หลัง deploy B2 ยังเจอ `P2028` ซ้ำที่ `/api/search/products/autocomplete` + webhook timeout 60s (ค้างที่ DB ช่วง ingest ก่อนถึง owner loop) ยืนยันจาก production = **DB pool หมดช่วง burst** (คนละตัวกับ B2; B2b ขัด DB call ที่ค้างกลางคันไม่ได้). สาเหตุ: bot/crawler ถล่ม autocomplete ทุก keystroke → ยิง V2 search ที่ใช้ `dbSearchTx` (transaction) แย่ง pool เล็ก (5/instance). แก้เฉพาะ path บอทใน `app/api/search/products/autocomplete/route.ts`: `isBot` → ข้าม V2/transaction ใช้ `db.product.findMany` แบบ LIKE-contains (name/code) ตรง ๆ (ถือ connection แค่ query เดียว) คืน shape เดิม; ลูกค้าจริง (UA ปกติ) ยังได้ V2 เต็ม. refactor: แยก `AUTOCOMPLETE_SELECT` + `toAutocompleteItem` ใช้ร่วม 2 path. ผล: ตัดตัวกิน pool หลัก → P2028 ลด/หาย + คืน connection ให้ LINE webhook. tsc/build ผ่าน. ตามด้วยเฝ้าดู P2028 + STALE หลัง deploy; Option B (ขยาย DB_POOL_MAX) ยังไม่ทำ รอประเมินจำนวน instance
      - **(4f)** 2026-07-10 (stale partType carryover — เคส "วาว์ลอัลติสแท้03" ตอบเป็นหม้อน้ำ) — ยืนยันจาก production `LineAiAuditLog`: เทิร์นก่อนหน้า `หม้อน้ำ commuter` ตั้ง frame `partType=หม้อน้ำ (Radiator)`. เทิร์นถัดมาลูกค้าเปลี่ยนรถ (Commuter→Altis) + พิมพ์อะไหล่ใหม่แต่สะกดเพี้ยน `วาว์ล` (=วาล์วแอร์) → คำนี้ตก `lineValueHasCustomerEvidence` → `groundedLatestPartType=null` → `reconcileInquiryFrame` สืบทอด `หม้อน้ำ` เดิม → categoryName=Radiator hard-filter → คืนหม้อน้ำ Altis 2 ชิ้น (classifier อ่านคำใหม่ถูก: consolidatedQuery="วาว์ลอัลติสแท้ 2003" ไม่ใช่หม้อน้ำ — ค่าเพี้ยนมาจาก frame carryover ล้วน ๆ). แก้:
        - **A** — `lib/chat-core/inquiry-frame.ts` `reconcileInquiryFrame`: เพิ่ม option `latestClassifierPartType`; เมื่อ **รุ่นรถเปลี่ยน** + classifier อ่าน partType ใหม่ที่ต่างจากที่ค้าง + `latest.partType` null → **ทิ้ง partType เก่า** (คืน `droppedStalePart`). Follow-up ที่พิมพ์แต่รถ ("แล้ว Vigo ล่ะ", classifier ไม่มี part ใหม่) ยังเก็บ part เดิม. wire ใน `lib/line-webhook-processor.ts` (ส่ง `classifierPartType`, log `droppedStalePartOnVehicleSwitch`+`classifierPartType` ใน INQUIRY_FRAME audit)
        - **B** — `lib/chat-core/fitment-resolve.ts`: เพิ่มคำสะกดเพี้ยน `วาว์ล/วาวล์/วาว์ลแอร์/วาวล์แอร์` → `(Expansion Valve)` (ปลอดภัย: ร้านไม่มีสินค้าเทอร์โมสตัท/วาล์วน้ำเลย — ตรวจ DB จริง `เทอร์โมสตัท`=0, `วาล์ว (Expansion Valve)`=44)
        - **อนาคต** — ถ้าเพิ่มสินค้าเทอร์โมสตัท/วาล์วน้ำ: สร้างหมวดใหม่ + เพิ่มแถว DB `CategoryAlias` (`วาล์วน้ำ`/`วาวน้ำ`/`เทอร์โมสตัท`/`thermostat`→หมวดใหม่ priority สูง) → DB alias ชนะ hardcoded อัตโนมัติ ไม่ต้องแก้โค้ด + ล้าง synonym ตาย `วาล์วน้ำ→เทอร์โมสตัท` (ชี้ 0 ชิ้น). ค่า default คำ "วาล์ว/วาว์ล" เปล่า = Expansion Valve (เจ้าของร้านยืนยัน)
        - **C (reply naming)** — `lib/chat-core/ai-service.ts` `buildLineReplyPrompt`: LLM เขียนข้อความเปิด "วาล์วน้ำของ Altis" ตามคำที่ลูกค้าพิมพ์ (เพี้ยน) ทั้งที่การ์ดเป็นวาล์วแอร์ → เพิ่มกฎ prompt: เมื่อมีรายการสินค้า ให้เรียกชื่อชนิดอะไหล่ **ตามรายการสินค้าที่พบ** เท่านั้น ห้ามเรียกตามคำลูกค้าที่อาจสะกดผิด/กำกวม
        - tests: inquiry-frame +3 (stale-part guard) → 22 ผ่าน; fitment-resolve +1 (สะกดเพี้ยน); processor suite 65 ผ่าน; ai-policy/deadline 11 ผ่าน; tsc ผ่าน
      - **(4g)** 2026-07-10 (unresolved-part → model-only drift — เคส "เช็ค เทอร์โมสตรัท Vios ปี2017" ตอบมั่ว) — ยืนยันจาก `LineAiAuditLog`: "เทอร์โมสตรัท" แมปหมวดไม่ได้ (categoryName null) + ไม่ใช่ required token (คำไทยไม่มีตัวเลข) → FTS ร่วงไปจับแค่รุ่น "Vios" → total 33, needsMoreInfo=false → โชว์อะไหล่ Vios 5 ตัวที่ไม่เกี่ยวเลย (คอมแอร์/โบลเวอร์/หม้อน้ำ/คอยล์เย็น). แก้ (Option A — anchor คำอะไหล่):
        - `lib/chat-core/product-search-bridge.ts`: เพิ่ม input `fitmentPartHeadNoun` — เมื่อมี + ไม่มี category filter → บังคับเป็น required token (contains-match ชื่อ/alias/fitment) และคงไว้ตลอด did-you-mean retry; **ต่างจาก `accessoryHeadNoun` คือไม่มี broaden-on-empty** → ว่างก็ว่าง (needsMoreInfo, reason `SEARCHED_FITMENT_PART_NO_MATCH`)
        - `lib/line-webhook-processor.ts`: คำนวณ `unresolvedFitmentPartHeadNoun` (no category + ไม่ใช่ accessory + guardedSearchIntent.partType + ไม่ใช่ catch-all "อะไหล่…") ส่งเข้า bridge
        - `lib/messenger/messenger-webhook-processor.ts`: `resolveMessengerFitmentHints` คืน `fitmentPartHeadNoun` ด้วย (parity LINE) แล้วส่งเข้า bridge
        - ผล: "เทอร์โมสตรัท Vios" → 0 → ตอบ "ยังไม่มีตัวนี้โดยตรง" ไม่โชว์การ์ดมั่ว; ของที่มีจริงในหมวด "อะไหล่อื่นๆ" (เทอร์โมเซ็นเซอร์แอร์) ยัง match ผ่าน contains; พาร์ทที่แมปหมวดได้ไม่กระทบ
        - tests: bridge +4 (anchor/no-broaden/category-ignore/retry) → 19 ผ่าน; processor 70 + search-guards ผ่าน; messenger (มี module-mock flag) 4 ผ่าน; tsc ผ่าน
      - **(4h)** 2026-07-25 (named-but-unreadable vehicle → ตอบด้วยรถคันเก่า — เคส "พัดลมโบซิ้ตี้ปี12" ตอบโบลเวอร์ ISUZU DECA) — ยืนยันจาก `LineAiAuditLog` conv `cmq6o1g1u`: ลูกค้าถาม "อีซูซุเดก้า270" (02:59) แล้ว 40 นาทีต่อมาพิมพ์ "พัดลมโบซิ้ตี้ปี12" (Honda City เขียนติดกัน + วรรณยุกต์เพี้ยน) → classifier อ่านรุ่นไม่ออก เลย merge "Deca" จาก history; `requiredTokens=[]` ⇒ `guardChatSearchIntent` ไม่ ground `carModel` ([search-guards.ts](lib/chat-core/search-guards.ts) เจตนาเดิม) + `latestHasProductSpecificity=false` ⇒ `droppedVehicleCarryover`/`droppedStaleModelYear` ไม่ทำงาน → frame = `{พัดลม, Isuzu, Deca, 2012}` → ค้น "พัดลม Isuzu Deca" → โชว์ P0120 ผิดคัน. วัดความถี่จริง 30 วัน: 215 เทิร์นสินค้า, 41 topicShift, 28 carry รถ, พลาดแบบนี้ 2 เคส (อีกเคส "คอมแอร์ Mu-x" → carBrand ค้าง `Toyota` ทั้งที่ MU-X เป็น Isuzu). แก้ (Option G — ให้ classifier ชี้ provenance แล้วเรา verify เอง):
        - `lib/chat-core/ai-service.ts`: เพิ่มฟิลด์ `carMentionInLatest` ใน JSON contract + กฎ prompt — คัดลอก "คำที่ลูกค้าใช้เรียกยี่ห้อ/รุ่นรถใน **ข้อความล่าสุด**" มาตรง ๆ (สะกดผิดก็คัดตามนั้น), `null` เมื่อข้อความล่าสุดไม่เอ่ยถึงรถ (ปีอย่างเดียว/รถจาก history ไม่นับ). type เป็น **optional** → ผู้ผลิต intent อื่น (rule fast path, multi-subject, tests) ไม่ต้องแก้
        - `lib/chat-core/search-guards.ts`: `chatCarMentionOccursInLatest()` — ไม่เชื่อ LLM ลอย ๆ, ตรวจว่า mention อยู่ในข้อความล่าสุดจริง (normalize + ตัดช่องว่าง เพราะไทยเขียนติดกัน) ค่าที่ verify ไม่ผ่าน = ถือว่าไม่มี → พฤติกรรม carry-over เดิม
        - `lib/line-webhook-processor.ts`: หลัง `reconcileInquiryFrame` — เมื่อ **mention ผ่าน verify** + ไม่ใช่เทิร์นรูป + classifier ไม่ได้ทั้ง brand และ model → แปลว่า "ลูกค้าเอ่ยถึงรถในเทิร์นนี้แต่เราอ่านไม่ออก" → ทิ้ง brand/model ที่ carry มา (คง `year` ของเทิร์นนี้ไว้ ลูกค้าจะได้ไม่ต้องพิมพ์ซ้ำ) → gate `PART_ONLY` ถามยี่ห้อ/รุ่นรถ. **ไม่ผูกกับ topicShift** เพราะพลาดแบบเดียวกันเกิดได้ตอนอะไหล่เดิมแต่เปลี่ยนรถ. audit `INQUIRY_FRAME` เพิ่ม `carMentionInLatest`/`carMentionVerified`/`namedUnreadableVehicle` ไว้วัดผลหลัง deploy
        - `scripts/test-live-car-mention-in-latest.ts` (ใหม่): เล่นซ้ำเทิร์นจริงจาก prod ยิง Gemini ตัวจริง + verify — ผล 22 เคสที่มีข้อความ: verified 17 / null 5 / unverifiable 0 / **false positive 0** (เคส carry-over ที่ถูกต้องอย่าง "2007", "ราคาเท่าไร" ได้ `null` ครบ). **ผลพลอยได้**: prompt ใหม่ทำให้ Gemini อ่าน "ซิ้ตี้" ออกเป็น Honda City และ "Mu-x" เป็น Isuzu ได้เอง ⇒ 2 เคสที่พลาดกลายเป็น **ตอบถูก** ตั้งแต่ชั้น classifier ส่วน guard ในข้อ 3 เป็นตาข่ายชั้นสองเมื่ออ่านไม่ออกจริง
        - ทิศทางความผิดพลาดปลอดภัยทุกทาง: LLM ไม่กรอกฟิลด์ / กรอกมั่ว / Gemini ล่ม → ตกกลับพฤติกรรมเดิม 100%
        - tests: processor +4 (ซิ้ตี้ case / unverifiable mention / ปี-only ไม่โดนแตะ / รถที่อ่านออกใช้ปกติ) → 102 (101 ผ่าน, 1 fail **มีอยู่ก่อนแล้วบน main**), intent parser +2 → LINE suites รวม 117 (115 ผ่าน, 2 fail B2c **มีอยู่ก่อนแล้วบน main**), messenger 4 ผ่าน, tsc ผ่าน, `npm run build` ✓ 47s
        - **ยังไม่ได้ทำ (หนี้ที่รู้ตัว)**: Messenger ไม่มี inquiry frame ([messenger-webhook-processor.ts](lib/messenger/messenger-webhook-processor.ts) เรียกแค่ `guardChatSearchIntent`) → guard นี้เป็น LINE-only; `normalizeSearchText` ไม่ตัดวรรณยุกต์ (`ซิ้ตี้ ≠ ซิตี้`) ยังไม่แก้เพราะกระทบ search ทั้งระบบ ต้อง audit แยกรอบ; 3 เทสต์ที่ fail อยู่ก่อนแล้วบน main ควรตามเก็บ
      - **(4i)** 2026-08-02 (รูปอะไหล่ถูกตอบด้วยชื่ออะไหล่ของเทิร์นก่อน — เคส "จูนเห็นรูปมูเล่หน้าครัชสำหรับ Honda Cityที่ส่งมา" ทั้งที่รูปเป็นมอเตอร์ปรับอากาศ) — ยืนยันจาก production `LineAiAuditLog` + `LineAiJob.payload` conv `cmq6o1g1u` (2026-08-01): **vision อ่านรูปถูกทั้ง 3 ใบ** (`partType=มอเตอร์ปรับอากาศ` ×2, `สวิตช์กุญแจ` ×1) แต่ได้ `confidence=MEDIUM` → [line-webhook-processor.ts](lib/line-webhook-processor.ts) รับ `imageFields` เฉพาะ `HIGH` เท่านั้น ⇒ `imagePartType=null`; เทิร์นรูปล้วนไม่มี classifier ข้อความ (`classifierPartType=null`) + frame เพิ่งอัปเดตเมื่อ 74 นาทีก่อน (`SESSION_IDLE_MS=120 นาที` ⇒ `sessionStale=false`) ⇒ frame คง `{มูเล่หน้าครัช, Honda, City, 1996}` จากข้อความ "มูเล่หน้าครัชซิตี้96" (04:35) → ค้นด้วย query ขัดแย้งกันเอง `"มูเล่หน้าครัช Honda City สวิตช์กุญแจ … มอเตอร์ปรับอากาศ …"` → `total=0` → เข้า `PART_IMAGE_NO_MATCH` ซึ่งอ่าน `inquiryFrame` **ก่อนเสมอ** (สาขา fallback ที่ใช้ค่าจากรูปเป็น dead branch เพราะเทิร์นสินค้าแทบไม่เคยมี frame เป็น null) ⇒ เอ่ยชื่ออะไหล่ของเทิร์นก่อนกลับไปหาลูกค้าที่เพิ่งส่งรูปอะไหล่คนละตัว. วัดความถี่จริง 60–90 วัน: part_image ทั้งหมด 99 ครั้ง = **HIGH 94 / MEDIUM 5** (MEDIUM ~5% เกิดใน 3 ห้อง, พังจริง 1 ห้อง) ⇒ ปัญหา "เสียงดังแต่ไม่บ่อย". แก้ (Option A + C ตามที่เจ้าของร้านยืนยัน):
        - **A — ข้อความต้องบรรยายรูป ไม่ใช่ frame**: branch `PART_IMAGE_NO_MATCH` ใน [line-webhook-processor.ts](lib/line-webhook-processor.ts) ให้ `imageClassification.partType` ชนะ `inquiryFrame.partType` (ประโยคคือ "จูนเห็นรูป<subject>ที่ส่งมา" — subject ต้องเป็นของในรูป) ส่วน **รถ** ยังเอาจาก frame (รูปแทบไม่เห็นป้ายทะเบียน และในเซสชันเดียวกันคือรถคันเดิม); `auditPayload.partType` เรียงลำดับตามกันเพื่อให้วัดผลตรงกับที่ลูกค้าเห็น
        - **A (ต่อ) — เว้นวรรคชื่อรถอังกฤษ**: `buildJunePartImageNoMatchReply()` ใน [ai-service.ts](lib/chat-core/ai-service.ts) เดิมต่อ `${part}สำหรับ ${car}` ชน `ที่ส่งมา` ตรง ๆ → เรนเดอร์เป็น `Cityที่ส่งมา` (เห็นในข้อความจริงใน DB); ใส่ช่องว่างเฉพาะเมื่อ subject ลงท้ายด้วยอักษรละติน/ตัวเลข/`)` — ลงท้ายด้วยไทยไม่ใส่ (ไทยไม่เว้นวรรคระหว่างคำ)
        - **C — guard คำขัดแย้งระหว่างรูปกับ frame**: เพิ่ม `isSamePartSubject()` ใน [inquiry-frame.ts](lib/chat-core/inquiry-frame.ts) (เทียบบน `normalizeSearchText` + containment สองทาง ⇒ `"หน้าครัช"` กับ `"หน้าครัช (Compressor Clutch)"` = เรื่องเดียวกัน, `"มูเล่หน้าครัช"` กับ `"มอเตอร์ปรับอากาศ"` = คนละเรื่อง). ในเทิร์น **รูปล้วน** ที่ `kind=part_image` + `confidence≠LOW` + vision อ่าน `partType` ได้ + ต่างจาก part ที่ carry มา → ใช้ part จากรูปแทนสำหรับ **เทิร์นนี้เท่านั้น** (คงช่องรถไว้) + `frameTopicShift=true` ให้ `consolidatedQuery` สร้างใหม่จาก subject ที่ถูก + audit `IMAGE_SUBJECT_OVERRIDES_FRAME {carriedPartType, imagePartType, confidence}`
        - **เจตนา: ไม่ persist** — frame ถูกบันทึกไปก่อนหน้าแล้ว และ MEDIUM ยังไม่น่าเชื่อพอจะเป็น subject ประจำเซสชัน (กฎ "ห้ามเดา" เดิม); กติกา `HIGH เท่านั้นที่เขียน frame` ไม่ถูกแตะ ⇒ 94/99 เคสเดิมพฤติกรรมเหมือนเดิมทุกประการ
        - tests: processor +2 (เคสขัดแย้ง = reproduce เทิร์นจริง 2026-08-01 / เคสรูปตรงกับ frame ต้องไม่ override) → **108 ผ่าน**; ตรวจแล้วว่าเทสต์ใหม่ **fail จริงบนโค้ดก่อนแก้** (stash แล้วรันซ้ำ) ไม่ใช่เทสต์ลอย; LINE suites รวม 156 ผ่าน, messenger 4 ผ่าน, `tsc --noEmit` ผ่าน
        - **Messenger ไม่กระทบ** — ไม่มี inquiry frame และไม่ได้ใช้ `buildJunePartImageNoMatchReply()` (หนี้เดิมข้อ 4h ยังอยู่)
        - **B — evidence-based promotion (ทำแล้ว ตามที่เจ้าของร้านสั่งต่อ)**: C แก้เฉพาะเทิร์นที่มีรูป เทิร์นถัดไปที่ลูกค้าพิมพ์สั้น ๆ ("ปี 12"/"ราคาเท่าไหร่") ยังกลับไปใช้ part เก่าใน frame. เพิ่มขั้น **promote** หลัง search ใน [line-webhook-processor.ts](lib/line-webhook-processor.ts): เขียน part จากรูปลง frame ถาวร **ก็ต่อเมื่อ catalog ยืนยัน** ครบทุกข้อ — guard C ยิง **และ** `fitmentFilters.categoryName` resolve ได้ (พิสูจน์ว่าแถวที่เจอมาจาก "คำอะไหล่" ไม่ใช่ "รถ" ล้วน ๆ) **และ** `productSearch.result.total > 0` **และ** `products.length > 0` หลังผ่าน guard ครบ (`vehicleUnresolvedGuard`/`weakCategoryMatchGuard`) **และ** ไม่ใช่คำกว้าง (`isBroadChatPartType`). ขาดข้อใดข้อหนึ่ง = ไม่เขียน frame คงเดิม 100%. audit `IMAGE_SUBJECT_PROMOTED_TO_FRAME {carriedPartType, imagePartType, categoryName, shownCount}`
          - **ไม่เชื่อ `confidence` ของ Gemini เป็นเกณฑ์ promote** — ใช้หลักฐานจากข้อมูลร้านแทน ตรงกฎ "ห้ามเดา"; เหตุผลที่ต้องมี `categoryName`: ถ้าแถวที่เจอมาจากรถอย่างเดียว การ promote จะฝัง part ที่ vision อ่านผิดไว้ถาวร ซึ่ง **แย่กว่า** การปนเปื้อนแค่เทิร์นเดียวของ C
          - เป็น **frame write รอบสองรอบเดียวในเทิร์น** และเข้าถึงได้เฉพาะเมื่อ guard C ยิงเท่านั้น ⇒ เส้นทางอื่นทั้งหมด (รวม HIGH 94/99 เคส) เขียน frame ครั้งเดียวเหมือนเดิม (มีเทสต์ยืนยันจำนวนครั้งที่เขียน)
          - **ข้อจำกัดที่รู้ตัว**: เคสจริง 2026-08-01 จะ **ไม่ถูก promote** เพราะ catalog ไม่มี `มอเตอร์ปรับอากาศ/actuator/air mix` เลยสักรายการ (เช็กแล้ว = 0) ⇒ B จะเริ่มมีผลก็ต่อเมื่อร้านลงสต็อกหมวดที่ vision อ่านออก หรือ MEDIUM เกิดถี่ขึ้น
        - **golden tests (12 เคส ครอบทุกกิ่งการตัดสินใจ)** — promote สำเร็จ (frame ใหม่ + เขียน 2 ครั้งพอดี) / search ว่าง (เคสจริง 2026-08-01) / เจอแต่ไม่มี category (vehicle-only match) / โดน relevance gate / คำกว้าง / รูปตรงกับ frame (ไม่ override) / HIGH (เส้นทางเดิมเป็นเจ้าของ frame) / LOW (ไม่ค้น) / สลิป / vision ไม่ได้ partType / รูปแรกไม่มี frame เดิม / **burst รูป+ข้อความ → ข้อความต้องเป็นคนขับ** (เทสต์นี้อยู่ใน `line-webhook-coalesce.test.ts` เพราะเฉพาะ harness นั้นที่ merge burst จริง — เพิ่ม knob `imageConfidence` ให้ harness) + คู่กันคือ burst รูปล้วนต้อง override ได้
          - ยืนยัน non-vacuous: ปิด B ชั่วคราว → เทสต์ promote **fail** ส่วนเทสต์ "ต้องไม่ promote" ยัง pass (เป็นตาข่ายกันการขยายเงื่อนไขในอนาคต ไม่ใช่ข้อพิสูจน์ B)
        - tests: LINE suites รวม **223 ผ่าน** (processor 119 / coalesce 14 / อื่น ๆ), messenger 4 ผ่าน, `tsc --noEmit` ผ่าน, `npm run build` ผ่าน, mojibake ผ่าน
        - เฝ้าดูหลัง deploy 3–5 วัน: `IMAGE_SUBJECT_OVERRIDES_FRAME` เทียบ `IMAGE_SUBJECT_PROMOTED_TO_FRAME` เทียบ `AI_PART_IMAGE_NO_MATCH` — override ยิงบ่อยผิดปกติ = vision อ่าน partType เพี้ยน ต้องกลับมาดู threshold; promote ยิงแล้วลูกค้าถูกตอบผิดหมวดในเทิร์นถัดไป = เกณฑ์ corroboration ยังหลวมไป

      - **(4j)** 2026-08-02 (รหัสรุ่นแบบตัวอักษรทำให้ผลค้นถูกกลืนทิ้ง — เคส "โบลเวอร์พัดลมแอร์ jazz ge มีสต็อกไหมค่ะ") — ยืนยันจาก production `LineAiAuditLog` conv `cmsa9w2pq`: **AI ค้นแล้วและเจอด้วย** (`total=6` แล้ว `total=12`) แต่ `carModelName: null` → [line-webhook-processor.ts](lib/line-webhook-processor.ts) `vehicleUnresolvedGuard` ยิง → กลืนผลทิ้งแล้วส่งแอดมิน (`AI_VEHICLE_UNRESOLVED_HANDOFF`). พิสูจน์ A/B ในห้องเดียวกัน: เทิร์นถัดมาแอดมินพิมพ์เหมือนเดิมแต่ตัด "ge" ออก → `carModelName: "Jazz"` → โชว์การ์ด 4 รายการทันที. สินค้ามีจริง (`P0119 โบลเวอร์มอเตอร์ HONDA CITY 09-13 / Jazz 08-13`). ต้นเหตุ: `SAFE_MODEL_QUALIFIER_RE` ใน [fitment-resolve.ts](lib/chat-core/fitment-resolve.ts) รับ qualifier ได้เฉพาะ **รหัสตัวเลข** (`G3`, `Gen 3`, `2.0`) แต่ Honda/Nissan/Mitsubishi ในไทยใช้ **รหัสตัวอักษร** (Jazz GE/GK, Civic FD/FB, Navara D40, Triton KA4) — รันตัว resolver จริงกับ lookup จริง 1,486 spellings ยืนยัน `Jazz GE → NULL` ขณะที่ `CR-V G3 → crv (g3)` ผ่าน. ความถี่ 120 วัน: `AI_VEHICLE_UNRESOLVED_HANDOFF` 9 ครั้ง = Jazz GE ×3 (เคสนี้), CR-V G3 ×3 (**ปิดไปแล้ว** โดย `9091eeb` 2026-07-17 วันเดียวกับที่เกิด), Hilux Tiger D4D ×2 (**ยังไม่ยืนยัน** อาการเดียวกันคนละสาเหตุ ต้องเจาะแยก), รถตัดอ้อย ×1 (guard ถูกต้อง). แก้ A+B ตามที่เจ้าของร้านสั่ง:
        - **research แบบอิงหลักฐาน 3 ทาง** — ขุดชื่อสินค้า 5,000 รายการ + ข้อความลูกค้า 5,000 ข้อความ หา pattern "ชื่อรุ่น + รหัส" ได้ 35 combination แล้วเทียบกับตลาดไทย (Wikipedia / paultan / WapCar / Mitsubishi service manual / BoxzaRacing) **Toyota ถูกตัดออกทั้งหมดโดยตั้งใจ** — หลักฐานในข้อมูลร้าน = 0 และลูกค้าไทยเรียก Toyota ด้วย "ปี" ไม่ใช่รหัสตัวถัง
        - **B — ขยาย qualifier grammar**: เพิ่ม `MODEL_GENERATION_CODES` เป็น **closed vocabulary** 60 รหัส (pattern กว้างอย่าง `[a-z]{2}\d?` จะกลืนชื่อรุ่นจริงอย่าง MG HS / Lexus IS) + ต่อท้ายด้วยตัวเลข 0-2 หลักเพื่อรองรับ GM2/GM6/GN2; ประกอบ `SAFE_MODEL_QUALIFIER_RE` ใหม่จาก `QUALIFIER_ATOM` เดียวกันทั้ง numeric และ letter path
        - **B — guard ที่ดูแลตัวเอง**: qualifier ที่ **เป็นชื่อรุ่นจริงใน lookup** จะถูกปฏิเสธ (`lookup.has(normalizeModelSpelling(qualifier))`) — "D-Max TFR" ต้องไม่ยุบเป็น D-Max เพราะ TFR เป็นรุ่น Isuzu จริง; รุ่นที่เพิ่มเข้า master data ภายหลังได้รับการป้องกันอัตโนมัติโดยไม่ต้องแก้ allowlist. ตรวจ collision จริงกับ DB แล้ว: มีแค่ `tfr` ตัวเดียว
        - **ผลของ B อย่างเดียว**: Tier 1 (จากข้อมูลร้านเอง) **31/31 ผ่าน**, Tier 2 (research) **44/56 ผ่าน** — ที่เหลือคือรหัส Toyota ที่ตั้งใจไม่รับ + `d-max tfr` ที่ guard ปฏิเสธถูกต้อง
        - **A — seed synonym**: [scripts/seed-car-model-generation-aliases.ts](scripts/seed-car-model-generation-aliases.ts) (ใหม่, `npm run seed:car-model-generation-aliases`) เติมสะกด "รุ่น+รหัส" เข้าคลัสเตอร์ `SearchSynonym` เดิม — idempotent, additive อย่างเดียว, cluster ที่ไม่มีจะรายงานแล้วข้าม (ไม่สร้างเอง เพราะเป็น master data ของร้าน), dry-run เป็น default
        - **A — ข้อค้นพบระหว่างทำ (สำคัญ)**: `MAX_SYNONYMS_PER_TERM = 10` ไม่ใช่แค่คำแนะนำ — [search-synonyms actions.ts](app/admin/(protected)/master/search-synonyms/actions.ts) validate ด้วย Zod (แถวที่เกินจะ **บันทึกจากหน้าแอดมินไม่ได้อีก**) และ `expandQueryTokens` ตัดคลัสเตอร์ที่ `MAX_SYNONYMS_PER_TERM + 1` (สะกดส่วนเกินหลุดจาก product-search expansion — แต่ **ไม่กระทบ resolver ของแชท** ซึ่งไม่มี cap). script จึง **ข้ามคลัสเตอร์ที่จะเกิน cap เป็น default** และมี `--allow-over-cap` ไว้ override
        - **A — ผลจริง**: เขียนแล้ว **36 สะกด / 23 คลัสเตอร์** (Almera, BRV, BT-50, Colorado, CRV, CX-5, Everest, Freed, Frontier, H-1, HRV, Lancer, March, Mazda2, Mobilio, Note, NP300, Swift, Sylphy, Teana, TFR, Urvan, X-Trail); **ข้าม 9 คลัสเตอร์ที่จะเกิน cap** = City, Civic, D-Max, Jazz, Mazda3, Navara, Pajero Sport, Ranger, Triton — **ไม่กระทบการแก้บั๊ก** เพราะ B ครอบทุกตัวนี้อยู่แล้ว (`jazz ge → jazz` ✅). หมายเหตุ: 5 ใน 9 (Jazz 11, Navara 14, Triton 16, Ranger 12, D-Max 30) **เกิน cap อยู่ก่อนแล้ว** จาก seed เดิม — cap บังคับเฉพาะตอนบันทึกผ่าน UI
        - **ค้างรอตัดสินใจ**: (1) จะเขียน 9 คลัสเตอร์ที่เกิน cap ไหม (2) จะขยาย `MAX_SYNONYMS_PER_TERM` ไหม — กระทบ expansion ของ search ทั้งระบบ ต้องประเมินแยก (3) 4 คำที่ไม่ใช่รหัสรุ่นที่เจอในชื่อสินค้า: `accord v6` (เครื่องยนต์) / `ranger xl` (trim) / `navara np` (พิมพ์ตกจาก NP300) / `brio at` (เกียร์ออโต้) — ยังไม่เพิ่ม
        - **golden tests 5 ชุดใน [line-fitment-resolve.test.ts](lib/__tests__/line-fitment-resolve.test.ts)** — รหัสที่ลูกค้าไทยพูดจริง 62 เคส / รหัสที่เป็นชื่อรุ่นจริงต้องถูกปฏิเสธ (D-Max TFR) / ชื่อรุ่นที่หน้าตาเหมือนรหัสต้องชนะ (MG HS, MG GT, MG ZS) / รหัส Toyota ต้องไม่ถูกรับ / คำทั่วไปหลังชื่อรุ่นยังถูกปฏิเสธ (Ranger XL, Accord V6, "Civic turbo diesel")
        - tests: LINE suites รวม **225 ผ่าน**, messenger 4 ผ่าน, `tsc --noEmit` ผ่าน, `npm run build` ผ่าน, mojibake ผ่าน
        - เฝ้าดูหลัง deploy: `AI_VEHICLE_UNRESOLVED_HANDOFF` ควรเหลือเฉพาะเคสที่ลูกค้าเอ่ยรถที่อ่านไม่ออกจริง ๆ (เช่น "รถตัดอ้อย") — ถ้ายังเจอชื่อรุ่น+รหัสโผล่อีก แปลว่ามีรหัสที่ยังไม่อยู่ใน `MODEL_GENERATION_CODES`

      - **(4k)** 2026-08-02 (ตัวกรองปีไม่ผูกกับรุ่นรถ + ข้อความตอบไม่ตรงกับการ์ด — 2 บั๊กที่เจอจากเคสโบลเวอร์ Jazz) — ยืนยันด้วย A/B บนข้อมูล production จริงทั้งคู่:
        - **① ตัวกรองปีไม่ correlate กับรุ่นรถใน path ที่ใช้ id (บั๊กจริง)** — หน้าแอดมินกรอง `โบลเวอร์ + Honda + Jazz + ปี 2010` ได้ 4 รายการ แต่ตรงจริงแค่ 1. เหตุ: [product-search.ts](lib/product-search.ts) ใส่ตัวกรองรุ่นเป็น `carModels: { some: { carModelId } }` และตัวกรองปีเป็น `carModels: { some: ... }` **อีกชุดหนึ่ง** ⇒ EXISTS 2 อันอิสระ สินค้าผ่านได้เมื่อ "แถวหนึ่งตรงรุ่น + อีกแถวหนึ่งตรงปี". P0109/P0419 (โบลเวอร์ Toyota Vigo) มีแถว Jazz เริ่ม **2014** แต่ผ่านปี 2010 ด้วยแถว Vigo (2005-2014) / Altis (2008-2017) ของตัวเอง. โค้ดเขียนกำกับไว้เองว่า `carModelId`/`carBrandId` "keep their original uncorrelated behaviour" ตอนทำ Option A รอบก่อน — path ที่ค้นด้วย **ชื่อ** (แชท) correlate แล้ว เหลือแต่ path ของ dropdown. แก้:
          - เพิ่ม `buildCorrelatedIdVehicleYearSome()` (export ไว้เทสต์) — คืน predicate เดียวที่บังคับให้ **แถว fitment เดียวกัน** ตรงทั้งรุ่น/ยี่ห้อ (จาก id) และครอบคลุมปี (รับทั้ง `fitmentYear` เดี่ยวและช่วง `yearMin`/`yearMax`); ใส่เป็น AND **เพิ่ม** จากของเดิม ⇒ แคบลงอย่างเดียว ตัดได้เฉพาะแถวที่ match คนละแถวเท่านั้น ไม่มีทางทำให้ผลกว้างขึ้น
          - V2 SQL path: ย้ายการคำนวณ `yearMinRange`/`yearMaxRange` ขึ้นก่อน แล้วฝังเงื่อนไขปีเข้าไปใน EXISTS ของ `carModelIdClause` / `carBrandIdClause` โดยตรง (ถ้ามี `carModelId` แล้ว จะไม่ใส่ซ้ำใน brand clause)
          - **ยืนยันกับข้อมูลจริง** (รันผ่าน predicate ตัวจริงและ `searchProductIdsV2` ตัวจริง ได้ผลตรงกันทั้ง 2 path): ปี 2010→2010 **4 → 2**, ปี 2011→2011 **4 → 2**, ปี 2015→2015 → 3 (P0119 ที่จบ 2013 หลุดออกถูกต้อง, P0109/P0419 ที่เริ่ม 2014 เข้ามาถูกต้อง), ไม่กรองปี → 4 เท่าเดิม
          - **ที่ยังเหลือ (ข้อ ② ยังไม่ทำ)**: 2010→2010 ได้ 2 ไม่ใช่ 1 เพราะ P0240 มีแถว Jazz ซ้ำ `2003–null (DIRECT)` คู่กับ `2003–2007 (COMPATIBLE)` — ปลายเปิดจึงกินทุกปี. ขอบเขต: `ProductFitment` 1,967 แถว → ปลายเปิด 248 แถว, ไม่มีทั้งต้น-ปลาย 411 แถว, และ **40 สินค้ามีทั้งแถวปลายเปิดและแถวมีปลายสำหรับรุ่นเดียวกัน** (P0242/P0243/P0249/P0252/P0315/P0328 ฯลฯ) เป็นงานข้อมูล รอเจ้าของร้านตัดสิน
          - golden tests 7 เคสใน [product-search-vehicle-year-correlation.test.ts](lib/__tests__/product-search-vehicle-year-correlation.test.ts) — model+ช่วงปี / brand+ช่วงปี / model ชนะ brand / ปีเดี่ยว / ปีเดี่ยว+ช่วงพร้อมกัน / ไม่มีปี = ไม่เพิ่มเงื่อนไข / ไม่มี id scope = ไม่ยุ่ง (ปล่อยให้ path ชื่อทำงานเหมือนเดิม)
        - **⑤ ข้อความตอบไม่ตรงกับการ์ดสินค้า (บั๊กจริง)** — เทิร์น "โบลเวอร์พัดลมแอร์ jazz" ส่งการ์ด 4 ใบแต่ข้อความลิสต์แค่ 2. เหตุ: การ์ดสร้าง deterministic จาก `products` ส่วนข้อความมาจาก Gemini — prompt สั่งเรื่อง **ลำดับ** ไว้แล้ว ("ต้องตรงกับการ์ดสินค้าที่ส่งคู่กัน") แต่ **ไม่เคยสั่งเรื่องความครบ** โมเดลเห็นชื่อ "Toyota Hilux Vigo" ในคำถามเรื่อง Jazz เลยตัดทิ้งเอง. ความถี่ 120 วัน: 245 เทิร์นที่มีสินค้า → ข้อความน้อยกว่าการ์ด **8 ครั้ง (3.3%)** หนักสุด cards=5 / text=1. แก้:
          - **A (prompt)**: เพิ่มกฎ "ต้องแสดงครบทุกรายการ ห้ามคัดออกเอง แม้จะดูไม่ตรงรุ่นรถ" คู่กับกฎลำดับเดิม — การตัดสินว่าเข้ากันไหมเป็นหน้าที่ร้าน ไม่ใช่โมเดล
          - **B (guard บังคับได้จริง)**: `chatReplyListsEveryProduct()` (export ไว้เทสต์) ตรวจว่ารหัสสินค้าทุกตัวปรากฏในข้อความที่ Gemini คืนมา (สินค้าไม่มีรหัส → เทียบด้วยชื่อ) ถ้าขาด → ตกกลับ `buildConservativeChatSuggestion` ที่ลิสต์ครบเสมอ + เขียน `reasoningSummary` ว่า "omitted matched products" ไว้ให้วัดอัตราการเกิดได้จาก `LineAiSuggestion`
          - ได้ทั้ง LINE และ Messenger เพราะใช้ `generateChatSuggestion` ร่วมกัน
          - golden tests 4 ชุดใน [chat-reply-product-completeness.test.ts](lib/__tests__/chat-reply-product-completeness.test.ts) — ตัวตรวจความครบ (รวมเคสจริง 2 ใน 4, ไม่มีสินค้า, ตัวพิมพ์เล็ก-ใหญ่, สินค้าไม่มีรหัส) / reply ที่ตัดรายการ → fallback ลิสต์ครบ + บันทึกเหตุผล / reply ที่ครบ → ใช้สำนวนจูนตามเดิม / prompt มีทั้งกฎครบและกฎลำดับ
        - tests: product-search suites **45 ผ่าน**, LINE suites **245 ผ่าน**, messenger 4 ผ่าน, `tsc --noEmit` ผ่าน, `npm run build` ผ่าน, mojibake ผ่าน
        - **ยังไม่ทำ (รอเจ้าของร้าน)**: ② เติม `yearEnd` ให้แถวปลายเปิด 40 สินค้า · ③ ปรับ UX ช่องปีให้ชัดว่าเป็น "ช่วงที่ทับซ้อน" ไม่ใช่ "ปีเดียว" · ④ ทบทวน tag `Honda/Jazz 2014–` ของ P0109/P0419 ว่าเข้ากันจริงหรือกรอกผิด (รูปสินค้าของร้านเองระบุแต่ Toyota)

      - **(4l)** 2026-08-02 (ลูกค้าใหม่ทักว่า "สอบถามอะไหล่รถครับ" แล้วโดน freeze ห้องทันที) — ยืนยันจาก production conv `cmsbfjzlo`: `AI_SEND_DECISION reason=BROAD_PART_TYPE_HANDOFF` → `AI_UNCERTAIN_PRODUCT_HANDOFF reason=BROAD_PART_TYPE`. คำว่า "อะไหล่รถ" ตรง pattern `/อะไหล่\s*รถ/` ใน `isBroadChatPartType` → บังคับเป็น handoff + `waiting_admin` + notify แอดมิน ทั้งที่ลูกค้าแค่ยังไม่ได้บอกว่าต้องการอะไร (เป็น **จุดเริ่มบทสนทนา** ไม่ใช่ทางตัน). แก้ตามที่เจ้าของร้านสั่ง:
        - `CHAT_BROAD_PART_INQUIRY_ASK_REPLY` ใหม่ใน [search-gate.ts](lib/chat-core/search-gate.ts) — ข้อความถาม 3 อย่าง (ยี่ห้อ/รุ่นรถ · อะไหล่ที่ต้องการ · รูปอะไหล่เก่า) ตามที่เจ้าของร้านกำหนดคำมาเป๊ะ
        - branch ใหม่ `broadPartInquiryOpener` ใน [line-webhook-processor.ts](lib/line-webhook-processor.ts): `handoff: false` → **ไม่ freeze ห้อง ไม่ notify แอดมิน** AI ทำงานต่อรอคำตอบ; audit `AI_BROAD_PART_TYPE_ASK`
        - **เงื่อนไข "ต้องไม่มีแชทเก่าค้าง"** (ตามที่เจ้าของร้านกำชับ): ใช้ข้อความนี้เฉพาะเมื่อ frame ว่างจริง — `hasCarriedInquiryContext` = มี `partType || carBrand || carModel || year` ค้างอยู่หรือไม่ (คือ **หมวดหมู่อะไหล่ / รุ่นรถ / ปีรถ**). ถ้ามี context เดิม = เป็น follow-up กลางบทสนทนา → คง handoff เดิมไว้ ไม่ถามซ้ำสิ่งที่ลูกค้าบอกไปแล้ว
        - **ครอบ 2 ประตูที่พาเข้ามา** (ค้นพบจากการยิง LLM จริง): classifier จริงจัดข้อความแบบนี้เป็น `product` (→ gate `BROAD_PART_TYPE`) **หรือ** `stock_availability` ("มีอะไหล่แอร์ไหมครับ", "หาอะไหล่แอร์ครับ" → `stockAvailabilityDirect` ซึ่ง freeze เหมือนกัน) — ถ้าดักแค่ประตูเดียวลูกค้าครึ่งหนึ่งยังโดน freeze อยู่ดี branch ใหม่จึงวางไว้ **ก่อน** stock-availability handoff และรับทั้งสองทาง
        - **ไม่แตะ** "มีของไหม" ล้วนที่ไม่มีคำว่าอะไหล่เลย (เช่น "ที่ร้านมีของใช้ไหมคัฟ") — ไม่ใช่ broad opener ยังส่งแอดมินยืนยันสต็อกเหมือนเดิมตามนโยบายร้าน
        - **golden tests (unit) 6 เคส** ใน [line-webhook-processor.test.ts](lib/__tests__/line-webhook-processor.test.ts) — opener ไม่มี context → ถาม 3 ข้อ + ไม่ freeze + ไม่ notify + ไม่ค้น · คำเดียวกันกลางบทสนทนา → handoff เดิม · มีแค่ **ปีรถ** ค้างก็นับเป็น context · opener แบบ "หาอะไหล่แอร์" · opener ที่ LLM จัดเป็น stock_availability → ถาม 3 ข้อ ไม่ freeze · "มีของไหม" ล้วน → handoff เดิม
        - **live test ยิง Gemini จริง**: [scripts/test-live-broad-part-inquiry.ts](scripts/test-live-broad-part-inquiry.ts) (`npm run test:live-broad-part-inquiry`) เล่นข้อความจริง 10 แบบผ่าน classifier ตัวจริงแล้วเช็กปลายทางที่ processor จะเลือก — **10/10 ผ่าน**. รอบแรกได้ 5/10 ซึ่งเป็นตัวที่ทำให้เจอว่าประตู `stock_availability` ยัง freeze อยู่ (unit test ที่ mock classifier มองไม่เห็นเคสนี้)
        - tests: LINE suites รวม **251 ผ่าน**, messenger 4 ผ่าน, `tsc --noEmit` ผ่าน, `npm run build` ผ่าน, mojibake ผ่าน
        - **หมายเหตุที่ยังไม่ทำ**: "สอบถามระบบแอร์หน่อยครับ" classifier จัดเป็น `general_faq` → ไป FAQ ก่อน ไม่เข้า branch นี้ ปล่อยไว้เพราะกำกวมจริง (อาจถามเรื่องบริการแอร์ ไม่ใช่ซื้ออะไหล่)

      - **(4m)** 2026-08-02 (③a — กรอกช่องปีเดียว = ปีนั้นปีเดียว ทั้งหน้าบ้านและหลังบ้าน) — หลัง deploy (4k) เจ้าของร้านกรองซ้ำแล้วยังได้ 4 รายการ ตรวจ DB จริงระดับแถวแล้วพบว่า**ไม่ใช่ deploy ไม่ติดและไม่ใช่ ① พัง**: P0109/P0419 มีแถว `Jazz 2014–null` ซึ่ง **ทับซ้อนกับช่วง `[2010, ∞)` จริง** เพราะหน้าจอกรอกแค่ช่องซ้าย (chip = `ปีรถ ≥ 2,010`) — ① เปลี่ยนผลเฉพาะตอนระบุปีครบสองด้าน (2010–2010: 4 → 2). ต้นเหตุที่ลูกค้าเข้าใจผิดคือ **ความหมายของตัวกรอง** ไม่ใช่ตัว query. แก้:
        - [lib/car-year-range.ts](lib/car-year-range.ts) (ใหม่) — `resolveCarYearRangeFilter()` + `resolveCarYearRangeFilterStrings()`: กรอกด้านเดียว → มิเรอร์เป็นปีเดียวกันทั้งสองด้าน, กรอกครบ/ไม่กรอกเลย → ปล่อยผ่าน. **ทำสองทางแบบสมมาตร** (กรอกเฉพาะช่องขวาก็เป็นปีเดียว) ไม่งั้นฟอร์มเดียวกันจะอ่านได้สองความหมาย. ช่วงกลับด้าน (2015–2010) ปล่อยตามที่พิมพ์ ไม่สลับให้เงียบ ๆ เพราะจะกลบ typo ของคนกรอก
        - ทำที่ **ชั้น parse** ทั้งสองฝั่ง เพื่อให้ chip ที่แสดงกับ query ที่รันตรงกันเสมอ และ URL ที่พิมพ์เอง/bookmark ได้กติกาเดียวกับฟอร์ม: หลังบ้าน [admin-product-filter-params.ts](lib/admin-product-filter-params.ts) (ครอบทั้ง `/admin/products`, `/admin/products/search`, export CSV/Excel, product report เพราะใช้ parser ตัวเดียวกัน) · หน้าบ้าน [app/products/page.tsx](app/products/page.tsx) + backstop ที่ [storefront-product-search.ts](lib/storefront-product-search.ts) (เผื่อ server action ที่ client เรียกตรง)
        - UI — **เติมค่าให้เห็นกับตาตั้งแต่ตอนกรอก ไม่ใช่ไปเซอร์ไพรส์ตอนกดค้นหา** (เจ้าของร้านขอเพิ่มรอบสอง): [ProductFilterForm.tsx](app/admin/(protected)/products/ProductFilterForm.tsx) `onBlur` ของช่องปี → ถ้าอีกช่องว่างให้เติมค่าเดียวกันลงไปเลย (หน้าจอจึงเป็น `2010 – 2010`) ทำทั้งสองทาง · [ProductFilterPanel.tsx](components/shared/ProductFilterPanel.tsx) commit แล้ว set ค่ากลับเข้าช่องทันที + ข้อความ "กรอกช่องเดียว = ค้นปีนั้นปีเดียว" · chip ทั้ง 2 หน้าแอดมินแสดง **"ปีรถ: 2010–2010"** เต็มช่วง (ไม่ยุบเหลือปีเดียว) เพื่อให้เห็นช่วงที่ค้นจริง
        - **ผลจริงกับข้อมูล production** (รันผ่าน parser ตัวจริง + predicate ตัวจริง): `?yearMin=2010` (URL ในรูป) → parsed `[2010..2010]` → **2 รายการ (P0119, P0240)** จากเดิม 4 · `?yearMax=2015` → `[2015..2015]` → 3 · `?yearMin=2010&yearMax=2015` → ช่วงจริง → 4 · ไม่กรอกปี → 4
        - **ผลพลอยได้**: `HeroFitmentFinder` ตั้งเฉพาะ `yearMin` เมื่อลูกค้าเลือกปีเดียวอยู่แล้ว (คอมเมนต์ในไฟล์ระบุเจตนานี้ตรง ๆ) — เดิมกลายเป็น "ตั้งแต่ปีนั้นขึ้นไป" ตอนนี้ตรงเจตนาเดิมพอดี
        - **golden tests 6 ชุด** ใน [car-year-range.test.ts](lib/__tests__/car-year-range.test.ts) — ช่องซ้ายอย่างเดียว / ช่องขวาอย่างเดียว / ช่วงจริงไม่ถูกแตะ (รวมช่วงกลับด้าน) / ไม่กรอกเลย / รูปแบบ string + ตัด whitespace / parser แอดมินใช้กติกาตั้งแต่ตอน parse
        - **เก็บกวาดที่เจอระหว่างทาง**: 2 เทสต์ใน [admin-product-filter-params.test.ts](lib/__tests__/admin-product-filter-params.test.ts) fail อยู่ก่อนแล้วบน main (ค้างจากตอน refactor `categoryId` → `categoryIds` และตอน `buildAdminProductFilterSearchParams` เปลี่ยนไปคืน array ของคู่ `[key, value]`) — ไม่เกี่ยวกับปี แต่แก้ให้ตรงพฤติกรรมจริงแล้วเพราะมันบังตาไม่ให้เห็น regression จริง
        - tests: suites ที่เกี่ยวข้องรวม **190 ผ่าน**, messenger 4 ผ่าน, `tsc --noEmit` ผ่าน, `npm run build` ผ่าน, mojibake ผ่าน
        - **ยังไม่ทำ**: ② เติม `yearEnd` ให้แถวปลายเปิด (P0240 แถว DIRECT เป็น `2003–null` ทั้งที่แถวคู่ COMPATIBLE ระบุ `2003–2007`) — ถ้าแก้ ผลของ `?yearMin=2010` จะเหลือ **1 รายการ (P0119)** ตามที่เจ้าของร้านคาดไว้ตั้งแต่แรก

        - **ต่อยอด — handoff + notify ฝั่งข้อความ (item 1):** เมื่อ reason=`SEARCHED_FITMENT_PART_NO_MATCH` (ลูกค้าระบุอะไหล่เฉพาะแล้วไม่เจอ) → ส่งต่อแอดมิน + notify (bell+Telegram) เหมือน no-match ฝั่งรูป. LINE `directNoMatchHandoff` OR เพิ่ม reason นี้; Messenger `shouldDirectNoMatchHandoff` early-return true (ครอบคำที่ไม่อยู่ใน `EXPLICIT_PRODUCT_NOUN_RE`). เปลี่ยนข้อความเป็นแบบระบุชื่ออะไหล่+รถ ผ่าน `buildJuneTextNoMatchHandoffReply(known)` ใหม่ ใน `lib/chat-core/ai-service.ts` (แทน generic `DIRECT_NO_MATCH_HANDOFF_MESSAGE` ที่ลบทิ้งทั้ง 2 processor); strip suffix "(English)" ของชื่อหมวด. tests: processor +1 (part ไม่มีรถ → handoff) → 66 ผ่าน, ปรับ assertion ข้อความ part-aware, messenger mock เพิ่ม export → 4 ผ่าน; tsc ผ่าน

  - [x] (20) AI Intent Classifier (hybrid routing — แทน regex จับกลุ่มคำที่เปราะ):
    - `lib/line-intent-groups.ts` (ใหม่): 12 กลุ่ม (`product`/`shop_info`/`general_faq`/`payment`/`shipping_address`/`order_status`/`price_negotiation`/`claim_or_return`/`purchase`/`greeting`/`social`/`other`) + `groupToRoute`/`intentToGroup`/`GUARD_GROUPS` (single source of truth)
    - `extractLineSearchIntent` → คืน `group` + product fields, ทำงาน **ทุกข้อความ** (รวมข้อความแรก, A=ข); ไม่มั่นใจ → `other`
    - processor: Layer-1 regex (guard payment/claim/price/purchase override + เมนู) → Layer-2 AI group → route ตามกลุ่ม (reuse forced-response/handoff/policy เดิม)
    - `general_faq`/`other` → ลอง FAQ ก่อน, ไม่เจอ → `buildJuneAskDetailsReply()` ถามต่อสไตล์จูน (ไม่ handoff/ไม่ dead-end)
    - `social` → `handleSocialTurn`: ack สั้น หรือนิ่งถ้าเป็น ack ปิดท้าย (กันวนซ้ำ)
    - safety: **ไม่มี flag เปิดใช้เลย**; classify ล่ม → fallback regex routing; รูปภาพไปทาง image path เดิม (text classifier เฉพาะข้อความ); เคารพ paused/waiting_admin (ไม่เด้งแทรกตอนแอดมินรับช่วง); audit `INTENT_CLASSIFIED {group, source, routedIntent}` + `SOCIAL_HANDLED`; classify timeout 15s
    - unit tests: groups mapping 6 + parse group 7 + processor (social/shop_info/paused/non-product/FAQ) + เดิมทั้งหมด

  - [x] (21) 2026-07-17 — Audit รอบใหญ่ (logic/ช่องโหว่/performance) + แก้ตามผลตรวจ A/B/D/E (ยืนยันแผนกับเจ้าของร้านแล้ว; log 14 วัน: ลูกค้ารอตอบ p50 26.3s / p90 47.9s, turn no-match p50 39s):
    - **A — Gemini key store cache** (`lib/google-ai-keys.ts` + `lib/google-ai-client.ts`): เดิมทุก Gemini call จ่าย DB ~17 query (upsert 15 key rows + findMany + await success write) → 1 product turn (2–5 calls) = ~35–85 query เฉพาะเรื่อง key. แก้: memoize `ensureKeyRows` ครั้งเดียวต่อ process, cache รายชื่อ key ที่ใช้ได้ TTL 15s (เฉพาะ strategy sticky; spread ไม่ cache เพราะต้องเรียง LRU สด), `markGeminiKeySuccess` เป็น fire-and-forget; mark ฝั่ง error (429/transient/disabled) + `resetAiApiKey` ล้าง cache ทันที (sync) กันหยิบ key ที่เพิ่งโดน cooldown ซ้ำ. ผลลัพธ์การเลือก key เหมือนเดิม (rotation bucket คำนวณต่อ call)
    - **B — ตัดงานซ้ำ/ทำขนานใน hot path**: (1) `resolveLinePriceTier` เป็น lazy getter แชร์ multi-subject/single path, เริ่มขนานกับ product search, non-product turn / ไม่มีของโชว์ = ไม่ยิง query เลย (เดิมยิงทุก turn + ซ้ำ 2 ครั้งเมื่อ multi ตกกลับ single) (2) webhook ข้าม LINE profile API เมื่อ conversation มี displayName และลูกค้า active ภายใน 24 ชม. (`getLineConversationProfileSnapshot` ใหม่ใน repo; ลูกค้ากลับมาหลังเงียบ >24 ชม. ยัง refresh ตามเดิม; `upsertLineRecipientFromWebhook` ไม่ clobber ชื่อเดิมอยู่แล้ว)
    - **D — ปิดช่องโหว่ logic ที่ตรวจพบ**: (1) multi-subject ถามราคา → ต่อ note `PRICE_HIDDEN_HANDOFF_NOTE` ท้ายบล็อกสินค้า + freeze `waiting_admin` (reason `MULTI_SUBJECT_PRICE_HANDOFF`) + notify — นโยบายราคาเดียวกับ single path (`priceAskThisTurn` hoisted ใช้ร่วมกัน) (2) multi-subject ได้ guard เทียบเท่า single path ต่อ subject: vehicle-unresolved (รถที่ resolve ไม่ได้ → ไม่โชว์อะไหล่รถอื่น) + weak-category-match (`STRONG_MATCH_REASONS` hoisted แชร์) → subject ที่โดน guard เปลี่ยนเป็น no-match line + notify (ไม่ freeze ตามเกณฑ์ จ) + audit `suppressedVehicleUnresolved`/`suppressedWeakMatch`/`priceAsk` ใน `AI_MULTI_SUBJECT` (3) escalation หลัง search ว่าง 2 ครั้ง deterministic: await audit `PRODUCT_SEARCH_SUMMARY` ของ turn ปัจจุบันก่อนนับ (เดิม fire-and-forget race → เด้งที่ครั้ง 2 หรือ 3 แบบสุ่ม; await เฉพาะ turn ที่ search ว่าง) (4) แก้เงื่อนไข push ของ multi-subject overflow: ส่งเฉพาะเมื่อ `allowPushFallback` (เดิม `|| !hasReplyToken` ทำให้ push ทั้งที่ปิด fallback — prod ไม่กระทบเพราะเปิดเสมอ)
    - **E — ห้องที่แอดมินถือ (PAUSED/WAITING/CLOSED ~32% ของ turn) ตัด Gemini calls ที่ผลไม่ถูกใช้**: ข้าม generate + purchase-intent fallback + FAQ; DRAFT ใน admin console ยังถูกเก็บครบ (เปลี่ยนจากสำนวน AI → template จูน `ADMIN_OWNED_TEMPLATE_DRAFT` พร้อมสินค้าชุดเดิมจริง); classifier + frame + search ยังรันตามเดิมเพื่อให้ context สด + draft มีของจริง; keyword purchase route ยังทำงาน
    - tests: processor +3 (multi price note/freeze/notify, subject vehicle-unresolved suppression, admin-owned skip Gemini) → lib+messenger รวม 415 (413 pass / 2 skipped เดิม), tsc + `npm run build` ผ่าน
    - ตามผล 3–5 วัน: `pipelineDurationMs` p50/p90 (AI_SEND_DECISION), จำนวน `AI_DEADLINE_FALLBACK`/`STALE_PROCESSING_TIMEOUT`, และเช็ก key `google-gemini-9` ที่ถูก DISABLED (โดน 400/401/403 — ต้องตรวจ key จริง)
    - **C0+C1 (2026-07-17, ยืนยันแล้ว)**: C0 — เพิ่ม `didYouMean` (suggestion ที่กู้สำเร็จ หรือ null) ใน audit `PRODUCT_SEARCH_SUMMARY` เพื่อวัดอัตรากู้สำเร็จจริง; C1 — cap did-you-mean retry ที่ **2 suggestion แรก** (`DID_YOU_MEAN_MAX_RETRIES` ใน `lib/chat-core/product-search-bridge.ts`, เรียงตาม trigram similarity อยู่แล้ว) ตัด worst case 1 search เต็มออกจากเทิร์น no-match (เทิร์นช้าสุดของระบบ p50 39s); มีผลทั้ง LINE+Messenger (bridge ร่วม), หน้าเว็บ/แอดมินที่โชว์ตัวเลือก did-you-mean ยังได้ 3 ตามเดิม (เรียก `suggestDidYouMean` ตรง). พฤติกรรมที่เปลี่ยน: เคสที่ suggestion อันดับ 3 เป็นตัวกู้ → จบเป็น no-match/ส่งแอดมิน (ความน่าจะเป็นต่ำ; ติดตามจาก flag C0). tests: bridge +2 (cap + กู้จากอันดับ 2) → 417 รวม (415 pass / 2 skipped เดิม)
    - [x] **(2026-08-01) แก้ did-you-mean ตอบสินค้าผิดปี — เคส "มูเล่หน้าครัชซิตี้96"**: ยืนยันจาก production `LineAiAuditLog` (conv `cmq6o1g1u000c04lbwivxi9nu`, msg `cms9vp76s000204jxnhdf3uyt`) — classifier แปลง `ซิตี้96` → `year: 1996` **ถูกต้อง** และ `fitmentYear: 1996` ถูกใส่เป็น hard filter แล้ว แต่ค้นรอบแรกได้ 0 แถว (DB จริง: CarModel "City" มี fitment เก่าสุดแค่ปี 2002 — ไม่มีของปี 96) → เข้า did-you-mean recovery ซึ่ง **ถอด `fitmentYear` ทิ้ง** (`retryFilters`) → ได้ P0198/P0199/P0200/P0201 (2003–2019) แบบ vector-only (`matchReasons` ว่างทุกตัว) แล้วคำตอบขึ้นต้นว่า "สำหรับ...City ปี 96 ... รายการที่มีให้" = ยืนยันปีผิด. **แก้แบบ C (กรองก่อน แล้วค่อย fallback พร้อมบอกความจริง)**:
      - `lib/chat-core/product-search-bridge.ts`: เพิ่ม `ResolveFitmentYearsFn` (injectable, default query `ProductFitment`) + `applyRecoveryYearCheck()` — หลัง retry สำเร็จ เอาปีที่ลูกค้าถามมากรองซ้ำ: ถ้ามีแถวที่ครอบคลุมปี → คืนเฉพาะแถวนั้น (`droppedYear=false`, appliedFilters คงปีไว้), ถ้าไม่มีเลย → คืนแถวเดิมทั้งหมดพร้อม flag ใหม่ `yearMismatch: { requestedYear }` และ reason ต่อท้าย `:YEAR_MISMATCH`. สินค้าที่ไม่มี fitment row เลย (universal) และช่วงปีปลายเปิด (`yearStart` ไม่มี `yearEnd`) นับว่า "ครอบคลุม" — กรองเฉพาะกรณีที่พิสูจน์ได้ว่าเป็นคนละเจเนอเรชัน. lookup ล้มเหลว → treat เป็น mismatch (ฝั่งปลอดภัย ไม่ยืนยันปีมั่ว)
      - `lib/chat-core/search-gate.ts`: เพิ่ม `buildYearMismatchNote()` และให้ `buildDidYouMeanNote(didYouMean, yearMismatch?)` ใช้ข้อความนี้แทนประโยคเดิม "ยังไม่ได้กรองตามปีรถ" (ซึ่งอ่านเป็น "แค่ไม่ได้กรอง" ไม่ใช่ "ไม่มีของปีนั้น")
      - **บั๊กซ้อนที่เจอจากภาพแชทจริง (bubble สุดท้าย)**: ข้อความเดิมลงท้ายว่า "ถ้าแจ้งปีมา จูนจะช่วยกรองให้ตรงรุ่นยิ่งขึ้นค่ะ" — แต่ `droppedYear` จะ `true` ได้**ก็ต่อเมื่อลูกค้าแจ้งปีมาแล้วเท่านั้น** (`baseFilters.fitmentYear !== null`) → ทุกครั้งที่ note นี้ออก คือการขอข้อมูลที่ลูกค้าเพิ่งพิมพ์ไป ("ซิตี้96") และยังขัดกับ bubble แรกที่เขียนว่า "สำหรับ City ปี 96" ด้วย. แก้โดยตัด branch นั้นทิ้งทั้งหมด — note ไม่ถามปีอีกต่อไป ใช้ `yearMismatch` เป็นตัวตัดสินข้อความเพียงอย่างเดียว (`droppedYear` เหลือไว้เป็นค่า audit ใน bridge result)
      - `lib/chat-core/ai-service.ts`: เมื่อ `yearMismatch` ถูกตั้ง → เติมคำสั่งใน reply prompt ห้ามเขียน "สำหรับ...ปี NNNN" / "รายการที่มีให้" คร่อมรายการ และให้ขึ้นต้นด้วยการบอกตรง ๆ ว่ายังไม่พบของปีนั้น
      - wire ทั้ง LINE (`line-webhook-processor.ts`) + Messenger (`messenger-webhook-processor.ts`) + audit ใหม่ `yearMismatchYear` ใน `PRODUCT_SEARCH_SUMMARY` (ใช้วัดช่องว่างสต็อกตามปี)
      - tests: bridge +3 (กรองเหลือเฉพาะปีที่ตรง / mismatch เคส 1996 City จริง / lookup ล้มเหลว) + search-gate +1 (ข้อความต้องบอกว่าไม่มีของปีนั้น) — lib+messenger 342 pass, `npx tsc --noEmit` + `npm run build` ผ่าน
    - [x] **(2026-08-14) coalescing แตกเทิร์นเพราะ vision ช้า — ลูกค้าได้ 2 ข้อความแทน 1 (A+C ยืนยันกับเจ้าของร้านแล้ว)**: ยืนยันจาก production `LineAiAuditLog` + `LineMessage` + `LineAiJob` conv `cmr0blzlh000104l4stv8hzk9` ("เจตไดนาโม"): ลูกค้าส่ง **รูปสายพาน (~08:38:44.8) → "สายพานแอร" (08:38:52.0) → "แอคคอต" (08:38:57.2)** แต่ได้ 2 คำตอบ — `GATE_ASK:PART_ONLY → need_car` (08:38:56.8) แล้ว `WEAK_CATEGORY_MATCH_HANDOFF` (08:39:15.5). ต้นเหตุ: [ingestLineEvent()](lib/line-webhook-processor.ts) เรียก `classifyLineImage()` (Gemini vision) **ก่อน** `appendLineMessage()` + `bumpLineInboundSeq()` — audit ยืนยันลำดับ `IMAGE_CLASSIFIED` (:59.599) มาก่อน `INBOUND_EVENT_ACCEPTED` (:59.608) = vision กิน **14.8 วิ** ระหว่างนั้นรูป "ไม่มีตัวตน" ทั้งใน `getUnansweredInboundLineMessages()` (กรอง `createdAt > lastOutbound`) และ `lastInboundSeq` ⇒ abort-on-newer ไม่ยิง ⇒ owner ของข้อความแรกตอบไปโดยไม่รู้ว่ามีรูปค้างอยู่ (และ "แอคคอต" มาช้ากว่าการส่งแค่ **0.43 วิ**). coalescing เองไม่ได้พัง — รอบ 2 merge รูป+ข้อความถูกต้อง (`consolidatedQuery: "สายพานหน้าเครื่อง Accord"`)
      - **A — ย้าย vision ออกจาก ingest path**: `ingestLineEvent()` รับ option `deferImageClassification` (โหมด coalesce ส่ง `true`) → persist + bump seq ทันที (~0.1s) ส่วน vision ไปทำใน `buildMergedTurnInput()` ซึ่งรองรับ classify-on-demand + merge structured fields ข้ามหลายรูปอยู่แล้ว ⇒ การจัดกลุ่ม burst ขึ้นกับ DB write ไม่ใช่ latency ของ Gemini อีกต่อไป. legacy path (`coalesce: false`) ยัง classify ที่ ingest เหมือนเดิม
      - **A — งานบังคับที่มากับการย้าย** (เดิม ingest เป็นคนทำให้): ฟังก์ชันใหม่ `finalizeFreshImageClassification()` ทำครบทุกอย่าง **ครั้งเดียวต่อรูป** (guard ด้วย flag `fresh` = ไม่ได้มาจาก cache/stored) — (1) `classByMessageId.set()` กันการ re-OCR ตอน abort-on-newer วนซ้ำ (2) `storeImageClassificationForMessage()` (repo ใหม่) เขียนสำเนา reuse ลง job payload ให้ owner re-run / cron recovery ใช้ต่อ (3) `updateLineMessageIntent()` (repo ใหม่) แก้ intent ของแถวรูปหลัง vision ตัดสิน — ingest บันทึกได้แค่ `PART_IMAGE_INQUIRY` แบบ generic สลิปจึงจะติดป้ายผิดใน admin console ถ้าไม่แก้ (4) `ingestPaymentSlip()` + audit `PAYMENT_SLIP_OCR` ย้ายมาที่นี่ (`createPaymentSlip` ไม่ idempotent — flag `fresh` คือสิ่งที่กันสลิปซ้ำ) (5) audit `IMAGE_CLASSIFIED`. ทุกขั้น best-effort (`.catch`) — พังแล้วต้องไม่ทำให้ลูกค้าไม่ได้คำตอบ
      - **C — ขยาย debounce 3s → 5s**: `DEFAULT_COALESCE_WINDOW_MS` + [app/api/line/webhook/route.ts](app/api/line/webhook/route.ts) (แก้ทั้งคู่ให้ cron recovery ใช้ค่าเดียวกัน) — ลูกค้าพิมพ์ทีละท่อนห่างกัน 4–5 วิเป็นปกติ, การรอถูกกว่าการ re-run Gemini ทั้ง pipeline มาก (เทิร์นนี้ `pipelineDurationMs: 12207`) และ typing dots แสดงอยู่แล้ว. ยังห่างจากเพดาน forced-final-pass 28s / reply token 55s / `maxDuration` 60s มาก
      - **พฤติกรรมที่เปลี่ยน**: burst ที่เคยแตก → ตอบข้อความเดียวและ **ไม่ถามข้อมูลที่ลูกค้ากำลังจะบอกอยู่แล้ว** (gate ได้ part+car ครบตั้งแต่รอบเดียว) · frame ถูกเขียนครั้งเดียวแทน 2 ครั้ง → ไม่เกิด `topicShift` ปลอมที่เกิดจากการแตกเทิร์นเอง · รูปโผล่ใน admin inbox / Telegram mirror เร็วขึ้น ~15s · ทุกเทิร์นตอบช้าลง ~2s จาก C · เทิร์นรูปล้วนใช้เวลาเท่าเดิม (vision ย้ายที่ ไม่ได้เพิ่มงาน) · สลิปถูกบันทึกช้าลงเท่าความยาว debounce
      - **ข้อจำกัดที่รู้ตัว**: ถ้า webhook ตายหลัง ingest แต่ก่อน owner classify สลิปจะยังไม่ถูกบันทึกจนกว่า cron `recoverStalledCoalescedConversations()` จะกู้ (เดิมบันทึกตั้งแต่ ingest) — ครอบคลุมด้วย failsafe เดิม (`lastInboundSeq > lastProcessedSeq`)
      - tests: coalesce +3 (**vision ช้าต้องไม่แตก burst** = reproduce เคสจริงด้วย 2 payload ขนาน + gate หน่วง classify / **abort แล้วต้องไม่ re-classify** = กันค่า Gemini บาน / **สลิปต้อง ingest ครั้งเดียว + ถูก relabel**) พร้อม knob ใหม่ `blockClassify` + counters `classifies`/`slipIngests`/`intentUpdates` ในฮาร์เนส — ตรวจแล้วว่า **fail จริงบนโค้ดก่อนแก้ 2 เคส** (สลับ `deferImageClassification` เป็น false แล้วรันซ้ำ) ไม่ใช่เทสต์ลอย · coalesce 24 ผ่าน, LINE+messenger 192 (190 pass / 2 suites), เต็มระบบ 804 (802 pass / 2 skipped เดิม), `tsc --noEmit` + eslint + `check:mojibake` + `npm run build` ผ่าน
      - ตามผลหลัง deploy 3–5 วัน: จำนวน `AI_COALESCE_ABORTED` (ควรลด), `pipelineDurationMs` p50/p90, และจำนวนห้องที่ได้ 2 คำตอบติดกันภายในนาทีเดียว

### 6. Image Egress Reduction — Vercel CDN Proxy (2026-06-11)
- บริบท: Supabase Free ใกล้ชน limit Egress (3.675/5GB) + Cached Egress (3.662/5GB, เข้า grace period ถึง 15 มิ.ย.) ขณะที่ Vercel Pro มี headroom เหลือมาก ($5.43/$20) → ย้ายการ serve รูปสินค้าไปผ่าน Vercel CDN
- หลักการ: client / crawler / next-image optimizer → `/img/<objectPath>` (Vercel CDN cache, immutable 1 ปี) → Supabase Storage (ดึงครั้งเดียวต่อ cache window) ไม่แตะข้อมูลใน DB
- [x] เพิ่ม `lib/product-image-url.ts` — pure URL helper (no `@supabase/supabase-js`, client-safe) + `toProductImageCdnPath()` (idempotent, no-op กับ url ที่ไม่ใช่ product bucket); `product-image-storage.ts` re-export ให้ importer เดิมไม่ต้องแก้
- [x] เพิ่ม route handler `app/img/[...path]/route.ts` — proxy public object จาก Supabase + `Cache-Control: public, max-age/s-maxage=31536000, immutable`; จำกัดเฉพาะ object path ใน bucket `products/` (กัน open proxy)
- [x] ยกเว้น `/img` จาก middleware matcher (`proxy.ts`) — ไม่ให้ auth()/bot-check รันต่อ request รูป
- [x] ชี้ `next/image` src ผ่าน `/img` ทุกจุดที่ render รูปสินค้า: `ProductCard`, `ProductImageGallery`, `ProductImageZoomLightbox`, `ProductAutocomplete`, admin `ProductImagePreview` / `ProductForm` (display เท่านั้น, formData/DB คงเป็น URL Supabase เดิม) / `products/search`
- [x] JSON-LD/ItemList ใช้ absolute CDN URL: `ProductJsonLd` (product detail), `products/page.tsx`, `products/[categorySlug]/page.tsx` → ตัด bot ดูดรูปจาก Supabase ตรง
- [x] 2026-06-12: ขยาย `/img` เป็น allowlisted public-storage proxy สำหรับ `products/settings/*`, `products/users/signatures/*`, `products/delivery-proofs/*`, และ bucket `line-chat/*` โดยยัง block path อื่นและ `payment-slips/*`
- [x] 2026-06-12: ชี้โลโก้ร้าน, LINE QR, ลายเซ็น, หลักฐานจัดส่ง, LINE chat image, icon/JSON-LD, และ LINE Flex placeholder ผ่าน `/img` แบบ render-time; ค่า URL ใน DB ยังเป็นค่าเดิม ไม่ต้อง migrate
- [x] 2026-06-12: payment slips ยังเป็น private signed URL และ render ด้วย native `<img>` แทน `next/image` เพื่อไม่นำข้อมูลส่วนตัวเข้า shared image optimizer/CDN cache
- [x] 2026-06-12: ลบ Supabase `images.remotePatterns` ออกจาก `next.config.ts` เพื่อบังคับไม่ให้ `next/image` ดึง Supabase ตรง; ยังเก็บ CSP `img-src` Supabase ไว้สำหรับ signed/private image และ fallback ที่ตั้งใจไว้
- [ ] (Ops) หลัง deploy: เฝ้าดู Supabase Egress/Cached Egress ควรลดลงชัดเจน + ตรวจ Vercel Image Optimization usage ไม่พุ่งเกินงบ
- [ ] (ทางเลือก, ยังไม่ทำ) migrate `Product.imageUrl`/`ProductImage.url` ใน DB ให้เป็น CDN path — ชะลอไว้เพราะกระทบ logic ownership ใน `product-image-storage.ts` และไม่จำเป็นเมื่อใช้ render-time helper แล้ว

### 7. Image Storage Migration — Supabase Storage → Vercel Blob (2026-06-23)
- บริบท/สาเหตุ: Supabase Free โดน restrict (Egress 7.16/5GB = 143%, Cached Egress 5.05/5GB = 101%) ขณะที่ Storage จริงมีแค่ 224MB → รูปถูกดึงซ้ำ ~22 เท่า. วินิจฉัยว่า `/img` route-handler cache เป็น per-deployment → deploy ถี่ (เกือบทุกวัน) + bot crawl ทำให้ Vercel CDN miss แล้ว re-pull จาก Supabase ซ้ำ. แก้ถาวรด้วยการย้าย object ไป Vercel Blob (public CDN อยู่ข้าม deploy, included ในโควต้า Pro)
- ขอบเขตที่ยืนยันแล้ว: ย้าย bucket `products` (public), `line-chat` (public), `payment-slips` (private/PII) — **ไม่ย้าย** `purchase-ocr` (ชั่วคราว, ไม่กระทบ egress, คงไว้บน Supabase → ยังลบ `@supabase/supabase-js` ทั้งหมดไม่ได้). ลำดับ: public ก่อน (หยุดดูผล egress) แล้วค่อย payment-slips
- การตัดสินใจที่อนุมัติแล้ว: payment-slips (private) เปลี่ยนจาก Supabase signed-URL → route สตรีมที่ตรวจ session admin (รออนุมัติดีไซน์ละเอียดก่อน Phase 3)
- [x] **Phase 0 — เตรียม (additive, ไม่เปลี่ยนพฤติกรรม)**: เพิ่ม dep `@vercel/blob@2.4.1`; เพิ่ม env `BLOB_READ_WRITE_TOKEN` + flag ต่อ bucket `IMAGE_STORAGE_{PRODUCTS,LINE_CHAT,PAYMENT_SLIPS}` ใน `.env.example`; เพิ่ม `**.public.blob.vercel-storage.com` ใน `next.config.ts` (`images.remotePatterns` + CSP `img-src`); เพิ่ม `lib/storage-backend.ts` (`getStorageBackend`/`isBlobBackend`, default `supabase`)
  - [x] (งานเจ้าของร้าน) สร้าง Blob store ใน Vercel Dashboard + ตั้ง `BLOB_READ_WRITE_TOKEN` ใน Vercel env — เสร็จแล้ว
- [x] **Phase 1 — Public dual-write** (2026-06-23): เพิ่ม facade `lib/products-bucket-storage.ts` (`uploadProductsBucketObject`, เลือก backend ตาม `IMAGE_STORAGE_PRODUCTS`); wire 4 จุดอัปโหลด bucket `products` → product image (`products/actions.ts`), โลโก้ (`settings/company/actions.ts`), ลายเซ็น (`users/actions.ts`), delivery-proof (`sales/actions.ts`); เพิ่ม Blob branch ใน `line-chat-image-storage.ts` (flag `IMAGE_STORAGE_LINE_CHAT`). รูปเก่ายังโหลดผ่าน `/img` ได้ (render helper ปล่อย Blob URL ผ่านอยู่แล้ว + remotePatterns รับ host). Default flag = supabase → พฤติกรรมเดิมไม่เปลี่ยนจนกว่าจะ flip. tsc/lint ผ่าน
  - ข้อจำกัดที่ยอมรับไว้ (จัดการใน Phase 4): เมื่อ flag=blob, copy-to-code-folder เดิม (Supabase) จะ no-op กับ Blob URL (รูปค้างใต้ folder uploadCode เดิม ใช้งานได้ปกติ) และรูปสินค้า Blob ที่ถูกแทนที่จะค้างเป็น orphan รอ sweep ใน Phase 4
- [x] **Phase 2 — Backfill public — DONE 2026-06-25** (รัน apply หลัง egress reset: migrated 2,375 + เก็บตก stragglers 3 = 2,378, เหลือ 0; flip `IMAGE_STORAGE_PRODUCTS`+`IMAGE_STORAGE_LINE_CHAT=blob` ใน Vercel แล้ว → รูปสาธารณะ serve จาก Blob หมด): `prisma/scripts/backfill-public-images-to-blob.ts` (`npm run backfill:public-images-blob [-- --apply]`) — download จาก Supabase → `put` ขึ้น Blob path เดิม → rewrite URL ใน DB (Product.imageUrl, ProductImage.url, SiteContent shop_logo_url, User.signatureUrl, DeliveryProof.signature/photo, LineMessage.imageUrl); idempotent (ข้าม Blob URL แล้ว), ไม่ลบ Supabase, dump rollback JSON (gitignored), เขียน AuditLog UPDATE/ImageStorageMigration. **dry-run พบ 2,375 แถว (products 2,373 + line-chat 2)**
  - download ใช้ `fetch(publicUrl)` ตรง (bucket เป็น public) → **ไม่ต้องใช้ `SUPABASE_SERVICE_ROLE_KEY`**, ต้องการแค่ `BLOB_READ_WRITE_TOKEN` (มีใน `.env.local` แล้ว)
  - [x] รันสำเร็จ 2026-06-25 หลัง egress reset (download `fetch(publicUrl)` ไม่ต้องใช้ service key) → เฝ้าดู egress ตก
- [x] **Phase 3 — Payment slips (private/PII)** — **เสร็จและเปิดใช้งานจริงแล้ว** (code + backfill 9 สลิป + ตั้ง token + flip `IMAGE_STORAGE_PAYMENT_SLIPS=blob`):
  - **บทเรียน: Blob store มี 2 ชนิด public/private ระดับ store** — public store (รูปสินค้า) เก็บ private blob ไม่ได้ → ต้องสร้าง **private store แยก** + token `BLOB_SLIPS_READ_WRITE_TOKEN` (wire เข้า put/get/del + backfill ผ่าน option `token`). รูปสินค้ายังใช้ default `BLOB_READ_WRITE_TOKEN` (public store)
  - backfill `backfill-payment-slips-to-blob.ts` รัน apply → migrated 9/9, idempotent (รันซ้ำ skip 9). ไม่ rewrite DB (imageUrl เก็บ path เดิม)
  - [x] (เจ้าของ) ตั้ง `BLOB_SLIPS_READ_WRITE_TOKEN` ใน Vercel env → push โค้ด slip-token → flip `IMAGE_STORAGE_PAYMENT_SLIPS=blob` — เสร็จแล้ว
  - storage `lib/line-payment-slip-storage.ts`: `storePaymentSlipImage` เพิ่ม Blob `put(access:'private')`; `deletePaymentSlipImage` ลบทั้ง Blob+Supabase (mixed-state); เพิ่ม `getPaymentSlipDisplayUrl` (route URL เมื่อ blob, signed URL เมื่อ supabase) + `readPaymentSlipImage` (Blob get → **fallback Supabase** ให้ slip ที่ยังไม่ backfill ใช้ได้หลัง flip)
  - route ใหม่ `app/api/admin/line-payment-slips/[id]/image/route.ts` (GET) — `requirePermission("line_payment_slips.view")` → stream, `Cache-Control: private, max-age=300`
  - display backend-aware: detail page `[id]/page.tsx` + gallery `line-payment-slip-gallery.ts` (blob → route URL, ข้าม signed-URL batch)
  - backfill `prisma/scripts/backfill-payment-slips-to-blob.ts` (`npm run backfill:payment-slips-blob`) — copy bytes Supabase→Blob private ที่ path เดิม, **ไม่ rewrite DB** (imageUrl เก็บ path), idempotent, AuditLog. dry-run พบ **9 slips**
  - security review: auth = view permission เดิม / by-id ไม่มี path injection / IDOR เท่าหน้าเดิม / Blob private + `private` cache (ไม่เข้า shared CDN) / token server-only / same-origin (CSP 'self')
  - [x] รัน apply backfill + flip flag เสร็จแล้ว: `npm run backfill:payment-slips-blob -- --apply` → `IMAGE_STORAGE_PAYMENT_SLIPS=blob`
- [x] **Phase 4 — Cleanup — DONE 2026-07-14**:
  - [x] ลบโค้ด Supabase storage เฉพาะ 3 bucket + เอา flag `IMAGE_STORAGE_*` / `lib/storage-backend.ts` / route `/img` ออก — storage libs เป็น Blob-only แล้ว; ผู้ใช้ Supabase Storage รายเดียวที่เหลือคือ `lib/purchase-invoice-storage.ts` (bucket `purchase-ocr-temp` — คงไว้ตามแผน)
  - [x] pre-fix ก่อนลบ (2026-07-14): preflight gate พบ backfill 25 มิ.ย. ไม่ได้ rewrite 3 คอลัมน์ — `Sale.signerSignatureUrl` (30 แถว) + `Receipt.signerSignatureUrl` (18 แถว) ชี้ไฟล์ลายเซ็นเดียวกันซึ่งมีบน Blob แล้ว → สลับ URL ด้วย `prisma/scripts/fix-dangling-supabase-refs.ts` (HEAD 200 gate + AuditLog); `PaymentSlip.imageSignedUrl` cache signed URL หมดอายุ 3 แถว (โค้ดเลิกใช้คอลัมน์นี้แล้ว) → NULL
  - [x] ลบ objects เก่า 3 bucket (2026-07-14): **2,515 ไฟล์ / 417.1MB** (products 2,502 / line-chat 4 / payment-slips 9) ด้วย `prisma/scripts/delete-supabase-storage-objects.ts` (`npm run cleanup:supabase-storage`) — dry-run default, preflight gate ในตัว (สแกนทุกคอลัมน์ text หา Supabase URL + เช็ค slip ทุกใบบน private Blob), manifest JSON gitignored, batch ละ 100, AuditLog, guard ห้ามแตะ `purchase-ocr`; **ตัว bucket คงไว้ ลบเฉพาะ objects**
  - cross-check ก่อนลบ: ทุก object เข้าเงื่อนไข "มีบน Blob แล้ว (1,839)" หรือ "orphan ก่อน migration ที่ไม่มี DB row อ้างถึง (676)" — referenced-but-missing-from-Blob = **0**; post-verify: `storage.objects` เหลือ 0 ใน 3 bucket, `purchase-ocr-temp` ไม่ถูกแตะ (ว่างเองอยู่แล้ว), spot-check HEAD รูปสินค้า+ลายเซ็น = 200

### 8. Database Egress Reduction — Supabase Pro (2026-07-14)
- บริบท: egress รอบบิล 6.44GB (~320MB/วัน ≈ 10GB/เดือน) เกินธงภายใน 5GB/เดือน — วินิจฉัยจาก `pg_stat_statements` + `pg_stat_activity` + `storage.objects`: ไม่มี replication/backup รั่ว, ตัวการคือ query ที่ดึง "ทั้ง catalog / ทั้งตาราง" ซ้ำบ่อย (Product ~64k แถว/วัน ที่แถวกว้าง ~4.9KB, ProductAlias ~157k แถว/วัน, StockCard ~87k แถว/วัน)
- [x] `lib/transaction-options.ts` — ลด egress ของ product-option superset: truncate `description` เหลือ 300 ตัวอักษรแรก (ใช้ค้นใน dropdown เท่านั้น ไม่เคยถูกบันทึก/แสดง), aliasSearchText เปลี่ยนเป็น `string_agg(DISTINCT lower(alias))` ฝั่ง SQL (902 แถวแทน 36k แถว — ยืนยัน membership ตรงกับ logic JS เดิม 100%), revalidate safety-net 300s → 86,400s (tag invalidation ยังเป็น path หลัก), bump cache key เป็น `-v3`; **เงื่อนไขแสดงสินค้าทุกตัว active+inactive คงเดิม**
- [x] `lib/stock-card-latest-balance.ts` (ใหม่) — `getLatestStockBalances()` ใช้ SQL `DISTINCT ON ("productId") … ORDER BY "productId","docDate" DESC,sorder DESC` แทน Prisma `distinct` (ซึ่ง dedupe ฝั่งแอป = ลาก StockCard ทั้งประวัติมาทุกครั้ง); ใช้ค่า 4 ตำแหน่งจาก ledger เหมือนเดิมทุกไบต์ (ยืนยันเทียบ 887 สินค้า mismatch=0) — เปลี่ยน call site ใน `lib/product-report-queries.ts` + `lib/ar-ap-stock-report-queries.ts`
- [x] sitemap revalidate 900s → 21,600s (6 ชม.) ทั้ง `app/sitemap.ts` + `lib/storefront-sitemap.ts` (bot crawl ทำให้ refetch สินค้าทุกตัวได้ ~96 รอบ/วัน)
- [ ] (Ops) เฝ้าดูกราฟ egress 3–5 วันหลัง deploy — คาดลดจาก ~320MB/วัน เหลือระดับ ~150MB/วัน (<5GB/เดือน)
- หมายเหตุที่พบระหว่างตรวจ: bucket `products` เก่าบน Supabase ยังมี 2,502 ไฟล์ / 417MB (public) แม้ DB ชี้ Blob ครบแล้ว — เป็นงาน Phase 4 Cleanup ของ ข้อ 7 อยู่แล้ว
- [x] (follow-up 2026-07-14) พบ+แก้ภาษาไทยเพี้ยน (cp1252 double-encode) 5 ไฟล์: `ar-ap-stock-report-queries.ts` (หัวตาราง export AR/AP เพี้ยนตั้งแต่ 16 เม.ย.), cash-bank report 2 หน้า, `CommandPalette.tsx`, `AGENTS.md` — กู้คืนด้วย decode ย้อน ยืนยันตรงกับ git ก่อนเพี้ยน + เพิ่ม guard กันเกิดซ้ำ: `scripts/check-mojibake.mjs` + pre-commit hook (`.githooks/`, เปิดใช้อัตโนมัติผ่าน postinstall → `scripts/setup-git-hooks.mjs`), สแกนมือได้ด้วย `npm run check:mojibake`

### 9. Facebook Messenger AI Agent (2026-07-04)
- สถานะ: **Phase B–E เสร็จครบ** (B: สมองกลาง `lib/chat-core/`; C: 4 model; D: webhook + Send API + processor เต็ม text/รูป/สลิป/coalescing/24h/cron; E: admin inbox + permission 5 ขั้น + audit กลาง + notification bell+Telegram + handoff escalation — reuse chat-core ไม่แตะ LINE) · เหลือ **Phase A (Meta App Review — งานเจ้าของร้าน)** + **Phase F (rollout/QA)** — checklist เต็มดู [PLAN-MESSENGER.md](/D:/autoparts/PLAN-MESSENGER.md)
- เป้าหมาย: AI ตอบ DM Facebook ใช้สมอง + logic ค้นหาชุดเดียวกับ LINE OA (แก้ที่เดียว) — ตารางแยกใหม่ `MessengerConversation`, parity เต็มเท่า LINE, webhook `www.sriwanparts.com/api/messenger/webhook`
- หลักการ: extract `lib/chat-core/` (channel-agnostic) แบบ clean repo-wide ไม่ใช้ shim → LINE ต้องทำงานเหมือนเดิม 100% (build เขียว + test เดิมผ่านทุกตัว)

### 10. ราคาสมาชิก — Price Tier ที่ 3 (2026-07-19)
- สถานะ: **โค้ดเสร็จครบ รอ `prisma db push` + seed บน production**
- เป้าหมาย: เพิ่มระดับราคา `MEMBER` (ราคาสมาชิก) แทรกระหว่างขายส่งกับขายปลีก — ลำดับราคา **ขายส่ง < สมาชิก < ขายปลีก**
- [x] Schema: `Product.memberPrice Decimal @default(0) @db.Decimal(10,2)` + `enum PriceTier` เพิ่มค่า `MEMBER`
- [x] Seed: `scripts/seed-customer-types.ts` เพิ่มประเภทลูกค้า "สมาชิก" (create-only ไม่แก้ข้อมูลเดิม)
- [x] สูตรคำนวณอัตโนมัติกลาง `lib/product-pricing.ts` — ขายปลีก = ขายส่ง × 1.7, สมาชิก = ขายส่ง × 1.4, ปัดขึ้นลงท้าย 0 ไม่มีทศนิยม
- [x] **แก้สูตรราคาสมาชิก (2026-07-19)**: เปลี่ยนฐานอ้างอิงเป็น **ราคาสมาชิก = ราคาขายปลีก − 30%** (`deriveMemberPriceFromRetail()` ใน `lib/product-pricing.ts`, ปัดขึ้นลงท้าย 0) — ราคาขายปลีกยังคิดจากขายส่ง × 1.7 เหมือนเดิม, ฟอร์มสินค้า auto-fill ตามสูตรใหม่อัตโนมัติ (ใช้ `derivePricesFromWholesale()` ตัวเดิม ไม่ต้องแก้ component)
- [x] Backfill สูตรใหม่: `scripts/recalc-member-price-from-retail.ts` (dry-run เป็นค่าเริ่มต้น, `--apply` ถึงเขียน) — คำนวณราคาสมาชิกใหม่ทุกรายการที่ `retailPrice > 0` โดย **เขียนทับค่าเดิมเสมอ** (ยืนยันโดยเจ้าของร้าน), `retailPrice <= 0` ข้ามทั้งแถว, transaction เดียว + AuditLog 1 แถว
  - มี guard ตรวจ `ราคาสมาชิกใหม่ <= ราคาขายส่ง` → ปฏิเสธ `--apply` เว้นแต่ใส่ `--allow-below-wholesale`
  - **รันจริงแล้ว 2026-07-19**: อัปเดต 892 รายการ (ข้าม 9 ที่ไม่มีราคาปลีก, ตรงสูตรอยู่แล้ว 23), เหลือรายการที่ต่ำกว่าขายส่ง 0 รายการ
- [x] แก้ราคาปลีกที่ตั้งมือต่ำกว่าสูตรจนสมาชิกไม่มีกำไร: `scripts/fix-retail-below-formula.ts` — P0427 (850→1,020) / P0430 (700→840) / P0438 (500→600) รีซิสเตอร์พัดลมแอร์ 3 ตัว, **รันจริงแล้ว 2026-07-19** ก่อน backfill (ยืนยันโดยเจ้าของร้าน)
- [x] ฟอร์มสินค้า: ช่อง "ราคาสมาชิก" + auto-fill จากราคาขายส่ง (ทับค่าเดิมเสมอ ผู้ใช้แก้ทับได้) + แก้ help text ที่เดิมเขียนผิดว่า "เว้น 0 ระบบจะใช้ราคาขายส่งแทน"
- [x] หน้า list + preview: แสดง 3 ระดับราคา ไล่ความเด่น ส่ง > สมาชิก > ปลีก (light + dark)
- [x] หน้าขาย: `SalePriceTier` รองรับ MEMBER — `memberPrice` **ไม่ fallback** (0 = ให้พนักงานกรอกเอง) + ไฮไลต์แถวราคา 0 + confirm ก่อนบันทึก
- [x] แชท LINE/Messenger: `ChatPriceTier` เพิ่ม `MEMBER` → ลูกค้าที่ผูกบัญชีแล้วเห็นราคาตามกลุ่มตัวเอง, `memberPrice = 0` → "สอบถามราคา"
- [x] Cache key `admin-transaction-product-options-gzip-v3` → `v4` (payload เดิมไม่มี memberPrice)
- [x] Test: `applyChatPriceTier` MEMBER (มีราคา / ราคา 0) + fixture เดิมทั้งหมด
- [ ] Backfill: `scripts/recalc-prices-from-wholesale.ts` (dry-run เป็นค่าเริ่มต้น, `--apply` ถึงเขียน) — เติมเฉพาะช่องที่ยังเป็น 0 จากราคาขายส่ง, **ราคาที่ตั้งไว้แล้ว (> 0) ไม่ถูกแตะ**, `salePrice <= 0` ข้ามทั้งแถว, transaction เดียว + AuditLog 1 แถว
- [x] แก้เคสลำดับราคาผิด: `scripts/fix-retail-below-member-price.ts` — สินค้า 3 ตัว (P0435 / P0437 / P0441) เคยตั้งราคาปลีกมือไว้ต่ำกว่าสูตร ×1.70 ทำให้ราคาสมาชิกแซงราคาปลีก → คำนวณราคาปลีกใหม่ตามสูตร (ยืนยันโดยเจ้าของร้าน 2026-07-19)
- รันบน production แล้ว (2026-07-19): db push + seed "สมาชิก" + เติมราคาสมาชิก 914 รายการ + เติมราคาปลีก 3 รายการ + แก้ลำดับราคา 3 รายการ — ตรวจสอบหลังรัน: ลำดับราคาผิด **0 รายการ**, สินค้าไม่มีราคาขายส่ง 10 รายการ (แสดง "สอบถามราคา")
- ไม่แตะ: storefront ยังใช้ `retailPrice` เหมือนเดิม, business logic สต็อก/MAVG/เอกสาร
- Audit Log: ครอบคลุมอยู่แล้วผ่าน `getProductAuditSnapshot()` (ใช้ `include` → เก็บ memberPrice อัตโนมัติ) และ audit เดิมของ customer-types

### 11. แจ้งราคาพิเศษให้ลูกค้าใหม่บนแชท LINE + Messenger (2026-07-19)
- สถานะ: **เสร็จครบ + db push แล้ว**
- เป้าหมาย: ลูกค้าที่ยังไม่ผูกบัญชีกับระบบร้านต้องรู้ว่าราคาที่เห็น (ราคาขายปลีก) **ยังไม่ได้ลด** ส่วนลูกค้าที่ผูกแล้วเห็นราคาตามกลุ่มของตัวเองโดยไม่มีข้อความรบกวน
- [x] เพิ่ม `ChatPriceTier = "UNLINKED"` — ใช้ `retailPrice` เท่ากับ RETAIL ต่างกันแค่ข้อความที่แนบท้าย
- [x] `resolveLinePriceTier` / `resolveMessengerPriceTier` คืน `UNLINKED` เมื่อ: ยังไม่ผูก / บัญชีลูกค้าปิด / ไม่ระบุประเภท / ประเภทถูกปิด (ยืนยันโดยเจ้าของร้าน)
- [x] `buildUnlinkedPriceNote()` — เลือกข้อความให้ตรงกับการ์ด: มีราคา → "ราคานี้เป็นราคาปกติค่ะ 🙏 เดี๋ยวแอดมินมาแจ้งราคาพิเศษให้อีกทีนะคะ รอสักครู่ค่ะ" / การ์ดทุกใบเป็น "สอบถามราคา" → "เดี๋ยวแอดมินมาแจ้งราคาให้นะคะ รอสักครู่ค่ะ 🙏"
- [x] ส่งเป็น **bubble สุดท้ายเสมอ** — ไม่เบียด `followUpBubble` เดิม (didYouMean / near-match) ซึ่งเป็นคำเตือนความแม่นของผลค้นหา ต้องได้ส่งทั้งคู่ (LINE ส่งได้ 5 ข้อความ/ครั้ง ปัจจุบันใช้ 4)
- [x] ครบทั้ง LINE single path + multi-subject path + Messenger (multi-subject รวมสินค้าทุก subject แล้วส่งข้อความครั้งเดียว ไม่ซ้ำต่อ subject)
- [x] **ไม่ freeze AI / ไม่แจ้งแอดมิน** — เป็นข้อความแจ้งให้ทราบ ลูกค้ายังไม่ได้ขออะไร ถ้า freeze ทุกครั้งลูกค้าใหม่จะคุยกับ AI ไม่ได้เลย (Messenger แทบทุกคนเป็น UNLINKED) · การ freeze เดิมตอนลูกค้า**ถามราคาเอง** (`hiddenPriceWithProducts`) ยังทำงานเหมือนเดิม
- [x] **ลบ `CustomerType.showPrice`** — เป็น dead field: schema ระบุว่าคุมการแสดงราคาบน LINE แต่ `applyChatPriceTier()` ไม่เคยอ่านค่านี้เลย เปลี่ยนป้ายในหน้าลูกค้าไปใช้ `priceTier` จริงแทน (`db push --accept-data-loss`)
- [x] Test: `buildUnlinkedPriceNote` 4 เคส + e2e "unlinked ได้ข้อความ + ไม่ freeze" / "linked ไม่ได้ข้อความ"

### 12. แบ่งกำไรผู้ร่วมทุน — Profit Distribution (2026-08-01)
- สถานะ: **เสร็จครบ + db push แล้ว (2026-08-01) — รอตั้งค่าผู้ร่วมทุนและมอบสิทธิ์**
- บริบท: ร้านจดทะเบียนพาณิชย์ (ไม่ใช่นิติบุคคล) ทำกันในครอบครัว 4 คน สิ้นเดือนนำกำไรสุทธิมาแบ่ง สัดส่วนตั้งต้นคงที่แต่ปรับได้รายเดือน · ไม่มีใครกินเงินเดือนประจำ → **ไม่ทำโมดูล Payroll**
- ตัดสินใจเชิงบัญชี (สำคัญที่สุดของงานนี้): การแบ่งกำไรให้เจ้าของ **ไม่ใช่ค่าใช้จ่าย** จึง **ห้ามสร้าง `Expense` และห้ามแตะ `FactProfit`** เด็ดขาด — ผลพลอยได้คือคีย์ย้อนหลังงวดไหนก็ไม่กระทบกำไรสุทธิของเดือนที่คีย์ (เช่น คีย์งวด ก.ค. ในวันที่ 01/08 กำไรเดือน ส.ค. ไม่ขยับแม้แต่บาทเดียว) เงินสดตัดตาม `payDate` จริงเท่านั้น
- [x] Schema: `enum UserType {EMPLOYEE|PARTNER}` + `User.userType` (+`@@index`) — ใช้ตาราง `User` เดิมตามที่เจ้าของสั่ง, `enum PartnerLedgerType`, `PartnerProfile` (1-1 กับ User, แยกตารางเพราะ `User` ถูก select เกือบทุกหน้า), `ProfitDistribution`, `ProfitDistributionItem`, `PartnerLedger`
- [x] 1 งวด = 1 เอกสาร ACTIVE บังคับด้วย `activePeriodKey String? @unique` (เซ็ต "YYYY-MM" ตอน ACTIVE / null ตอนยกเลิก) — ตามแบบ `DeliveryCommissionItem.activeSaleId` เพราะ Prisma ไม่รองรับ partial unique index
- [x] `CashBankSourceType += PARTNER_PAYOUT` (+ `DEFAULT_SOURCE_ORDER` = 58, label/href ใน `lib/cash-bank-links.ts`, ตัวกรองหน้า cash-bank ledger)
- [x] `NotificationType += PROFIT_DISTRIBUTION_DECLARED | PROFIT_DISTRIBUTION_CANCELLED` + helper ใน `lib/notifications.ts` + label/emoji ใน `lib/telegram.ts` — **ข้อความไม่มีตัวเลขเงิน** (กลุ่ม Telegram มีคนอ่านมากกว่าคนที่ควรเห็นยอด) แจ้งแค่เลขที่เอกสาร + งวด
- [x] `lib/profit-distribution.ts`: snapshot กำไรจาก `aggregateProfitSummary()`, `computeCarryForward()`, `getYearOverview()`, `getPartnerYearSummaries()`, `getCashHealth()`, `maskBankAccountNo()`
- [x] **ยกยอดขาดทุน (ยืนยันโดยเจ้าของ)**: `carryForwardAmount` มี 2 แหล่งที่ไม่ทับกัน — (1) UNDECLARED = เดือนที่จบแล้วแต่ไม่มีเอกสาร ACTIVE (เดือนขาดทุน/ข้าม/ถูกยกเลิก) ยกกำไรสุทธิทั้งก้อนไป (2) RESTATED = เดือนที่ประกาศแล้วแต่ `FactProfit` ถูก rebuild ย้อนหลัง (ลูกค้าคืนของ ฯลฯ) ยกเฉพาะผลต่างจาก snapshot ไป **ไม่ย้อนไปแก้เอกสารเก่า** · ไม่ยกยอดก่อนเอกสารใบแรกเพราะไม่มียอดยกมาตอนเริ่มระบบ
- [x] `lib/partner-ledger.ts`: บัญชีเดินสะพัดรายคน (`balanceAfter` + `sorder` + recalculate จากวันที่เก่าสุดที่กระทบ) แบบเดียวกับ `CashBankMovement` — 1 เอกสารลง 2 แถวต่อคน (`PROFIT_SHARE` + `PAYOUT`) แม้จ่ายครบทันที เพราะประวัติสะสมคือสิ่งที่พิสูจน์สัดส่วนความเป็นเจ้าของ และรองรับกรณีค้างจ่าย/ลงทุนเพิ่มในอนาคตโดยไม่ต้อง migrate
- [x] `lib/doc-number.ts`: `generateProfitDistributionNo()` → `PD{YYMM}{4}` (เลขวิ่งตามวันที่ประกาศ ไม่ใช่งวด)
- [x] สิทธิ์ครบ 5 ขั้น: `profit_distributions.view / .view_all / .create / .cancel / .partners.manage` — **จงใจไม่ใส่ใน `STAFF_OPERATIONS_PERMISSIONS` และ `STAFF_VIEWER_PERMISSIONS`** ต้อง grant รายคน · `ADMIN_ROUTE_RULES` + `getRoutePermission()` + `requirePermission()` ทุก page/Server Action + `ADMIN_NAVIGATION` (กลุ่มการเงิน)
- [x] `view_own` vs `view_all`: คนที่ไม่มี `view_all` เห็นเฉพาะคอลัมน์/บรรทัดของตัวเอง ทั้งหน้าสรุปและหน้ารายละเอียด (ถ้าไม่มีบรรทัดของตัวเองเลย → `notFound()`)
- [x] **ปิดช่องรั่วผ่าน Audit Log**: `PROFIT_SENSITIVE_AUDIT_ENTITY_TYPES` (`ProfitDistribution`, `PartnerProfile`) ถูกกรองออกจากหน้า `/admin/audit-log` และหน้ารายละเอียด สำหรับคนที่ไม่มี `profit_distributions.view_all` — เพราะ `before/after` JSON มียอดรายคนอยู่ข้างใน
- [x] Server Actions: `createProfitDistribution` (คำนวณเงินใหม่ทุกตัวฝั่ง server ไม่เชื่อ client), `cancelProfitDistribution`, `savePartnerProfile` — ทุกตัวเขียน `AuditLog` + `try/catch` ครอบ notification
- [x] Guard ตอนสร้าง: ประกาศได้เฉพาะเดือนที่จบแล้ว · `payDate` ต้องไม่ก่อนวันเริ่มงวดและไม่เป็นอนาคต · ฐานที่แบ่งได้ ≤ 0 → ปฏิเสธพร้อมบอกว่ายอดจะยกไปเดือนถัดไป · สัดส่วนรวมต้อง 100% (±0.01) · ยอดรายคนต้องรวมเท่ายอดที่แบ่ง (±0.05) · retry เลขเอกสาร 3 ครั้ง · ชน `activePeriodKey` → แจ้งให้รีเฟรช
- [x] ยกเลิกเอกสาร: `clearCashBankSourceMovements()` + `clearPartnerLedgerSourceEntries()` (recalculate ยอดคงเหลือทั้งเส้น) + `activePeriodKey = null` ให้ประกาศงวดนั้นใหม่ได้ — **ไม่ต้องแตะ `FactProfit`/`revalidateProfitDashboardCache()` เพราะไม่เคยเขียน**
- [x] UI `/admin/profit-distributions`: ตัวเลือกปี, KPI 4 ใบ, การ์ดผู้ร่วมทุนพร้อม **แผงความเป็นธรรมสะสม** (ได้จริง % เทียบสัดส่วนตั้งต้น, เตือนเมื่อเบี่ยงเกิน ±5 จุด), กราฟแท่งคู่ 12 เดือน (กำไรสุทธิ vs ยอดที่แบ่ง, render ฝั่ง server ไม่ใช้ lib นอก), ตารางรายเดือน + **แถวสีเหลืองพร้อมปุ่ม "ประกาศ" ในแถว** สำหรับเดือนที่จบแล้วแต่ยังไม่ประกาศ (จุดที่ตอบโจทย์คีย์ย้อนหลังโดยตรง)
- [x] UI `/admin/profit-distributions/new`: เลือกงวด → ดึงกำไรมาเป็นค่าเริ่มต้น, ช่อง "ยอดที่จะแบ่ง" **แก้ไขได้ ค่าเริ่มต้น = ฐานที่แบ่งได้เต็มจำนวน**, ส่วนที่เหลือคำนวณเป็น "กันไว้ในร้าน" อัตโนมัติ, แก้ % รายคนแล้วยอดคำนวณสด (เศษปัดลงแถวสุดท้ายให้รวมตรงเสมอ), แสดงที่มาของยอดยกมาเป็นรายเดือน, **แผงเตือนเงินสดอยู่เหนือปุ่มยืนยัน** (เงินคงเหลือหลังโอน + กำไรที่จมในสต็อก + ลูกหนี้ค้าง)
- [x] UI `/admin/profit-distributions/[id]`: ที่มาของยอด (snapshot ทั้งหมด), ส่วนแบ่งรายคน, ปุ่มยกเลิกพร้อมช่องเหตุผล · `/partners`: ตั้งผู้ใช้เป็นผู้ร่วมทุน + สัดส่วนตั้งต้น + บัญชีรับเงิน (แสดงแบบ mask 4 ตัวท้ายตาม PDPA, แก้ต้องกรอกใหม่)
- [x] ครบตามกฎ repo: `loading.tsx` ทุก segment (4 ไฟล์) · `force-dynamic` ทุกหน้า · light + dark mode พร้อมกัน · ตารางมี `overflow-x-auto` + สลับเป็นการ์ดบนมือถือ · `SearchableSelect` สำหรับบัญชี/งวด/ผู้ใช้ · วันที่ผ่าน `lib/th-date.ts` ทั้งหมด (ค.ศ.)
- [x] Test: `npm run test:profit-distribution` → [lib/__tests__/profit-distribution-carry-forward.test.ts](lib/__tests__/profit-distribution-carry-forward.test.ts) 9 เคสผ่านทั้งหมด — ไม่ยกยอดก่อนเอกสารใบแรก / ยกขาดทุน / ยกเฉพาะผลต่างเมื่อกำไรถูกคำนวณใหม่ / รวม 2 แหล่งโดยไม่นับซ้ำ / เดือนที่ถูกยกเลิกถือเป็นยังไม่ประกาศ / จำกัด lookback 36 เดือน / ข้ามปีถูกต้อง / ปัดเศษต่ำกว่า 1 สตางค์ทิ้ง / `getPeriodBounds` ครอบคลุมทั้งเดือนตามเวลาไทย (รวมปีอธิกสุรทิน)
- [x] `npm run db:push` บน production (2026-08-01) — ตรวจยืนยันแล้วว่าตาราง `PartnerProfile` / `ProfitDistribution` / `ProfitDistributionItem` / `PartnerLedger`, คอลัมน์ `User.userType` และค่า enum ใหม่ทั้ง 9 ค่าอยู่ครบ, ทุกตารางใหม่ยังว่าง (0 แถว) · ใช้ `npm run db:push` ไม่ใช่ `prisma db push` เปล่า ๆ เพราะต้อง re-apply `setup-search-v2` + `setup-knowledge-rag` ต่อท้าย
- [x] **กำหนดเดือนเริ่มต้น = ก.ค. 2026 (2026-08-01)** — `PROFIT_DISTRIBUTION_START_PERIOD` hardcode ใน `lib/profit-distribution.ts` (ยืนยันโดยเจ้าของร้าน: ขาดทุน พ.ค. −110,383.10 + มิ.ย. −21,445.25 รวม 131,828.35 บาท เกิดก่อนตกลงแบ่งกำไร → **ทิ้งไปเลย ไม่ยกมาหัก**)
  - `computeCarryForward()` มี hard floor: ไม่ย้อนก่อนงวดเริ่มต้นไม่ว่ากรณีใด + คืน 0 ทันทีถ้า target อยู่ก่อนงวดเริ่มต้น
  - `listSelectablePeriods()` ไม่เสนอเดือนก่อนเริ่ม · `createProfitDistribution()` ปฏิเสธพร้อมข้อความบอกงวดเริ่มต้น
  - `getYearOverview()` เพิ่ม `isBeforeStart` — KPI "กำไรสุทธิสะสม" และ `pendingClosedMonths` ไม่นับเดือนก่อนเริ่ม, สเกลกราฟก็ไม่นับ (ไม่งั้นขาดทุน 110k จะกดแท่งเดือนอื่นจนแบน)
  - ตารางรายเดือน + การ์ดมือถือ: เดือนก่อนเริ่มแสดงเป็นสีเทา (`opacity-60`) ป้าย "ก่อนเริ่มใช้ระบบ" — ยังเห็นตัวเลขจริงแต่ประกาศไม่ได้และไม่ถูกนับ
- [x] แก้บั๊กป้ายสถานะ: เดือนที่กำไร ฿0.00 เคยติดป้าย "ขาดทุน · ไม่แบ่ง" (เงื่อนไขเป็น `<= 0`) → แยกเป็น `NO_PROFIT` = "ไม่มีกำไร · ไม่แบ่ง" กับ `LOSS` = "ขาดทุน · ไม่แบ่ง" พร้อม epsilon 0.005 กันปัญหา floating point
- [x] `TabsBar.ROUTE_LABELS` เพิ่ม `/admin/profit-distributions` + `/admin/profit-distributions/partners` — ก่อนหน้านี้แท็บด้านบนขึ้นเป็นช่องว่างเพราะไม่ได้ลงทะเบียนชื่อไว้
- [x] เลิก mask เลขบัญชีผู้ร่วมทุน (ยืนยันโดยเจ้าของร้าน) — แสดงเต็มด้วย `font-mono` ทั้งหน้าผู้ร่วมทุนและหน้าประกาศ, ฟอร์มแก้ไข prefill เลขเดิมได้แล้ว (เดิมต้องพิมพ์ใหม่เพราะค่าที่แสดงถูก mask) · ลบ `maskBankAccountNo()` ทิ้ง
- [x] หน้าผู้ร่วมทุนเพิ่มคอลัมน์ "หมายเหตุ"
- [ ] ตั้งค่าเริ่มต้นหลัง db push: เข้า `/admin/profit-distributions/partners` เพิ่มผู้ร่วมทุน 4 คน + สัดส่วนตั้งต้นให้รวมได้ 100%
- [ ] มอบสิทธิ์: grant `profit_distributions.*` รายคนที่หน้าผู้ใช้ (ผู้ที่ไม่ใช่ ADMIN จะไม่เห็นเมนูจนกว่าจะได้รับสิทธิ์)
- ไม่แตะ: สต็อก/MAVG/`writeStockCard()`, เอกสารขาย-ซื้อ-รับชำระ, `FactProfit`/Profit Dashboard, ระบบสิทธิ์เดิม, storefront

## AI chat model grounding shadow — Option B (2026-08-14)
- สถานะ: **IMPLEMENTED SHADOW / ยังไม่ flip hard filter** — production reply/search/filter เหมือน baseline ทุกกรณี
- ที่มา: `guardChatSearchIntent` ground `carModel` เฉพาะเทิร์นที่มี numeric required token ทำให้ model ที่ classifier แต่งในข้อความไทยล้วนมีโอกาสหลุดเป็น hard filter
- แยก lookup 2 หน้าที่:
  - recall lookup เดิมยังใช้ SearchSynonym แบบกว้างตามเดิม
  - grounding lookup ใหม่ merge canonical ที่ต่างแค่ตัวพิมพ์ และตัด spelling ที่เป็นเจ้าของร่วมหลาย model ออกจาก safe evidence (`hiace`, `ปาเจโร`, `altis` ฯลฯ)
- candidate policy ทุกเทิร์นคืนเหตุผล `LITERAL_CANONICAL / SAFE_SYNONYM / LATEST_MENTION_TYPO / NO_EVIDENCE / LOOKUP_UNAVAILABLE`; typo fallback ใช้ได้เฉพาะ `carMentionInLatest` ที่ตรวจว่ามีในข้อความล่าสุดจริง และ model cluster ต้องไม่กำกวม
- observability ทั้ง LINE + Messenger ใช้ action `MODEL_GROUNDING_SHADOW` ใน `LineAiAuditLog`; LINE เก็บ downstream inquiry-frame model, Messenger เก็บ downstream resolved model; ไม่เก็บข้อความลูกค้าซ้ำใน payload
- failure isolation: lookup/telemetry ล้ม → candidate เป็น unavailable และ production behavior ไม่เปลี่ยน
- golden แยกเรื่องใหม่: [scripts/test-model-grounding-shadow-golden.ts](scripts/test-model-grounding-shadow-golden.ts) (`npm run test:model-grounding-shadow-golden`)
  - persona อู่ baseline `3/5` → candidate `5/5`
  - persona ช่าง baseline `5/6` → candidate `6/6`
  - persona เจ้าของรถ baseline `2/4` → candidate `4/4`
  - รวม baseline `10/15` → candidate `15/15`; direct canonical model `144/144`; production delivery delta = 0
- report rollout: `npm run report:model-grounding-shadow` — รออย่างน้อย 200 event/ช่องทาง, lookup unavailable = 0, LINE different-model frame carry risk = 0 และต้อง manual-review candidate DROP ก่อน flip จริง
- เคสแปลกที่ suite ล็อกไว้: `tida→Tiida`, `มาร์ค→March`, `คัมรี่→Camry`, classifier คืน `Nissan March` แทน canonical `March`, ambiguous `ไฮเอซ`, recall-only `all new/spark`, LLM เติม Vios/Altis/Mega 500 เอง, follow-up ที่รุ่นอยู่ใน history

## AI chat robustness — พิมพ์ผิด/พิมพ์ตกหล่น ให้เข้าใจมากขึ้นโดยไม่ตอบมั่ว (2026-08-13)
- ที่มา: เจ้าของสั่งตรวจ logic แชท AI ทั้งหมดว่ามีวิธีทำให้ครอบคลุมกว่านี้ไหม รองรับลูกค้าพิมพ์ตกหล่น/พิมพ์ผิด/พิมพ์ไม่ครบ โดยห้ามตอบผิดหรือตอบมั่ว · เงื่อนไขที่ตกลง: **ผลลัพธ์ต้องดีกว่าเดิม แย่สุดต้องเท่าเดิม**
- ผลตรวจ: pipeline เดิม 9 ชั้นแน่นอยู่แล้ว (normalize → regex router → rule fast-path → LLM classifier → evidence guard → slot frame → completeness gate → alias/LLM resolution → relevance gate) · ช่องโหว่ที่เจอ 5 ข้อ ทำไปแล้ว 3 (+cache) เหลือข้อใหญ่สุดรอยืนยัน schema

### ข้อ 3 — typo backstop สำหรับเจตนากลุ่มเสี่ยงสูง
- ปัญหา: กฎ router เป็น regex ตรงตัว และ branch สุดท้ายเดาว่าเป็น "คำถามสินค้า" → พิมพ์ผิดกลางคำ ("เครม", "สลิบ", "โอนเงืน") ตกไปเป็นการค้นสินค้า ทั้งที่ลูกค้ากำลังจะเคลม/แจ้งโอน
- [x] แยก `osaDistance` + `typoMaxEdits` ออกจาก `search-guards.ts` → [lib/chat-core/typo-distance.ts](lib/chat-core/typo-distance.ts) (pure, เทสต์เดิม 10 ตัวผ่านครบ ไม่เปลี่ยนพฤติกรรม)
- [x] [lib/chat-core/intent-typo-guard.ts](lib/chat-core/intent-typo-guard.ts) — 28 คำสำคัญ 6 กลุ่ม, งบแก้ **1 edit คงที่** (ไม่ scale ตามความยาว), fold วรรณยุกต์ทั้งสองฝั่งก่อนเทียบ
- [x] ต่อใน [intent-router.ts](lib/chat-core/intent-router.ts) เป็น**กฎสุดท้ายก่อน branch default product** → ข้อความที่กฎเดิมจับได้อยู่แล้วไม่ถูกแตะเลย · ใช้ `reason` เดิมของกฎ literal เป๊ะ ๆ เพื่อให้ `hardGuard`/`quotationRequest` ใน processor ทำงานเหมือนเดิม แล้วแยก observability ไปที่ฟิลด์ใหม่ `matchedVia: "typo"` + `matchedTypoKeyword`
- [x] audit `INTENT_TYPO_GUARD` ใน [line-webhook-processor.ts](lib/line-webhook-processor.ts) — เป็นสัญญาณเดียวที่บอกว่า backstop ทำงาน (ใช้วัด false positive) · Messenger ใช้ `routeChatIntent` ตัวเดียวกันจึงได้ผลด้วย แต่ยังไม่มี audit row
- [x] **golden suite จับ false positive จริง**: `เคลม` ห่างจาก `แคลมป์รัดท่อ` (สินค้าจริงในคลัง) แค่ 1 edit → ถ้าใส่ไว้ ลูกค้าหาแคลมป์จะถูกเด้งเป็นงานเคลม · **ถอด `เคลม` เดี่ยวออก** ใช้รูปประสมแทน (`เคลมของ`/`เคลมสินค้า`/`ขอเคลม`) ซึ่งยังจับ "เครมของ" ได้ · กฎ literal เดิมยังจับ `เคลม` ที่สะกดถูกตามปกติ

### ข้อ 4 — ซ่อมการพิมพ์ไทย (ไม่ใช่ fold) ในชั้น recall
- [x] [lib/thai-spelling-fold.ts](lib/thai-spelling-fold.ts) แยกเป็น **2 สัญญา ห้ามสลับกัน**:
  - `foldThaiSpelling` — lossy/symmetric ตัดวรรณยุกต์ ใช้เทียบใน memory เท่านั้น (typo backstop)
  - `repairThaiTyping` — **non-lossy** ซ่อม `ํา` → `ำ` และยุบตัวซ้ำ 3+ ตัว ได้ภาษาไทยที่สะกดถูก จึงส่งเข้า search index ได้
- [x] **บทเรียนจาก golden suite**: ตอนแรกใช้ fold ยิงค้นหา → วัดได้ **26 → 0 รายการ** เพราะ index เก็บข้อความดิบ การ fold ฝั่งเดียวยิ่งแย่ · แก้เป็น repair แล้ววัดได้ **26 → 127** (เท่ากับคำที่สะกดถูกเป๊ะ)
- [x] ต่อใน [product-search-bridge.ts](lib/chat-core/product-search-bridge.ts) เป็น retry ก่อน did-you-mean · ยิงเฉพาะตอน `total === 0` และเฉพาะข้อความที่มีรอยพิมพ์จริง (`needsThaiTypingRepair`) · **คงตัวกรองครบทุกตัวรวมทั้งปีรถ + required token** ต่างจาก did-you-mean ที่ต้องทิ้งปี จึงไม่ต้องมีคำเตือนต่อท้ายให้ลูกค้า · หาไม่เจอ = ผลเดิมไม่ถูกแตะ
- [x] **ไม่แตะ `buildSearchVariants`/`normalizeSearchText`** ทั้งที่แผนแรกจะแตะ — เพราะ evidence guard (`lineValueHasCustomerEvidence`, `lineQueryContainsRequiredTokens`) ใช้อยู่ การขยายนิยาม "เท่ากัน" จะทำให้ guard **หลวมลง** (ยี่ห้อที่ LLM แต่งขึ้นจะผ่าน evidence ได้) ซึ่งขัดเงื่อนไข "แย่สุดต้องเท่าเดิม"
- [x] ไม่ retry กรณีวรรณยุกต์หายเฉย ๆ — engine กู้ได้เองอยู่แล้ว (วัดไว้แล้วใน § 2026-08-03 `ไดรเออร์↔ไดเออร์` = 100%) การ retry จะเสีย query ฟรีบนเทิร์นที่ช้าที่สุด

### ข้อ 5 — cache ผล classifier
- [x] [lib/chat-core/search-intent-cache.ts](lib/chat-core/search-intent-cache.ts) — LRU+TTL ในหน่วยความจำ (5 นาที / 300 entry), key = ข้อความล่าสุด + **ประวัติทั้งชุด** (classifier อ่านประวัติด้วย ถ้า key เฉพาะข้อความล่าสุดจะ replay หัวข้อเก่า)
- [x] **ห้าม cache ค่า `null`** — null = Gemini timeout/พัง การ cache จะตรึงปัญหาชั่วคราวไว้อีก 5 นาที
- [x] คืนค่าเป็น copy — processor เขียนทับ intent จริง (`stock_availability` → `product`) ต้องไม่รั่วไปเทิร์นถัดไป
- ผล: ตัด Gemini call แรกจาก 3 call ที่ยิงเรียงกันในเทิร์นสินค้า เมื่อลูกค้าพิมพ์ซ้ำ (พฤติกรรม "ส่งไปแล้วหรือยัง" ที่พบบ่อย) และทำให้คำตอบคงเส้นคงวาขึ้น

### Golden test
- [x] [scripts/test-chat-robustness-golden.ts](scripts/test-chat-robustness-golden.ts) (`npm run test:chat-robustness-golden`) — **39/39 ผ่าน** เทียบกับแคตตาล็อกจริง 4,825 คำ:
  - A: คำใน typo backstop ทั้ง 28 คำ ต้องไม่อยู่ในรัศมี 1 edit ของ Product/Category/CarModel/CarBrand/CarBrandAlias/SearchKeyword/CategoryAlias ใด ๆ
  - A': ชื่อสินค้าจริง 400 รายการ + คำถาม "หมวด+รุ่นรถ+ราคา" 40 แบบ ต้องไม่ถูก backstop จับ
  - B: fold ต้องไม่รวมคำที่ระบบยังแยกอยู่ (เทียบกับ baseline `normalizeSearchText` ไม่ใช่ข้อความดิบ)
  - C: repair ต้องคืนคำที่สะกดถูกเป๊ะ และผลค้นต้องเท่ากับคำที่สะกดถูก
  - C': คำค้นที่สะกดถูก 51 คำ ต้องไม่ยิง query เพิ่มเลย
- [x] ไม่ถอยหลัง: `npm test` 780 ตัว (778 ผ่าน / 0 fail / 2 skip เดิม) · `test:search-recall-golden` 43/43 · `test:semantic-golden` 12/12 · `npm run build` ผ่าน · `check:mojibake` ผ่าน
- [x] เทสต์ยูนิตใหม่: [line-intent-typo-guard.test.ts](lib/__tests__/line-intent-typo-guard.test.ts), [thai-spelling-fold.test.ts](lib/__tests__/thai-spelling-fold.test.ts), [chat-search-intent-cache.test.ts](lib/__tests__/chat-search-intent-cache.test.ts) + 4 เคสใหม่ใน [line-product-search-bridge.test.ts](lib/__tests__/line-product-search-bridge.test.ts)

### ข้อ 2 — วงจรซ่อมชื่อรถ (ทำแล้ว **ไม่แก้ schema เลย**)
- ตรวจ production ก่อนออกแบบ (`npm run diagnose:car-model-alias`, read-only) แล้ว**ล้มข้อเสนอเดิมที่จะสร้างตาราง `CarModelAlias`**:
  - `matchMode` **ไม่ต้องมี** — รุ่นรถ 144 แถว มีคู่ที่ชื่อหนึ่งอยู่ในอีกชื่อ **14 คู่** (ยี่ห้อเดียวกัน 8 คู่ เช่น `Corolla ⊂ Corolla Cross`, `Altis ⊂ Altis Limo`, `Pajero ⊂ Pajero Sport`) · รุ่นยาว 2 ตัวอักษร 5 รุ่น โดย **`IS` (Lexus) อยู่ใน Yaris/Altis/Wish/GRANDIS** · การสะกดที่มีอยู่ 25 จาก 1,041 แบบจะกลืนรุ่นอื่นถ้าตั้ง CONTAINS → **EXACT อย่างเดียว** ตรงกับ `CarBrandAlias` (45 แถว ไม่มี matchMode) และ model cluster ใน `SearchSynonym`
  - **ไม่ต้องมีตารางใหม่** — `SearchSynonym` เก็บรุ่นรถอยู่แล้ว 137 รุ่น / 1,041 การสะกด และต่ออยู่ใน resolution 3 จุด (`resolveCanonicalCarModelHint`, model-synonym evidence ใน guard, synonym promotion ใน `resolveKnownQueryIntent`) · ตารางใหม่จะเป็นแหล่งความจริงที่ 2 ของเรื่องเดียวกัน
  - **วงจร closed-loop มีครบอยู่แล้ว**: `ProductSearchLog` → `ProductSearchClusterCache` → `ProductSearchReviewOutcome` → หน้ารีวิว `/admin/reports/product-search-no-result` → auto-apply เข้า `SearchSynonym`
- **ช่องโหว่จริง**: แชทไม่เคยส่งข้อมูลเข้าวงจรนี้เลย — `ProductSearchLog` มี `admin` 2,211 + `storefront` 330 + **แชท 0 แถว** · `ClusterCache` ว่าง · `ReviewOutcome` 2 แถว · ขณะที่แชทมี 314 เทิร์นค้นหาตั้งแต่ 15 มิ.ย. และ **44.9% resolve รุ่นรถไม่ได้**
- [x] `ProductSearchLogSource` เพิ่ม `"line" | "messenger"` + ตัวเลือก `flush: "await"` ใน [product-search-telemetry.ts](lib/product-search-telemetry.ts) — แชทรันใน queue worker/coalescing loop ที่ไม่มี request scope ซึ่ง `after()` จะ throw แล้วแถวหายเงียบ
- [x] [lib/chat-core/search-telemetry.ts](lib/chat-core/search-telemetry.ts) — บันทึก **จำนวนที่ลูกค้าเห็นจริงหลังผ่าน guard ทุกตัว** ไม่ใช่ยอดดิบจาก engine (ถ้าใช้ยอดดิบ เคสที่สำคัญที่สุดจะหาย: search เจอแต่ guard กรองทิ้งหมดเพราะยืนยันรุ่นไม่ได้)
- [x] ต่อทั้ง 2 ช่องทาง: LINE เป็น `void` (ห้ามกินงบ reply-token) · Messenger `await` ที่ทางออกทั้ง 4 จุดพร้อมจำนวนจริง
- [x] [vehicle-llm-fallback.ts](lib/chat-core/vehicle-llm-fallback.ts) `correctVehicleSpelling` — ฝาแฝดฝั่งรถของ `correctPartSpelling` · ไม่เลือกรถเอง แค่เสนอคำที่ถูก
- [x] [vehicle-synonym-staging.ts](lib/chat-core/vehicle-synonym-staging.ts) — re-resolve ผ่าน `CarModel`/`CarBrand` จริง ใช้ต่อเมื่อ **ตรงรุ่นเดียว** (2 แถว = กำกวม → ทิ้ง) แล้ว stage เป็น `ProductSearchReviewOutcome` `status=PENDING` `candidateAction="vehicle-synonym"` (คอลัมน์เป็น `VarChar(50)` + TS union → เพิ่มค่าใหม่ = แก้โค้ดอย่างเดียว) · `appliedRef` เก็บรุ่นที่เสนอไว้ให้ฟอร์ม prefill
- [x] [vehicle-synonym-guardrails.ts](lib/chat-core/vehicle-synonym-guardrails.ts) — กัน 3 ชั้น: รูปแบบคำ (ยอมให้มีตัวเลข/`-` ต่างจากกฎหมวดที่ห้ามตัวเลข เพราะชื่อรุ่นมีจริง), คำที่**เป็นชื่อรถจริงอยู่แล้ว** (ไม่ใช่คำผิด), คำอะไหล่ (ห้ามกลายเป็นคำพ้องรุ่นรถ)
- [x] Idempotent: ไม่เขียนทับแถวที่แอดมินตัดสินไปแล้ว (status ≠ PENDING) และไม่ทับ note ที่แอดมินพิมพ์เอง
- [x] หน้ารีวิวเดิมเพิ่มพาเนล "AI เสนอชื่อรุ่นรถที่ลูกค้าพิมพ์ผิด (รออนุมัติ)" — ปุ่ม "อนุมัติ" ใช้ `applySearchSynonymCandidate` ตัวเดิม (เขียน `SearchSynonym` + ปิดคิวเป็น APPLIED) · ปุ่ม "ไม่ใช่" ใช้ `markProductSearchReviewOutcome` เดิม · ไม่แตะ auto-apply (กรอง `!== "search-synonym"` อยู่แล้ว จึงไม่ยุ่งกับ lane ใหม่)
- [x] audit `VEHICLE_SYNONYM_SUGGESTED`
- [x] **แก้บั๊กที่ทบทวนเจอเองหลังส่งมอบรอบแรก** — เดิม stage ด้วย `guardedSearchIntent.carModel` ซึ่งเป็นชื่ออังกฤษมาตรฐานจาก classifier ("Fortuner") ไม่ใช่คำที่ลูกค้าพิมพ์ ("ฟอจูเนอ") → guardrail `ALREADY_KNOWN_SPELLING` จะปัดทิ้งทุกครั้ง **วงจรจะไม่เคยทำงานเลย** · เปลี่ยนไปใช้ `correction.original` และ**ตรวจว่ามีอยู่ในข้อความลูกค้าจริง**ด้วย `chatCarMentionOccursInLatest` (กฎเดียวกับ `carMentionInLatest` — คำที่ LLM อ้างว่าลูกค้าพิมพ์ ต้องตรวจก่อนเชื่อเสมอ) · ตรวจไม่ผ่าน = ไม่ stage ไม่เดา
- [x] [vehicle-synonym-attempt-cache.ts](lib/chat-core/vehicle-synonym-attempt-cache.ts) — กันยิง Gemini ซ้ำเมื่อลูกค้าพิมพ์ข้อความเดิม (audit จริงพบพิมพ์ซ้ำ 2–3 ครั้งติดกันเป็นปกติ) · แยกเป็นโมดูลไม่มี DB import ตามแบบ `category-alias-guardrails` จะได้ unit-test โดยไม่ต้องมีฐานข้อมูล
- [x] golden suite เพิ่มหมวด D (**45/45 ผ่าน**): ชื่อรุ่น/ยี่ห้อจริง 163 ชื่อ + การสะกดที่รู้จัก 1,556 แบบ ต้องถูกกันไม่ให้กลายเป็นคำผิด · คำอะไหล่ 277 คำ ต้องไม่หลุดไปเป็นชื่อรุ่น · รุ่นที่ไม่มีจริงต้อง resolve ไม่ได้ · **ชื่อกำกวม `MIRAGE / Mirage` ต้อง resolve เป็น null** (ห้ามเดาข้างใดข้างหนึ่ง)
- ⚠️ ผลข้างเคียงที่ต้องรู้: พอแชทเริ่มเขียน `ProductSearchLog` ตัวเลขในรายงานค้นหาจะสูงขึ้นทันที เทียบย้อนหลังตรง ๆ ไม่ได้ (แบบเดียวกับหมายเหตุ 7 ส.ค.) — ฟิลด์ `source` แยกให้กรองได้

### พบระหว่างทาง (ยังไม่แก้ รอเจ้าของสั่ง)
- [ ] **`CarModel` มีแถวซ้ำต่างกันแค่ตัวพิมพ์: `MIRAGE` กับ `Mirage`** (144 ชื่อ → ระบบเห็นเป็น 143 คำ) · ทั้งระบบ normalize เป็นตัวเล็กอยู่แล้วจึงไม่ทำให้ค้นผิด แต่แยก fitment ออกเป็น 2 รุ่นในข้อมูล ควรรวมเป็นแถวเดียว
- [ ] Messenger ยังไม่มี audit row สำหรับ `INTENT_TYPO_GUARD` (ใช้ router ตัวเดียวกันจึงได้พฤติกรรม แต่วัดผลไม่ได้)

### ยังไม่ได้ทำ — รอเจ้าของสั่ง
- [ ] **ข้อ 1: หน้าแอดมินคุณภาพแชท** สรุป `LineAiAuditLog` (เจ้าของเลือกทำ 2+3+4+5 ยังไม่รวมข้อ 1) — ตอนนี้ audit ที่เพิ่มไว้ (`INTENT_TYPO_GUARD`, `VEHICLE_SYNONYM_SUGGESTED`) ยังต้องเปิดดูราย conversation
- [ ] เก็บสถิติ 1–2 สัปดาห์ว่าพาเนล "AI เสนอชื่อรุ่นรถ" มีของเข้ากี่รายการ/อนุมัติกี่ % ก่อนตัดสินใจว่าจะเปิด auto-apply ให้ lane นี้ไหม

## Code review ทั้ง repo — test infra, security, กฎวันที่, rate limit (2026-08-07)

- บริบท: รีวิวทั้ง repo (1,306 ไฟล์, 113 models, 279 indexes) แล้วไล่แก้ตามรายการที่เจ้าของร้านยืนยันทีละข้อ · ผลรวม: `npm test` **728 tests / 0 fail** · `tsc --noEmit` 0 error · `eslint` **0 error** (จากเดิม 1) · `npm run build` ผ่าน

**ตาข่ายรองรับ (ทำก่อนเพราะข้ออื่นพึ่งของนี้)**
- [x] `npm test` — เดิมมีไฟล์เทสต์ 104 ไฟล์แต่ผูกกับ npm script แค่ 4 ไฟล์ และ **ไม่มี CI เลย** → ต่อสายครบทุกไฟล์ + เพิ่ม `npm run typecheck` / `npm run verify`
- [x] [scripts/test-env.mjs](scripts/test-env.mjs) ปัก DATABASE_URL ปลอมเสมอ — เทสต์แตะ Supabase production ไม่ได้ (ห้ามเติม `--env-file=.env.local` เข้า `npm test`)
- [x] [.github/workflows/ci.yml](.github/workflows/ci.yml) — lint + typecheck + test + mojibake · **ไม่รัน `next build`** เพราะหน้า storefront เป็น static generation ที่ต้องต่อ DB จริง และ Vercel build ทุก push อยู่แล้ว
- [x] เทสต์แดงค้าง 4 ตัวตั้งแต่ `ba1097e` (9 มิ.ย.) — โค้ดเปลี่ยน poll interval 5→10 นาทีแต่ไม่ได้อัปเดตเทสต์ ไม่มีใครเห็นเพราะไม่มีอะไรรันมัน · แก้แล้วและผูกค่ากับ constant จริงเพื่อไม่ให้ค้างซ้ำ

**Security**
- [x] `next-auth` 5.0.0-beta.30 → **beta.32** (`@auth/core` 0.41.0 → 0.41.3) — ปิด critical *"auth check fail open"* · critical เหลือ **0** (จาก 2) · ยังเหลือ 11 high ที่ยังไม่แตะรอบนี้: `sharp`, `next`, `prisma`, และ transitive ของ dev tooling
- [x] `searchProductsAction` / `loadMoreSearchProductsAction` เดิม **ไม่มี rate limit เลย** — proxy.ts จำกัดเฉพาะ GET แต่ server action เป็น POST → query หนักที่สุดในระบบเรียกได้ไม่จำกัด · ใส่เพดาน 60/นาที/IP (เท่า GET) ผ่าน `ApiThrottle`
- [x] `/api/liff/tracking/[token]` เดิมใช้ Map ใน process → เพดานจริง = 10 × จำนวน instance · เปลี่ยนเป็น DB-backed (traffic ต่ำ ไม่ใช่ hot path) · **คง in-memory ไว้ที่ `/api/search/products/autocomplete` ตั้งใจ** เพราะยิงทุกตัวอักษรที่พิมพ์
- [x] [lib/client-ip.ts](lib/client-ip.ts) — เดิม parse `x-forwarded-for` เองซ้ำ 3 ที่ด้วย fallback ไม่เหมือนกัน
- [x] [lib/admin-route-access.ts](lib/admin-route-access.ts) — ยก authorization gate ของ `/admin` ออกจาก `authorized()` callback มาเป็นฟังก์ชัน pure (พฤติกรรมเดิมทุกบรรทัด) เพื่อให้เทสต์ได้ · **20 golden tests** ครอบทุกสาขา รวมเคส fail-closed

**บั๊กจริงที่เจอระหว่างทาง**
- [x] `node-cron` ใน [instrumentation.ts](instrumentation.ts) **ไม่เคยทำงานบน Vercel** (instance ถูก freeze/recycle) → `Notification` ไม่เคยถูกล้างเลยตั้งแต่ 9 มิ.ย. · ย้ายเป็น Vercel Cron `/api/notifications/cron/cleanup` เวลา 19:00 UTC = 02:00 ไทย (node-cron อ่าน TZ ของเซิร์ฟเวอร์ = UTC จึงเพี้ยนอยู่แล้วต่อให้ยิงติด) · ถอด `node-cron` + `@types/node-cron` ออก
- [x] telemetry ค้นหาใช้ `void promise` → โดนทิ้งเมื่อ instance freeze หลังส่ง response · เปลี่ยนเป็น `after()` · **ตัวเลขใน `ProductSearchLog` จะสูงขึ้นจากเดิม เทียบกับช่วงก่อน 7 ส.ค. ตรง ๆ ไม่ได้**
- [x] [lib/knowledge-public.ts](lib/knowledge-public.ts) ใช้ `toISOString().slice(0,10)` = วัน UTC → บทความที่ activate 00:00–07:00 น. แสดงวันย้อนหลัง 1 วัน ทั้งบนหน้าเว็บและใน JSON-LD `datePublished`/`dateModified` · เปลี่ยนเป็น `getThailandDateKey()`
- [x] `prisma/scripts/audit-stock-card.ts` พิมพ์ `docDate` ด้วย `toISOString()` — `docDate` เก็บที่เที่ยงคืนไทย (17:00 UTC วันก่อน) จึงรายงานผิดวันทุกแถว
- [x] Backup Center ใช้ `Intl.DateTimeFormat("th-TH")` → แสดง 2569 แทน 2026 (จุดเดียวในทั้ง repo)

**บังคับกฎอัตโนมัติ แทนการพึ่งความจำ**
- [x] [eslint.config.mjs](eslint.config.mjs) เพิ่ม `no-restricted-syntax` 3 ข้อ: ห้าม `"th-TH"` ใน `DateTimeFormat`/`toLocale{Date,Time}String` (ไม่แตะ `toLocaleString` ของตัวเลข) และห้าม `toISOString().slice()` กับวันที่
- [x] แก้ lint error ค้างที่ `ProductImageZoomLightbox` ด้วย [components/shared/use-mounted.ts](components/shared/use-mounted.ts) (`useSyncExternalStore` — ผลลัพธ์เท่าเดิม ไม่มี render รอบสอง)

**UX / คุณภาพ**
- [x] เพิ่ม `app/error.tsx`, `app/global-error.tsx`, `app/not-found.tsx`, `app/admin/(protected)/error.tsx` — เดิม **ไม่มีสักไฟล์ทั้ง repo** ผู้ใช้จึงเจอหน้า error ภาษาอังกฤษของ framework · ไม่แสดง `error.message` (กัน Prisma/PII รั่วตาม .rules §7) แสดงแค่ `digest` ไว้อ้างอิง
- [x] SearchableSelect ครบตามกฎ: แปลง dropdown บัญชี cash-bank 8 จุดใน 6 ไฟล์ (เดิม native `<select>` ขณะที่อีก 7 หน้าใช้ SearchableSelect กับข้อมูลชนิดเดียวกันแล้ว) · `direction` (2 ตัวเลือก) คง native ตามข้อยกเว้น
- [x] `select` แทน `include`/ดึงทุกคอลัมน์: `master/car-brands` (ตัด `notes` + timestamps ที่ติดมาทุก alias/model), `master/suppliers`, `master/expense-codes` · prop type ฝั่ง client ผูกกับ `select` แล้ว การขยาย query จึงต้องตั้งใจ
- [x] [lib/env.ts](lib/env.ts) — ตรวจ env ตอน boot · fatal เฉพาะตัวที่ระบบตายแน่ ที่เหลือเตือนพร้อมบอกผลกระทบ (เช่น `CRON_SECRET` ไม่ตั้ง = cron ตอบ 401 เงียบ ๆ ทุกวัน) · **การเลื่อน warning เป็น error ต้องดูทีละตัว** ไม่งั้น deploy พัง
- [x] ย้าย `/home3`, `/home4` (route ที่มีแค่ `permanentRedirect("/")`) ไป `redirects()` ใน next.config → 308 เดิมแต่จบที่ edge · **`/home2` ไม่รวมด้วย** เพราะกลับมาเป็นหน้าจริงที่กำลังพัฒนาอยู่ · ย้าย `lot_flow_aircon_spare_parts.docx` ไป `docs/`

**ตรวจแล้วตัดสินใจ "ไม่ทำ" (พร้อมเหตุผล)**
- ตัด `revalidatePath("/")` ใน `master/categories/actions.ts` — ซ้ำซ้อนกับ `updateTag()` จริง แต่การแก้หมวดหมู่เกิดเดือนละไม่กี่ครั้ง ประหยัดแทบไม่มี ขณะที่ถ้าเข้าใจ route-cache tag inheritance ของ Next 16 คลาดเคลื่อน หน้าแรกจะค้าง 1 ชม. — **ไม่คุ้ม**
- เปลี่ยน `where: { OR: keys }` (≤300 คู่) ใน `lots/expiry` เป็น `productId: { in: [...] }` — มีขอบเขตชัดด้วย `take: 300` อยู่แล้ว และเสี่ยงดึงแถวมากกว่าเดิมถ้าสินค้าตัวหนึ่งมีหลายร้อย lot
- อัปเกรด `sharp` / `next` / `prisma` — เจ้าของร้านเลือกแยก deploy ต่างหาก
- Sentry / log drain — เลือกไม่ใช้ (ดูหัวข้อ Error monitoring ด้านล่าง): Sentry ต้องแก้ CSP `connect-src` เพิ่ม domain ภายนอก + เพิ่ม client bundle ~30KB gz ซึ่งขัดกับงาน Core Web Vitals ที่ลงแรงไว้ · ปัญหาจริงคือ "ไม่รู้ว่ามี error" ไม่ใช่ "วิเคราะห์ error ไม่ลึกพอ"

**ค้างไว้ (ยืนยันแล้วว่าเลื่อน)**
- [ ] ข้อ 11 — รวบ `upsert` ทีละ lot ใน [lib/lot-control.ts](lib/lot-control.ts) (ใบซื้อ 20 lot = 60 round-trip เรียงกันขณะถือ `FOR UPDATE` + `lock_timeout` 8 วิ) · **ต้องเขียน golden test คลุม `writePurchaseLots`/`writeAdjustmentLots` ก่อน** เพราะกระทบต้นทุนสินค้าโดยตรง
- [ ] ข้อ 18 — แตกไฟล์ >800 บรรทัด 28 ไฟล์ (`line-webhook-processor.ts` 4,414) · ทำแบบ opportunistic ตอนแก้ไฟล์นั้นอยู่แล้ว
- [ ] อัปเกรด `sharp` (CVE libvips บน path ที่รับ upload จากลูกค้า) — ควรทำเป็นลำดับถัดไป
- [ ] ตรวจหลัง deploy: cron `/api/notifications/cron/cleanup` รอบแรกจะลบ notification ที่อ่านแล้ว + เก่ากว่า 30 วัน **ที่สะสมมาตั้งแต่ 9 มิ.ย. ในครั้งเดียว**

## Error monitoring — เตือนผ่าน Telegram ที่มีอยู่แล้ว (2026-08-07)

- **สถานะก่อนแก้ (นับจริง)**: `console.error` 165 · catch ที่ไม่ log 337 (ส่วนใหญ่ตั้งใจ เช่น parse fallback/permission check) · ในนั้น **46 จุดคืน "เกิดข้อผิดพลาด" ให้ผู้ใช้โดยไม่เก็บสาเหตุไว้เลย** · การดักจับ error ฝั่ง browser = 0 · Vercel runtime log ไม่มี alert → ธุรกรรมล้มเหลวจะรู้ต่อเมื่อลูกค้าโทรมาบ่น
- **จุดที่แย่ที่สุด**: `warranty-claims/actions.ts` มี 14 catch แต่ `console.error` = **0** — เอกสารเคลมทั้งประเภทล้มเหลวแบบไร้ร่องรอย
- **ทำไมไม่ใช้ Sentry**: CSP `connect-src` ล็อกไว้ ([next.config.ts](next.config.ts)) ต้องเปิด domain ภายนอกเพิ่ม + client bundle ~30KB gz ขัดกับงาน CWV · ระบบมีชื่อ/เบอร์/ที่อยู่ลูกค้าเต็มไปหมด scrubbing พลาดครั้งเดียวคือ PII รั่วออกนอกประเทศ · ปัญหาจริงคือ "ไม่รู้ว่ามี error" ไม่ใช่ "วิเคราะห์ไม่ลึกพอ" — dashboard ที่ไม่มีใครเปิดมีค่าน้อยกว่าข้อความที่เด้งเข้ามือถือ
- [x] [lib/error-reporting.ts](lib/error-reporting.ts) — ใช้ `lib/telegram.ts` (retry + จัดการ 429) ที่มีอยู่แล้ว ไม่เพิ่ม dependency ไม่แตะ CSP · **ไม่ใช่ logger ทั่วไป**: `console.error` ยังเป็นบันทึกหลัก ตัวนี้ต่อเฉพาะ path ที่แพงถ้าพลาด
- **3 คุณสมบัติที่สำคัญกว่าความครบถ้วน**: (1) พังไม่ได้ — ทุก error ภายในถูกกลืน · (2) ถล่มไม่ได้ — 1 ข้อความ/signature/15 นาที ผ่าน `ApiThrottle` · (3) ไม่พก payload อิสระ — ส่งได้แค่ `scope`/`docNo`/`entityId`/`userId`
- **หัวใจคือการจัดกลุ่ม**: `normalizeErrorMessage()` ตัด uuid/เลขเอกสาร/timestamp ออก → ใบขาย 50 ใบที่พังด้วยสาเหตุเดียวกันได้ **1 ข้อความ ไม่ใช่ 50** · ถ้าตรงนี้พัง alert จะกลายเป็น noise แล้วคนเลิกอ่าน = แย่กว่าไม่มี · golden test 14 เคสล็อกคุณสมบัตินี้ไว้
- [x] ต่อเข้า 32 จุด: warranty-claims 7 · sales/purchases/receipts/credit-notes/purchase-returns (create/cancel/update) 15 · cron ทั้งหมด 8 · LINE + Messenger webhook 2 · ตัด `console.error` ซ้ำซ้อนออก 14 บรรทัด
- **ตัดสินใจต่างจากแผนตอนเสนอ**: ไม่ใส่ reporting ใน `writeStockCard` โดยตรง เพราะถูกเรียกในทรานแซกชันและจะได้ 2 alert ต่อ 1 ความล้มเหลว — การรายงานที่ขอบ action ครอบคลุมอยู่แล้วและได้ scope ที่มีความหมายกว่า (`sales.create` บอกได้มากกว่า `stock-card`) · `catch` ที่คืน "ไม่มีสิทธิ์เข้าถึง" ไม่ต่อ เพราะเป็นการปฏิเสธสิทธิ์ปกติ ไม่ใช่ความผิดพลาด
- [x] ไม่แตะตามที่ตกลง: 165 call site เดิม · client-side capture · CSP · dependency ใหม่
- [ ] `ALERT_COOLDOWN_MS` = 15 นาที เป็นค่าที่เดาไว้ก่อน — ใช้จริงสัก 1-2 สัปดาห์แล้วปรับ (แก้ที่เดียวใน [lib/error-reporting.ts](lib/error-reporting.ts))
- [ ] ถ้าอยากได้สถิติย้อนหลัง/ค้นหา log ค่อยเพิ่ม structured logging + Vercel Log Drain ทีหลัง — ทาง Telegram นี้ไม่ขวาง

## ค้นหาช้า — ตัด `similarity(search_text)` ออกจากบล็อกให้คะแนน (2026-08-04)
- บริบท: ตามรอยมาจาก error `timeout exceeded when trying to connect` ตอน revalidate cache (§ด้านล่าง 2026-07-31) ที่เกิดซ้ำเช้า 4 ส.ค. → เจาะจนพบว่าตัวกินเวลา connection จริงคือ query ค้นหาสินค้า ไม่ใช่ config ของพูล
- **Metric ฐานข้อมูล (อ่านอย่างเดียว)**: `max_connections = 90` (ไม่ใช่ 200 ตามคอมเมนต์ใน [lib/db.ts](lib/db.ts)) · `product_search_documents` มีแค่ 949 แถว → ที่ช้าคือ **ต้นทุน CPU ต่อแถว** ไม่ใช่ข้อมูลเยอะ · `search_text` ยาวเฉลี่ย 2,389 ตัวอักษร (`trgm_text` 702)
- **ยืนยันว่า `12060c1` (probe `trgm_text` ตอน recall) ได้ผลแล้ว** — แยกด้วย `pg_stat_statements.stats_since`: ranked-search mean ถ่วงน้ำหนัก **629 ms → 287 ms (−54%)**, worst case **83.3 วิ → 2.3 วิ** · query shape ที่ mean 2,376 ms เป็นของเก่าตั้งแต่ 11 มิ.ย. ไม่ถูกเรียกแล้ว
- [x] ตัด `similarity(f_unaccent(lower(psd.search_text))) * 120` ออกจาก `GREATEST()` ในบล็อกให้คะแนน [lib/product-search.ts](lib/product-search.ts) และออกจาก `match_trigram_high` (chat relevance gate)
- **หลักฐานก่อนแก้ — replay คำค้นจริงทั้งหมดจาก `ProductSearchLog` 1,496 คำ (ไม่ใช่บอท, 21 พ.ค.–4 ส.ค.)**
    - สินค้าที่ได้: **หาย 0 คำ · เพิ่ม 0 คำ** (recall เหมือนเดิมทุกประการ)
    - ลำดับเปลี่ยน 19 คำ (1.3%) · ใน 24 อันดับแรก 8 คำ (0.5%) · ใน 5 อันดับแรก 3 คำ (0.2%) — ตรวจรายตัวแล้ว **ทุกเคสเป็นการสลับแถวที่คะแนนเท่ากันเป๊ะ** (เช่น `frontier` อันดับ 3↔4 คะแนน 271.8 เท่ากันทั้งคู่) · 5 ใน 8 เป็นคำพิมพ์มั่ว 2 ตัวอักษร (`ยจ` `ผา` `สน` `ทท`)
    - chat gate (`match_trigram_high >= 0.5`): **0 คำ** ที่มีสินค้าผ่านเกณฑ์ได้เพราะ `search_text` เพียงอย่างเดียว · ค่า `similarity(search_text)` สูงสุดทั้งคลังคำค้น = **0.24** (เกณฑ์ต้องการ 0.5) → คำตอบแชทไม่เปลี่ยน
    - เหตุผลเชิงหลักการ: `search_text` รวมทุกฟิลด์ (~2.4KB) เทียบกับคำค้น 2–20 ตัวอักษร → ให้คะแนน 0–1.8 จากคะแนนรวม 240–2,642 (<0.07%) ในบล็อกที่คู่แข่งคือ `product_code × 420` / `product_name × 250` — **ไม่เคยชนะด้วยความเกี่ยวข้องจริง เป็นแค่ตัวตัดสินเสมอ แต่กิน CPU ~575 ms/คำค้น**
- **ผลด้านความเร็ว (วัดบน production, best-of-3)**: `คอมแอร์` 673→444 ms (−34%) · `คอยล์ร้อน` 588→471 ms (−20%) · `วาล์วบล็อก` 257→197 ms (−23%) · `โบลเวอร์` 487→396 ms (−19%) · รวม 43 คำ −16% (ตัวเลขรวม network RTT ~120–150 ms จากเครื่อง dev → ของจริงฝั่งเซิร์ฟเวอร์ประหยัดมากกว่านี้)
- **ทางเลือกที่ตรวจแล้วตัดทิ้ง**: แยก recall/scoring เป็น 2 เฟส (CTE candidate ก่อน) — วัด A/B 8 คำค้นได้ **+0 ถึง +1% (ช้าลง)** เพราะ Postgres ให้คะแนนเฉพาะแถวที่ผ่าน recall อยู่แล้ว · ขยาย `DB_POOL_MAX` 5→8 — **ไม่ทำ** เพราะเพดานจริงคือ 90 ไม่ใช่ 200 เสี่ยงกว่าปัญหาเดิม
- [x] ทดสอบผ่าน: `test:search-recall-golden` **43/43** · `test:semantic-golden` **12/12** · `evaluate:search-v2` ผ่านทุกเคส · `npx eslint lib/product-search.ts` 0 error · `npm run build` ผ่าน
- [ ] เฝ้าดูหลัง deploy 3–5 วัน: `pg_stat_statements` mean ของ ranked-search ควรต่ำกว่า 287 ms + นับ `timeout exceeded` ใน Vercel log ควรลดลง
- [ ] ค้างไว้ (คนละเรื่อง): [product-search.ts:991](lib/product-search.ts:991) `db.$queryRaw` ตรง ๆ และ Prisma `contains` fallback (`Product` LEFT JOIN `Category`, mean 1.96 s / max 31 s) ยัง **ไม่มี statement_timeout 8 วิ** ครอบ — ต่างจาก `dbSearchRaw`/`dbSearchTx`

## Prisma `contains` fallback — ตัด `count()` ที่ไม่จำเป็น (2026-08-04)
- บริบท: ต่อจากข้อค้าง statement_timeout ด้านบน · `searchProductIdsFallback()` ([product-search.ts:964](lib/product-search.ts:964)) ถูกเรียก 2 ทาง — (1) ไม่มีคำค้น (กรอง/เรียกดูเฉย ๆ) ซึ่งถูกและเร็ว 7.3 ms และ (2) **เป็น error handler ตอน `searchProductIdsV2` โยน error** ([2042](lib/product-search.ts:2042)) ซึ่งยิง query หนักกว่าเดิมตอนที่ DB กำลังตึงพอดี
- **ต้นทุนจริง** (`pg_stat_statements`): `findMany` 50 calls · mean 1,959 ms · **max 31,004 ms** · `count` คู่แฝด 48 calls · mean 1,426 ms · max 10,248 ms — เกิดน้อย (~100 ครั้งใน 2 เดือน) แต่เป็นความเสี่ยงหางยาว
- **สาเหตุ** (EXPLAIN ANALYZE): `where` เป็น OR 12 เงื่อนไข `ILIKE '%q%'` + EXISTS 7 ตัว → **seq scan 8 ตาราง** รวม `ProductAlias` (38,778 แถว/19 MB) และ `ProductCarModel` ที่ถูกสแกน **4 รอบแยกกัน** · `ILIKE '%…%'` ใช้ index ไม่ได้เลย (btree ช่วยไม่ได้ · GIN trigram ที่มีอยู่สร้างบน `f_unaccent(lower())` ซึ่งไม่ตรงกับ `ILIKE` ที่ Prisma สร้าง)
- [x] **D2 — ยิง `count()` เฉพาะเมื่อจำเป็น** ([product-search.ts:1005](lib/product-search.ts:1005)): `rows.length < take && (rows.length > 0 || skip === 0)` → `total = skip + rows.length` · เงื่อนไขต้องเข้มแบบนี้เพราะหน้าว่างที่ `skip > 0` อนุมานยอดรวมไม่ได้ · **แม่นยำ 100% ทุกกรณี ไม่แตะ `where`**
    - วัดผล (best-of-3, production): คำค้นที่ผลไม่เต็มหน้า `P0104` 316→158 ms · คำที่ไม่มีผลเลย 415→212 ms (**~−50%**) · คำหมวดกว้างที่ผลเกิน 30 รายการยังยิง count เหมือนเดิม
- **ทางเลือกที่ตรวจแล้วตัดทิ้ง**
    - **ตัด predicate ทิ้ง 5 ตัว (`description`/`submodel`/`engineCode`/`engineSize`/`note`)** — replay คำค้นจริง 1,498 คำ: **78 คำ (5.2%) จะสูญสินค้า** และ **4 คำผลกลายเป็นศูนย์** (`ยจ` 23→0, `ผา` 3→0, `2063` 1→0) · แยกรายตัว: `description` ทำให้หาย 621 hit (ตัวการหลัก), `note` 59, `submodel` 6, **`engineCode`/`engineSize` = 0** · แต่พอตัดเฉพาะ 2 ตัวที่ปลอดภัยจริง **ประหยัดแค่ ~1%** → **ไม่คุ้ม ไม่ทำ**
    - **`COUNT(*) OVER()` นัดเดียว (raw SQL)** — เร็วขึ้น ~50% ทุกเคส แต่ต้องเขียน `where` เป็น SQL คู่ขนานกับที่ Prisma ประกอบจากตัวกรอง 20+ ตัว → หนี้ทางเทคนิคสูง เพี้ยนแล้วผลค้นหาผิดเงียบ ๆ
    - **ดึง id ทั้งหมดแล้วตัดหน้าใน JS** — เร็วเท่ากัน (วัดแล้ว) และเป็น Prisma ล้วน แต่ไม่มี cap จำนวนแถว = ระเบิดเวลาเชิงสเกล (ตอนนี้เพดาน 921 สินค้า active) · ถ้าจะทำต้องมี `FALLBACK_MAX_SCAN_IDS` กำกับ
    - **`Promise.all` ยิงขนาน** — ใช้ 2 connection พร้อมกันตอนพูลกำลังตึง = สวนทางเป้าหมายทั้งหมด
- [x] ทดสอบผ่าน: `test:search-recall-golden` 43/43 · `npx eslint lib/product-search.ts` 0 error · `tsc --noEmit` ผ่าน
- [ ] ยังไม่ทำ: ครอบ `statement_timeout` ให้ fallback (ทางเลือก A เดิม) — เป็นตัวปิดความเสี่ยง 31 วินาทีจริง ๆ ส่วน D2 แค่ลดงานลง
- ไม่แตะ: recall predicate, น้ำหนักคะแนนตัวอื่น, semantic/vector, สต็อก/ราคา/เอกสาร, schema, config พูล

## LINE chat ตอบสินค้าไม่ครบ — `บล็อควาล์ว revo` เจอ 1 จาก 2 ตัว (2026-08-03)
- บริบท: ลูกค้าถาม `บล็อควาล์ว revo` (conv `cmscwh8ie…`, 14:26 น.) จูนตอบแค่ P0093 ทั้งที่แคตตาล็อกมี P0362 (DENSO) ตรงรุ่นด้วย · เทิร์นถัดมาที่ระบบเขียนคำค้นเองเป็น `วาล์ว (Expansion Valve) Hilux Revo` ได้ครบ 2 ตัว → ตัวกรองและข้อมูลสินค้าไม่ได้ผิด ปัญหาอยู่ที่ "คำ"
- **สาเหตุ**: lexeme `บล็อควาล์ว` (สะกดด้วย **ค**) มีอยู่ใน `keyword_text` ของสินค้าแค่ 5 จาก 47 ตัวในหมวดวาล์ว และไม่มีแถว `SearchSynonym` เชื่อมไปหา `วาล์วแอร์` (คำเดียวที่ครอบคลุมครบ 47/47) → precise AND query เจอแถวเดียว · ตัวช่วยทั้งสองชั้นไม่ทำงานเพราะออกแบบให้ยิงเฉพาะตอนได้ **0** แถว: broad OR fallback ([lib/product-search.ts](lib/product-search.ts) `rows.length === 0 && hasMultipleConcepts`) และ semantic recall (`allowVectorOnly = lexicalRows.length === 0`) → **ผลลัพธ์ "ถูกแต่ไม่ครบ" หลุดทุกด่านโดยไม่มีสัญญาณเตือน**
- [x] ขยาย `MAX_SYNONYMS_PER_TERM` 10 → 18 ใน [lib/search-synonyms.ts](lib/search-synonyms.ts) + ค่า mirror ใน [SearchSynonymsClient.tsx](app/admin/(protected)/master/search-synonyms/SearchSynonymsClient.tsx) (hardcode ไว้คนละที่ ต้องแก้คู่กันเสมอ) — คลัสเตอร์ `วาล์วแอร์` เต็ม 10/10 อยู่แล้ว ถ้าไม่ขยายต้องลบคำที่ใช้งานได้ทิ้งเพื่อเอาคำใหม่เข้า
- [x] วัดต้นทุนความเร็วบน production ก่อนขยาย: FTS เป็น GIN index scan → concept ที่โตจาก 11 เป็น 16 lexeme ช้าลง **+0.03 ms** (0.499 → 0.532 ms) และ +0.05 ms ในกรณีเลวร้ายสุด (3 concept เต็มเพดานพร้อมกัน) — เทียบกับ network RTT ไป Supabase ~41 ms ต่อ query ถือว่าวัดไม่ออก · broad fallback ไม่เปลี่ยนเลย (634 → 636 ms, ถูกครองด้วย trigram)
- [x] [scripts/seed-expansion-valve-block-synonyms.ts](scripts/seed-expansion-valve-block-synonyms.ts) (`npm run seed:expansion-valve-block-synonyms`, dry-run เป็นค่าเริ่มต้น + `-- --apply`) — เขียนคลัสเตอร์ `วาล์วแอร์` เป็น 18 คำ รันบน production แล้ว
- [x] คำที่เพิ่ม 2 ตระกูล: **บล็อก** (บล็อควาล์ว, บล็อกวาล์ว, วาล์วบล็อก, วาล์วบล็อค, วาล์วบล๊อก, บ๊อกวาล์ว, บล๊อกวาล์ว, บล๊อควาล์ว, วาล์วบล๊อค) และ **วาว์ล** (วาว์ลแอร์, วาว์ล, วาว์ว) · ตัดออก 4 คำที่ไม่มีสินค้าตรงและไม่เคยพบว่าลูกค้าพิมพ์ (บ๊อกวาล์วแอร์, วาวล์แอ, เอกแพนชันวาล์ว, วาล์วขยาย)
- [x] ผลหลังแก้ (วัดด้วย `searchProductIdsV2` bypass cache): ทั้ง 14 การสะกดคืน **47/47 (100%)** จากเดิม 2–64% · เคสที่แจ้งคืนครบ P0093 + P0362
- [x] Research พฤติกรรมลูกค้าจริง 452 ข้อความ LINE (7 มิ.ย.–2 ส.ค. 2026): 7 จาก 16 ครั้งที่พูดถึงวาล์วสะกดผิด (44%) · พบว่า `วาว` / `วาร์ว` **ไม่ต้องเพิ่มคำพ้อง** เพราะ LLM classifier เขียนคำค้นใหม่เป็น `วาล์วแอร์` ให้อยู่แล้ว (ยืนยันจาก `LineAiAuditLog`: `วาววีออส` → 13 รายการ, `วาร์วแอร์` → 9) แต่ `วาว์ล` ไม่ถูกแก้ จึงต้องใส่เป็นคำพ้อง
- [x] ช่องโหว่หมวดอื่น (ทำต่อ 2026-08-03 หลังเจ้าของยืนยัน): [scripts/seed-customer-misspelling-synonyms.ts](scripts/seed-customer-misspelling-synonyms.ts) (`npm run seed:customer-misspelling-synonyms`, additive/idempotent, dry-run เป็นค่าเริ่มต้น) — เพิ่ม 15 คำใน 7 คลัสเตอร์ รันบน production แล้ว: `พัดลมแอร์` +โบว์เวอร์,พัดลมโบ (11%,48%→100% ของ 54 ตัว) · `คอยล์ร้อน` +แผงคอยร้อน,คอยร้อน (11%,84%→100% ของ 89) · `หน้าครัช` +มู่เล่,มูเล่,มูเล่ย์,มู่เล่ย์,ชุดคลัช,ชุดครัช (33–95%→100% ของ 73) · รุ่นรถ: Jazz +แจ็ค,ฮอนด้าแจ็ค / City +ซิ้ตี้ / Fortuner +ฟอจูเนอร์ / Hiace +ไฮเอด — เคส production ที่เคยพังกลับมาครบทั้ง 3: แผงคอยร้อนฮอนด้าแจ็ค 2→4, หม้อน้ำฟอจูเนอร์ 1→2, พัดลมโบซิ้ตี้ (เคยถูกอ่านเป็น Isuzu Deca) 1→4
- [x] คำที่ตรวจแล้ว "ไม่เพิ่ม" (บันทึกเหตุผลไว้หัวสคริปต์): `ไดรเออร์` วัดด้วย engine ได้ 100% อยู่แล้ว (trigram จับ ไดรเออร์↔ไดเออร์ — การนับ lexeme ตอน research แรกทำให้เข้าใจผิดว่าเป็น 0) · `สายกลาง`/`สายใหญ่` เป็นชื่อชนิดย่อยของสายไม่ใช่สะกดผิด · `พัดลมเป่า` กำกวมข้ามหมวด · `มาร์ค`/`ดีแมด`/`มอเตอน์พัดลม` LLM classifier แก้ให้ก่อนค้นอยู่แล้ว (ยืนยันจาก audit log)
- [ ] ค้างไว้ (คนละปัญหา ไม่ใช่เรื่องคำพ้อง): `ฟองน้ำ` และ `สวิตช์พัดลม` มีสินค้าอยู่ (4 และ 3 ตัว) แต่ค้นด้วยคำตรง ๆ เจอ 0 เพราะคำไปติดกับข้อความอื่นในชื่อสินค้า — ต้องแก้ที่ข้อมูลสินค้า
- [x] Broad-clause ~636 ms (เจาะ + แก้แล้ว 2026-08-03): เจาะพบว่า arm เดียวคือ `search_text % query + similarity ≥ 0.12` กิน 584.5 จาก 801.6 ms — GIN bitmap ที่ floor 0.12 รับทั้งตาราง (923/921 แถว) แล้ว recheck ต้องคำนวณ similarity บนเอกสาร ~3KB ทุกแถว (CPU ล้วน) · วิ่งบ่อยกว่าที่คิด: **64% ของเทิร์นแชท** (ทุกครั้งที่ semantic rerun ที่ lexical <5 ผล ไม่ใช่แค่ตอน 0 ผล) · จูน threshold ไม่ช่วย (0.12→0.3 ลดแค่ 590→427 ms) · เทิร์น 0 ผล p50=33.4s vs ปกติ 18.4s
- [x] **แก้ด้วยคอลัมน์ `trgm_text`** (Option D — เจ้าของยืนยัน ไม่ทำ Option E เพราะวัดแล้วผลลัพธ์เปลี่ยน 49/60): generated column (code+name+alias+keyword+oem+car_model ไม่รวม description, เฉลี่ย 702 ตัวอักษร vs 2,389) + GIN index ใน [prisma/scripts/setup-search-v2.ts](prisma/scripts/setup-search-v2.ts) sync อัตโนมัติไม่แตะ build/refresh function · ย้ายเฉพาะ fuzzy candidate arm ใน [lib/product-search.ts](lib/product-search.ts) `buildCandidateTextMatchSql` — score block ยังคำนวณบน `search_text` เต็มเพื่อให้ ranking เดิม · รัน `npm run db:setup-search-v2` บน production แล้ว
- [x] วัดผลบน production: fuzzy arm 591.6→141.4 ms (4.2x) · broad clause เต็ม 744.9→328.5 ms (2.3x) · **replay query จริงทั้งหมด 1,925 คำ** (chat+admin+storefront, bypass cache): e2e p50 1,653→1,364 ms, p90 2,196→1,725 ms · ผลลัพธ์: identical 1,889/1,925 (98.1%), **top-5 ที่ลูกค้าเห็น identical 1,918/1,925 (99.6%)** และทั้ง 7 query ที่ต่าง ตัวตรงจริงยังอยู่อันดับ 1 ครบ — ที่ต่างคือ near-match อันดับ 4-5 (ส่วนใหญ่ "ได้เพิ่ม" เพราะ similarity บนข้อความสั้นไม่ถูก description เจือจาง; แถวที่ "หาย" 6 แถวเป็นแถวผิดรถ/ผิดชิ้นที่เคยเข้าทาง vector-only ตอน lexical=0)
- [x] Self-healing ของคอลัมน์ generated: `ADD COLUMN IF NOT EXISTS` จะ **ข้ามเงียบ ๆ** ถ้าคอลัมน์มีอยู่แล้ว → แก้สูตรในอนาคตแล้วรัน `db:setup-search-v2` ซ้ำจะไม่มีผลและไม่มี error เตือน · แก้ด้วย DO block ที่เทียบ **version tag ใน column comment** (`v1:code+name+alias+keyword+oem+car_model`) ไม่ใช่เทียบตัว SQL เพราะ Postgres เขียน `generation_expression` ใหม่เป็นรูป normalized ของตัวเอง (เติมวงเล็บ + `::text`) การเทียบข้อความจะเปราะหรือ rebuild ทุกรอบ · **เวลาแก้ field list ต้องบั๊มเลข version** (เขียนเตือนไว้ในสคริปต์แล้ว) · ทดสอบครบ 3 branch บน production: created / rebuilt-on-drift (ทดสอบด้วย tag ปลอมแล้ว rollback) / up-to-date — รัน 2 รอบซ้ำไม่ rebuild ซ้ำ
- [x] **Golden suite ถาวรตัวใหม่**: [scripts/test-search-recall-golden.ts](scripts/test-search-recall-golden.ts) (`npm run test:search-recall-golden`) — 43 assertion กับแคตตาล็อกจริง: เคส บล็อควาล์ว revo ต้องมี P0093+P0362, การสะกด 35 แบบต้องครบ 100% ของหมวด (นับจากแคตตาล็อกสด), 3 เคส production ที่เคยตอบไม่ครบ (แจ็ค/ฟอจูเนอร์/ซิ้ตี้), fuzzy-rescue 3 เคส (พิสูจน์ trigram arm ยังช่วยคำสะกดผิดที่ไม่มี synonym) — **ผ่าน 43/43** · `npm run test:semantic-golden` ผ่าน 12/12
- ไม่แตะ: engine ค้นหา (`lib/product-search.ts`), chat pipeline, ข้อมูลสินค้า, สต็อก/ราคา/เอกสาร

## Log noise — `timeout exceeded when trying to connect` ตอน revalidate cache หน้า `/products` (2026-07-31)
- บริบท: เจ้าของส่ง Vercel log `level:error` (30/07 13:55 GMT+7, iPhone) — `POST /products` ตอบ **HTTP 200** แต่มี `prisma:error timeout exceeded when trying to connect` ตอน background revalidate cache key `storefront-products-landing-products` ([lib/storefront-catalog.ts](lib/storefront-catalog.ts))
- **สาเหตุ**: ข้อความนี้เป็น error ของ node-postgres pool ตอนรอ connection ครบ `connectionTimeoutMillis` (15s) — pool 5 ตัว/instance เต็ม (Vercel Fluid compute รวมหลาย request ไว้ instance เดียว) query ยังไม่เคยถึง Postgres · จุดพลาดจริงคือ `TRANSIENT_DB_ERROR_PATTERN` ใน [lib/db.ts](lib/db.ts) **ไม่ match ข้อความนี้** (มีแค่ `connection timeout` ซึ่งเรียงคำคนละแบบ) → `withDbRetry` โยน error ทันทีโดยไม่ retry สักครั้ง ทั้งที่ตั้งใจให้ retry
- [x] เพิ่ม `timeout exceeded when trying to connect` เข้า `TRANSIENT_DB_ERROR_PATTERN` → `withDbRetry` retry ได้จริง (ปลอดภัย: caller ทั้ง 17 จุดเป็น read-only ล้วน — `findMany`/`count`/`findUnique` ไม่มี write)
- [x] แยก `POOL_ACQUIRE_TIMEOUT_PATTERN` + `POOL_ACQUIRE_MAX_RETRIES = 1` — error ประเภทนี้เผาเวลาไปแล้ว 15s ต่อครั้ง ถ้าปล่อย retry ตาม default (2) จะกิน ~45s และไปขัดเจตนาเดิมของ `DEFAULT_DB_CONNECTION_TIMEOUT_MS` ที่ตั้งใจให้ fail fast ไม่ pin Vercel function · จำกัด 1 retry → worst case ~30s
- [x] ไม่แตะ `DB_POOL_MAX`, `connectionTimeoutMillis`, query, หรือ cache config — พฤติกรรมฝั่งผู้ใช้เหมือนเดิม (เดิมก็ได้ cache เก่าเสิร์ฟอยู่แล้ว) ต่างแค่ revalidate สำเร็จบ่อยขึ้น
- [ ] เฝ้าดูหลัง deploy 3–5 วัน: กรอง Vercel log ด้วย `timeout exceeded` (ควรลดลงชัดเจน) + เช็ค Supabase → Connection Pooling ว่า client count ไม่แตะ 200
- [ ] ค้างไว้ (รอ metric): ขยาย `DB_POOL_MAX` 5 → 8–10 ให้เข้ากับ Fluid compute — ยังไม่ทำเพราะยังไม่มีข้อมูลจำนวน warm instance จริง เสี่ยงชนเพดาน Supavisor 200 clients (ตรงกับข้อค้าง "Option B" ใน §5 (4e))

## Log noise — `EAUTHTIMEOUT` + `Connection terminated` ตอน revalidate cache (2026-08-12)
- บริบท: เจ้าของส่ง Vercel log 2 เหตุการณ์ ห่างกัน ~11 ชม. — `GET /admin/products/new` (11/08 18:39 GMT+7) และ `GET /product/ford-escape-...` (12/08 05:38 GMT+7) **ตอบ HTTP 200 ทั้งคู่** (panel ยืนยัน Cache = `STALE`, Reason = `Time-based revalidation`) แต่ background revalidate ล้ม
- **สาเหตุที่ 1 — เคส `storefront-products-landing-products`** ([lib/storefront-catalog.ts](lib/storefront-catalog.ts)): error จริงคือ `DriverAdapterError (EAUTHTIMEOUT) timeout while waiting for message`, SQLSTATE `08006`, `severity: FATAL`, `kind: postgres` → ข้อความนี้ส่งกลับมาจาก **Supavisor เอง** (pooler รับ socket เราแล้วแต่ auth กับ Postgres ต้นทางไม่ทัน) ไม่ใช่ error ของ node-postgres — ยืนยันด้วย grep ทั้ง `node_modules` ไม่พบคำว่า `EAUTHTIMEOUT` เลย · `TRANSIENT_DB_ERROR_PATTERN` ไม่ match คำนี้เลยสักข้อ → `withDbRetry` โยนทิ้งตั้งแต่ครั้งแรก ไม่ retry
- **สาเหตุที่ 2 — เคส `admin-master-car-brands-v1`** ([lib/admin-master-options.ts](lib/admin-master-options.ts)): log มี `Connection terminated due to connection timeout` 3 บรรทัดติด = retry ครบ (1+2) แล้วยังล้ม → พิสูจน์ว่า backoff เดิม 150→300ms (+jitter) ครอบ downtime ได้แค่ ~450ms สั้นเกินไปเทียบกับ burst ระดับวินาทีของ Supavisor
- [x] **A** — เพิ่ม `EAUTHTIMEOUT|timeout while waiting for message` เข้า `TRANSIENT_DB_ERROR_PATTERN` ใน [lib/db.ts](lib/db.ts) (ปลอดภัย: connection-level fault ที่ query ยังไม่ถึง Postgres + caller ทั้ง 25 จุดใน 9 ไฟล์เป็น read-only ล้วน)
- [x] **A** — `collectErrorMessages()` อ่าน `cause` ที่เป็น **plain object** ได้แล้ว (เดิมเช็ค `instanceof Error` อย่างเดียว ทำให้ทิ้ง payload ของ `DriverAdapterError` ซึ่งเก็บเหตุผลจริงไว้ในนั้น)
- [x] **B** — ขยาย `RETRY_BASE_BACKOFF_MS` 150 → 400 (400→800 +jitter ≈ 2s worst case) · แยก `POOL_ACQUIRE_RETRY_BASE_BACKOFF_MS = 150` ไว้เท่าเดิม เพราะ error กลุ่มนั้นเผาไปแล้ว 15s ต่อครั้ง ห้ามนอนเพิ่ม
- [x] test ใหม่ [lib/__tests__/db-transient-error.test.ts](lib/__tests__/db-transient-error.test.ts) — 8 เคส ใช้ข้อความ error จาก production ตรง ๆ (match auth timeout / connect timeout / plain-object cause / ไม่ retry error ปกติ / pool-acquire retry ได้ครั้งเดียว)
- [x] `npm run lint` 0 error · `npx tsc --noEmit` 0 error · `npm test` 753 tests pass 751 fail 0
- [x] ไม่แตะ `DB_POOL_MAX`, `connectionTimeoutMillis`, query, cache config หรือ business logic — ผู้ใช้ไม่เห็นความต่าง (เดิมก็ได้ cache เก่าอยู่แล้ว) ต่างแค่ revalidate สำเร็จบ่อยขึ้น + log error ลดลง
- [ ] เฝ้าดูหลัง deploy 3–5 วัน: กรอง Vercel log ด้วย `EAUTHTIMEOUT` และ `Connection terminated` — ควรลดลงชัดเจน
- [ ] เปิด Supabase → Database → Connection Pooling ดูกราฟ client count ย้อนหลังช่วง 11/08 18:39 และ 12/08 05:38 GMT+7 เพื่อตัดข้อสงสัยเรื่องชนเพดาน 200 (ถ้าไม่แตะ = ปัญหาฝั่ง Supavisor ล้วน ๆ ไม่ต้องขยาย pool)

## คิวจัดส่ง — ตัวกรองช่วงวันที่ + popup เลือกผู้ส่งตอนกด "ส่งแล้ว" (2026-07-30)
- บริบท: เจ้าของสั่งเพิ่มตัวกรอง `ตั้งแต่ - ถึง` ทั้ง `/admin/delivery` และ `/admin/delivery/update` และให้ปุ่ม "ส่งแล้ว" หน้าเดสก์ท็อปเด้ง popup เลือกชื่อผู้ส่ง (เดิม stamp ผู้ล็อกอินอัตโนมัติเท่านั้น)
- **ตัวกรองช่วงวันที่** — helper ใหม่ [lib/delivery-date-filter.ts](lib/delivery-date-filter.ts) (`resolveDeliveryDateRange` / `appendDeliveryDateParams`) ใช้ร่วมกันทั้งสองหน้า
    - [x] กรองด้วย `Sale.saleDate` **ทุกแท็บ** ตามมาตรฐาน `.rules` §Date Range Filter (ยืนยันกับเจ้าของแล้ว) — แท็บ "ส่งแล้ว" ของหน้า `/update` เลิกใช้ `deliveredDate` + `updatedAt` เดิม
    - [x] ค่าเริ่มต้น `ตั้งแต่` = วันนี้ (`getThailandDateKey()`) **เฉพาะแท็บ "ส่งแล้ว"** · แท็บอื่นเว้นว่าง (ไม่กรองวันที่) เพื่อไม่ให้บิลค้างเก่าหายจากคิว · `ถึง` เว้นว่างเสมอ
    - [x] แยก `isDefaultFrom` — ค่า default ที่ระบบใส่เองจะ**ไม่ถูกส่งต่อในลิงก์แท็บ/โหลดเพิ่ม** (ไม่ให้ "วันนี้" รั่วไปทับแท็บรอจัดส่ง) · ผู้ใช้ล้างช่องแล้ว submit → มาเป็น `from=""` ซึ่งถือว่า "ไม่กรอง" (ต่างจาก undefined)
    - [x] UI ใช้ `AdminSearchForm` + `AdminSearchSubmitButton` ตาม `.rules` §11 ทั้งเดสก์ท็อป (ปุ่ม "ค้นหา") และมุมมองมือถือ (ปุ่ม "แสดงรายการ", 2 คอลัมน์) — ครบทั้ง light/dark
    - [x] `MobileStatusTabs` รับ `fromDate`/`toDate` เพื่อคงช่วงวันที่ตอนสลับแท็บ; `loadMoreHref` คงช่วงวันที่เช่นกัน
    - [x] **เพิ่มตามคำสั่งรอบที่ 2 (2026-07-30)**: หน้า `/admin/delivery/update` ยุบตัวกรองวันที่เป็นปุ่ม "ช่วงวันที่" กด แสดง/ซ่อน **ค่าเริ่มต้น = ซ่อน** (state ในหน้า ไม่ persist) · ปุ่มโชว์ป้ายช่วงวันที่ที่กรองอยู่ (`formatDateThai`, C.E.) เพื่อไม่ให้ตัวกรองที่ทำงานอยู่ถูกซ่อนแบบมองไม่เห็น — เช่นค่า default "วันนี้" ของแท็บส่งแล้ว · ครบทั้ง light/dark
- **popup เลือกผู้ส่ง** ([DeliveryStaffDialog.tsx](app/admin/(protected)/delivery/DeliveryStaffDialog.tsx) + [DeliveryUpdateButton.tsx](app/admin/(protected)/delivery/DeliveryUpdateButton.tsx))
    - [x] กด "ส่งแล้ว" → modal ให้เลือกผู้ส่งด้วย `SearchableSelect` (ตาม `.rules` §Dropdown) **บังคับเลือกก่อนยืนยัน** (ปุ่มยืนยัน disabled จนกว่าจะเลือก) · ยังเช็คเลข Tracking ก่อนเปิด popup เหมือนเดิม
    - [x] เพิ่มปุ่ม "แก้ผู้ส่ง" บนแถวที่สถานะ = ส่งแล้ว (ตามที่เจ้าของสั่ง) — เปิด modal เดิม ค่าเริ่มต้นคือผู้ส่งปัจจุบัน
    - [x] รายชื่อผู้ส่ง = `User` ที่ `isActive = true` เรียงตามชื่อ (query รวมใน `Promise.all` เดิมของหน้า) — ต่างจากหน้าทำจ่ายค่าส่งที่ยังดึง user ทั้งหมด
    - [x] `updateShippingStatus()` รับ `deliveryStaffId` (optional) เพิ่ม — ถ้าส่งมาจะ**ชนะ auto-stamp** ผู้ล็อกอิน, validate ว่ามี user จริงและ `isActive`, และ**บล็อกการเปลี่ยนผู้ส่งเมื่อบิลถูกทำจ่ายค่าส่งแล้ว** (`deliveryCommissionItems` active) เพราะ payout ผูกกับตัวผู้ส่ง · path เดิม (มุมมองมือถือ/ปุ่มออกส่ง) ไม่ส่งค่านี้ → พฤติกรรม auto-stamp เดิมคงอยู่ครบ
    - [x] AuditLog: `getSaleDeliveryAuditSnapshot()` เก็บ `deliveryStaffId` อยู่แล้ว → diff before/after บันทึกการเปลี่ยนผู้ส่งโดยไม่ต้องแก้เพิ่ม
- [x] `npm run build` ผ่าน + `npx eslint` ไฟล์ที่แก้ 0 error — 5 error เดิมใน `lib/__tests__/category-alias-resolver.test.ts` มีมาก่อนรอบนี้
- [x] ลบ `app/admin/(protected)/delivery/DeliveryStaffPicker.tsx` (dead code ไม่มีใคร import มาตั้งแต่ก่อนรอบนี้ — เจ้าของสั่งลบ 2026-07-30) + ปรับรายการใน [docs/admin-ui-ux-refresh-checklist.md](docs/admin-ui-ux-refresh-checklist.md) ให้ชี้ `DeliveryStaffDialog.tsx` แทน

## หน้าค้นหาสินค้าแอดมิน `/admin/products/search` — search ตามคำสั่ง + bug fix (2026-07-29)
- บริบท: ตรวจบัคตามที่เจ้าของแจ้ง ("กดล้างแล้ว filter ไม่เคลียร์") + เปลี่ยน logic การค้นหาให้ตัวกรองไม่ยิงค้นหาเอง
- **Logic ใหม่ 3 ชั้น** ([MobileProductSearchForm.tsx](app/admin/(protected)/products/search/MobileProductSearchForm.tsx)): `draft` (ติ๊กอยู่ในชีต) → `confirmedFilters` (กดตกลงแล้ว = จำไว้ ยังไม่ค้น URL ไม่เปลี่ยน) → `urlFilters` (ใช้จริง ตรงกับผลลัพธ์). ตัดสินใจร่วมกับเจ้าของ:
    - [x] เพิ่มปุ่ม **"ค้นหา"** ข้างปุ่มตัวกรอง — เป็นจุดเดียวที่ยิง `confirmedFilters` ออกไปจริง; **Enter ในช่องค้นหายังค้นทันทีเหมือนเดิม** (เทียบเท่ากดปุ่ม)
    - [x] **"ตกลง"** = จำไว้ + ปิดชีต ไม่ค้นหา · **"ล้าง"** = เคลียร์เฉพาะ draft ไม่ปิดชีต ไม่ค้นหา · ปิดด้วย X/แตะพื้นหลัง = ทิ้งค่าที่เพิ่งติ๊ก กลับไปค่าที่ยืนยันล่าสุด (seed `draft` ใหม่ทุกครั้งที่เปิดชีต)
    - [x] filter ที่จำไว้เป็น **in-memory เท่านั้น** (ไม่ใช้ sessionStorage) — refresh/back-forward แล้ว sync กลับตาม URL เสมอ
    - [x] แถบแจ้งเตือนสีฟ้าเมื่อ `confirmedFilters` ยังไม่ตรงกับ URL → "ตัวกรองที่เลือกไว้ยังไม่ถูกใช้ — กดปุ่มค้นหา"
- **บัคที่แก้ในรอบเดียวกัน**
    - [x] `clearFilters()` เดิม navigate ทิ้ง filter ใน URL แต่**ไม่ reset state `draft`** → เปิดชีตซ้ำ checkbox ยังติ๊กค้าง และกดตกลงต่อค่าเก่าถูกยิงกลับมา (บัคที่เจ้าของเจอ) · ปุ่ม "ล้างทั้งหมด" ก็มีปัญหาเดียวกัน
    - [x] `draft` ไม่เคย sync กับ props/URL (`useState` รันแค่ตอน mount) → กด back/forward แล้วตัวกรองในชีตไม่ตรงผลลัพธ์ → เพิ่ม `useEffect` sync จาก `urlFilters`
    - [x] spinner/ปุ่ม disable ค้างถาวร — เดิมล้าง `pendingFeedback` ด้วยการเทียบ `href === currentSearchHref` ซึ่งไม่มีวันตรงถ้าผู้ใช้กด back ระหว่าง transition → เปลี่ยนไปผูกกับ `isRoutePending` ของ `useTransition` ตรง ๆ (จบเสมอ)
    - [x] badge บนปุ่มตัวกรองเดิมนับคำค้นหาเป็นตัวกรองด้วย → นับเฉพาะตัวกรองจริง และช่วงปีนับเป็น 1 (`countFilters()`)
    - [x] ช่วงปีรถ: เพิ่มข้อความเตือน "ปีเริ่มต้องไม่มากกว่าปีสิ้นสุด" (ไม่บล็อกการค้น)
    - [x] ชีตกด Escape ปิดได้ (เดิม `role="dialog" aria-modal` แต่ไม่มี key handler)
    - [x] ตรวจ dark mode เดิมใช้ `document.querySelector(".dark")` ซึ่งจับ element ไหนก็ได้ในหน้า → เหลือเช็คแค่ `<html>`/`<body>` ตามที่ `LiffThemeProvider` toggle จริง
- **การ์ดสินค้า** ([search/page.tsx](app/admin/(protected)/products/search/page.tsx))
    - [x] คลิกรูป → popup รูปใหญ่ **ทุกรูปของสินค้านั้น** ผ่าน `ProductImageZoomLightbox` ตัวเดียวกับหน้ารายการสินค้า/preview (component ใหม่ [ProductCardImage.tsx](app/admin/(protected)/products/search/ProductCardImage.tsx)) — เดิมคลิกรูปแล้วเด้งไปหน้า preview และเห็นรูปเดียว; ลิงก์ชื่อสินค้า/ลูกศรยังไปหน้า preview เหมือนเดิม
    - [x] เพิ่มปุ่ม **"แก้ไข"** ท้ายการ์ด (แนบ `returnTo` กลับผลค้นหาเดิม) gate ด้วย `products.update` แบบเดียวกับหน้า `/admin/products`
    - [x] แยก `buildProductZoomImages()` เป็น [lib/product-zoom-images.ts](lib/product-zoom-images.ts) ใช้ร่วมกับ `ProductImagePreview` (DRY — ลำดับรูป/การกันรูปซ้ำเหมือนกันทั้งระบบ)
    - [x] **Fix ตามมา (2026-07-29, จากภาพหน้าจอจริง): popup รูปไม่เต็มจอ ไปแทรกอยู่ในช่องการ์ดของ grid** — [ProductImageZoomLightbox.tsx](components/shared/ProductImageZoomLightbox.tsx) ใช้ `fixed inset-0 z-[9999]` แต่ render อยู่กับที่ ไม่ได้ portal; การ์ดสินค้ามี `hover:-translate-y-0.5` → ระหว่าง hover ตัว `transform` ทำให้ `<article>` กลายเป็น **containing block ของ `position: fixed`** → overlay ถูกขังในกรอบการ์ด (หน้า `/admin/products` แบบตารางไม่เจอเพราะไม่มี ancestor ที่ transform — เป็นความบังเอิญ ไม่ใช่ความถูกต้อง). แก้ที่ตัว lightbox ให้ `createPortal(..., document.body)` + `mounted` guard กัน SSR → มีผลกับทุกหน้าที่ใช้ (search card / products list / preview) ในครั้งเดียว
    - [x] **Fix ตามมา #2 (2026-07-29): ตัวอักษรในชีตตัวกรอง dark mode อ่านไม่ออก (ดำบนดำ)** — regression ที่เกิดจากรอบก่อนเอง: ฝั่งแอดมิน class `dark` ถูกใส่ที่ `.admin-theme-root` ([AdminShell.tsx](components/shared/AdminShell.tsx)) **ไม่ใช่ `<html>`/`<body>`** และ Tailwind ประกาศ `@custom-variant dark (&:is(.dark *))` → ชีตที่ portal ไป `document.body` อยู่นอก root นั้น `dark:` ตายทั้งหมด เหลือแต่ global override ใน [globals.css](app/globals.css) ที่ครอบเฉพาะ `bg-white`/`gray-*` (พื้นจึงดำถูก แต่ `text-slate-950`/`text-slate-600`/`text-slate-700` ไม่ถูก override). โค้ดเดิมมี `document.querySelector(".dark")` ซึ่งหา `.admin-theme-root.dark` เจอพอดี — รอบก่อนถอดออกเพราะเข้าใจกลไกธีมแอดมินผิด. แก้ด้วย `useOptionalAdminTheme()` (แพตเทิร์นเดียวกับ `SearchableSelect`/`ProductSearchSelect`, context ไหลผ่าน portal ได้) แล้วติด class `dark` ที่ portal wrapper เอง — ตัด DOM sniffing + MutationObserver ทิ้ง
    - [x] `ProductImageZoomLightbox` เจอปัญหาเดียวกันหลังเปลี่ยนเป็น portal (มี `dark:` 2 จุด) → ใช้ `useOptionalAdminTheme()` เหมือนกัน; หน้าบ้านไม่กระทบเพราะ `dark` อยู่ที่ `<html>` (body เป็น descendant อยู่แล้ว)
    - [x] ย้ายปุ่ม "แก้ไข" จากแถบล่างขวาของการ์ด → ใต้รูปในคอลัมน์ซ้าย กว้างเท่ารูป (`w-28`/`sm:w-32`) ตามที่เจ้าของระบุตำแหน่ง; ตัดแถบ `border-t` ท้ายการ์ดออก
    - [x] `npm run test:admin-theme-regression` ผ่าน
- [x] เพิ่ม prop `onValueChange` (optional) ใน [ProductAutocomplete.tsx](components/shared/ProductAutocomplete.tsx) — ให้ปุ่มค้นหาภายนอกรู้ข้อความที่พิมพ์ (จำเป็นเพราะ `enhanced="mobile"` ให้ input ตัวนอกเป็น readOnly trigger); additive ไม่กระทบผู้เรียกเดิม
- [x] `npm run build` ผ่าน (TypeScript 0 error) — 5 error เดิมใน `lib/__tests__/category-alias-resolver.test.ts` มีมาก่อนรอบนี้ ไม่เกี่ยวกับการแก้

## Log noise — `[auth][error] CredentialsSignin` บน production (2026-07-29)
- บริบท: เจ้าของส่ง Vercel log `level:error` ให้ตรวจ — `POST /api/auth/callback/credentials` ตอบ **HTTP 200** แต่มี `[auth][error] CredentialsSignin` + stack ยาว (28/07 17:53 GMT+7, iPad)
- **สรุปผลตรวจ: ไม่ใช่บัค** — `@auth/core/lib/actions/callback/index.js:236` โยน `CredentialsSignin` ทุกครั้งที่ `authorize()` return `null` (= login ไม่ผ่านตามปกติ) แล้ว log เป็น level `error` เสมอ; ถ้า `authorize()` throw จริงจะขึ้นเป็น `CallbackRouteError` แทน · flow ทำงานถูกต้องอยู่แล้ว (ผู้ใช้เห็นข้อความผิดพลาด, AuditLog `LOGIN_FAILED` ถูกเขียนพร้อม `meta.reason`)
- [x] เพิ่ม custom `logger.error` ใน [auth.ts](auth.ts) — ดักเฉพาะ `CredentialsSignin` ให้ลดเป็น `console.warn("[auth] credentials sign-in rejected")` (ไม่พิมพ์ stack/ข้อมูลผู้ใช้) · error อื่นของ Auth.js ยัง `console.error` ครบเหมือนเดิม → log `level:error` บน production เหลือแต่ปัญหาจริง
- [x] `isCredentialsSigninError()` เช็ค 3 ทาง (`instanceof` → `error.name` → `error.type`) เผื่อ `@auth/core` ถูก bundle ซ้ำจน class identity ไม่ตรง
- [x] ไม่แตะ `authorize()`, throttle, AuditLog, หรือข้อความบนหน้า login — พฤติกรรมฝั่งผู้ใช้เหมือนเดิมทุกอย่าง
- [x] `npm run build` ผ่าน (TypeScript 0 error) — 5 error เดิมใน `lib/__tests__/category-alias-resolver.test.ts` มีมาก่อนรอบนี้
- [ ] ค้างไว้ (รอตัดสินใจ): [lib/login-rate-limit.ts](lib/login-rate-limit.ts) นับ failure ที่ key `ip:` ด้วย → พนักงานหลายคนหลัง IP เดียวกัน (เน็ตร้าน) กรอกผิดรวมกันครบ 5 ครั้ง/5 นาที จะถูกล็อกยกชุด 5 นาที และหน้าเว็บยังขึ้นว่า "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง" (แยกไม่ออกจากรหัสผิด) — แก้ได้ 2 ทาง: แยกข้อความตอนโดน throttle หรือผ่อนเกณฑ์ของ IP key ให้สูงกว่า username key

## ลำดับสต็อกการ์ดวันเดียวกัน (2026-07-24)
- บริบท: พบยอดคงเหลือติดลบ 2 รายการ (P0488/P0489 เอกสาร SAC26050001 vs RR26050036 ลงวันที่ 27/05/2026) — ใบขายถูกคีย์ก่อน แล้วใบซื้อวันเดียวกันถูกแก้ทีหลังจึงได้ `sorder` ต่อท้าย ทำให้ต้นทุนขายเป็น 0 และมูลค่าซื้อ 1,769.64 บาทหายจาก MAVG (`isBackdated` เดิมใช้ `<` ล้วน จึงไม่ถือว่าวันเดียวกันคือการลงย้อนหลัง)
- [x] `lib/stock-card.ts` — เพิ่ม `STOCK_SOURCE_SEQUENCE_GROUP` (BF → ของเข้าทุกชนิด → ของออกทุกชนิด) + `sortRowsForReplay()` / `buildSorderUpdates()`; `recalculateStockCard()` และ `recalculateStockCardMany()` จัดลำดับ `sorder` ใหม่แบบ diff-write ก่อนรีเพลย์ MAVG เสมอ → ข้อมูลเก่าที่ลำดับสลับถูกซ่อมเองทุกครั้งที่คำนวณใหม่
- [x] `writeStockCard()` — เปลี่ยนเงื่อนไข `isBackdated` เป็น `needsFullRecalc` = มีแถวที่ต้องเรียงทีหลัง (วันหลังกว่า **หรือ** วันเดียวกันแต่กลุ่ม source ต้องมาทีหลัง) · เอกสารขาออกไม่มีกลุ่มที่ต้องมาทีหลังจึงเข้า fast path เสมอ ไม่มี overhead
- [x] Performance: query ตรวจลำดับใช้ index `[productId, docDate, sorder]` (index seek) และแทนที่ query `latestDateRow` เดิม → จำนวน round-trip ของการบันทึกเอกสารปกติเท่าเดิม
- [x] ปุ่ม "คำนวณใหม่" หน้า `/admin/stock/card` — เปลี่ยนจากวน 1 transaction/สินค้า เป็น `recalculateStockCardMany()` ทีละ 100 สินค้า/transaction (914 สินค้า: ~914 tx → ~10 tx) ผลลัพธ์เท่าเดิมทุกไบต์
- [x] Test: `npm run test:stock-card-sequence` (10 เคส pure) + `prisma/scripts/verify-recalc-diff-write.ts` (รันจริงบน DB แล้ว rollback — 914 สินค้า/1,174 แถว ผล IDENTICAL)
- [x] แก้ข้อมูลจริงเฉพาะ P0488/P0489 ด้วย `prisma/scripts/recalculate-stock-card-products.ts --codes=...` (สำรอง DB ก่อน + เขียน AuditLog RECALCULATE) → ยอดติดลบเหลือ 0 ทั้งระบบ, stock/avgCost ไม่เปลี่ยน
- [x] ยืนยันว่าไม่กระทบ lot: `StockMovementLot` ผูกกับ `stockCardId` และยอดคงเหลือมาจาก `LotBalance` — การเรียง `sorder` ใหม่ไม่แตะแถว lot และเครื่อง MAVG ไม่เคยอ่าน lot
- [ ] ค้างไว้ (รอตัดสินใจ): `Product.avgCost` เป็น `Decimal(10,2)` แต่ `StockCard.priceBalance` เป็น `Decimal(10,4)` — append fast path อ่านต้นทุนตั้งต้นจาก `Product.avgCost` จึงคลาดจาก path ที่ recalculate สูงสุด ~0.008 บาท/ชิ้น (80 แถว / 203 สินค้า) ตรวจซ้ำได้ด้วย `prisma/scripts/audit-stock-card.ts`

## Source Of Truth Map
### Product and Inventory
- Stock movement + MAVG: `lib/stock-card.ts`
- Document numbers: `lib/doc-number.ts`
- VAT calculations: `lib/vat.ts`
- Prisma schema: `prisma/schema.prisma`
- Transaction item display snapshots: [docs/decisions/document-item-display-snapshot.md](/D:/autoparts/docs/decisions/document-item-display-snapshot.md)

### Admin Rules
- Cross-module delivery/print/theme/search rules: [AGENTS.md](/D:/autoparts/AGENTS.md)

### Planning Docs
- Architecture overview: [docs/architecture.md](/D:/autoparts/docs/architecture.md)
- Active roadmap: [docs/roadmap/active.md](/D:/autoparts/docs/roadmap/active.md)
- Completed roadmap: [docs/roadmap/completed.md](/D:/autoparts/docs/roadmap/completed.md)
- Decision log index: [docs/decisions/README.md](/D:/autoparts/docs/decisions/README.md)
- Spec index: [docs/specs/README.md](/D:/autoparts/docs/specs/README.md)

## Admin navigation performance — auth dedupe + tab prefetch (2026-08-05)
- บริบท: เจ้าของแจ้งว่าสลับแท็บบนแถบ `TabsBar` (แท็บแบบเบราว์เซอร์ใต้ header) แล้ว "รีเฟรชทุกครั้ง ต้องรอ" — ตรวจแล้วพบ 2 สาเหตุหลัก, แก้รอบนี้ทั้งคู่ (ยืนยันแผน A + B1 กับเจ้าของก่อนลงมือ)
- Baseline วัดจริงก่อนแก้ ([scripts/measure-admin-auth-baseline.ts](scripts/measure-admin-auth-baseline.ts), `npm run measure:admin-auth-baseline`, 12 รอบ): Q1 jwt() revocation+permission p50 **153.0ms** · Q2 `getUserPermissionKeys()` p50 **151.9ms** → ต่อการคลิก 1 ครั้ง **458ms** (2×Q1 + 1×Q2) หรือ **611ms** เมื่อ layout re-render — ทั้งหมดนี้เกิดก่อน query ข้อมูลของหน้าจะเริ่ม
- [x] (A) Request-scoped session dedupe — เพิ่ม [lib/auth-session.ts](lib/auth-session.ts) `getSession = cache(() => auth())` แล้วให้ layout + 11 หน้า admin ที่เรียก `auth()` ตรง ๆ + `getRequiredSession()` ใช้ตัวเดียวกัน → `auth()` (และ jwt callback ที่ยิง DB) รันครั้งเดียวต่อ request แทน 2–3 ครั้ง
- [x] (A) เลิกยิง `getUserPermissionKeys()` ใน `requirePermission()` / `requireAnyPermission()` — jwt callback เขียน `token.permissions` ใหม่จาก DB ทุก `auth()` อยู่แล้ว จึงอ่านจาก `session.user.permissions` ได้ **ความสดเท่าเดิม** (การถอนสิทธิ์/ปิดบัญชียังมีผลทันทีในคลิกถัดไป ไม่ throttle) · key ที่หลุดจาก catalog แล้วไม่มีทางตรงกับ `PermissionKey` จึงไม่ต้องกรองซ้ำ · `getUserPermissionKeys()` ยังคง export ไว้แต่ไม่มีผู้เรียกเหลือแล้ว
- [x] (B1) [components/shared/TabsBar.tsx](components/shared/TabsBar.tsx) เปลี่ยนจาก `<div onClick={router.push}>` เป็น `<Link>` จริง — เดิม prefetch ไม่ทำงานเลยเพราะ Next prefetch เฉพาะ `<Link>` (คลิกแท็บจึงช้ากว่าคลิกเมนูเดียวกันจาก sidebar ที่ใช้ `<Link>` อยู่แล้ว)
- [x] (B1) Prefetch เมื่อ pointer แตะแท็บครั้งแรกเท่านั้น (`onPointerEnter` / `onPointerDown` / `onFocus` → `warmedPaths` แล้วสลับ prop `prefetch` เป็น `true`) — ยืนยันจาก type ของ Next 16.2.1 ว่า `prefetch={true}` ดึง full route + data ของ dynamic route ส่วน `prefetch={false}` ปิดทั้ง viewport และ hover · จงใจไม่ prefetch ทุกแท็บพร้อมกัน เพื่อไม่ให้เปิดค้าง 8 แท็บ = server render 8 หน้าฟรี (ขัดกับงานลด Supabase egress)
- [x] (B1) ปุ่มปิด (×) ย้ายออกมาเป็น sibling ของ `<Link>` (ห้าม `<button>` ซ้อนใน `<a>`) + เพิ่ม `aria-label` · spinner เปลี่ยนไปใช้ `useLinkStatus()` โดยคง `forcePending` ไว้สำหรับ navigation ตอนปิดแท็บที่ไม่ได้มาจากการคลิกลิงก์ · หน้าตา/ธีม light+dark เหมือนเดิม
- ผลข้างเคียงที่ผู้ใช้จะเห็น: Ctrl+คลิก / คลิกกลาง / คลิกขวา "เปิดในแท็บใหม่" / Tab ด้วยคีย์บอร์ด บนแท็บ **ใช้งานได้แล้ว** (เดิมเป็น `<div>` จึงทำไม่ได้)
- ข้อควรรู้ตอนทดสอบ: **prefetch ทำงานเฉพาะ production build** (`npm run build && npm start`) — รันด้วย `npm run dev` จะไม่เห็นผลของ B1
- [x] (2026-08-05 รอบต่อเนื่อง) เติม `loading.tsx` ให้ครบตาม `.rules` §UI/UX — เดิมขาด 8 segment: `backup-center`, `knowledge/{approval,new,quality,sync,test,[id]}`, `products/search` · ตอนนี้ทุก route segment ใต้ `/app/admin/(protected)/` ที่มี `page.tsx` มี `loading.tsx` แล้ว (ตรวจด้วยสคริปต์ไล่ทุกโฟลเดอร์)
  - [x] 6 หน้า knowledge ใช้ shell ร่วมกัน [KnowledgeLoadingSkeleton.tsx](app/admin/(protected)/knowledge/KnowledgeLoadingSkeleton.tsx) (DRY) — วาดทั้ง header **และแถบแท็บ 6 pill** เพราะหน้า knowledge เรนเดอร์ `KnowledgeTabs` ไว้ใน body ไม่ใช่ layout; ถ้า skeleton ไม่วาด แถบแท็บจะหายระหว่างโหลดแล้วเด้งกลับมา (CLS) · export `SkeletonStatCards` / `SkeletonTable` ให้หน้า quality + sync ใช้ตามโครงจริง · หน้า `new` / `[id]` ส่ง `showTabs={false}` เพราะไม่มีแท็บ
  - [x] `products/search` skeleton เลียนโครงการ์ดสินค้าจริง (คอลัมน์รูป + คอลัมน์ข้อความ, 6 การ์ด, grid เดียวกับของจริง) เพื่อกัน CLS · `backup-center` skeleton = header + 2 การ์ด + ตารางประวัติ
  - [x] ทุก skeleton รองรับ light + dark ครบ และใส่ `aria-busy` + `aria-label` ภาษาไทย
  - ผลพลอยได้: `<Link>` บน dynamic route จะ prefetch ถึง `loading.tsx` boundary ที่ใกล้ที่สุด — 8 segment นี้เดิมไม่มีอะไรให้ prefetch เลย
- [x] (2026-08-05 รอบต่อเนื่อง #2) Suspense streaming หน้ารายงานหนัก 5 หน้า — baseline ([scripts/measure-report-query-baseline.ts](scripts/measure-report-query-baseline.ts), `npm run measure:report-queries -- 90`, ช่วง 90 วัน, p50): `queryDailyPaymentRows` **482.8ms** · `queryPurchaseRows` **353.8ms** (903 แถว) · `querySalesRows` **197.5ms** · `queryDailyReceiptRows` **176.1ms** · `queryCreditNoteRows` **124.4ms** · ส่วน totals/count/dropdown อยู่ที่ 42–45ms
  - หมายเหตุความถูกต้องของ baseline: รอบแรกวัด `queryReceiptRows`/`queryPaymentRows` ผิดตัว — หน้า receipts/payments เรนเดอร์ `queryDaily*Rows` ซึ่งหนักกว่า พอแก้แล้วอันดับเปลี่ยน (payments แซง purchases ขึ้นเป็นหนักที่สุด)
  - [x] แยกครึ่งที่ต้อง await ออกเป็น async component แยกไฟล์: `SalesReportResults` / `PurchasesReportResults` / `PaymentsReportResults` / `ReceiptsReportResults` / `CreditNotesReportResults` — `page.tsx` เหลือ await แค่ `requirePermission()` + dropdown บัญชี (42ms) แล้วปล่อย header + ฟอร์มกรองออกไปก่อน
  - [x] fallback ร่วม [ReportResultsSkeleton.tsx](app/admin/(protected)/reports/ReportResultsSkeleton.tsx) เลียนโครง summary line + ตาราง (light+dark) กัน CLS
  - [x] `<Suspense key={from|to|filters|page}>` — เปลี่ยนตัวกรองแล้ว boundary ต้อง re-suspend ใหม่ ไม่ค้างผลเดิม
  - [x] หน้า payments/receipts มีการ์ดสรุปที่คำนวณจาก rows ชุดเดียวกัน จึงย้ายเข้า boundary ด้วย และแยก `PaymentSummaryCards`/`ReceiptSummaryCards` + `EMPTY_*_TOTALS` ให้ทั้ง fallback และสถานะ "ยังไม่เลือกช่วงวันที่" ใช้ซ้ำ — การ์ดศูนย์ยังแสดงเหมือนเดิมทุกกรณี
  - [x] ปุ่มแบ่งหน้าที่ปิดใช้งาน (หน้าแรก/หน้าสุดท้าย) เปลี่ยนจาก `onClick={e => e.preventDefault()}` (event handler บน Server Component) มาเป็น `pointer-events-none` + `aria-disabled` — กดไม่ได้จริงและไม่ต้องพึ่ง handler ฝั่ง client
- [x] (2026-08-06 รอบต่อเนื่อง #3) Suspense streaming หน้ารายงานที่เหลือ — baseline ([scripts/measure-report-query-baseline-rest.ts](scripts/measure-report-query-baseline-rest.ts), `npm run measure:report-queries-rest -- 90`, p50): `getReportsData` (summary) **880.1ms หนักสุดในระบบ** · `queryStockRows` **303.9ms** (925 SKU) · `queryCashBankLedgerData` **174.6ms** (313 แถว) · AR/AP/transfers/adjustments **76–85ms** · dropdown ลูกค้า/ซัพพลายเออร์ 39–40ms
  - [x] แยก results component เพิ่ม 8 ไฟล์: `StockReportResults` · `CashBankLedgerResults` · `CashBankTransfersResults` · `CashBankAdjustmentsResults` · `ARReportResults` · `APReportResults` · `ClaimStockReportResults` · (line-daily-summary แยกเป็น 2 ส่วน ดูด้านล่าง) — `page.tsx` ทุกหน้าเหลือ await แค่ `requirePermission()` + dropdown ของฟอร์มกรอง
  - [x] AR/AP มี 2 มุมมอง (outstanding / register) — แยกเป็น `ARRegisterView`/`AROutstandingView` และ `APRegisterView`/`APOutstandingView` ใน component เดียว **ยิง query เฉพาะ view ที่เปิดอยู่** เหมือน conditional `Promise.all` เดิมเป๊ะ
  - [x] claim-stock: ย้าย where-builder + 3 query + sort เข้า results component; label map ภาษาไทยที่ทั้งฟอร์มกรองและตารางใช้ร่วมกัน ย้ายไป [report-labels.ts](app/admin/(protected)/reports/claim-stock/report-labels.ts) (DRY) · guard `submitted ? … : [[], null, 0]` ตัดออกได้เพราะ component เรนเดอร์เฉพาะตอน submitted
  - [x] line-daily-summary: `summary` ถูกใช้ 2 จุดที่ไม่ติดกัน (stat cards + Flex preview) จึงแยกเป็น `LineDailySummaryStatCards` + `LineDailySummaryPreview` และให้ทั้งคู่เรียกผ่าน [summary-loader.ts](app/admin/(protected)/reports/line-daily-summary/summary-loader.ts) ที่ห่อ `buildLineDailySummary` ด้วย `cache()` → **build ครั้งเดียวต่อ request** ไม่ใช่ 2 ครั้ง · helper การแสดงผล (`StatCard`/`PreviewMetric`/`FlexPreviewSection`/`fmtMoney`/`fmtPercent`/`keepPreviewItem`) ย้ายไป [summary-presentation.tsx](app/admin/(protected)/reports/line-daily-summary/summary-presentation.tsx) ใช้ร่วมกับ shell (ส่วน "สถานะการส่งปัจจุบัน" ยังใช้ `StatCard` และไม่ต้องรอ summary)
  - [x] summary: streaming อยู่แล้วตั้งแต่เดิม — เพิ่มเฉพาะ `key` ให้ boundary re-suspend เมื่อเปลี่ยนช่วงวันที่ + รวม `requirePermission()` เข้า `Promise.all` กับ `searchParams`
  - หมายเหตุ: วัด `buildLineDailySummary` จากสคริปต์ตรง ๆ ไม่ได้ (ข้างในใช้ `unstable_cache` ซึ่งต้องมี Next runtime) — จึงไม่มีตัวเลข p50 ของหน้านี้
  - ตอนนี้ทุกหน้าใต้ `/admin/reports` ที่ query ข้อมูลใช้ Suspense streaming ครบแล้ว
- [x] (2026-08-06 รอบต่อเนื่อง #4) เจาะ query จริงของ `getReportsData` / `queryStockRows` — **ผลตรวจกลับด้าน: ไม่ใช่ปัญหา query หรือ index**
  - หลักฐาน ([scripts/diagnose-slow-report-queries.ts](scripts/diagnose-slow-report-queries.ts), `npm run diagnose:slow-reports`): EXPLAIN ANALYZE ทั้ง 2 query ใช้เวลาใน Postgres **3.5ms** ขณะที่ฝั่ง client วัดได้ 232–356ms · index ที่มีอยู่ (`StockCard_productId_docDate_sorder_idx`, `Product_categoryId_isActive_…`) เป็น Index Scan อยู่แล้ว ไม่มี Seq Scan · **การเพิ่ม index จะไม่ช่วยอะไรเลย**
  - ขนาดข้อมูลจริง: Product 956 (active 928) · StockCard 1,236 · Sale 93 · Receipt 48
  - สาเหตุจริง ([scripts/diagnose-report-roundtrips.ts](scripts/diagnose-report-roundtrips.ts), `npm run diagnose:report-roundtrips`): **Prisma ยิง 1 statement เพิ่มต่อ 1 nested relation** — `getReportsData` ที่ดูเหมือน 12 query จริง ๆ ยิง **43 statement** (creditNotes 7, openClaims 5, sales 4, …) และ `queryStockRows` ยิง 3 (product/units/category) + 1 DISTINCT ON · `SELECT 1` เปล่า ๆ = 42ms จึงเป็น RTT × จำนวน statement เกือบทั้งหมด
  - หมายเหตุความถูกต้อง: 42ms วัดจากเครื่อง dev → Supabase **บน Vercel ที่อยู่ region เดียวกับ DB ตัวเลขสัมบูรณ์จะต่ำกว่านี้มาก** แต่โครงสร้าง "43 statement" ยังอยู่และคูณกับ RTT จริงของ production
  - [x] แก้ตามทางเลือก C: `getReportsData` เลิกดึงสินค้า active ทั้ง 928 แถว (+ round trip ที่ 2 ของ Category) มา derive ในหน่วยความจำ → รวมเป็น **raw SQL ตัวเดียวด้วย CTE** (`base` / `low_stock` / `highest_value` / `totals`) คืน JSON ก้อนเดียว
    - ลองแบบแยก 3 query ก่อน (findMany + 2 raw) แล้ว **วัดได้ว่าช้าลง** (p50 904ms vs เดิม 827ms) เพราะ statement ไม่ใช่ logical query คือสิ่งที่แย่ง connection pool 5 ตัว — จึงยุบเหลือ statement เดียว
    - A/B จริงด้วย `git stash` 3 รอบ: **ใหม่ 724 / 739 / 766ms · เดิม 771 / 790 / 794ms** → เร็วขึ้น ~45ms p50 (~6%) · ที่ได้ไม่มากเพราะ critical path ของหน้านี้อยู่ที่ batch แรก (sales 290ms / creditNotes 315ms) ไม่ใช่ stock section
    - `stockValue` เรียงใน SQL (คำนวณจาก column) และ tie-break `stock ASC, code ASC` เพื่อจำลอง stable sort ของ JS เดิม · `lowStockCount` นับทั้งหมดด้วย `COUNT(*) FILTER` ส่วน `lowStockItems` ยัง LIMIT 100 เท่าเดิม
  - [x] พิสูจน์ค่าเท่าเดิมก่อน merge ([scripts/verify-reports-data-stock-section.ts](scripts/verify-reports-data-stock-section.ts), `npm run verify:reports-stock-section` + [verify-stock-summary-equivalence.ts](scripts/verify-stock-summary-equivalence.ts)): `activeProductCount` / `totalUnitsOnHand` / `lowStockCount` / `lowStockItems` / `highestValueItems` **ตรงกันเป๊ะทุกค่า** ทั้งกรณีไม่กรองรหัสสินค้าและกรองเป็นช่วง
  - **ค่าเดียวที่ต่างคือ `totalStockValue` ต่างกัน 2.3e-10** — Postgres รวมเป็น numeric แบบเป๊ะ ส่วนของเดิมสะสม float drift จาก `reduce` (เช่น `1092725.6000000003` → `1092725.6`) แสดงผล 2 ตำแหน่งเท่ากันทุกกรณี ค่าใหม่ถูกต้องกว่า
  - ยังไม่ได้แตะ `queryStockRows` (หน้า `/admin/reports/stock` ต้องแสดงครบ 928 แถวจริง ๆ) — ถ้าจะลดจาก 3 statement เหลือ 1 ต้องเขียน raw SQL ที่รวม `units` เข้ามาเองแล้วทำ `resolveReportUnit` ต่อ เป็นงานแยกที่ต้องยืนยันก่อน
- [x] (2026-08-06 รอบต่อเนื่อง #5) ประเมิน `relationJoins` preview ของ Prisma แล้ววาง orderBy ปูทางไว้ก่อน (ยังไม่เปิด flag)
  - วัดจริงโดย generate client ทดสอบแยกไว้ใน `node_modules` (schema + generated client ของโปรเจกต์ไม่ถูกแตะ แล้วลบทิ้งหลังวัด): **29 statement / 869.5ms → 7 statement / 478.8ms (−45%)** · creditNotes 7→1 stmt (−81ms), openClaims 5→1 (−79ms), expenses 4→1 (−75ms)
  - เจอความเสี่ยง 2 ข้อ: (A) **เปิด flag แล้ว `join` เป็น default ของทุก query ทั้งระบบทันที** ไม่ใช่ opt-in ทีละจุด (ทดสอบด้วย query ที่ไม่ระบุ `relationLoadStrategy` → ได้ 1 statement) (B) **ลำดับของ array ใน nested relation เปลี่ยน** เมื่อไม่ได้ระบุ `orderBy` (`sales.items` และ `product.units` คืนคนละลำดับระหว่าง 2 strategy)
  - audit ทั้ง repo: to-many relation ที่ถูก select **290 จุด** · มี orderBy แล้ว 117 · **ไม่มี 173** (ในนั้นมี `take:` 13 จุด = ไม่มีคำตอบแน่นอนว่า "เอาแถวไหน")
  - [x] Tier 1+2 — ใส่ `orderBy` ครบ **69 จุด** (ยืนยันกับเจ้าของแล้ว: `lotItems` เรียงด้วย `id asc`, รุ่นรถเรียงด้วยชื่อรุ่น)
    - `items:` ทุกจุด → `orderBy: { lineNo: "asc" }` — ทุกตาราง item ลูกมี `lineNo` + index `[parentId, lineNo]` อยู่แล้ว จึง**ไม่มีต้นทุนเพิ่ม** และข้อมูลจริงไม่มี lineNo ซ้ำเลยสักกลุ่ม (SaleItem 210/93, PurchaseItem 1041/122, CreditNoteItem 2/2, ExpenseItem 55/55, ReceiptItem 52/48)
    - `lotItems:` ทุกจุด → `orderBy: { id: "asc" }`
    - `carModels:` (storefront 6 จุด + chat bridge) → `[{ carModel: { name: "asc" } }, { yearStart: "asc" }, { id: "asc" }]` · ต้องประกาศเป็น `FITMENT_ORDER_BY` ที่ระบุ type ไว้ เพราะ select ที่เป็น `as const` ทำให้ array กลายเป็น readonly ซึ่ง Prisma ไม่รับ
    - `aliases:` → `id asc` · `revisions:` (knowledge, take:1) → `revisionNo desc` · `deliveryCommissionItems:` (take:1) → `createdAt desc`
  - **ผลข้างเคียงที่พบว่าเป็นการแก้บั๊กเดิม**: [scripts/verify-nested-orderby-parity.ts](scripts/verify-nested-orderby-parity.ts) (`npm run verify:nested-orderby`) เทียบลำดับก่อน/หลัง — CreditNote / Expense / Receipt / lotItems เหมือนเดิมทุกแถว แต่ **Sale 9 จาก 93 ใบ และ Purchase บางใบ ปัจจุบันแสดงบรรทัดสลับกันอยู่** เช่น `SAC26060005` คืนลำดับ `4,5,6,7,8,1,2,3` — หลังใส่ orderBy เป็น `1..8` ตามที่กรอกจริง (หน้า detail หลายหน้าใช้ `lineNo` อยู่แล้ว จุดที่ขาดไปคือจุดที่ไม่สอดคล้อง)
  - codemod เจอ false positive 1 จุด (แทรกเข้าไปใน type annotation ของ `buildStorefrontProductDescription`) — ตรวจ diff แล้วแก้คืน
  - ตรวจแล้ว: `tsc` ผ่าน · `eslint` ไม่มี error ใหม่ (error เดียวที่เหลือคือ `ProductImageZoomLightbox.tsx` ซึ่งมีอยู่ก่อนแล้ว ไม่ได้แตะ) · `npm run build` ผ่าน · `check:mojibake` ผ่าน · verify ของงาน C ยังผ่านครบหลัง codemod
- ยังไม่ได้ทำ (เสนอไว้ รอเจ้าของตัดสินใจ): **เปิด `relationJoins` flag** (ตอนนี้ปูทางเสร็จแล้ว รอยืนยัน) · ยุบ nested relation ของตาราง master เล็ก ๆ เป็น in-memory join · ลด statement ของ `queryStockRows` · เก็บ URL เต็ม+query ในแท็บเพื่อคืนฟิลเตอร์เดิม · `experimental.staleTimes`
- ตรวจแล้ว: `tsc --noEmit` ผ่าน · `eslint` ไฟล์ที่แก้ผ่าน · `npm run build` ผ่าน · `check:mojibake` ผ่าน

## รูปหมวดหมู่หน้าแรก — แนบไฟล์เองที่ `/admin/master/categories` + ตัด icon setting (2026-08-10)
- ที่มา: section "หมวดหมู่สินค้า" บนหน้าแรก (`components/storefront/HomeCategoryStrip.tsx`) ใช้รูปสินค้าขายดีอันดับ 1 ของหมวดเป็น thumbnail เท่านั้น แอดมินเลือกรูปเองไม่ได้ · เจ้าของสั่งเพิ่มการแนบไฟล์ + ให้เอาการตั้งค่าไอคอน/โทน/โมชั่น (ที่ไม่ได้ใช้แล้ว) ออก
- [x] Schema: `Category.imageUrl String?` — **ไม่ได้ใช้ `prisma db push`** เพราะ push จะ drop `product_search_documents.trgm_text` (generated column ของ `prisma/scripts/setup-search-v2.ts` ที่จงใจอยู่นอก schema) → ใช้ `prisma db execute` ยิง `ALTER TABLE "Category" ADD COLUMN IF NOT EXISTS "imageUrl" TEXT` แทน แล้ว `prisma generate`
  - ⚠️ ข้อควรระวังสำหรับงานถัดไป: `prisma db push` ใน repo นี้ต้องดู warning ก่อนเสมอ — schema drift ของ `trgm_text` เป็นของจริง ห้ามใส่ `--accept-data-loss`
- [x] Admin: `uploadCategoryImage()` (permission `master.create` **หรือ** `master.update`, sniff magic bytes, jpg/png/webp ≤ 2MB, path `settings/categories/<id>/<ts>-<uuid>.<ext>` บน Vercel Blob `cacheControlMaxAge` 1 ปี) + ช่องแนบ/เปลี่ยน/ลบรูปในฟอร์มเพิ่มและแก้ไขหมวด (light + dark) · `createCategory`/`updateCategory` รับ `imageUrl` ผ่าน hidden input · audit snapshot รวม `imageUrl` แล้ว
  - (รอบ 3, จาก code review) เดิมเช็ก `master.update` อย่างเดียว → staff ที่มีแค่ `master.create` เห็นปุ่มแนบรูปในฟอร์ม "เพิ่มหมวดหมู่" แต่กดแล้วได้ "ไม่มีสิทธิ์เข้าถึง" · เปลี่ยนเป็น `requireAnyPermission(["master.create", "master.update"])`
  - (รอบ 3, จาก code review) zod ของ `imageUrl` เดิมรับ `https://` อะไรก็ได้ → ถ้าหลุด URL นอก host Blob เข้าไป `next/image` จะ 400 ทั้ง tile (`next.config.ts` `remotePatterns` อนุญาตแค่ `**.public.blob.vercel-storage.com`) · เพิ่ม `isOwnedBlobCategoryImageUrl()` ใน `lib/products-bucket-storage.ts` (host Blob + root `settings/categories/`) มาใช้ใน `.refine()` — ใช้ helper ตัวเดียวกับที่ gate การลบไฟล์อยู่แล้ว
- [x] ลบไฟล์เก่าบน Blob อัตโนมัติเมื่อเปลี่ยน/ลบรูป — `deleteCategoryImageObjects()` ใน `lib/products-bucket-storage.ts` gate เฉพาะ host Blob ของเราและ root `settings/categories/` (best-effort ไม่ throw)
- [x] Storefront fallback 3 ชั้น: `Category.imageUrl` → รูปสินค้าขายดีในหมวด → ไอคอนที่เดาจากชื่อหมวด (`inferCategoryVisual`)
- [x] ตัดการตั้งค่าไอคอนออกทั้งชุด: ลบ `lib/category-visual-settings.ts` (SiteContent `category_visual_settings`), ลบ `resolveCategoryVisual`/guards ออกจาก `lib/category-visual-config.ts` — ไอคอนเดาจากชื่อหมวดล้วน ๆ จึงหาย DB read ไป 1 ครั้งทั้งหน้า `/` และ `/home2` (แถว `SiteContent` เดิมยังอยู่ในฐานข้อมูล ไม่ได้ลบ)
- [x] Perf/egress หน้า `/`:
  - `getHomeCategories()` แยก query หารูป fallback (`orderBy: { saleItems: { _count: "desc" } }` = aggregate SaleItem ต่อสินค้า — ตัวแพงสุดของหน้านี้) ไปเป็น query ที่ 2 ที่ยิงเฉพาะหมวดที่ยังไม่มีรูป และ **ข้ามทั้งก้อนเมื่อทุกหมวดแนบรูปครบ**
  - `getHome2WeeklyBestSellersForDate()` เดิม `saleItem.findMany` ไม่มี where วันที่/ไม่มี take = ลากทุก sale line ทั้งตารางมา rank ใน JS → ใส่กรอบ `sale.saleDate >= 180 วันย้อนหลัง` ในระดับ DB (ใช้ index `[saleDate desc, status]` ที่มีอยู่); ranking รายสัปดาห์เหมือนเดิม ต่างเฉพาะ top-up ที่เดิมนับ all-time
- [x] (รอบ 2) `getStorefrontProductFilters()` — 3 query master (category / carBrand+carModel / partsBrand) เดิม `await` เรียงกัน → `Promise.all` (pool `DB_POOL_MAX` default 5 จึงขนานได้จริง) · cache/tag/ผลลัพธ์เหมือนเดิมทุกไบต์
- [x] (รอบ 2) Fitment finder หน้าแรกไม่ส่ง brand→model map ลง client อีกต่อไป — หน้า `/` ส่งแค่**ชื่อยี่ห้อ** (19 ยี่ห้อ ~169B) ส่วน map รุ่นรถ (144 รุ่น ~1.8KB × 2 เพราะ RSC ใส่ทั้งใน HTML และ flight payload) ย้ายไปโหลดจาก `/api/storefront-filters` (`force-static`) ตอนผู้ใช้แตะช่องยี่ห้อจริง
  - `lib/storefront-filter-data-client.ts` (ใหม่) — ดึงตรรกะ fetch+memo ที่เดิมฝังใน `StorefrontFilterTrigger.tsx` ออกมาใช้ร่วมกัน → filter drawer บน header กับ finder ยิง request เดียวต่อการโหลดหน้า (share ทั้ง cache และ in-flight promise)
  - prefetch ตอน pointerenter/focus ช่องยี่ห้อ เพื่อให้แทบไม่เห็น state "กำลังโหลดรุ่น..."; ถ้า fetch ล้มเหลว ช่องรุ่นว่างแต่ยี่ห้อ/ปี/หมวด ยังค้นได้ปกติ
  - ทดสอบจริงบน dev: โหลดหน้าแรก **ไม่มี** request ไป `/api/storefront-filters` → กดเลือก Toyota → ยิง 1 ครั้ง 200 → รุ่นขึ้นครบ (Alphard, Altis, Camry, …) ไม่มี console error
  - `/home2` (legacy, noindex) ยังใช้ `HeroFitmentFinder` แบบเดิม — ไม่ได้แตะ
- [x] (รอบ 4 — แก้รูปโดนครอป) รูปที่แอดมินแนบเองถูกวงกลม `object-cover` เฉือนมุมทิ้ง (พบจริงกับไส้กรองอากาศทรงสี่เหลี่ยม) → แยกการแสดงผลตามที่มาของรูป ผ่านฟิลด์ใหม่ `StorefrontCategoryData.imageSource` (`"category" | "product" | null`)
  - รูปที่แอดมินแนบ → `object-contain` + padding เห็นทั้งชิ้น และ **ตัด hover zoom ออก** (ซูมแล้วมุมจะกลับไปโดนเฉือน ซึ่งคือสิ่งที่ padding มีไว้กัน)
  - รูปสินค้าขายดี (fallback) → คง `object-cover` + hover zoom เหมือนเดิม เพราะเป็นรูป catalog ที่ครอปเต็มวงกลมแล้วดูดีกว่า
  - ⚠️ กับดักที่เจอ: ใส่ `p-3` บนวงกลมเฉย ๆ **ไม่มีผล** กับ `next/image` แบบ `fill` เพราะ absolute + `inset-0` อ้างอิง padding box ของ ancestor (ซึ่งรวมพื้นที่ padding เข้าไปด้วย) → ต้องมี `<span className="relative h-full w-full">` ชั้นในให้ `fill` วัดขนาดแทน
  - พรีวิวหน้าแอดมิน (ช่องแนบไฟล์ 72px + thumbnail ในตาราง 40px) ปรับให้ตรงกับหน้าร้าน (วงกลม พื้น `#f7fafe` contain + padding) เพื่อให้สิ่งที่แอดมินเห็นตอนอนุมัติ = สิ่งที่ลูกค้าเห็น
  - วัดจริงบน dev: รูปแนบเองเรนเดอร์ที่ 54×54 ในวงกลมรัศมี 40 → มุมภาพห่างจุดศูนย์กลาง 38.2 < 40 = ไม่โดนเฉือน แม้สินค้าจะกินเต็มกรอบสี่เหลี่ยม
- [x] (รอบ 5 — crop วงกลมในหน้าแอดมิน) เจ้าของเลือกแนวทาง **WYSIWYG เต็มวงกลม** จึงเพิ่มขั้นตอน crop ก่อนอัปโหลด และ**ย้อนงานรอบ 4 ออก**: `imageSource` ถูกลบทิ้ง รูปทั้งสองแหล่งกลับมาใช้ `object-cover` เต็มวงเส้นทางเดียว (crop ทำให้การแยก 2 เส้นทางไม่จำเป็นอีก)
  - สาเหตุที่รูปแรกดูเล็ก **ไม่ใช่ CSS**: วัดไฟล์จริงบน dev แล้วเป็น **256×171 (3:2 แนวนอน)** ไม่ใช่จัตุรัส และมีขอบว่างซ้าย-ขวาข้างละ 21.9% → `object-contain` ย่อตามด้านกว้าง เหลือสินค้าจริงราว 32px ในวงกลม 80px
  - ใช้ `CropImageDialog` ตัวเดิมของหน้าสินค้าซ้ำ โดยเพิ่ม prop แบบ opt-in 3 ตัว (`circular` → `circularCrop` ของ react-image-crop, `backgroundColor`, `subtitle`) — พฤติกรรมหน้าสินค้าเดิมไม่เปลี่ยน (ยัง 1:1 พื้นขาว JPEG)
  - รูปหมวดหมู่: ครอปวงกลม → เซฟเป็น JPEG 800×800 พื้น `#f7fafe` (สีเดียวกับช่องวงกลมหน้าร้าน จึงกลืนสนิท) · ปุ่ม "ข้าม" = ยกเลิกไม่อัปโหลด เหมือนหน้าสินค้า
  - พรีวิวหน้าแอดมิน (72px + 40px ในตาราง) ตัด padding/contain ออก กลับมาเต็มวงกลมให้ตรงกับที่ลูกค้าเห็น
  - ⚠️ ผลต่อ prompt สร้างรูป: WYSIWYG เต็มวงหมายความว่ามุมของสี่เหลี่ยมจะถูกวงกลมตัด → prompt ต้องกลับไปให้สินค้ากินราว **70%** ของผืนผ้าใบจัตุรัส (ไม่ใช่ 90% แบบตอน contain) เพื่อให้เส้นทแยงมุมของสินค้าอยู่ในวงกลมพอดี
  - ทดสอบแล้ว: หน้าร้านทุก tile เป็น `object-cover` 78×78 padding 0 ไม่มี console error · **หน้าแอดมินยังไม่ได้ทดสอบ runtime** เพราะต้องล็อกอิน ซึ่งผมไม่กรอกรหัสผ่านให้ — ผ่านแค่ `tsc` + `build`
- [x] (รอบ 6 — โมเดล Shopee: วงกลมเป็นพื้นหลัง ไม่ใช่หน้ากากครอป) เจ้าของสังเกตจากหน้า shopee.co.th ว่าสินค้าล้นออกนอกวงกลมเทาได้ = วงกลมเป็นแค่ decoration ยืนยันแล้วจากภาพอ้างอิง (จักรยาน/ทีวี/แล็ปท็อป/เตียง ล้นวงชัดเจน และไม่มีรูปไหนเห็นเป็นแผ่นสี่เหลี่ยม = เป็น PNG โปร่งใสทั้งหมด) · เปิด shopee.co.th เพื่ออ่าน CSS จริงไม่ได้ ติด login wall
  - tile ใหม่: กล่องรูป 80px → วงกลมพื้นหลัง 64px (`#eef3fa`, absolute, `aria-hidden`) อยู่ข้างหลัง → รูปวางทับ **ไม่มี `overflow-hidden` ไม่มีเส้นขอบ** สินค้าจึงล้นวงได้ข้างละ 8px
  - รูปที่แอดมินแนบ (`imageSource === "category"`) → `object-contain` เต็มกล่อง 80px ล้นวงได้ · รูปสินค้าขายดี (fallback) → ยังครอปเต็มวง 64px เพราะเป็นภาพทึบมีพื้นหลังจริง ถ้าปล่อยล้นจะเห็นเป็นสี่เหลี่ยมลอย → `imageSource` ที่ลบไปรอบ 5 **ถูกนำกลับมา** (cache key bump เป็น `-v3`)
  - `CropImageDialog`: เอา `circular`/`backgroundColor` ออก เหลือ prop เดียว `transparent` — เมื่อ true จะไม่ `fillRect` และ export **PNG คงค่า alpha** (JPEG ไม่มี alpha จึงใช้ไม่ได้กับโมเดลนี้); หน้าสินค้ายังเป็น JPEG พื้นขาวเหมือนเดิม
  - แจ้งเตือน (ไม่บล็อก) เมื่อแอดมินเลือกไฟล์ JPG ว่าจะขึ้นเป็นสี่เหลี่ยมทึบทับวงกลม
  - ⚠️ prompt สร้างรูปกลับไปเป็น **สินค้ากิน ~90%** ของผืนผ้าใบจัตุรัส และ **PNG โปร่งใสเป็นข้อบังคับ** (ไม่มีการตัดมุมอีกแล้ว)
  - ทดสอบจริงบน dev: รูปแนบเอง 2 หมวด → `object-contain` กล่อง 80×80 บนวง 64×64 `overflow: visible` · อีก 19 หมวด → `object-cover` 64×64 ครอปในวง · ไฟล์ที่แนบไว้เป็น PNG โปร่งใสจริง (พื้นที่โปร่งใส 51.1% และ 34.4%, alpha มุมบนซ้าย = 0) · ไม่มี console error
  - **หน้าแอดมินยังไม่ได้ทดสอบ runtime** (ต้องล็อกอิน) — ผ่าน `tsc` + `build` + `lint`
- [x] (รอบ 7 — เลิกบังคับ 1:1 เฉพาะรูปหมวดหมู่) เจอตอนแอดมินครอปแผงคอนเดนเซอร์: กรอบ 1:1 ใหญ่ได้สูงสุดเท่า**ด้านที่สั้นที่สุดของภาพ** สินค้าที่กว้างกว่าภาพสูง (แผงแอร์ ท่อ สายแอร์) จึงครอบไม่หมด ปลายซ้าย-ขวาโดนตัดเสมอ — เป็นข้อจำกัดเชิงเรขาคณิต ไม่ใช่ลากกรอบไม่เป็น
  - ข้อสังเกตที่ทำให้ตัดสินใจ: **หลังเปลี่ยนเป็น `object-contain` สัดส่วนไฟล์ไม่มีผลต่อขนาดที่แสดงอีกแล้ว** — contain ย่อตามด้านที่ยาวที่สุดเสมอ ไฟล์ 800×534 ที่สินค้ากินเต็มด้านกว้าง กับไฟล์เดียวกันที่เติมขอบเป็น 800×800 เรนเดอร์สินค้าออกมา 72px เท่ากันเป๊ะ ตัวแปรเดียวที่มีผลคือ**สินค้ากินกี่ % ของด้านที่ยาวที่สุด** ข้อบังคับ 1:1 จึงเป็นมรดกตกค้างจากตอนที่วงกลมยังครอป
  - เพิ่ม prop `freeAspect` ใน `CropImageDialog` (opt-in, หน้าสินค้าไม่ส่ง → คง 1:1 พื้นขาว JPEG 800×800 เหมือนเดิมทุกประการ): ปลด `aspect` lock, กรอบเริ่มต้นครอบทั้งภาพ (กด "ยืนยัน" โดยไม่ลาก = ใช้ไฟล์เดิม), export ตามสัดส่วนที่ครอปโดยย่อด้านยาวสุดเป็น 800px
  - หัว dialog แสดงข้อความตามโหมด: "ลากกรอบได้อิสระ ด้านยาวสุด 800px" แทน "อัตราส่วน 1:1"
  - ⚠️ prompt สร้างรูป: **ตัดข้อบังคับ 1:1 ออกได้แล้ว** เหลือแค่ต้องเป็น PNG โปร่งใส + กรอบชิดตัวสินค้า (สินค้ากิน ~90% ของด้านยาวสุด)
  - ตรวจแล้ว: `tsc` ผ่าน · `build` ผ่าน · `lint` 0 error · `check:mojibake` ผ่าน · ยืนยันว่า `ProductForm` ไม่ได้ส่ง prop ใหม่ทั้ง 2 ตัว จึงไม่กระทบหน้าสินค้า · **หน้าแอดมินยังไม่ได้ทดสอบ runtime** (ต้องล็อกอิน)
- ตรวจแล้ว: `tsc --noEmit` ผ่าน · `npm run lint` 0 error · `npm run build` ผ่าน · `check:mojibake` ผ่าน

## Header: ย้ายเมนูหมวดหลักขึ้นแถบบน + คืน internal link ให้ทุกหน้า (2026-08-10)
- ที่มา: code review พบว่า `variant="compact"` ซ่อนทั้ง utility strip และแถวเมนู → ทุกหน้าที่ไม่ใช่หน้าแรก (`/products`, `/products/[slug]`, `/product/[slug]`, `/about`, `/faq`, `/knowledge`, `/return-warranty-policy`) เหลือลิงก์ใน header แค่โลโก้ · เจ้าของสั่งย้ายเมนูแถวที่ 3 ขึ้นไปต่อจากสโลแกนในแถวที่ 1 และให้ใช้ทุกหน้า
- [x] `STOREFRONT_NAV_LINKS` (4 ลิงก์) ย้ายเข้า utility strip ต่อจาก `shopSlogan` — คงขนาด **14px (`text-sm`)** เท่าตอนเป็นแถวของตัวเอง ส่วนสโลแกน/เบอร์/LINE ยังเป็น 12px (`text-xs`) ตามเดิม (เจ้าของยืนยันตัวเลือกนี้)
- [x] ลบแถวเมนู (แถวที่ 3) ออกทั้งก้อน · utility strip เลิก gate ด้วย `isHome` แสดง**ทั้งแถบ** (สโลแกน + เมนู + เบอร์ + LINE) ทุกหน้า (เจ้าของยืนยัน) · ยังเป็น `hidden lg:block` เหมือนเดิม มือถือใช้ drawer ของ `StorefrontHeaderMobileNav` ต่อไป
- [x] ลบ prop `variant` ทิ้ง (กลายเป็น dead code เพราะทั้งสองค่าให้ผลเหมือนกันแล้ว) — เอาออกจาก 10 จุดที่เรียกใช้
- กันล้น: กลุ่มซ้าย `min-w-0` + สโลแกน `truncate`, เมนูกับกลุ่มขวา `shrink-0` → ที่ความกว้างแคบสโลแกนจะย่อด้วย ellipsis แทนที่จะดัน layout ล้น
- ผลด้าน SEO: internal link บน desktop กลับมาครบทุกหน้า (เดิมมีแค่หน้าแรก) · ความสูง header หน้าแรก **ลดลง ~34px** (เสียแถวเมนูไป) หน้าอื่น **เพิ่ม ~30px** (ได้แถบบนมา)
- ⚠️ ยังค้าง (เจ้าของสั่งแยกงานทีหลัง): `<h1>` เปลี่ยนจากชื่อร้าน (4xl–6xl) เป็น "ค้นหาอะไหล่ตามรถของคุณ" (xl–2xl) ทำให้ brand entity signal อ่อนลง · ส่วน `WhyUs` ยังไม่ถูกนำกลับ (เนื้อหาเชิง intent ได้ `HomeSeoIntro` มาแทนบางส่วนแล้ว ดูหัวข้อถัดไป)

## หน้าแรก: บล็อกเนื้อหาท้ายหน้าแบบ shopee.co.th (2026-08-10)
- ที่มา: เจ้าของส่งภาพบล็อก SEO ท้ายหน้าแรกของ shopee.co.th มาให้ทำแบบเดียวกัน วางถัดจากการ์ด "สินค้ามาใหม่" · ปิดช่องว่างที่ review พบว่าหน้าแรกใหม่เหลือแต่ชื่อสินค้ากับราคา ไม่มีข้อความบรรยายให้ crawler/answer engine เก็บ
- [x] `components/storefront/HomeSeoIntro.tsx` (ใหม่) — Server Component ล้วน ไม่มี `"use client"` จึงไม่เพิ่ม JS ลง bundle เลย · โครงเดียวกับ Shopee: H2 + ย่อหน้า 3 ย่อ แล้ว H2 ที่สอง + ย่อหน้ารวมลิงก์
- [x] วางใน `app/page.tsx` ระหว่าง `HomeNewArrivals` กับ `HomeLineCta` ตามที่สั่ง
- [x] เนื้อหา: **ข้อเท็จจริงจากระบบเท่านั้น** (เจ้าของยืนยันตัวเลือกนี้) — จำนวนสินค้า/หมวด/ยี่ห้อ ใช้ตัวเลขชุดเดียวกับที่ hero เรนเดอร์อยู่แล้ว (ส่งเป็น prop ไม่มี query เพิ่มแม้แต่ query เดียว) · ประโยคบริการอิงจาก trust row + `/about` ที่มีอยู่ · **ไม่มีข้อความส่งฟรี / โค้ดส่วนลด / รับประกันคืนเงิน** เพราะร้านไม่มีบริการเหล่านั้นจริง
- [x] ลิงก์ในบล็อกที่สอง: เฉพาะหน้าข้อมูลร้าน (เจ้าของเลือก) — `/about`, `/faq`, `/return-warranty-policy`, `/knowledge` · บวกลิงก์ `/products` ที่แทรกในเนื้อความบล็อกแรกตามรูปแบบของ Shopee
- [x] หน้าตา: พื้นขาวเต็มความกว้างแบบ Shopee เป๊ะ ไม่มีกรอบการ์ด (เจ้าของเลือก) · ลิงก์ใช้ **สีน้ำเงิน** ไม่ใช่ส้ม เพราะธีมหน้านี้สงวนส้ม (`#f97316`) ไว้ให้ราคาเท่านั้น
- [x] (รอบ 2) เพิ่มบล็อกลิงก์หมวดหมู่ + ยี่ห้อรถ ตรงกลางระหว่างบล็อกแรกกับบล็อกข้อมูลร้าน — เจ้าของดูตัวอย่าง 3 แบบ (ย่อหน้าคั่นจุลภาค / ชิปกดได้ / คอลัมน์แบบ footer) แล้วเลือก **แบบย่อหน้าคั่นจุลภาคเหมือน shopee** และให้ **แสดงชื่อหมวดเต็มตาม DB** (คงวงเล็บภาษาอังกฤษไว้ เพื่อเก็บคีย์เวิร์ด compressor / condenser / evaporator)
  - หมวด 21 ลิงก์ → `getCategoryPath()` (`/products/<slug>`) · ยี่ห้อ 19 ลิงก์ → `/products?carBrand=<ชื่อ>` ซึ่งเป็น param เดียวกับที่ filter drawer ใช้ กดแล้วได้ result set เดียวกัน
  - วนจาก `categories` และ `productFilters.carBrands` ที่หน้าโหลดอยู่แล้ว **ไม่มี query เพิ่ม** — เพิ่มหมวด/ยี่ห้อในแอดมินแล้วลิงก์ขึ้นเองทันที (ตัดตัวเลือกคอลัมน์แบบจัดกลุ่มทิ้งเพราะต้อง hardcode การจัดกลุ่ม แล้วหมวดใหม่จะตกกลุ่ม)
  - ทดสอบจริง: บล็อกมี 3 H2 / ลิงก์รวม 45 ตัว (หมวด 21 + ยี่ห้อ 19 + หน้าข้อมูล/สินค้า 5) · `/products?carBrand=Toyota` คืน "พบ 293 รายการ" · `/products/air-filter-...` ตอบ 200 · ไม่มี horizontal overflow · ไม่มี console error
- ตรวจแล้ว: `tsc --noEmit` ผ่าน · `eslint` 0 error · `check:mojibake` ผ่าน · ทดสอบบน dev จริง: บล็อกอยู่ถัดจาก "สินค้ามาใหม่" และก่อน LINE CTA, พื้นหลัง `rgb(255,255,255)`, H2 2 ตัว / 5 ย่อหน้า / 5 ลิงก์ครบ, ตัวเลขในเนื้อความออกมาเป็น 958 รายการ / 21 หมวด / 19 ยี่ห้อ ตรงกับ hero, ข้อความรวม ~1,570 ตัวอักษร, ไม่มี horizontal overflow, ไม่มี console error
- ตรวจแล้ว: `tsc --noEmit` ผ่าน · `eslint` 0 error · `npm run build` ผ่าน · `check:mojibake` ผ่าน · ทดสอบบน dev จริง: หน้าแรกและ `/products` มีเมนูครบ 4 ลิงก์ในแถบบน, nav 14px / สโลแกน 12px, เหลือ `<nav>` เดียวใน header, ไม่มี horizontal overflow, ไม่มี console error

## Storefront copy + AEO: เติม "หม้อน้ำรถยนต์" และเปิดบอท AI answer (2026-08-11)
- ที่มา: เจ้าของสั่งแก้สโลแกนใต้ชื่อร้านบน header จาก "อะไหล่แอร์รถยนต์" เป็น "อะไหล่แอร์รถยนต์และหม้อน้ำรถยนต์" แล้วให้ไล่แก้จุดอื่นที่ยังเขียนแค่แอร์อย่างเดียว
- [x] ข้อความที่ผู้ใช้เห็น: `StorefrontHeader.tsx` (สโลแกน), `Hero.tsx` + `HeroLegacy.tsx` (H1), `HeroShowcase.tsx` (eyebrow), `WhyUs.tsx`, `SeoIntentSection.tsx`, `products/[categorySlug]/page.tsx` (eyebrow) + `opengraph-image.tsx`
- [x] metadata: `app/page.tsx` (title / OG / Twitter / description), `app/products/page.tsx` (title / OG / description)
- [x] คีย์เวิร์ด: `lib/seo.ts` เพิ่ม `ร้านหม้อน้ำรถยนต์`, `ขายหม้อน้ำรถยนต์`, `หม้อน้ำรถยนต์ นครสวรรค์`, `ฝาหม้อน้ำรถยนต์`, `อะไหล่แอร์รถยนต์และหม้อน้ำรถยนต์`
- [x] AEO: `public/llms.txt` เพิ่มคำตอบสั้นสำหรับ intent "หม้อน้ำรถยนต์"
- [x] `app/robots.ts` — แยก `AI_ANSWER_BOTS` (GPTBot, ChatGPT-User, OAI-SearchBot, ClaudeBot, anthropic-ai, Claude-User, PerplexityBot, Google-Extended) ออกจาก `AGGRESSIVE_BOTS` แล้วเปิดให้เข้าเฉพาะหน้า hub (`/`, `/products`, `/about`, `/faq`, `/knowledge`, `/llms.txt`) · disallow `/product/`, `/products/*/*`, `/products/search`, `/_next/image` ซึ่งเป็นตัวกิน egress หลัก · `crawlDelay: 30`
  - เดิมบอทกลุ่มนี้ถูกปิดรวมกับ SEO scraper ตั้งแต่คอมมิต `3d9e9be` (ลด Supabase egress) ทำให้ `llms.txt` ไม่มีใครอ่าน · `CCBot`, `Bytespider`, `Amazonbot`, `Diffbot` และกอง SEO scraper ยังปิดเหมือนเดิม
- [ ] ติดตาม Supabase egress 1-2 สัปดาห์หลัง deploy — เป้า ≤5GB/เดือน ถ้าเกินให้ถอย allow list ของบอท AI ลง
- ยังไม่แตะ (รอเจ้าของสั่ง): prompt แชท AI/LINE (`lib/chat-core/*`, `lib/content-ai.ts`), บทความ `lib/knowledge-content.ts`, alt text การ์ดสินค้า, title หน้าหมวด (`${category.name} | อะไหล่แอร์รถยนต์ นครสวรรค์` — เติมแล้วยาวเกิน ~60 ตัวอักษรใน SERP)
- ตรวจแล้ว: `npm run build` ผ่าน (Compiled successfully) ทั้งรอบข้อความและรอบ robots

## ช่องค้นหาหน้าร้าน: loading โลโก้ร้าน + คำค้นค้างตอนกรอง (2026-08-11)
- ที่มา: เจ้าของสั่งให้กด Enter แล้วค้นหาได้พร้อมมี loading เป็นโลโก้ร้าน แล้วสั่งเพิ่มให้คลิกคำใน dropdown ก็ต้องมี loading เหมือนกัน · ระหว่างทางเจอบั๊กแยก: ลบ `508` ในช่องค้นหาแล้วติ๊กหมวด "กรองอากาศ" กด "ตกลง" แต่ `q=508` ยังค้างใน URL
- [x] ตรวจแล้วว่า Enter + คลิกคำใน dropdown **ค้นหาได้อยู่แล้ว** ([ProductAutocomplete.tsx](components/shared/ProductAutocomplete.tsx) `handleKeyDown` / `selectKeyword`) ของที่ขาดจริงคือ loading — เจ้าของยืนยันตรงกัน
- [x] loading โลโก้ร้าน: ห่อ `onSubmit` (ที่ parent ยิง `router.push`) ไว้ใน `useTransition` แล้ว portal `<BrandLoader size="lg" label="กำลังค้นหา..."/>` คลุมเต็มจอ z-210 (สูงกว่า modal ค้นหามือถือ z-100) + backdrop เบลอ light/dark · ครอบทั้ง 3 ทางเข้า (Enter, ปุ่มแว่นขยาย, คลิกคำใน dropdown) เพราะทุกทางวิ่งผ่าน `submitQuery()`
  - เดิม `router.push` ยิงตรงไม่มี transition → ระหว่างรอเซิร์ฟเวอร์ไม่มีสัญญาณอะไรเลย ส่วน `loading.tsx` ของ `/products` ไม่ขึ้นเมื่ออยู่หน้าเดิมแล้วเปลี่ยนแค่ `?q=`
  - เกตด้วย `mode === "storefront" && enhanced` → ฝั่งแอดมิน (รวม `MobileProductSearchForm` ที่ก็ใช้ `enhanced="mobile"`) พฤติกรรมเดิมทุกอย่าง · toast "กำลังเปิดสินค้า..." ถูกกันไม่ให้ขึ้นซ้อน (คนละ flow)
- [x] คำค้นค้าง: [SearchResults.tsx](app/products/search/SearchResults.tsx) `handleApplyFilters` ทำ `{ ...filters }` จึงสืบทอด `q` จาก URL เสมอ — ปุ่มตกลงไม่เคยรู้จักช่องค้นหาบน header
  - แก้ด้วย [lib/storefront-search-query-bus.ts](lib/storefront-search-query-bus.ts): ช่องค้นหาหน้าร้านประกาศข้อความที่ลูกค้า "พิมพ์เอง" เก็บไว้ในโมดูล แล้ว `handleApplyFilters` + `handleClearAll` อ่านผ่าน `resolveStorefrontSearchQuery(filters.q)` → ลบคำค้นทิ้งแล้วกดตกลง `q` หายจริง
  - ประกาศเฉพาะตอนลูกค้าแก้ข้อความ ไม่ประกาศตอน mount/sync จาก URL เพราะ `StorefrontHeaderSearch` render `ProductAutocomplete` พร้อมกัน 2 ตัว (mobile + desktop) ถ้าประกาศตอน mount ตัวที่ไม่ได้แตะจะเขียนทับกัน · เคลียร์ override หลัง submit กันค่าค้างตอนกด Back
- [x] (รอบต่อเนื่อง) หลังค้นหาเสร็จให้ `blur()` ช่องค้นหา ไม่คา cursor ไว้ — เจ้าของเลือก blur เฉยๆ ไม่ย้าย focus ไปหัวข้อผลลัพธ์ · กระทบเฉพาะเดสก์ท็อป (Enter ค้างที่ input ตามดีฟอลต์เบราว์เซอร์ · คลิกคำใน dropdown ค้างเพราะ `onMouseDown preventDefault` ที่กัน bug Safari) ฝั่งมือถือ modal ปิดแล้ว input หายอยู่แล้ว แต่ blur ช่วยปิดคีย์บอร์ดก่อน modal ปิด
- ตรวจแล้ว: `npx tsc --noEmit` ไม่มี error · `npm run build` ผ่าน (Compiled successfully, 71/71 static pages)

## LIFF ชำระเงิน: QR sheet หลุดตำแหน่ง + บันทึก QR ไม่สำเร็จ (2026-08-12)
- ที่มา: เจ้าของแคปหน้าจอ LINE ให้ดู — กด "สร้าง QR สำหรับทุกบิล" แล้ว sheet ไปโผล่ล่างสุดของเนื้อหา ไม่ใช่ล่างจอ · แถบเมนูล่างทับ sheet · กดปุ่ม "บันทึก / แชร์ QR" แล้วขึ้น error
- [x] **สาเหตุตำแหน่ง**: `.liff-page-transition` ใน [globals.css](app/globals.css) ประกาศ `will-change: transform, opacity` ค้างไว้ถาวร (ไม่ใช่แค่ตอนอนิเมชันวิ่ง) → กลายเป็น containing block ของ `position: fixed` + สร้าง stacking context → `fixed inset-0` ของ sheet อ้างอิงกล่องเนื้อหาทั้งหน้าแทน viewport และ `z-[100]` ถูกขังไว้ต่ำกว่า `.liff-bottom-nav` (z-30)
  - แก้ด้วย `createPortal` ไปที่ `.liff-theme-root` (ไม่ใช่ `document.body`) เพราะพาเลตต์ dark ของ LIFF ผูกกับ selector `.liff-theme-root[data-liff-theme="dark"] ...` — portal ออก body แล้วสีจะหลุด · `.liff-theme-root` ไม่มี transform/will-change/z-index จึงไม่บล็อก `fixed` · ยก z เป็น `z-[1200]` (สูงกว่าปุ่มแชท LINE ที่ `z-[1100]`)
  - ล็อกสกอร์ลพื้นหลังด้วยการเซ็ต inline `overflow:hidden` ที่ `.liff-scroll-region` (ตัวสกอร์ลจริง — document ไม่เคยสกอร์ลในแอปเชลล์นี้) แล้วลบ inline ตอนปิดเพื่อคืนค่าให้ stylesheet · เพิ่มปิดด้วย Escape + คลิก backdrop
- [x] **จัดเนื้อหาใหม่ให้เห็นปุ่มบันทึกโดยไม่ต้องเลื่อน**: ย้ายปุ่ม "บันทึก / แชร์ QR" ขึ้นมาไว้ใต้การ์ด QR ทันที (ก่อนกล่องบัญชี) · ลด QR จาก 260 → 220px · ข้อความแนะนำ + ปุ่ม "ตรวจยอดและสร้าง QR ใหม่" ลงไปล่างสุด (เลื่อนดูได้ ไม่ใช่ปุ่มหลัก) → เนื้อหาช่วงบน ~500px พอดีจอมือถือ
- [x] **หัว sheet ไม่ขยับตอนเลื่อน**: ทำ header เป็น `sticky top-0` พร้อม negative margin (`-mx-5 -mt-5`) ให้พื้นหลังกินทับ padding ของกล่อง + เส้นคั่นล่าง
- [x] **สาเหตุ "บันทึก QR ไม่สำเร็จ"**: `fetch(payload.qrDataUrl)` บน data URL อยู่ใต้ CSP directive `connect-src` ซึ่งใน [next.config.ts](next.config.ts) **ไม่มี `data:`** → ถูกบล็อก throw เข้า catch ทุกครั้ง (พังทุกเบราว์เซอร์ ไม่ใช่เฉพาะ LINE)
  - แก้โดย decode base64 เองด้วย `atob` → `Uint8Array` → `Blob` (ไม่แตะ CSP ไม่ต้องผ่อนนโยบาย) · fallback ดาวน์โหลดเปลี่ยนจาก data URL ยาวๆ เป็น `URL.createObjectURL(blob)` ซึ่ง WebView ในแอปรองรับดีกว่า · ข้อความ error สุดท้ายเปลี่ยนเป็น "กดค้างที่รูป QR แล้วเลือกบันทึกรูปภาพ" ให้ทำตามได้จริง
- [x] **ไฮไลต์บิลเลยกำหนดในการ์ดเลือกบิล**: [outstanding/page.tsx](app/liff/outstanding/page.tsx) คำนวณ `overdue` อยู่แล้ว — ส่ง `overdue` + `dueDateLabel` เข้า [PaymentBillSelector.tsx](components/liff/PaymentBillSelector.tsx) แล้วใช้พาเลตต์ rose + `AlertCircle` ชุดเดียวกับการ์ดบิลด้านล่างของหน้าเดิม (ทั้งเคสบิลเดียวและหลายบิล) พร้อมบรรทัด "เลยกำหนด {วันที่}"
- [x] **PICKUP ไม่ต้องขึ้น "รอจัดส่ง"**: ใบขายรับหน้าร้านคง `shippingStatus = PENDING` ไว้ ป้ายจึงอ่านว่ารอจัดส่งซึ่งไม่มีความหมายกับลูกค้า — เจ้าของเลือกให้ซ่อนบรรทัดสถานะไปเลย และเปลี่ยนหัวการ์ดเป็น "ข้อมูลการรับสินค้า" ([orders/[id]/page.tsx](app/liff/orders/[id]/page.tsx)) · โหมด DELIVERY เหมือนเดิมทุกอย่าง
- [x] **QR เบลอ**: `buildPromptPayQrDataUrl()` gen ที่ 180px แต่แสดง 220px — เพิ่ม parameter `width` (default 180 เท่าเดิม ผู้เรียกอื่นไม่กระทบ) แล้วให้ [api/liff/payments/qr](app/api/liff/payments/qr/route.ts) ส่ง 440 (2x) · `next/image` รับ `width/height` = 440 แล้ว CSS บังคับแสดง 220
- [ ] ตามต่อ (ยังไม่ทำ): ฟอร์มพิมพ์ใบเสร็จ/ใบแจ้งหนี้/ใบส่งของ + หน้า sale detail ยัง gen QR ที่ 180px — ถ้าอยากให้ QR บนกระดาษคมขึ้นก็ส่ง width สูงกว่านี้ได้ด้วย parameter เดียวกัน
- ตรวจแล้ว: `npx tsc --noEmit` ไม่มี error · `npx eslint` ไฟล์ที่แก้ทั้งหมดไม่มี error (เหลือ warning `ReceiptText` unused ที่มีอยู่ก่อนแล้ว) · ยังไม่ได้รัน `npm run build`

## LIFF ชำระเงิน: ปุ่มบันทึก QR กดแล้วนิ่ง + เนื้อหาทะลุ header (2026-08-15)
- ที่มา: เจ้าของแคปหน้าจอ LINE บน Android — กด "บันทึก / แชร์ QR" แล้วนิ่ง ไม่มี error ไม่มีรูปให้บันทึก · ถามว่าปุ่มแชร์ไปไหนได้บ้าง ทำแบบ share sheet ของแอป LINE (รายชื่อเพื่อน / Keep Memo / VOOM) ได้ไหม · และเลื่อนเนื้อหาใน sheet แล้วยอดเงินทะลุขึ้นไปทับ header
- [x] **สาเหตุ "กดแล้วนิ่ง"**: รอบก่อน (2026-08-12) แก้ CSP `connect-src` ถูกแล้ว แต่ทางสำรองที่เหลือใช้ไม่ได้ใน LINE — Android LINE ใช้ Android System WebView ซึ่ง **ไม่มี Web Share API** (`navigator.share` เป็น undefined) และ **ไม่มี download manager** (`<a download>` + `blob:` เป็น no-op) · iOS WKWebView ก็เมิน attribute `download` เช่นกัน · ทั้งสองทาง **ไม่ throw** → `catch` ไม่ทำงาน → ไม่มีข้อความอะไรขึ้นเลย
  - เสริม: ชื่อไฟล์เดิมเป็น `promptpay-ยอดค้างชำระทั้งหมด.png` — ภาษาไทย+ช่องว่างใน attribute `download` ทำให้บาง browser ทิ้งไฟล์ → เปลี่ยนเป็น ASCII คงที่ `promptpay-qr.png`
- [x] **สาเหตุ "เนื้อหาทะลุ header"**: header เป็น `sticky top-0` + negative margin (`-mx-5 -mt-5`) อยู่ในกล่อง `overflow-y-auto` เดียวกับเนื้อหา — ตำแหน่ง flow ของ header ถูกดันขึ้นเหนือขอบบนของ containing block (content box ของกล่องที่มี `p-5`) ซึ่ง Android WebView คำนวณ sticky offset ผิดในเคสนี้
  - แก้โดย**เลิกใช้ sticky ทั้งชุด**: เปลี่ยน sheet เป็น flex column — header เป็น `shrink-0` ที่ไม่สกรอลล์ และ body เป็น `min-h-0 flex-1 overflow-y-auto` ตัวเดียวที่สกรอลล์ → เนื้อหาออกนอกกล่องตัวเองไม่ได้เลย ปิดปัญหาทั้งคลาส ไม่ใช่แค่ patch อาการ (โครงเดียวกับ `.liff-app-frame` + `.liff-scroll-region` ที่ใช้อยู่แล้ว)
- [x] **ตอบเรื่องแชร์**: share sheet ในรูปที่เจ้าของส่งมาเป็นของ**ตัวแอป LINE เอง** หน้าเว็บใน LIFF เรียกตรงไม่ได้ · `liff.shareTargetPicker()` ได้แค่ครึ่งบน (รายชื่อเพื่อน/กลุ่ม) ส่งให้คนอื่นเท่านั้น เซฟลงเครื่องตัวเองไม่ได้ → **ไม่ตอบโจทย์ ตัดทิ้ง** · เจ้าของเลือกทาง **A+C**
- [x] **A — ส่ง QR เข้าแชท 1:1 ของลูกค้า (ปุ่มหลัก)**: [api/liff/payments/qr/send](app/api/liff/payments/qr/send/route.ts) push ข้อความ text (ยอด+บัญชี+วิธีใช้) + image message เข้า `session.lineUserId` ด้วย `pushLineMessages()` ที่มีอยู่แล้ว → ลูกค้าแตะรูปในแชทได้ image viewer ของ LINE ที่มีปุ่มเซฟ/แชร์ native ครบ · rate limit 5 ครั้ง/นาที (แน่นกว่า preview เพราะกิน push quota จริง)
- [x] **C — เปิดรูปในเบราว์เซอร์นอก (ปุ่มรอง)**: อยู่ใน LINE → `liff.openWindow({ external: true })` ไปที่ URL รูป PNG แล้วกดค้าง Save Image ได้ตามปกติ · อยู่นอก LINE → ใช้ Web Share / download เดิมซึ่งทำงานได้จริงในเบราว์เซอร์ทั่วไป · **ถ้าอยู่ใน LINE แล้วเปิดไม่ได้ ขึ้นข้อความบอกให้กดค้างที่รูป** — ไม่มีทางไหนเงียบอีกแล้ว
- [x] **endpoint รูป PNG จริง**: [api/liff/payments/qr/image](app/api/liff/payments/qr/image/route.ts) — LINE image message รับเฉพาะ HTTPS URL ใช้ `data:` ไม่ได้ · เป็น public โดยตั้งใจเพราะ**เซิร์ฟเวอร์ LINE เป็นคนไปดึงรูป** และเบราว์เซอร์นอกก็ไม่มี cookie LIFF ติดไป · กันด้วย [payment-qr-image-token.ts](lib/payment-qr-image-token.ts) — HMAC/`DOC_VERIFY_SECRET` อายุ 30 นาที ตาม pattern เดียวกับ `signLiffPrintDocumentToken()` · **token ถือแค่ยอดเงิน ไม่มี customerId/saleId** ลิงก์หลุดก็ไม่บอกว่าเป็นของใคร · token ผิด/หมดอายุ/ไม่มี ตอบ 404 เหมือนกันหมด
  - เพิ่ม `buildPromptPayQrPngBuffer()` ใน [payment-qr.ts](lib/payment-qr.ts) (gen 720px ให้สแกนจากคลังภาพชัด, ต่ำกว่าลิมิต preview 1MB ของ LINE มาก) — `buildPromptPayQrDataUrl()` เดิมไม่แตะ ผู้เรียกอื่นไม่กระทบ
- [x] **DRY**: ดึง `resolvePaymentTarget()` ออกจาก route มาเป็น [liff-payment-qr-target.ts](lib/liff-payment-qr-target.ts) ให้ preview / send ใช้ตัวเดียวกัน — ยอดกับ label จะไม่มีวันเพี้ยนกันระหว่าง 2 เส้นทาง
- [x] `/api/liff/payments/qr` คืน `imageUrl` เพิ่ม (null เมื่อไม่มี `DOC_VERIFY_SECRET` → ฝั่ง client fallback แทนที่จะโชว์ลิงก์เสีย)
- env: ใช้ของเดิมครบ (`DOC_VERIFY_SECRET`, `APP_BASE_URL`, `LINE_MESSAGING_API_CHANNEL_ACCESS_TOKEN`) ไม่ต้องเพิ่มใน `.env.example`
- [ ] ตามต่อ (ยังไม่ได้ทดสอบ): เจ้าของทดสอบบน Android แล้ว · **iOS ยังไม่ได้เทสจริง** — โค้ดรองรับไว้แล้ว (WKWebView เข้าทาง `isInClient()` เหมือนกัน) แต่ต้องยืนยันบนเครื่องจริงว่า `openWindow({external:true})` เด้ง Safari และเซฟรูปได้
- ตรวจแล้ว: `npx tsc --noEmit` ไม่มี error · `npx eslint` ไฟล์ที่แก้ทั้งหมดไม่มี error · `npm run build` ผ่าน (Compiled successfully, 71/71 static pages, route ใหม่ขึ้นครบทั้ง `/api/liff/payments/qr/image` และ `/api/liff/payments/qr/send`)

## LIFF ชำระเงิน: เหลือปุ่มเดียว "บันทึก QR" + header Thai QR Payment (2026-08-16)
- ที่มา: เจ้าของแคปหน้า QR sheet มาให้ดู — ขอ (1) เอาปุ่ม "เปิดรูป QR เพื่อบันทึกเอง" ออก (2) เปลี่ยนปุ่ม "ส่ง QR เข้าแชท LINE" เป็น "บันทึก QR" ที่บันทึกรูปลงเครื่องอัตโนมัติ (3) เปลี่ยน header เป็นแถบ Thai QR Payment พื้นน้ำเงินแบบสลิป ThaiQR แต่คงปุ่ม X ไว้
- ยืนยันกับเจ้าของก่อนลงมือ (เพราะชนข้อจำกัดจริงของ LINE WebView ที่บันทึกไว้ตั้งแต่รอบ 2026-08-15): **เลือกทาง "เปิดเบราว์เซอร์นอกแล้วดาวน์โหลดอัตโนมัติ"** · header เอา**โลโก้อย่างเดียว + ปุ่ม X** (ตัดข้อความ "ชำระด้วย PromptPay / QR พร้อมยอดชำระ" ออก) · **คง route `/send` ไว้ ไม่ลบ**
- [x] **ปุ่มเดียว "บันทึก QR"** ใน [PaymentQrButton.tsx](components/liff/PaymentQrButton.tsx): อยู่ใน LINE → `liff.openWindow({ external: true })` ไปที่ URL รูป QR ที่ตอบ `Content-Disposition: attachment` → Chrome/Safari ดาวน์โหลดเองทันที ลูกค้าไม่ต้องกดค้างหรือแตะซ้ำ · อยู่นอก LINE → `blob:` + `<a download>` เซฟลงเครื่องในหน้าเลย
  - ตัด `navigator.share` ทิ้ง (share sheet ต้องให้ลูกค้าเลือกเมนูเอง = ไม่ใช่ "อัตโนมัติ" ตามที่ขอ) · ตัดปุ่ม "เปิดรูป QR เพื่อบันทึกเอง" + บล็อก "ส่ง QR เข้าแชทแล้ว / เปิดแชท LINE" ออกทั้งชุด
  - เพิ่มบรรทัดยืนยันใต้ปุ่ม 6 วินาที ("บันทึกรูป QR ลงเครื่องแล้ว" / "เปิดเบราว์เซอร์เพื่อบันทึกรูป QR ลงเครื่องแล้ว") — ปุ่มจะไม่เงียบเหมือนรอบก่อน · เคสไม่มี `DOC_VERIFY_SECRET` ใน LINE ยังขึ้นข้อความให้กดค้างที่รูปเหมือนเดิม
- [x] **`?download=1` ที่ endpoint รูป**: [payment-qr-image-token.ts](lib/payment-qr-image-token.ts) รับ `options.download` แล้วต่อท้าย query · [api/liff/payments/qr/image](app/api/liff/payments/qr/image/route.ts) สลับ `Content-Disposition` เป็น `attachment` เมื่อ `download=1` · **การ push เข้าแชทยังใช้ URL แบบ `inline` เหมือนเดิม** เพราะ media proxy ของ LINE ต้องการรูปแบบ inline
- [x] `/api/liff/payments/qr` เปลี่ยน field `imageUrl` → `downloadUrl` (สร้างด้วย `{ download: true }`) · ผู้เรียกมีแค่ `PaymentQrButton.tsx` ตัวเดียว ไม่มีที่อื่นกระทบ
- [x] **header Thai QR Payment**: แถบพื้น `#00427a` (สีเดียวกับใบพิมพ์) + `public/Thai_QR_Logo_white.svg` (`unoptimized` เพราะ next.config ไม่ได้เปิด `dangerouslyAllowSVG`) + ปุ่ม X พื้น `bg-white/15` สีขาว · สีแถบคงที่ทั้ง light/dark ตามอัตลักษณ์ ThaiQR · กล่อง sheet มี `overflow-hidden` อยู่แล้วมุมบนจึงโค้งตาม
- [x] **คง `/api/liff/payments/qr/send` ไว้เฉยๆ** ตามที่เจ้าของเลือก — UI ไม่เรียกแล้ว แต่ route + rate limit + `resolveLiffPaymentTarget()` ยังใช้ได้ทันทีถ้าจะกลับมาเปิดใช้
- [ ] ตามต่อ (ยังไม่ได้ทดสอบบนเครื่องจริง): Android คาดว่าลง Downloads/แกลเลอรี · **iOS Safari จะเซฟลงแอป Files ไม่ใช่คลังรูป** — ถ้าลูกค้า iOS ต้องสแกน QR จากคลังรูปแล้วหาไม่เจอ ให้กลับมาคุยเรื่องทางเลือกอีกครั้ง
- ตรวจแล้ว: `npx tsc --noEmit` ไม่มี error · `npx eslint` 4 ไฟล์ที่แก้ไม่มี error · `npm run build` ผ่าน (Compiled successfully, 71/71 static pages)

## ช่องค้นหาหน้าร้าน: ปุ่ม X ล้างคำค้น (2026-08-16)
- ที่มา: เจ้าของแคปช่อง autocomplete หน้าบ้านมาให้ดู — ขอปุ่ม X "ทำหน้าที่แค่ลบข้อความออกเท่านั้น" · ยืนยันขอบเขต = **หน้าบ้านทั้ง desktop + mobile** (ฝั่งแอดมินไม่แตะ)
- [x] [ProductAutocomplete.tsx](components/shared/ProductAutocomplete.tsx): ปุ่ม X gate ด้วย `isStorefrontSearchField && value.length > 0` → ขึ้นเฉพาะ `mode="storefront"` + `enhanced` เท่านั้น · `ProductFilterForm` / `MobileProductSearchForm` (admin) ไม่มีอะไรเปลี่ยน · ช่องใน modal มือถือมี X อยู่ก่อนแล้ว คงไว้เหมือนเดิม
- [x] `clearQuery()` ทำแค่ `handleUserInput("")` + `setActiveIndex(-1)` — ไม่ยิงค้นหาใหม่ ไม่แตะ URL (อยู่หน้า `?q=508` ผลลัพธ์เดิมยังอยู่) · เพราะผ่าน `handleUserInput` จึง publish ค่าว่างเข้า `storefront-search-query-bus` ให้ปุ่ม "ตกลง" ในแผงกรองไม่ยึด q เก่า (พฤติกรรมเดียวกับ X ใน modal มือถือ)
- [x] desktop คืน focus ให้ช่องเพื่อพิมพ์ต่อได้ทันที + `onMouseDown preventDefault` กัน Safari ปิด dropdown ทิ้งก่อน `onClick` ทำงาน · mobile **ห้าม**คืน focus เพราะ input ตัวนอกเป็นแค่ตัวเปิด modal (focus แล้ว modal จะเด้ง)
- [x] ตำแหน่ง: X `right-14` (h-7 w-7) หลบปุ่มค้นหา header (`right-1`, w-12) เหลือช่องไฟ 4px · input เผื่อ `pr-[5.5rem]` **เฉพาะตอนมีข้อความ** (ยังไม่พิมพ์ = padding เดิม placeholder ไม่ถูกตัดสั้นลง) · ส่ง `pr-*` คลาสเดียวเสมอ กัน Tailwind สลับลำดับกันเอง · dark mode ทำมาพร้อมกันในรอบเดียว
- ตรวจแล้ว: `npx tsc --noEmit` ไม่มี error · `npm run build` ผ่าน · `scripts/test-storefront-autocomplete-focus-regression.ts` ผ่าน · วัด DOM จริงบน dev server: X = 28×28 ที่ right 56px, ปุ่มค้นหา 48×32 ที่ right 4px, `padding-right` 88px → ไม่ทับกัน
- [ ] ยังไม่ได้ทดสอบ "กดจริง" ในเบราว์เซอร์ (browser pane ของ session นี้ไม่ render → React ไม่ hydrate จึงคลิกทดสอบไม่ได้) — รบกวนเจ้าของกดยืนยันบนหน้าจริงอีกรอบ

## แจ้งเตือนสินค้าหมดสต๊อก: heartbeat เมื่อไม่มีของหมด (2026-08-17)
- ที่มา: เจ้าของแจ้งว่า Telegram "สินค้าหมดสต๊อก (Stock = 0)" ของวันที่ 16/08 เวลา 18:30 ไม่มีส่งมา
- ตรวจแล้วไม่ใช่บั๊ก: cron `30 11 * * *` UTC = 18:30 ไทย ทำงานปกติ (มี `STOCK_OUT_DAILY` ครบทุกวัน 06–15/08) แต่ `notifyOutOfStockDaily()` เดิม `return 0` ทันทีเมื่อ list ว่าง → 16/08 ไม่มีสินค้า TRACKED ที่ stock ≤ 0 (P0460 + P0486 จากรอบ 15/08 เติมสต๊อกแล้ว) จึงเงียบ · `P0000 "อื่นๆ"` stock 0 แต่เป็น `NON_TRACKED` ถูกกรองออกตามเจตนา
- ปัญหาจริงคือ "เงียบ" แยกไม่ออกระหว่าง *ไม่มีของหมด* กับ *cron ตาย* → เจ้าของเลือกให้ส่ง heartbeat
- [x] [lib/notifications.ts](lib/notifications.ts): เพิ่ม `buildOutOfStockHeartbeatBody()` + `notifyOutOfStockDaily()` เคส list ว่างส่ง `createNotification()` severity INFO title "ตรวจสอบสต๊อกประจำวัน (ไม่มีสินค้าหมด)" ไม่มี `link` (ไม่มีอะไรให้กดดู) → เข้ากระดิ่ง + Telegram พร้อมกันตามกฎเหล็ก §8 · ใช้ `NotificationType.STOCK_OUT_DAILY` เดิม **ไม่แตะ schema**
- [x] body เป็น **บรรทัดเดียว** และจงใจไม่มีคำว่า `รวม N รายการ` → `parseOutOfStockCount()` ใน [use-admin-system-notification-source.tsx](components/shared/use-admin-system-notification-source.tsx) คืน `null` กระดิ่งจึง render body ธรรมดา ไม่ต้องแก้ UI และไม่โชว์ "ต้องสั่งเพิ่ม 0 รายการ"
- [x] [lib/telegram.ts](lib/telegram.ts): `resolveNotificationEmoji()` + `INFO_VARIANT_EMOJI` — `STOCK_OUT_DAILY` severity INFO ขึ้นหัวด้วย ✅ แทน 🔴 (ข้อความ all-clear ต้องไม่ดูเหมือนเตือนภัย) · เคส WARNING ยังเป็น 🔴 เหมือนเดิม
- ตรวจแล้ว: `npx tsc --noEmit` ไม่มี error · เพิ่มเทสต์ ✅/🔴 ใน [telegram-format.test.ts](lib/__tests__/telegram-format.test.ts) · `telegram-format` + `notifications-handoff` ผ่านทั้งหมด
- [ ] รอยืนยันของจริง: เย็นวันที่ยังไม่มีสินค้าหมด ต้องเห็นข้อความ ✅ เข้า Telegram เวลา 18:30

## Backup อัตโนมัติเข้า Google Drive สัปดาห์ละครั้ง (2026-08-17)
- ที่มา: เจ้าของขอ backup DB + ไฟล์รูปสัปดาห์ละ 1 ครั้งเข้า Google Drive · ตรวจของเดิมแล้วพบว่า Backup Center ทำ auto ไม่ได้ — `pg_dump` ไม่มีใน Vercel runtime (`PG_DUMP_NOT_AVAILABLE`) จึงใช้ได้เฉพาะตอนรัน dev บนเครื่อง · เจ้าของไม่มีเครื่องเปิดตลอด → Windows Task Scheduler ตกไป → เลือก **GitHub Actions** ที่ให้ทั้ง cron อัตโนมัติและปุ่มสั่งเอง
- [x] [.github/workflows/backup.yml](.github/workflows/backup.yml): cron `0 19 * * 0` (จันทร์ 02:00 ไทย) + `workflow_dispatch` · ติดตั้ง `postgresql-client-17` จาก PGDG (Ubuntu ให้ 16 ซึ่ง dump server 17 ไม่ได้) + `rclone` จาก apt · timeout 120 นาที, concurrency group กันรันซ้อน
- [x] **RLS guard**: role อ่านอย่างเดียวไม่ bypass row-level security → ถ้าตารางใน `public` เปิด RLS `pg_dump` จะได้ข้อมูลไม่ครบแบบเงียบ ๆ workflow จึงเช็ก `pg_class.relrowsecurity` แล้ว fail พร้อมชื่อตาราง (ตอนนี้ตาราง Prisma ไม่เปิด RLS จึงผ่าน)
- [x] verify dump ด้วย `pg_restore --list` + เช็กขนาดขั้นต่ำ 10KB — dump ที่อ่านไม่ออกแย่กว่าไม่มี dump เพราะดูเหมือนสำเร็จ
- [x] [scripts/backup-blob-sync.ts](scripts/backup-blob-sync.ts): mirror Vercel Blob แบบ incremental · rclone อ่าน Vercel Blob ตรงไม่ได้ (ไม่ใช่ S3-compatible) จึง diff กับ `state/blob-index.json` ที่เก็บบน Drive แล้วโหลดเฉพาะไฟล์ที่ `etag` เปลี่ยน → รอบแรกหนักครั้งเดียว หลังจากนั้น egress เกือบศูนย์ (คุมตามเป้า ≤5GB/เดือน)
- [x] ลำดับอัปโหลดสำคัญ: `blob-mirror` → `db` → `reports` → `state` ท้ายสุด · ถ้าเขียน state ก่อนไฟล์ขึ้นจริง รอบหน้าจะข้ามไฟล์ที่ยังไม่เคยถูก backup
- [x] `blob-mirror` เก็บสะสม **ไม่เคยลบ** (ใช้ `rclone copy` ไม่ใช่ `sync`) — เป็นด่านเดียวที่กันไฟล์ถูกลบพลาด · manifest บันทึก `removedSinceLastRun` ไว้ให้ตรวจ · dump + report retention 90 วัน
- [x] แจ้ง Telegram `if: always()` ทั้งสำเร็จและล้มเหลว — ยิงตรงจาก workflow ไม่ผ่าน `createNotification()` เพราะเป็นงาน CLI นอกแอป ไม่ใช่ business flow ของ admin (เจ้าของเลือกทางนี้ เพื่อไม่ต้องแตะ schema)
- [x] ปุ่ม one-click ในแอป: [lib/github-backup.ts](lib/github-backup.ts) (dispatch + อ่านสถานะ run, parse ด้วย zod, timeout 10 วิ) · [route.ts](app/api/admin/backup-center/github-backup/route.ts) GET/POST หลัง `requirePermission("system.backup")` + เขียน AuditLog `EXPORT` entityType `BackupWorkflow` · [BackupCenterClient.tsx](app/admin/(protected)/backup-center/BackupCenterClient.tsx) panel ใหม่บนสุด แสดง 5 run ล่าสุด poll ทุก 10 วิเฉพาะตอนมีงานค้าง · dark mode ทำพร้อมกันในรอบเดียว
- [x] ของเดิมไม่แตะตามที่เจ้าของสั่ง — ปุ่ม Blob Manifest / Blob Archive / PostgreSQL Download และ Backup Job แบบ copy เข้า prefix `backups/` ยังอยู่ครบ ใช้เป็น backup เฉพาะกิจก่อนงานใหญ่ได้เหมือนเดิม (สคริปต์ใหม่ข้าม prefix นี้)
- [x] [docs/backup-automation-runbook.md](docs/backup-automation-runbook.md): SQL สร้าง role `backup_reader`, ตั้ง rclone + `root_folder_id` ล็อกโฟลเดอร์เดียว, GitHub Secrets 6 ตัว, PAT สิทธิ์ Actions เท่านั้น, ขั้นตอน restore ทั้ง DB และไฟล์รูป, troubleshooting
- ตรวจแล้ว: `npm run typecheck` ไม่มี error · `npm run lint` ไม่มี error/warning ในไฟล์ใหม่ · `npm test` 803 ผ่าน 0 fail · `npm run check:mojibake` ผ่าน · YAML parse ได้ ภาษาไทยใน workflow ไม่เพี้ยน
- [ ] **รอเจ้าของตั้งค่าก่อนใช้งานจริง** — สร้าง role, ตั้ง rclone, ใส่ Secrets, ใส่ env ใน Vercel ตามคู่มือ แล้วกดปุ่มทดสอบ 1 ครั้ง (รอบแรกช้าที่สุดเพราะโหลดรูปทั้งหมด)
- [ ] ยังไม่มีสคริปต์ restore ไฟล์รูปกลับเข้า Blob (`backup-blob-restore.ts`) — จงใจ เพราะการอัปโหลดทับ Blob store ต้องคุมมือ จะเขียนตอนที่ต้องใช้จริง
- [ ] ตั้งรอบซ้อม restore เข้า Supabase project ใหม่ไตรมาสละครั้ง

## How To Use This Repo As AI
1. อ่าน [AGENTS.md](/D:/autoparts/AGENTS.md) ก่อนเสมอ
2. อ่านไฟล์นี้เพื่อดู current focus และ source of truth
3. เปิด [docs/architecture.md](/D:/autoparts/docs/architecture.md) ถ้าต้องเข้าใจภาพรวมระบบ
4. ถ้างานยัง active ให้ดู [docs/roadmap/active.md](/D:/autoparts/docs/roadmap/active.md)
5. ถ้าต้องตัดสินใจเชิงธุรกิจหรือ implementation rule ให้ดู `docs/decisions/`
6. ถ้าต้องลงลึกเป็นโมดูล ให้ดู `docs/specs/`

## Archive
- Master plan เดิมแบบละเอียดมาก: [docs/archive/PLAN-legacy-2026-05-21.md](/D:/autoparts/docs/archive/PLAN-legacy-2026-05-21.md)
- ห้ามใช้ archive เป็น entrypoint หลัก ยกเว้นต้องตาม historical detail
