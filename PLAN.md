# ศรีวรรณ อะไหล่แอร์ - Active Plan

## Purpose
- ไฟล์นี้เป็น entrypoint สำหรับ AI และคนที่เข้ามาทำงานต่อใน repo
- เก็บเฉพาะภาพรวมระบบ, งานที่กำลังทำ, งานที่ต้องตัดสินใจ, และลิงก์ไป source of truth
- รายละเอียดเก่าทั้งหมดถูก archive ไว้ที่ [docs/archive/PLAN-legacy-2026-05-21.md](/D:/autoparts/docs/archive/PLAN-legacy-2026-05-21.md)

## Summary
- โปรเจกต์นี้คือระบบร้านอะไหล่แอร์รถยนต์ มี public storefront และ admin backoffice
- Tech หลัก: Next.js App Router, TypeScript strict, Prisma, PostgreSQL, NextAuth, Tailwind, Shadcn UI
- ระบบ stock ใช้ `StockCard` เป็น source of truth สำหรับ movement และ moving average cost
- กฎ cross-module สำคัญอยู่ใน [AGENTS.md](/D:/autoparts/AGENTS.md)
- Cash/Bank Lite (Phase 6.6) implement เสร็จและใช้งานจริงแล้ว — เหลือเฉพาะงานติดตามหลัง rollout บางส่วน

## Current Focus
- `Phase 6.6` โมดูลบัญชีธนาคาร/เงินสด Lite — **เสร็จและใช้งานจริงแล้ว** (schema 4 model + 5-step permission + nav + posting wire ครบทุกเอกสารเงิน + ledger/transfer/adjustment report). รายละเอียดด้านล่าง Active Workstreams #1
- ติดตาม manual/ongoing work ของ `Phase 7` ด้าน SEO, verification, และ content expansion
- Product Search Quality phase ถัดไป: Review Outcome Tracking, Fitment/Year remediation, Closed-Loop Measurement, และ Guarded Auto-Apply
- **Warranty Manual Mode + Cancel Flow (2026-05-28)** — เพิ่ม 2-mode บน `/admin/warranties/new` (WITH_SALE / NO_SALE) + ฟังก์ชันยกเลิกประกันที่สร้างเอง (เฉพาะ `createdVia = MANUAL`, hard delete, block ถ้ามี active claim) รายละเอียดอยู่ใน [docs/roadmap/active.md](/D:/autoparts/docs/roadmap/active.md)
- **Purchase Invoice OCR (2026-06-16)** — ตัวช่วย AI (Gemini) อ่านรูปใบส่งของซัพพลายเออร์แล้วเติม draft รายการสินค้าในหน้า `/admin/purchases/new` เท่านั้น (จับคู่สินค้าด้วย semantic search — part no. ก่อน แล้ว fallback ข้อความ) เป็น read-only ไม่แตะ `createPurchase`/StockCard/MAVG; สเปก+checklist อยู่ใน [docs/purchase-invoice-ocr-plan.md](/D:/autoparts/docs/purchase-invoice-ocr-plan.md). เหลือทดสอบกับใบจริงเพื่อวัด match rate
- **Product AI Research (2026-06-23)** — ปุ่ม "AI Research" บนฟอร์มสินค้า (`/admin/products/new` + `[id]/edit`) เรียก Gemini (เปิด Google Search grounding) เตรียม draft: ชื่อสินค้า/คำอธิบาย/หมวด/แบรนด์/alias 8 หมวด/รุ่นรถ VERIFIED+POSSIBLE แล้วเปิด modal ให้ admin ตรวจ/แก้ก่อนกรอกลงฟอร์ม (ยังไม่บันทึกสินค้า). Read-only ต่อ business logic — ไม่แตะ รูป/ราคา/สต็อก/หน่วย/lot/ประกัน; permission `products.create`/`products.update`; กรอง source ต้องห้าม (sriwanparts + เว็บต่างประเทศ); ปีรถบังคับ ค.ศ. (auto-แปลง พ.ศ.); key rotation วนได้ถึง 10 key (`maxDuration=300`). สเปก/prompt อยู่ใน [docs/specs/product-research-agent-prompt.md](/D:/autoparts/docs/specs/product-research-agent-prompt.md). เหลือทดสอบกับสินค้าจริงเพื่อวัดคุณภาพ draft + ตัดสินใจว่าจะเก็บ draft history/confidence ลง DB หรือไม่
- **Keyword-First Search + LINE-Grade Submit (2026-06-27)** — autocomplete หน้าบ้านเปลี่ยนเป็น keyword-first (ตาราง `SearchKeyword` + endpoint `/api/search/keywords`, prefix lookup ~40ms แทน trigram 139–475ms ต่อ keystroke) แสดงคำล้วนแบบ Shopee; ตอน submit ใช้ pipeline เดียวกับ LINE OA (`resolveStorefrontSearchIntent`: classify→guard→fitment hard-filter + year-drop retry) คุมให้ผลตรงหมวด/ยี่ห้อ/ปี ไม่กว้าง. cache classify 1 วัน + degrade ปลอดภัยเมื่อไม่มี Gemini. admin search ไม่เปลี่ยน. สเปก: [docs/specs/keyword-first-search.md](/D:/autoparts/docs/specs/keyword-first-search.md)
- **Document Mutation Guard — UI Lock บนหน้าแก้ไข (2026-06-30)** — ต่อยอดจาก transaction mutation guard (server) ให้หน้า `/edit` ของ 5 เอกสาร (sales, purchases, credit-notes, purchase-returns, supplier-advances) เรียก `checkDocumentMutation()` แล้วแสดง `DocumentMutationBlockedNotice` (พร้อมลิงก์เอกสารปลายทาง) + ส่ง prop `submitLocked` เข้าฟอร์มเพื่อ disable ปุ่มบันทึก + กัน submit เมื่อเอกสารถูกดึงไปใช้ที่เอกสาร ACTIVE ปลายทาง. เพิ่ม helper `buildMutationBlockReferenceLinks()` (รวม mapping entityType→route) ใน `lib/document-mutation-guard.ts`. การเช็ก reference-chain เดิมในแต่ละ Server Action ยังคงไว้ครบ (defense-in-depth). tsc + test guard ผ่าน
- **ส่วนลดระดับรายการ — SaleItem list price + line discount (2026-07-02)** — เพิ่ม 2 ฟิลด์ใน `SaleItem`: `unitListPrice` (ราคาตั้งก่อนลด/หน่วย) + `lineDiscount` (ส่วนลดรวมของบรรทัด, บาท) แบบ **additive ล้วน** — `salePrice` ยังเป็นราคาสุทธิต่อหน่วยและเป็น source of truth ของทุกการคำนวณ (กำไร/VAT/ยอด/ใบลดหนี้ไม่แตะ). Invariant: `unitListPrice ≥ salePrice`, `lineDiscount = (unitListPrice − salePrice) × qty` บังคับซ้ำใน Zod `.transform()` ฝั่ง server. UI หน้าขาย (`SaleForm.tsx`) แยก 3 คอลัมน์ "ราคาขาย/หน่วย" (ก่อนลด)/ส่วนลดต่อหน่วย/ราคาสุทธิ คำนวณสองทางอัตโนมัติ + สรุป "ราคาขายรวม" และ "ส่วนลดระดับรายการ" ท้ายบิล (แยกจาก "ส่วนลดท้ายบิล" = `Sale.discount`). Print (`SharedSalesDeliveryPrintDocument` + delivery/LIFF selects) โชว์ราคาตั้งขีดฆ่าเมื่อมีส่วนลด. รายงาน: `salesSummary.lineDiscountTotal` (memo อ้างอิง ไม่เข้าการคำนวณกำไรซ้ำ) + แถบใน `ReportsContent` + แถว CSV. Backfill `unitListPrice = salePrice` ให้ 99 row เดิมแล้ว (`prisma/scripts/backfill-saleitem-listprice.ts`). Audit snapshot เก็บ 2 ฟิลด์เพิ่ม. `db push` + `tsc --noEmit` ผ่าน
- **ซ่อนราคาหน้าบ้าน + ประเภทลูกค้าคุมราคาบน LINE (2026-07-02)** — (1) เว็บ storefront ซ่อนราคาทั้งหมด แสดง "สอบถามราคา" แทน ผ่านธงกลาง `HIDE_STOREFRONT_PRICE` ใน [lib/storefront-pricing.ts](/D:/autoparts/lib/storefront-pricing.ts) (ครอบ ProductCard, หน้ารายละเอียดสินค้า, autocomplete `mode="storefront"`, ตัด price ออกจาก `ProductJsonLd`, ซ่อนฟิลเตอร์ช่วงราคา — logic query คงไว้). (2) เพิ่มโมเดล master ใหม่ `CustomerType { name, showPrice, isActive, sortOrder, isSystem }` + `Customer.customerTypeId` (nullable → ถือเป็นทั่วไป/ซ่อนราคา). Seed 2 ค่าเริ่มต้น: `ลูกค้าทั่วไป` (showPrice=false, isSystem) + `อู่ซ่อมรถ` (showPrice=true) ผ่าน [scripts/seed-customer-types.ts](/D:/autoparts/scripts/seed-customer-types.ts). เมนูใหม่ `/admin/master/customer-types` (ใช้สิทธิ์ `master.*` เดิม + AuditLog `CustomerType` + กันแก้/ปิด `isSystem`) + nav item + ฟอร์มลูกค้าเลือกประเภทด้วย SearchableSelect + badge ในหน้า list/detail. (3) LINE OA gate ราคา: `resolveLineShowPrice(lineUserId)` ใน [lib/line-conversation-repository.ts](/D:/autoparts/lib/line-conversation-repository.ts) (linked + customerType.showPrice → เห็นราคา, ที่เหลือ/unlinked → ซ่อน) + helper `applyLinePriceVisibility()` เซ็ต salePrice=0 (sentinel เดิม → Flex การ์ด + ข้อความ AI fallback เป็น "สอบถามราคา" อัตโนมัติ ไม่ต้องแก้ formatter/prompt) เสียบใน `line-webhook-processor` 2 จุดสร้าง products. รายงานเจ้าของร้าน (daily summary) + เอกสารขาย/ใบเสร็จ + logic ราคา/สต็อก ไม่แตะ. `db push` + `tsc` + เทสต์ (flex 8, processor 53) ผ่าน. แถมแก้บั๊กเดิม `getLatestDate` ใน [lib/storefront-sitemap.ts](/D:/autoparts/lib/storefront-sitemap.ts) (unstable_cache deserialize Date→string ทำ `.getTime()` พังตอน build) ให้ coerce เป็น Date
- **ราคาที่ 2 (ราคาขายปลีก) ตามประเภทลูกค้า (2026-07-09)** — เพิ่มระดับราคาที่สองแบบ Customer Price Group (SAP/Dynamics style). Schema: `Product.retailPrice @db.Decimal(10,2) @default(0)` + enum `PriceTier { WHOLESALE, RETAIL }` + `CustomerType.priceTier @default(RETAIL)`. ตรรกะเลือกราคาในหน้าขาย (`SaleForm.tsx`) ผูกกับ **ประเภทลูกค้าที่เลือก** ผ่าน helper `priceForTier()`: กลุ่ม `WHOLESALE` (เช่น อู่ซ่อมรถ) ใช้ `salePrice` (ราคาขายส่งเดิม), กลุ่ม `RETAIL`/ลูกค้าไม่มีประเภท ใช้ `retailPrice` และ fallback ไป `salePrice` เมื่อ retailPrice=0 (ไม่มีราคาเป็น 0). เปลี่ยนลูกค้ากลางบิลแล้วระดับราคาต่างเดิม + มีสินค้าในบิล → `window.confirm` ถามยืนยันก่อน `repriceItemsToTier()` (ไม่ทับราคาที่แก้มือถ้าไม่ยืนยัน). Admin: ฟอร์มสินค้าเพิ่มช่อง "ราคาขายส่ง/ราคาขายปลีก" (`ProductForm` + Zod + create/update + audit snapshot ผ่าน `include`); หน้า `master/customer-types` เพิ่ม dropdown เลือกระดับราคา + badge + column (`priceTier` ใน schema/parse/create/update/audit). `getTransactionCustomers()` ดึง `customerType.priceTier` มาด้วย; sales new+edit page ดึง `retailPrice` + flatten `priceTier` เข้าฟอร์ม. **Additive ล้วน** — `SaleItem.salePrice` ยัง snapshot ราคาสุทธิเป็น source of truth, ไม่แตะ createSale/updateSale/รายงานกำไร/CN/ใบพิมพ์/Shopee; storefront + LINE คงซ่อนราคาลูกค้าทั่วไปเหมือนเดิม (ยังไม่โชว์ retailPrice หน้าบ้าน). ข้อมูลเก่า retailPrice=0 → ราคาเท่าเดิมทุกบิลจนกว่าจะกรอก. `db push` + `tsc --noEmit` + eslint ผ่าน. **Backfill (2026-07-09, รันแล้ว)** ผ่าน [prisma/scripts/backfill-retail-price.ts](/D:/autoparts/prisma/scripts/backfill-retail-price.ts): +20% หมวด `คอมแอร์ (Compressor)`; +30% หมวด `คอยล์เย็น/คอยล์ร้อน(=แผงแอร์)/หม้อน้ำ/โบเวอร์ พัดลมแอร์/มอเตอร์พัดลมหน้าเครื่อง`; หมวดที่เหลือ retailPrice=salePrice เป๊ะ. ราคาที่บวก% ปัดขึ้นทีละ 50 (`CEIL(ROUND(salePrice×mult,2)/50)*50`), จับเฉพาะหมวดหลัก (ไม่รวมหมวดย่อยที่มีคำเดียวกัน), salePrice≤0 → retailPrice 0. อัปเดต 858 แถว (มี dry-run โหมด default + `--apply` รันจริงในทรานแซกชันเดียว timeout 60s)
- **LINE: ลูกค้าซ่อนราคาถามราคา → ส่งเรื่องแอดมิน (2026-07-02)** — ต่อยอดจากข้อบน. ใน [lib/line-webhook-processor.ts](/D:/autoparts/lib/line-webhook-processor.ts) เมื่อ `showPrice===false` (ลูกค้าทั่วไป/unlinked) และลูกค้าถามราคา (`route.intent===PRICE_NEGOTIATION` หรือข้อความมีคำว่า "ราคา/กี่บาท/เท่าไร/เท่าไหร่" — ตั้งใจไม่รวม PURCHASE_INTENT ล้วนอย่าง "เอาตัวนี้") จะส่งเรื่องให้แอดมิน โดยแยก 2 เคสด้วย **`guardedSearchIntent` (evidence-gated intent ของข้อความเทิร์นนี้ ไม่รวม history)**: (A) ข้อความ**ระบุสินค้าเอง**ในเทิร์นนี้ (มี part/car เช่น "คอยเย็นวีโก้ เท่าไรครับ") + เจอสินค้า → โชว์การ์ด ("สอบถามราคา") + note "ส่งเรื่องให้แอดมิน" แล้ว freeze `WAITING_ADMIN` + แจ้งแอดมิน (ปลด purchase-intent เพื่อไม่ข้ามการ์ด); (B) **ถามราคาล้วนไม่ระบุสินค้า** (เช่น "ราคาเท่าไร" ต่อจากที่โชว์ไปแล้ว) → forcedResponse handoff ตรง (`AI_PRICE_HIDDEN_HANDOFF`) ไม่โชว์การ์ดซ้ำ. อู่ (showPrice=true) เห็นราคาปกติ ไม่ handoff. reuse กลไก handoff เดิม (bell+Telegram). เพิ่มเทสต์ 4 เคส (processor รวม 56 ผ่าน). ปรับ 2 รอบหลังทดสอบจริงจาก log DB: (1) regex เดิมไม่จับ "เท่าไร" → เพิ่ม, (2) เดิมใช้ topicShift แต่ topicShift=false ตอน first-turn/ไม่ได้สลับ part → เปลี่ยนมาใช้ `guardedSearchIntent` แทน
- รักษาเอกสารให้ AI อ่านง่าย: ใช้ไฟล์นี้เป็น index และย้ายรายละเอียดลงเอกสารเฉพาะเรื่อง

## Current Priorities
- [x] `Phase 6.6` Cash/Bank Lite — implement เสร็จและใช้งานจริงแล้ว (schema/posting/transfer/adjustment/report ครบ)
- [ ] ทยอยย้าย decision สำคัญจาก archive ไป `docs/decisions/`
- [ ] ทยอยย้าย detailed module specs จาก archive ไป `docs/specs/`

## Active Workstreams
### 1. Cash/Bank Lite
- สถานะ: **เสร็จและใช้งานจริงแล้ว** (Phase 6.6)
- เอกสาร spec: [docs/specs/cash-bank-lite.md](/D:/autoparts/docs/specs/cash-bank-lite.md)
- สิ่งที่ทำเสร็จ:
  - schema: `CashBankAccount` / `CashBankMovement` / `CashBankTransfer` / `CashBankAdjustment` (+opening balance/date, primary transfer account, low-balance threshold)
  - posting: `lib/cash-bank.ts` (`replaceCashBankSourceMovements` / `clearCashBankSourceMovements` / `recalculateCashBankAccount`) wire เข้า sales, receipts, purchases, purchase-returns, credit-notes, expenses, supplier-payments, supplier-advances, delivery-commissions, และ Shopee create-sale/escrow
  - UI: `/admin/cash-bank` (account manager) + `/ledger` + `/transfers` + `/adjustments` ครบ loading.tsx
  - permission ครบ 5-step (`cash_bank.*` ใน access-control + route rules + requirePermission ใน page/action + nav `admin-navigation.ts`)
  - report: `lib/cash-bank-report-queries.ts` → `/admin/reports/cash-bank-ledger` / `cash-bank-transfers` / `cash-bank-adjustments` + Excel/CSV export
- Out of scope (ตาม spec, ยังไม่ทำตามตั้งใจ): full bank reconcile, bank statement import, payment run/clearing, slip attachment

### 1.1 Multi-Channel Payment (Split Payment)
- สถานะ: **Phase 1 นำร่อง Receipt เสร็จ** — ขยายเอกสารอื่นต่อ
- เอกสาร spec: [docs/specs/multi-channel-payment.md](/D:/autoparts/docs/specs/multi-channel-payment.md)
- สิ่งที่ทำเสร็จ:
  - schema: `DocumentPayment` (polymorphic docType+docId, no FK) + enum `DocumentPaymentDocType`
  - helper กลาง `lib/document-payments.ts` (parse/validate/replace/clear/toCashBankEntries) — reuse ได้ทุกเอกสาร
  - Receipt: form split rows (บัญชี+จำนวนเงิน+เพิ่ม/ลบแถว), actions create/update/cancel, detail breakdown, audit snapshot มี payments[]
  - shared UI `components/shared/PaymentChannelsInput.tsx` (reuse ทุกเอกสาร)
  - back-compat: `<doc>.cashBankAccountId` = บัญชีแถวแรก (primary) — ข้อมูลเก่า/print/report เดิมไม่พัง; flow programmatic (Shopee/LINE) ยัง post movement บัญชีเดียวโดยไม่สร้าง DocumentPayment (detail fallback รองรับ)
- [x] Phase 2: **Expense** (target=netAmount), **Purchase** (เฉพาะ CASH_PURCHASE, draft เก็บ payments), **Sale** (เฉพาะ CASH_SALE, draft เก็บ payments, `resolveSalePaymentMethodFromAccounts`) — form/actions/detail/edit ครบทุกตัว
- [x] Phase 3: **SupplierPayment** (target=totalCashPaid), **SupplierAdvance** (target=totalAmount), **CreditNote** (เฉพาะ CASH_REFUND, OUT), **PurchaseReturn** (เฉพาะ CASH_REFUND, IN) — form/actions/detail/edit + audit payments[] ครบทุกตัว
- [x] Phase 4 (print ใบเสร็จ): แสดง "รายละเอียดการรับชำระ" ทุกช่องทาง + ยอดรวม ในกล่อง "ชำระโดย" ครบทุกฟอร์ม (admin detail + LIFF + liff-print ผ่าน prop `payments`)
- [x] Phase 4b (print Sale): `SharedSalesDeliveryPrintDocument` แสดง "รายละเอียดการรับชำระ" หลายช่องทางแล้ว — ครบทั้ง 6 caller (admin sale detail, delivery batch print, LIFF/liff-print receipt+invoice)
- [x] Phase 5: report-queries account filter เป็น **multi-channel-aware** — helper `resolveAccountDocIds` + `accountWhereFilter` (OR ระหว่าง `cashBankAccountId` primary กับ docId ที่มี DocumentPayment บนบัญชีนั้น). Additive-only → ผลลัพธ์ข้อมูลเดิม (single-channel/legacy) ไม่เปลี่ยน; Cash/Bank Ledger (`cash-bank-report-queries.ts`) อ่าน `CashBankMovement` อยู่แล้วจึงถูกต้อง per-account เต็มรูปแบบ
- **Multi-channel payment ครบทุก phase** ✅ (10 เอกสาร: Receipt, Sale, Purchase, Expense, CreditNote, PurchaseReturn, SupplierAdvance, SupplierPayment + print + reports)

### 2. SEO / AEO / AIO Follow-up
- สถานะ: baseline หลักเสร็จแล้ว แต่ยังมี external/manual work ต่อเนื่อง
- เอกสารอ้างอิง:
  - [docs/seo/phase-7-final-status-2026-04-03.md](/D:/autoparts/docs/seo/phase-7-final-status-2026-04-03.md)
  - [docs/seo/phase-7-follow-up-2026-04-03.md](/D:/autoparts/docs/seo/phase-7-follow-up-2026-04-03.md)
  - [docs/seo/phase-7-external-verification-2026-04-03.md](/D:/autoparts/docs/seo/phase-7-external-verification-2026-04-03.md)

### 3. Product Search Quality Follow-up
- สถานะ: Phase E + F1 + F2 เสร็จแล้ว, phase ถัดไปถูกเพิ่มใน roadmap
- เอกสาร active: [docs/specs/product-search-log-analysis.md](/D:/autoparts/docs/specs/product-search-log-analysis.md)
- สิ่งที่ต้องทำต่อ:
  - [x] Phase F3 - Review Outcome Tracking
  - [x] Phase F4 - Fitment/Year Remediation Flow
  - [x] Phase F5 - Closed-Loop Measurement
  - [x] Phase F6 - Guarded Auto-Apply
  - [x] Phase F7 - Search-quality hardening (2026-06-03, จาก production diagnostic)
    - [x] แก้ year-fragment bug: เลขชิ้นส่วน 4 หลัก (เช่น `1950`, `446610-1950`) ไม่ถูกตีความเป็นปีรถอีกต่อไป — `extractYearFromQuery` รับเฉพาะ token ปีเดี่ยว และ year-only query ใช้ UNION (text/code/oem ∪ fitment-year) แทน strict-only (`lib/product-search.ts`)
    - [x] กรอง bot/keyboard-mash/foreign-spam ออกจาก telemetry + จำแนกเป็น `review-noise` (`lib/search-noise.ts` wired เข้า `product-search-telemetry.ts` + `product-search-log-analysis.ts`)
    - [x] เติม synonym EN↔TH รุ่นรถยอดนิยมเชิงรุก 51 cluster (idempotent + audited): `prisma/scripts/seed-model-synonyms.ts` — SearchSynonym 134→143 terms
    - [x] (Phase F / concept-synonym) เติม synonym กลุ่มเคมีภัณฑ์/หมวดที่ขาด 4 cluster — น้ำยาล้างคอยล์ (P0482), น้ำยาล้างระบบแอร์ F-11 (P0458, แยกกลุ่ม), ใบพัดลม, คอนโทรลวาล์วคอมแอร์ — ทุกคำ grounded จาก ProductAlias จริง (idempotent + audited): `prisma/scripts/seed-concept-synonyms.ts` — SearchSynonym 143→147 terms
    - [x] (Brand-synonym) เติม synonym ชื่อแบรนด์อะไหล่ที่เพิ่งเพิ่มใน production 6 cluster — Aeroflex/AEROTAPE, Fujikoki, COCO, ATC, K.AIR, Mazda Genuine Parts (ตัวสะกด/ทับศัพท์/เว้นวรรคของชื่อแบรนด์เท่านั้น ไม่ใส่คำหมวดสินค้าทั่วไป, idempotent + audited): `prisma/scripts/seed-brand-synonyms.ts` — SearchSynonym 200→206 terms
    - [x] (LINE skip rule) แก้ต้นเหตุจริงของอาการ "LINE ตอบคอยล์ทั้งที่ถามน้ำยาล้าง": `matchPartTypeToCategoryHint` ใช้ `.includes()` substring → ชื่อเคมีภัณฑ์/อุปกรณ์ (น้ำยาล้าง, ฟองน้ำ, น็อต, ฝาปิดวาล์ว ฯลฯ) ถูก map เข้าหมวดอะไหล่ผิดเป็น **hard category filter** (พบ 17/45 ตัวในหมวด "อะไหล่อื่นๆ" เสี่ยง). เพิ่ม `isAccessoryOrChemicalIntent()` + skip rule ใน `lib/line-fitment-resolve.ts` (เช็คทั้ง partType และ query text เต็ม ผ่าน `queryText` ที่ wire จาก `line-webhook-processor.ts`) → ข้าม category filter เฉพาะกลุ่มนี้ คง brand/model ไว้. ยืนยันจาก production `LineAiAuditLog` ว่า AI ไม่ได้ตัดคำ — `consolidatedQuery` ครบ แต่ `categoryName="คอยล์เย็น (Evaporator)"` คือตัวตัดทิ้ง
    - [x] (A-light / Thai segmentation) เพิ่ม `lib/thai-segment.ts` (`segmentThaiQueryTokens` ใช้ ICU `Intl.Segmenter`) ตัดคำไทยที่เขียนติดกัน → ป้อนเข้า required-token (LIKE-contains AND) ผ่าน `runStorefrontProductSearchWithRequiredTokenFallback` เฉพาะเมื่อตัดได้ ≥2 คำ (compound จริง) + อาศัย strict→fallback เดิมกัน regression. ไม่แตะ DB/index/trigger. ฝั่งเว็บได้ AND-precision วลีไทย; **note:** data issue 3 ตัว "น้ำมันคอมแอร์ EMKARATE/SUNOCO" ไฟล์ผิดหมวด (อยู่ "อะไหล่อื่นๆ" ควรอยู่ Compressor Oil) — ยังไม่แตะ รอยืนยัน
    - [ ] (พิจารณาภายหลัง) A-full: ตัดคำฝั่ง index (search_document) เพื่อ FTS เต็มรูป — งานใหญ่ ต้องย้าย doc-building มา app layer + re-index ทั้งหมด; ทำเฉพาะถ้า telemetry ชี้ว่า ranking ยังอ่อนหลัง A-light
    - [x] (LINE year-range shorthand) แก้ต้นเหตุ "LINE ตอบไม่พบ คอยเย็น Avanza 12-15" ทั้งที่มีสินค้าจริง: เลขช่วงปีย่อ `12-15`/`2012-2015` ถูกดันเป็น **required-token (hard anchor)** ใน `extractProductSearchRequiredTokens` (มีตัวเลข ≥3 ตัว ไม่ใช่ปี 4 หลัก) → ไม่มีชื่อสินค้าตัวไหนมีสตริง `12-15` → ค้นเจอ 0 เสมอ + ไม่เคยถูกแปลงเป็น `fitmentYear`. ยืนยันจาก production `LineAiAuditLog` (conv `cmqexstoe…`): เทิร์นที่พลาด `requiredTokens:["12-15"], fitmentYear:null, total:0`; เทิร์นถัดมาที่ระบบ consolidate เป็น `2012-2015`+`fitmentYear:2012` เจอสินค้า 1 รายการ (พิสูจน์ว่าสินค้ามีจริง). **A**: `lib/car-year-shorthand.ts` (`isCarYearRangeToken`) + skip ใน `product-search-required-tokens.ts` → range ไม่เป็น anchor. **B**: `parseCarYearRangeStart()` ดึงปีเริ่มต้นจากข้อความลูกค้าตรงๆ เป็น fallback ของ `fitmentYear` ใน `line-webhook-processor.ts` (grounded ในข้อความลูกค้า จึงผ่าน year guard). เพิ่ม unit test `car-year-shorthand.test.ts` + regression ใน `product-search-required-tokens.test.ts` (STB-2116 ยังเป็น anchor). ไม่แตะ DB/index/business logic
    - [x] (Option A / correlated model+year) แก้ต้นเหตุ "LINE ตอบวาล์ว/คอยล์ Vios 2013 ปนของผิดรุ่นปี (Vios 07-12 / Vigo / Fortuner) + ตัวที่ตรงเป๊ะ (P0368) กลับหลุด top-5": เดิม `carModelClause`/`carBrandClause` กับ `yearFilterClause`+`yearBoostExpr` เป็น `EXISTS` แยกกัน ไม่ผูก fitment row เดียวกัน → สินค้าที่มีแถว "Vios 07-12" **และ** แถว "Vigo 06-13" ผ่านทั้งตัวกรองรุ่น (Vios) และปี (2013 via Vigo) ทั้งที่ไม่มี fitment Vios-2013 จริง; และ +700 yearBoost แจกให้ทุกตัวที่มีแถวใดคลุมปี → ของถูก/ของผิดคะแนนเท่ากัน แล้วแพ้ tiebreak. ยืนยันจาก production `LineAiAuditLog` (conv `cmra797i1…`): valve turn `categoryName="วาล์ว (Expansion Valve)", carModelName=Vios, fitmentYear=2013, total=9` แต่ 5 ตัวที่โชว์มีของผิดรุ่นปี 3 ตัว (P0092/P0358/P0371) และตกของถูก 4 ตัว. **แก้**: correlate รุ่น/แบรนด์ (by name) + ปี ให้อยู่ fitment row เดียวกัน — ทั้ง V2 raw SQL path (`buildVehicleYearExists()` ใช้ทั้ง `yearFilterClause` และ `yearBoostExpr`) และ Prisma fallback path (`buildCorrelatedVehicleYearSome()` + `mergeAndCondition()`) ใน `lib/product-search.ts`. ขอบเขต: มีผลเฉพาะเทิร์นที่ระบุ **รุ่น หรือ แบรนด์ (ชื่อ) + ปี** พร้อมกัน (`hasNameVehicleScope`); year-only / ไม่มีรุ่น-แบรนด์ / id-based UI scope (`carModelId`/`carBrandId`) + range filter (`yearMin`/`yearMax`) คงพฤติกรรมเดิม. ยืนยันกับข้อมูลจริง: หลังแก้ correlated filter คืน 6 ตัวที่ fit Vios-2013 จริง (รวม P0368) ตัด P0092/P0358/P0371 ออกครบ. ไม่แตะ DB/index/schema
    - [x] (Generation marker / required-token) แก้ความเสี่ยงแฝง: คำบอกเจเนอเรชันรถ ("gen3", "เจน3", "จีเอ็น8") เข้าเกณฑ์ `extractProductSearchRequiredTokens` (ยาว ≥3 + มีเลข) → ถ้า AI consolidation ล่ม (Gemini off/error) แล้ว raw text "Vios gen3…" ถูกส่งเข้า search ตรงๆ, "gen3" จะกลายเป็น **hard required-token** ทั้งที่สินค้าที่ระบุเจนผ่านช่วงปี (เช่น Evaporator alias "Vios13-19") ไม่มีสตริง "gen3" → ยุบผล/0. Research prod data (2026-07-07): **ไม่มี CarModel/Product code ใดใช้ "gen" เป็น identifier** (Volkswagen เป็นแบรนด์ substring, "Genuine" ไม่มีเลขจึงไม่เข้าเกณฑ์อยู่แล้ว); "Gen1/Gen3/Gen4/Gen8" ปรากฏเฉพาะเป็นตัวบอกเจเนอเรชันใน ProductAlias/ชื่อ. แก้: เพิ่ม `GENERATION_MARKER_RE = /^(?:gen|เจน|จีเอ็น)\d+$/` skip ใน [product-search-required-tokens.ts](lib/product-search-required-tokens.ts) (pattern เดียวกับ year-range) → ไม่เป็น hard anchor แต่ยังอยู่ในสตริง query สำหรับ FTS ranking (soft). unit test เพิ่มใน `product-search-required-tokens.test.ts` (STA-7065 ยัง anchor). ไม่แตะ DB/business logic
    - [x] (Option A / partType grounding) แก้ต้นเหตุ "ลูกค้าส่งรูปหน้าเว็บ **วาล์ว** P0368 → AI ตอบ **คอยล์เย็น**": รูปถูก classify ถูกต้อง (`IMAGE_CLASSIFIED` HIGH → frame `partType:"วาล์วแอร์"`, category "วาล์ว (Expansion Valve)", ถามรุ่นรถ) แต่พอลูกค้าตอบแค่ "Vios gen3 ปี2013" (ไม่มีคำอะไหล่) AI classifier กลับ**เดา `partType="คอยล์เย็น"`** ขึ้นมาเอง → `reconcileInquiryFrame` ตี `topicShift=true` เขียนทับวาล์วที่รูปตั้งไว้ → ค้นผิดหมวด. ยืนยันจาก production `LineAiAuditLog` (conv `cmra797i1…`): 05:21 frame=วาล์วแอร์ → 05:22 (text "Vios gen3 ปี2013") frame flip เป็นคอยล์เย็น topicShift=true. **ต้นเหตุ**: `partType` **ไม่ถูก evidence-gate** ต่างจาก brand/model/year ที่ `guardChatSearchIntent` กรองด้วย `lineValueHasCustomerEvidence` (ทั้งที่ comment ใน `reconcileInquiryFrame` ระบุว่า `latest` ควร gated มาแล้ว). **แก้** ใน [line-webhook-processor.ts](lib/line-webhook-processor.ts): ก่อนสร้าง `latest` frame — เชื่อ partType จาก classifier เฉพาะเมื่อมาจาก **รูปใหม่** (`imageFields.partType`) หรือ **มีร่องรอยในข้อความลูกค้า** (`lineValueHasCustomerEvidence`); ถ้าไม่ และมี partType เดิม stored อยู่ → drop เป็น null ให้ frame carry over ของเดิม (gate เฉพาะเมื่อมี stored part จึงไม่กระทบการ classify ครั้งแรก). unit test เพิ่มใน `line-webhook-processor.test.ts` (รูปวาล์ว → follow-up "Vios gen3 2013" → frame ยังเป็นวาล์วแอร์; และ topic-shift จริงที่ลูกค้าพิมพ์ชื่ออะไหล่ใหม่ยังทำงาน). ไม่แตะ DB/business logic
    - [x] (Option A / product-code fast-path) รองรับเคสลูกค้าดูสินค้าจากเว็บ/แอปแล้วส่ง**รหัสสินค้า** ("สอบถามราคา P0368") หรือ**แคปรูปหน้าเว็บ**ที่มีรหัส → AI ตอบสินค้านั้นตรงๆ. เดิม gate (`decideChatSearchGate`) ไม่รู้จักรหัสสินค้า → รูปที่ vision ดึง `partNumber` ได้ (เช่น DI261411-0300 = P0368) ยังโดนถาม "แจ้งรุ่นรถ" เพราะ code-anchor bypass เช็คแค่ `requiredTokens` (text) + `searchHints` แต่**โค้ดอยู่ใน `partNumber`** (ไม่ใช่ searchHints). ยืนยันจาก production `LineAiAuditLog` (conv `cmra797i1…`): image classification เก็บ `partNumber:"DI261411-0300"`, `searchHints` ไม่มีโค้ด → gate `PART_ONLY` ask need_car. **แก้** ใน [line-webhook-processor.ts](lib/line-webhook-processor.ts): เพิ่ม product-code fast-path — รวม candidate โค้ดจาก customer text + image `partNumber` + code-like searchHints → validate กับ catalog ผ่าน `resolveCatalogCodes` (export ใหม่จาก [product-search-bridge.ts](lib/chat-core/product-search-bridge.ts)) → ถ้า resolve ได้ให้ค้นแบบ exact-code (ส่ง `extractedPartNumber` ให้ `buildSearchQuery` ชนะ query), **bypass gate + low-confidence guard + ไม่ใส่ fitment hard filter** (โค้ดระบุตัวสินค้าเองแล้ว). ข้าม hardGuard (payment/claim) + payment-slip image. audit `PRODUCT_CODE_DIRECT`. เพิ่ม unit test 3 เคส (รูปมีโค้ด resolve ได้ → ค้นด้วยโค้ด, โค้ดไม่มีในระบบ → fallback ถามรุ่น, text มีโค้ด → ค้น exact). ไม่แตะ DB/business logic; ราคายัง defer ให้แอดมินตามนโยบายซ่อนราคา
    - [x] (Messenger parity / product-code fast-path) นำ Option A ไปใช้กับ Facebook Messenger ด้วย ([messenger-webhook-processor.ts](lib/messenger/messenger-webhook-processor.ts)): เพิ่ม `resolveDirectProductCode()` + `MESSENGER_PRODUCT_ROUTE` — (ก) **text path**: ก่อน `route.requiresAdmin` escalation ถ้าข้อความมีรหัสที่ resolve กับ catalog ได้ ("สอบถามราคา P0368", "P0368 ราคาเท่าไหร่") → ค้น exact-code ตอบเลย (กันเคส price keyword → escalate แอดมินโดยไม่ค้น); (ข) **image path**: ถ้า OCR `partNumber`/hint resolve ได้ → ค้น exact-code แบบ **ไม่ใส่ fitment filter** (กัน partType/car ที่ classifier เดา มากรองโค้ดทิ้ง). อีก 3 fix ครอบ Messenger อยู่แล้ว: correlated model+year + gen-marker อยู่ใน `product-search.ts`/`product-search-required-tokens.ts` ที่ใช้ `searchChatProductInquiry` ร่วมกัน; partType grounding เป็นของ LINE inquiry-frame (Messenger ไม่มี frame → N/A). tsc + LINE 62 tests + build clean (messenger test runner ใช้ `node:test mock.module` ที่ runtime นี้ไม่รองรับ — fail เหมือนกันบนโค้ดเดิม ไม่เกี่ยวกับการแก้)
    - [x] (2026-07-10 / 3 บั๊ก LINE จับหมวดผิด — ยืนยันจาก production `LineAiAuditLog` ทุก event) (1) **alias `คอม`/`com` ตะกละ** (CONTAINS) ไปจับสตริงในชื่อรุ่นรถ "commuter"/"คอมมิวเตอร์" → hard filter หมวด = คอมแอร์ ทำให้ "หม้อน้ำ commuter" คืนแต่คอมแอร์ → ปิด 2 alias นี้ใน DB (data fix, live). (2) **partType หลุดในพาธ rule dictionary**: `resolveKnownQueryIntent` ตั้ง partType เป็นชื่อหมวดเต็ม "หม้อน้ำ (Radiator)" แล้ว evidence gate ที่ line ~1408 ปัดทิ้ง (ลูกค้าไม่พิมพ์ "(Radiator)") → gate ถาม CAR_ONLY → ข้าม evidence gate เมื่อ `usedRuleIntent` (พาธ rule มโนไม่ได้). (3) **คำสะกดผิด "วาว์ล"** ไม่มีใน dictionary → เพิ่ม CategoryAlias + SearchKeyword synonym → Expansion Valve (data fix, live)
    - [x] (2026-07-10 / LLM category fallback + auto-stage) เมื่อ resolver จับหมวดไม่ได้ (`categoryName=null`) เฉพาะเทิร์น text ที่จะค้นจริง → [category-llm-fallback.ts](lib/chat-core/category-llm-fallback.ts) ให้ Gemini **แก้คำผิดอย่างเดียว** (`correctPartSpelling`, inject ได้ผ่าน dependencies) แล้ว **re-map คำที่แก้ผ่าน resolver เดิม** — ใช้เป็น hard filter เฉพาะเมื่อได้หมวดเดียว (95% เชิงโครงสร้าง ไม่เชื่อ self-confidence). สำเร็จ → stage คำผิดเป็น CategoryAlias **`matchMode=EXACT, isActive=false, source=AI_AUTO, reviewStatus=PENDING`** (fire-and-forget หลังตอบลูกค้า, guardrails + collision check ใน [category-alias-guardrails.ts](lib/chat-core/category-alias-guardrails.ts), กันซ้ำ/กัน rejected เสนอซ้ำ). Schema: enum `CategoryAliasSource`/`CategoryAliasReviewStatus` + field `source/reviewStatus/aiCorrectedTerm` + index. แอดมินอนุมัติ/ปฏิเสธผ่านแท็บ "AI เสนอ (รออนุมัติ)" ในหน้า `/master/categories` (route เดียว, สิทธิ์ `master.update`); อนุมัติ = เปิดใช้ + สร้าง SearchKeyword synonym + AuditLog, ปฏิเสธ = `REJECTED` คง row กันเสนอซ้ำ. `db push` + `tsc` + tests (webhook 65 / staging guardrails 4 / fitment / resolver) + build ผ่าน; ยืนยัน e2e กับ Gemini จริง (typo "คอมมแอร์" → "คอมเพรสเซอร์แอร์" → Compressor) แล้วลบ row ทดสอบ
    - [x] (2026-07-10 / Messenger parity — category resolution) ยกระดับ Facebook Messenger text path ([messenger-webhook-processor.ts](lib/messenger/messenger-webhook-processor.ts)) ให้ resolve หมวด/รุ่น/แบรนด์/ปี เป็น **hard filter** เหมือน LINE (เดิมเส้นทาง text ส่งแค่ `{route,text}` เข้า bridge = free-text ล้วน ไม่จับหมวด). เพิ่ม `resolveMessengerFitmentHints()`: classify (`extractChatSearchIntent`) → evidence-gate (`guardChatSearchIntent`) → `resolveChatFitmentFilters` → **LLM fallback** (`correctPartSpelling` → re-map → apply เมื่อได้หมวดเดียว) + **auto-stage** คำผิดผ่าน `stageAiCategoryAlias` (ใช้ helper ร่วมกับ LINE ทั้งหมด). Messenger ไม่มี inquiry-frame → resolve จากข้อความเทิร์นปัจจุบัน (history ใช้แค่ให้ classifier). Safety-first: ทุก step catch → ถ้าล้มเหลว/ไม่ resolve = ไม่ใส่ filter (free-text เดิม). เพิ่ม Gemini classifier call ต่อเทิร์น product text (ต้นทุนของ parity, เหมือน LINE). ยืนยัน e2e จริง: "คอมแอร์ vigo"→Compressor+Vigo, "วาว์ลอัลติสแท้03"→Expansion Valve+Altis+2003. `tsc` + build ผ่าน. อนุมัติ alias ที่ Messenger stage ใช้แท็บเดียวกับ LINE
    - [x] (2026-07-11 / A+F — LINE+Messenger ตอบรุ่นรถผิด "สายแอร์ใหญ่สตาด้า2500" → D-Max/Revo/Colorado) ยืนยันจาก production `LineAiAuditLog` (conv `cmq4ziq6l…`): classifier ได้ carModel "Strada" แต่ **evidence-grounding ทิ้ง** เพราะ "สตาด้า"(ไทย)≠"Strada"(อังกฤษ) — transliteration ไม่อยู่ใน evidence data; เหลือ hard filter แค่หมวด A/C Hose + requiredToken "2500"(ซีซี) → คืนสายแอร์รถรุ่นอื่นที่มีรุ่น 2500cc. **มาสเตอร์มี CarModel Strada + SearchSynonym "สตาด้า"→Strada ครบ** — พังที่ grounding ต้นน้ำ ไม่ใช่ data ขาด. **F (model synonym grounding)**: ใหม่ [car-model-alias-cache.ts](lib/car-model-alias-cache.ts) + [car-model-alias-loader.ts](lib/car-model-alias-loader.ts) โหลด `SearchSynonym` แบบ cache (TTL 60s, mirror brand-alias) → ส่ง `modelLookup` เข้า `guardChatSearchIntent` ([search-guards.ts](lib/chat-core/search-guards.ts) `lineModelHasCustomerSynonymEvidence`) ให้ "สตาด้า" ground "Strada" ได้; wire ทั้ง LINE + Messenger. **A (vehicle-unresolved gate)**: carModel grounded แต่ resolve เป็น hard filter ไม่ได้ (carModelName/carBrandName ว่าง) + มีผลจะโชว์ → ไม่โชว์การ์ด, ตอบ `CHAT_VEHICLE_UNRESOLVED_HANDOFF_REPLY` (ยืนยันรุ่นรถ) + ส่งแอดมิน (audit `AI_VEHICLE_UNRESOLVED_HANDOFF`), ทำทั้ง 2 processor. **ไม่แตะ engine `product-search.ts`** → ไม่กระทบ storefront. Test: `line-search-guards.test.ts` (Strada grounding + proof drop-without-lookup), `car-model-alias-cache.test.ts`, `line-webhook-processor.test.ts` (Strada gate e2e). ดูรายละเอียด [docs/line-messenger-ai-product-mismatch-audit.md](docs/line-messenger-ai-product-mismatch-audit.md) §9. เหลือ C/D/E ในเอกสารเดียวกัน
    - [x] (2026-07-11 / G1+G2 — LINE ตอบรุ่นรถผิดจาก frame carryover "อะไหล่แอร์ สิบล้อ HINO ISUZU" → สายแอร์ D-Max กระบะ) ยืนยันจาก production `LineAiAuditLog` (conv `cmq4ziq6l…`, เทิร์นถัดจาก Strada): classifier เทิร์นนี้คืน `partType=null` → frame **carry "สายแอร์" + หมวด A/C Hose** จากเทิร์นก่อน → completeness gate ตัดสินจาก frame partType (ไม่ใช่ข้อความจริง) → `isBroadChatPartType("สายแอร์")=false` → ค้น + hard-filter หมวดเดิม → คืนสายแอร์ D-Max (กระบะ) ให้คำถามรถสิบล้อ. **G1 (broad-gate อ่านข้อความจริง)**: gate เช็ค `isBroadChatPartType(consolidatedQuery ‖ processText)` เพิ่ม → ถ้าเทิร์นนี้กว้าง ("อะไหล่แอร์/สิบล้อ/ระบบแอร์") บังคับ BROAD_PART_TYPE handoff แม้ frame carry part เฉพาะ (parity: Messenger เช็ค `isBroadChatPartType(processText)` อยู่แล้ว → G1 เป็น LINE-only). **G2 เล็ก (ตัดสินโดยเจ้าของ: ความกว้างมาก่อน + connective คง frame)**: เมื่อเทิร์นใหม่ระบุ**คลาสรถ** (สิบล้อ/รถบรรทุก/เทรลเลอร์ — ไม่ใช่รุ่นจริง) + classifier ไม่มี part ใหม่ + **ไม่มีคำเชื่อมถามต่อ** (แล้ว/และ/หรือ/…ล่ะ) → drop partType เดิมไม่ให้หมวดเดิม hard-filter → gate ถามชนิดอะไหล่ (helper `namesVehicleClassTerm`/`hasFollowUpConnective` ใน [inquiry-frame.ts](lib/chat-core/inquiry-frame.ts), audit flag `droppedCarriedPartOnVehicleClassSwitch`; LINE-only เพราะ Messenger ไม่มี frame). ไม่แตะ engine. Test: `line-inquiry-frame.test.ts` (helper), `line-webhook-processor.test.ts` (G1 broad-after-specific, G2 drop-on-class-switch, G2 keep-on-continuation). ดู [docs/line-messenger-ai-product-mismatch-audit.md](docs/line-messenger-ai-product-mismatch-audit.md) §9
    - [x] (2026-07-11 / Fix1+2 — LINE ถามชนิดอะไหล่ซ้ำทั้งที่ลูกค้าพิมพ์มาแล้ว "คอล์ยเย็นนิสสันมาร์ค") ยืนยันจาก production `LineAiAuditLog` (conv `cmr2xbf16…`): classifier **แก้คำผิดสำเร็จแล้ว** (`classifierPartType="คอยล์เย็น"`) แต่ **partType-grounding gate** ([line-webhook-processor.ts](lib/line-webhook-processor.ts) `groundedLatestPartType`) โยนทิ้งเป็น null → gate เห็น `hasCar && !hasPart` → CAR_ONLY ถามชนิดอะไหล่ (ที่ลูกค้าบอกแล้ว) + search ถูกบล็อกก่อน LLM category fallback จะทำงาน. **ต้นเหตุ 2 ชั้น:** (1) gate เช็ค `stored?.partType` โดยไม่ดู `sessionStale` → frame เก่า 9 วัน (session ใหม่ `sessionStale=true`) ยังเปิด gate; (2) evidence check เอาคำที่แก้ถูกแล้ว ("คอยล์เย็น") ไปเทียบ substring กับข้อความพิมพ์ผิด ("คอล์ย" สลับ ล/ย) → ไม่เจอ → drop. **หลักการที่ยึด (เจ้าของสั่ง): ก่อนตัดต้องผ่าน resolve/LLM ก่อน หาก map สำเร็จต้องโชว์สินค้า.** **Fix 1:** เพิ่ม `!sessionStale` — session ใหม่ไม่มี part เก่าให้ปกป้อง → เชื่อ classifier. **Fix 2:** เพิ่ม `lineValueHasCustomerTypoEvidence` ([search-guards.ts](lib/chat-core/search-guards.ts), Damerau-OSA distance + sliding window บน glued Thai) — คำพิมพ์ผิดที่เป็น typo ของ partType (เช่น "คอล์ย"↔"คอยล์") นับเป็น evidence → ไม่ตัด → ปล่อยไป resolve หมวด + ค้น + โชว์. hallucination guard เดิม (follow-up รถล้วน ไม่มีคำอะไหล่) ยังทำงาน (ไม่มีทั้ง literal + typo evidence → drop). ไม่แตะ engine. Test: `line-search-guards.test.ts` (typo evidence), `line-webhook-processor.test.ts` (Fix1 stale-session keep+search, Fix2 live typo keep, + hallucination guard เดิมยังผ่าน). ดู [docs/line-messenger-ai-product-mismatch-audit.md](docs/line-messenger-ai-product-mismatch-audit.md) §9
    - [ ] (Ops) รีวิว no-result report รายสัปดาห์ → เติม SearchSynonym/ProductAlias สำหรับคำที่ miss จริง (ใช้หน้า `reports/product-search-no-result` ที่ตอนนี้กรอง noise แล้ว)

### 4. Shopee Open Platform Integration (โมดูลแยกอิสระ)
- สถานะ: **Phase A–F เสร็จ** (schema + sale-core + create-sale service + UI ครบ: dashboard `/admin/marketplace/shopee`, orders list/detail, products, stock, channel filter ในรายงาน) · คงเหลือเฉพาะงานที่ต้อง verify กับ Shopee credentials จริง (live OAuth/item fetch/order pull/stock push/logistics/cancel-refund/escrow) — ดู [docs/shopee/PLAN-shopee.md](/D:/autoparts/docs/shopee/PLAN-shopee.md)
- หลักการ: แยกโมดูลออกจากระบบเดิมทั้งหมด (`lib/shopee/*`, `app/admin/(protected)/marketplace/*`, `app/api/shopee/*`) ไม่กระทบ logic หน้าร้าน/backoffice เดิม
- เอกสาร active:
  - [docs/shopee/README.md](/D:/autoparts/docs/shopee/README.md) — ภาพรวม + isolation principles
  - [docs/shopee/PLAN-shopee.md](/D:/autoparts/docs/shopee/PLAN-shopee.md) — checklist เต็มทุก phase
  - [docs/shopee/USER-TASKS.md](/D:/autoparts/docs/shopee/USER-TASKS.md) — งานที่เจ้าของร้านต้องทำเอง

### 5. LINE OA + AI Chat Hardening (2026-06-09)
- สถานะ: **review-driven hardening รอบแรกเสร็จสมบูรณ์** (13 ข้อจาก code review + `prisma db push` รัน index ใหม่แล้ว 2026-06-09)
- หลักการ: ปรับ reliability/performance ของ webhook + AI pipeline โดยไม่เปลี่ยน business logic / พฤติกรรมที่ลูกค้ารู้สึกได้
- ไฟล์หลัก: `app/api/line/webhook/route.ts`, `lib/line-webhook-processor.ts`, `lib/line-conversation-repository.ts`, `lib/line-ai-job-worker.ts`, `lib/line-intent-router.ts`, `app/api/line/ai-jobs/reconcile/route.ts`
- งานที่ทำเสร็จ:
  - [x] (1+3) แยก webhook เป็น ACK-ทันที + รัน profile lookup/AI pipeline ใน `after()` background → LINE ไม่ re-deliver, reply token ยังสดเมื่อ pipeline ถึงขั้นตอบ
  - [x] (2) Idempotency: catch P2002 บน `LineMessage.lineEventId` → `DuplicateLineEventError` กัน double-process จาก LINE re-delivery race
  - [x] (4) Atomic job claim ใน cron worker ด้วย `UPDATE ... FOR UPDATE SKIP LOCKED` กัน cron 2 รอบ pick job ซ้ำ
  - [x] (5) Reconciler cron `/api/line/ai-jobs/reconcile` (ทุก 10 นาที) mark OUTBOUND ที่ค้าง PENDING > 5 นาที เป็น FAILED
  - [x] (6) Per-event try/catch ใน processor loop → 1 event พังไม่ทำให้ทั้ง batch หยุด
  - [x] (7) แก้ comment ที่ระบุผิดว่า handoff notify ไม่ส่ง Telegram (จริงๆ ส่งทั้ง bell+Telegram ตาม Iron Rule)
  - [x] (8) ลบ `PRODUCT_HINT_RE` dead branch (default route ทำงานเหมือนกันอยู่แล้ว)
  - [x] (9) เปลี่ยน audit writes 7 จุด (debug/metric) เป็น fire-and-forget ตัด round-trip ออกจาก reply latency
  - [x] (10) Dedupe LINE profile fetch ต่อ userId ใน webhook payload เดียวกัน
  - [x] (11) เก็บ `pipelineDurationMs` ใน AI_SEND_DECISION audit เพื่อวัด p95
  - [x] (12) เพิ่ม composite index `LineAiAuditLog [conversationId, action, createdAt desc]` (schema แก้แล้ว + `prisma db push` แล้ว 2026-06-09)
  - [x] (13) Cap history แต่ละ turn ที่ 400 ตัวอักษร กัน Gemini prompt budget ล้น/ตอบถูกตัด
  - [x] (14) Sticker handler: สติกเกอร์ไม่เข้า search/handoff/notify pipeline อีกต่อไป — เดิมตก UNKNOWN → handoff → ตอบ "รับทราบค่ะ เดี๋ยวแอดมินมาดูแล" ซ้ำ + freeze AI + ping แอดมินทุกใบ. ใหม่: ทักทายครั้งเดียวเมื่อเป็น contact ใหม่/ห่าง > 6 ชม. (`lastCustomerMessageAt` snapshot), นอกนั้นนิ่งสนิท ไม่ตั้ง waiting_admin ไม่แจ้งเตือน (`handleStickerEvent` ใน `lib/line-webhook-processor.ts`) + เพิ่ม audit `STICKER_HANDLED` + 2 unit tests
  - [x] (15) AI-consolidated search query (แก้ปัญหา drip-feed): เดิมลูกค้าทยอยพิมพ์ ("คอยเย็น d max" → "ปี 06") แล้ว search ใช้แค่ข้อความล่าสุด "ปี 06" → เจอ 516 รายการมั่ว (ทั้งการ์ดสินค้า + ลิงก์ `?q=ปี 06`). ใหม่: `extractLineSearchIntent()` ใน `lib/line-ai-service.ts` (เดิม `consolidateLineSearchQuery`) ให้ Gemini รวมหัวข้อที่ลูกค้าตามหาจากบทสนทนาเป็นคำค้นเดียว (ชนิดอะไหล่+รุ่น+ปี, แปลงปีย่อ 2 หลัก) แล้ว `processLineAiReply` ป้อน query นั้นเข้า search แทน raw text (เรียกเฉพาะเทิร์น follow-up ที่มี history → เทิร์นแรกไม่เพิ่ม call). Fallback ปลอดภัย: Gemini off/error/ตอบ NONE → ใช้ logic เดิม (latest text + fitment carryover). แถมปิดรูรั่ว regex ใน `lib/line-fitment-extract.ts`: รองรับ "d max" เว้นวรรค + ขยาย "ปี NN" 2 หลัก (00–35) → ค.ศ. + audit `SEARCH_QUERY_CONSOLIDATED` + unit tests
  - [x] (16) จำกัดขอบเขตการค้นหา (option C — เคส "หม้อน้ำ mazda 2" เจอ 516):
    - **A. core search** (`lib/product-search.ts` + `lib/search-synonyms.ts`): เดิม FTS แตก token แล้ว **OR** กันทั้งหมด + เลขสั้น "2" กลายเป็น `2:*` → match ทุกปี 20xx → ระเบิด. ใหม่: `expandQueryTokenGroups()` แยก query เป็น "คอนเซ็ปต์" แล้ว `buildTsQueryExpression()` ทำ **AND ข้ามคอนเซ็ปต์ / OR ภายใน synonym** + เลข 1-2 หลักเป็น exact lexeme (ไม่ prefix). มี **OR-fallback**: ถ้า AND (หลายคอนเซ็ปต์) ได้ 0 แถว → รันซ้ำด้วย OR กันเคสเจอ 0. กระทบทั้งเว็บ+LINE
    - **B. LINE structured filters**: `extractLineSearchIntent()` คืน {query, partType, carBrand, carModel, year}; `resolveLineFitmentFilters()` (`lib/line-fitment-resolve.ts`) map ชื่อ → master data จริง (CarBrand/CarModel/Category, case-insensitive) แล้วส่งเป็น **hard filter** เข้า search ผ่าน bridge `fitmentHints` (+เพิ่ม `categoryName`). ปลอดภัย: hint ที่ resolve ไม่ได้จะถูก drop (ไม่ทำให้เจอ 0)
    - unit tests: tsquery builder 6 + parseLineSearchIntent 4 + processor (resolved fitment hints) 1
  - [x] (17) Hybrid semantic search (Phase 1 — ยกระดับคุณภาพ retrieval) — **เปิดใช้งานจริงแล้ว** (`PRODUCT_SEARCH_SEMANTIC=on` ใน Vercel):
    - **Gated ทั้งหมด** ด้วย env `PRODUCT_SEARCH_SEMANTIC=on` (default off) → ถ้าไม่เปิด/ไม่มี embedding/ล้มเหลว = lexical เดิมเป๊ะ ไม่กระทบ logic อื่น
    - embedding client: `generateGeminiEmbedding()` ใน `lib/google-ai-client.ts` (**gemini-embedding-001** ผ่าน `embedContent` + `outputDimensionality=768` — text-embedding-004 ไม่รองรับบน key นี้) ใช้ key rotation เดียวกับ generate (400/404 ไม่ cooldown key); helper `lib/embeddings.ts` (`embedQuery`, `embedTexts`, `buildProductEmbeddingText`, `toPgVectorLiteral`, `isSemanticSearchEnabled`)
    - schema (อนุมัติแล้ว, รันผ่าน `npm run db:setup-search-v2`): `CREATE EXTENSION vector` + `product_search_documents.embedding vector(768)` + HNSW cosine index. คอลัมน์ embedding ถูกกันออกจาก `refresh_product_search_document` upsert (เหมือน sales_count) → text refresh ไม่ลบ vector
    - backfill: `npm run backfill:embeddings` (`prisma/scripts/backfill-embeddings.ts`, batch 50, idempotent, `--all` เพื่อ re-embed)
    - sync ตอนแก้สินค้า: `reembedProductSearchDocument()` (`lib/product-embedding-sync.ts`) เรียกผ่าน `after()` ใน createProduct/updateProduct (fire-and-forget, ไม่ block response)
    - hybrid ใน `searchProductIdsV2`: vector recall (cosine, reuse exactScope filters เดิม) → inject เป็น candidate (`OR v.product_id IS NOT NULL`) + score boost (sim × 500, ต่ำกว่า exact/oem/contains) ใน ranked query เดียว → pagination/total semantics คงเดิม
    - unit tests: embeddings helpers 4
    - resolver part-type→category: `matchPartTypeToCategoryHint()` ใน `lib/line-fitment-resolve.ts` map คำที่ลูกค้า/AI พูด ("วาล์วแอร์", "คอยเย็น", "แผงแอร์"...) → หมวดจริงในระบบ (19 หมวด, alias เรียง specific→generic กันชน "วาล์ว"/"หม้อน้ำ"/"คอม") ก่อน fallback equals/contains → category hard filter ทำงานแม้คำพูดไม่ตรงชื่อหมวด + unit tests 3
    - **ขั้นตอนเปิดใช้:** 1) `npm run db:setup-search-v2` ✅ 2) `npm run backfill:embeddings` ✅ (embed 598/598) 3) ตั้ง `PRODUCT_SEARCH_SEMANTIC=on` ใน Vercel env ✅ **(เปิดใช้งานจริงแล้ว)**
    - ทดสอบจริงแล้ว: "หม้อน้ำ mazda 2" → หม้อน้ำ Mazda 2 ขึ้นอันดับ 1 (sim 0.84), "ตัวทำความเย็นแอร์วีออส" → เจอคอยล์เย็น/แผงแอร์ Vios (semantic เข้าใจคำบรรยาย)

  - [x] (18) Intent-gated retrieval (กันสินค้าหลุดมาตอบคำถามทั่วไป — เคส "ร้านอยู่ที่ไหน" ดันรายการคอมแอร์ Vigo + การ์ดมาด้วย):
    - **A.** ขยาย `SHOP_INFO_RE` ใน `lib/line-intent-router.ts` ครอบ "ร้านอยู่ที่ไหน/อยู่ที่ไหน/พิกัด/แผนที่/ไปยังไง/เปิดไหม/สาขา..." → route SHOP_INFO (ตอบ canned ไม่ค้น ไม่แปะการ์ด)
    - **B.** `extractLineSearchIntent` คืน `isProductQuery` — ถ้าข้อความล่าสุดไม่ใช่การหาสินค้า (ถามข้อมูลร้าน/ทักทาย/ขอบคุณ/คุยเล่น) → `isProductQuery=false`, query=null. `processLineAiReply` gate: non-product turn → **ข้าม search + ไม่แปะการ์ด + ไม่ carryover** (audit `SEARCH_SKIPPED_NON_PRODUCT`) ไม่ให้บริบทสินค้าเก่าหลุดเข้าคำตอบ
    - unit tests: processor non-product 1 + parse isProductQuery 2

  - [x] (19) Latency: ตอบให้ทันใน reply-token window (ฟรี ไม่ต้องใช้ push) — ข้อมูลจริงพบ tail หลุด 60-118 วิ จาก Gemini call ค้างชน timeout 30 วิ แล้ว rotate key สะสม:
    - **(1)** เพิ่ม `timeoutMs`+`maxKeyAttempts` ใน `generateGeminiContent`; chat calls (generate/extract/purchase-intent/faq) ใช้ `CHAT_CALL_TIMEOUT_MS=15s` + `CHAT_MAX_KEY_ATTEMPTS=3` → key ที่ค้าง fail over เร็ว ไม่เผา 30 วิ (งาน embedding/backfill ใช้ default เดิม)
    - **(2)** รัน `classifyPurchaseIntent` + `generateLineSuggestion` แบบขนาน (เริ่ม generate ล่วงหน้าใน product path) → ตัด 1 call ออกจาก critical path; ผลลัพธ์เหมือนเดิม
    - **(3)** Deadline guard: race reply gen กับเวลาที่เหลือใน token window (margin 5s); ถ้าไม่ทัน → `buildJuneDeadlineReply()` ตอบสไตล์จูน + แสดงสินค้า/การ์ดชุดเดิมครบ (ต่างแค่ถ้อยคำ) ส่งทัน reply token เสมอ + audit `AI_DEADLINE_FALLBACK`
    - unit tests: deadline fallback 1; รวม LINE suite 43+ ผ่าน
    - **(4)** 2026-06-20 — แก้เคส log จริง (ส่งรูปคอมแอร์ + พิมพ์ "24v"): vision OCR ใช้ ~31 วิ เผางบ reply-token จน `shouldUseReplyTokenDeadlineFallback` เด้ง → ข้าม search ทั้งที่ frame รวม `partType=คอมแอร์`+`24v` ได้แล้ว เลยตอบถามรถ/ปีแทน (audit `DEADLINE_FALLBACK_BEFORE_SEARCH`). แก้:
      - turn ที่มีรูป OCR (`isImageOcrTurn`) **ไม่ตัดจบก่อน search** อีกต่อไป — ค้นเสมอ แล้วส่งทาง reply token ถ้าทัน ไม่ทันจริง → PUSH ตามหลัง (กลไกเดิม `allowPushFallback`)
      - ขยาย reply-token window `replyTokenMaxAgeMs` 45s → **55s** (margin 5s ใต้เพดาน serverless 60s) ให้รูปที่ vision ช้ายังตอบทัน free token
      - "ห้ามเดา": รูปล้วนที่ OCR `confidence=LOW` → ข้าม search + ถามยืนยันสไตล์จูน (`buildJuneAskDetailsReply`, audit `AI_IMAGE_LOW_CONFIDENCE_ASK`); รูป LOW ไม่ feed fields เข้า inquiry frame (กัน frame เพี้ยน)
      - unit tests: ปรับ deadline-image test เป็น "ค้นต่อ ไม่ bail" + เพิ่ม low-confidence ask 1; processor suite 51 ผ่าน, coalesce 12 ผ่าน
    - **(4b)** 2026-06-23 — แก้เคส log จริง (ห้อง sunantha: พิมพ์ "อีซูซุเดก้า360" + ส่งรูปสายน้ำยาแอร์): ยืนยันจาก production `LineAiAuditLog` ว่า **OCR ทำงานถูก** (`IMAGE_CLASSIFIED kind=part_image confidence=HIGH`) และ search ทำงานแต่ `total=0` (ของไม่มีในสต็อกจริง). ปัญหาคือ branch `productSearch.total===0` ตก **FAQ** → ตอบ generic "ส่งรูปอะไหล่เดิมมาให้จูนช่วยเช็ก" ทั้งที่ลูกค้าเพิ่งส่งรูป → ดูเหมือน "ไม่ได้อ่านรูป". แก้:
      - เพิ่ม flag `partImageNoMatch` (รูป part_image, confidence≠LOW, search แล้ว total=0) → **ข้าม FAQ branch** + เพิ่ม forced-response branch ใหม่ใช้ `buildJunePartImageNoMatchReply()` (ใน `lib/line-ai-service.ts`) ที่ยอมรับว่าเห็นรูป[ชนิดอะไหล่] + บอกว่ายังไม่เจอในระบบ + handoff แอดมิน (audit `AI_PART_IMAGE_NO_MATCH`, set `waiting_admin`, notify bell+Telegram)
      - unit test: "part image recognized but search empty → acknowledges the part + hands off" (processor suite 52 ผ่าน)
    - **(4c)** 2026-06-23 (B1 — bound เวลาต่อ step กัน pipeline ทะลุ 60s): root ความช้าจาก log จริงคือ product turn ยิง Gemini chat call **เรียงต่อกันหลายครั้ง** (extract-intent → purchase-intent → faq/suggest) ช่วง Gemini latency spike แต่ละ call คืนที่ ~14s (ชิด timeout เดิม 15s) → ทบกันทะลุเพดาน serverless 60s → reply token หมด (PUSH ช้า) + coalesce lease หมด → job ถูกฆ่าเป็น `STALE_PROCESSING_TIMEOUT` (~126s). แก้: ลด `CHAT_CALL_TIMEOUT_MS` 15s→**8s** และ `CHAT_MAX_KEY_ATTEMPTS` 3→**2** ใน `lib/line-ai-service.ts` (กระทบ extract-intent/purchase-intent/faq/suggest/scoped-reply ทุกตัวที่ใช้ค่านี้)
      - 8s ยังพอสำหรับ latency ปกติ (~1–4s); เฉพาะช่วง spike เท่านั้นที่ call จะ abort → ตก **deterministic fallback สไตล์จูน** (สินค้า/การ์ดชุดเดิม เปลี่ยนแค่ถ้อยคำ) ที่ทันใน free reply-token window; spike กระทบทุก key พร้อมกัน → key ที่ 3 เดิมแค่เสียเวลา (2 พอสำหรับเคส 429/rotate)
      - **ไม่แตะ** vision OCR (`VISION_CALL_TIMEOUT_MS=20s/3` ใน `lib/line-image-service.ts`) — คนละ knob, การลดจะกระทบความแม่นของการอ่านรูป; เก็บไว้ประเมินแยก
      - tests: processor + faq + ai + coalesce 78 ผ่าน; ติดตามผลจริงด้วย `pipelineDurationMs` (AI_SEND_DECISION) + จำนวน `STALE_PROCESSING_TIMEOUT` 3–5 วัน
      - **(4d)** 2026-06-23 (B2a+B2b+B2c) — แก้ root ความช้า/งานซ้ำของ coalescing + เพิ่ม multi-subject (ยืนยันแผนกับเจ้าของร้านครบ):
        - **B2a — persist classification, ไม่ re-OCR:** ingest เก็บ classification เต็ม (kind/confidence/hints/partType/carBrand/carModel/year/partKind ฯลฯ ยกเว้น content/ocr) ลง job payload (`serializeClassificationForReuse`); repo `getStoredImageClassificationsByMessageRowIds()` ดึงกลับ; `buildMergedTurnInput` ใช้ in-memory cache → stored → ค่อย vision → owner re-run/cron recovery ไม่ re-OCR (vision รันครั้งเดียวต่อรูป) — ไม่แก้ schema
        - **B2b — bound owner loop, invariant OCR→search→reply:** `OWNER_LOOP_FINAL_PASS_AFTER_MS=40s` วัดจาก `receivedAt`; พ้นงบ → ข้าม debounce + force-send 1 ครั้ง (`shouldAbortBeforeSend`→false) ก่อนเพดาน 60s (กัน Vercel kill → กัน STALE 169s + cron-recovery 2 นาที); การันตีรัน OCR+search ครบก่อนเสมอ (fallback อิงผลจริง) + audit `AI_OWNER_BUDGET_FORCED_SEND`
        - **B2c — multi-subject split:** classifier (`extractLineSearchIntent`) คืน `subjects[]` เมื่อมี ≥2 ชนิดอะไหล่ต่างกัน (เกณฑ์ 1=ก: แยกตามหมวด ไม่แยกตามรุ่นรถ); `respondMultiSubject()` resolve แต่ละ subject → group ตามหมวด (distinct<2 → คืน single path), cap **3 หัวข้อ** (เกิน → N2 note), ค้น+บล็อกต่อหมวด, deterministic (ไม่ยิง AI gen ต่อหัวข้อ), pack reply ≤5 + push ส่วนเกิน; หัวข้อไม่เจอ → N1 line + notify แอดมิน **ไม่ freeze ห้อง** (เกณฑ์ จ); C1: คำว่า "แทน/เปลี่ยน" → เอาเฉพาะหัวข้อล่าสุด; กำกวม (back-ref/partType=null) ใช้ gate-ask เดิม; audit `AI_MULTI_SUBJECT`
        - เคสหัวข้อเดียว/รูปเดียวคงพฤติกรรมเดิมทุกอย่าง (รวม Option A handoff). tests: +B2a/B2b/B2c (coalesce) + subjects parser → line suite **187 ผ่าน**, build ผ่าน
      - **(4e)** 2026-06-24 (Bot DB-load guard — ดับ P2028 ที่ root) — หลัง deploy B2 ยังเจอ `P2028` ซ้ำที่ `/api/search/products/autocomplete` + webhook timeout 60s (ค้างที่ DB ช่วง ingest ก่อนถึง owner loop) ยืนยันจาก production = **DB pool หมดช่วง burst** (คนละตัวกับ B2; B2b ขัด DB call ที่ค้างกลางคันไม่ได้). สาเหตุ: bot/crawler ถล่ม autocomplete ทุก keystroke → ยิง V2 search ที่ใช้ `dbSearchTx` (transaction) แย่ง pool เล็ก (5/instance). แก้เฉพาะ path บอทใน `app/api/search/products/autocomplete/route.ts`: `isBot` → ข้าม V2/transaction ใช้ `db.product.findMany` แบบ LIKE-contains (name/code) ตรง ๆ (ถือ connection แค่ query เดียว) คืน shape เดิม; ลูกค้าจริง (UA ปกติ) ยังได้ V2 เต็ม. refactor: แยก `AUTOCOMPLETE_SELECT` + `toAutocompleteItem` ใช้ร่วม 2 path. ผล: ตัดตัวกิน pool หลัก → P2028 ลด/หาย + คืน connection ให้ LINE webhook. tsc/build ผ่าน. ตามด้วยเฝ้าดู P2028 + STALE หลัง deploy; Option B (ขยาย DB_POOL_MAX) ยังไม่ทำ รอประเมินจำนวน instance
      - **(4f)** 2026-07-10 (stale partType carryover — เคส "วาว์ลอัลติสแท้03" ตอบเป็นหม้อน้ำ) — ยืนยันจาก production `LineAiAuditLog`: เทิร์นก่อนหน้า `หม้อน้ำ commuter` ตั้ง frame `partType=หม้อน้ำ (Radiator)`. เทิร์นถัดมาลูกค้าเปลี่ยนรถ (Commuter→Altis) + พิมพ์อะไหล่ใหม่แต่สะกดเพี้ยน `วาว์ล` (=วาล์วแอร์) → คำนี้ตก `lineValueHasCustomerEvidence` → `groundedLatestPartType=null` → `reconcileInquiryFrame` สืบทอด `หม้อน้ำ` เดิม → categoryName=Radiator hard-filter → คืนหม้อน้ำ Altis 2 ชิ้น (classifier อ่านคำใหม่ถูก: consolidatedQuery="วาว์ลอัลติสแท้ 2003" ไม่ใช่หม้อน้ำ — ค่าเพี้ยนมาจาก frame carryover ล้วน ๆ). แก้:
        - **A** — `lib/chat-core/inquiry-frame.ts` `reconcileInquiryFrame`: เพิ่ม option `latestClassifierPartType`; เมื่อ **รุ่นรถเปลี่ยน** + classifier อ่าน partType ใหม่ที่ต่างจากที่ค้าง + `latest.partType` null → **ทิ้ง partType เก่า** (คืน `droppedStalePart`). Follow-up ที่พิมพ์แต่รถ ("แล้ว Vigo ล่ะ", classifier ไม่มี part ใหม่) ยังเก็บ part เดิม. wire ใน `lib/line-webhook-processor.ts` (ส่ง `classifierPartType`, log `droppedStalePartOnVehicleSwitch`+`classifierPartType` ใน INQUIRY_FRAME audit)
        - **B** — `lib/chat-core/fitment-resolve.ts`: เพิ่มคำสะกดเพี้ยน `วาว์ล/วาวล์/วาว์ลแอร์/วาวล์แอร์` → `(Expansion Valve)` (ปลอดภัย: ร้านไม่มีสินค้าเทอร์โมสตัท/วาล์วน้ำเลย — ตรวจ DB จริง `เทอร์โมสตัท`=0, `วาล์ว (Expansion Valve)`=44)
        - **อนาคต** — ถ้าเพิ่มสินค้าเทอร์โมสตัท/วาล์วน้ำ: สร้างหมวดใหม่ + เพิ่มแถว DB `CategoryAlias` (`วาล์วน้ำ`/`วาวน้ำ`/`เทอร์โมสตัท`/`thermostat`→หมวดใหม่ priority สูง) → DB alias ชนะ hardcoded อัตโนมัติ ไม่ต้องแก้โค้ด + ล้าง synonym ตาย `วาล์วน้ำ→เทอร์โมสตัท` (ชี้ 0 ชิ้น). ค่า default คำ "วาล์ว/วาว์ล" เปล่า = Expansion Valve (เจ้าของร้านยืนยัน)
        - **C (reply naming)** — `lib/chat-core/ai-service.ts` `buildLineReplyPrompt`: LLM เขียนข้อความเปิด "วาล์วน้ำของ Altis" ตามคำที่ลูกค้าพิมพ์ (เพี้ยน) ทั้งที่การ์ดเป็นวาล์วแอร์ → เพิ่มกฎ prompt: เมื่อมีรายการสินค้า ให้เรียกชื่อชนิดอะไหล่ **ตามรายการสินค้าที่พบ** เท่านั้น ห้ามเรียกตามคำลูกค้าที่อาจสะกดผิด/กำกวม
        - tests: inquiry-frame +3 (stale-part guard) → 22 ผ่าน; fitment-resolve +1 (สะกดเพี้ยน); processor suite 65 ผ่าน; ai-policy/deadline 11 ผ่าน; tsc ผ่าน
      - **(4g)** 2026-07-10 (unresolved-part → model-only drift — เคส "เช็ค เทอร์โมสตรัท Vios ปี2017" ตอบมั่ว) — ยืนยันจาก `LineAiAuditLog`: "เทอร์โมสตรัท" แมปหมวดไม่ได้ (categoryName null) + ไม่ใช่ required token (คำไทยไม่มีตัวเลข) → FTS ร่วงไปจับแค่รุ่น "Vios" → total 33, needsMoreInfo=false → โชว์อะไหล่ Vios 5 ตัวที่ไม่เกี่ยวเลย (คอมแอร์/โบลเวอร์/หม้อน้ำ/คอยล์เย็น). แก้ (Option A — anchor คำอะไหล่):
        - `lib/chat-core/product-search-bridge.ts`: เพิ่ม input `fitmentPartHeadNoun` — เมื่อมี + ไม่มี category filter → บังคับเป็น required token (contains-match ชื่อ/alias/fitment) และคงไว้ตลอด did-you-mean retry; **ต่างจาก `accessoryHeadNoun` คือไม่มี broaden-on-empty** → ว่างก็ว่าง (needsMoreInfo, reason `SEARCHED_FITMENT_PART_NO_MATCH`)
        - `lib/line-webhook-processor.ts`: คำนวณ `unresolvedFitmentPartHeadNoun` (no category + ไม่ใช่ accessory + guardedSearchIntent.partType + ไม่ใช่ catch-all "อะไหล่…") ส่งเข้า bridge
        - `lib/messenger/messenger-webhook-processor.ts`: `resolveMessengerFitmentHints` คืน `fitmentPartHeadNoun` ด้วย (parity LINE) แล้วส่งเข้า bridge
        - ผล: "เทอร์โมสตรัท Vios" → 0 → ตอบ "ยังไม่มีตัวนี้โดยตรง" ไม่โชว์การ์ดมั่ว; ของที่มีจริงในหมวด "อะไหล่อื่นๆ" (เทอร์โมเซ็นเซอร์แอร์) ยัง match ผ่าน contains; พาร์ทที่แมปหมวดได้ไม่กระทบ
        - tests: bridge +4 (anchor/no-broaden/category-ignore/retry) → 19 ผ่าน; processor 70 + search-guards ผ่าน; messenger (มี module-mock flag) 4 ผ่าน; tsc ผ่าน
        - **ต่อยอด — handoff + notify ฝั่งข้อความ (item 1):** เมื่อ reason=`SEARCHED_FITMENT_PART_NO_MATCH` (ลูกค้าระบุอะไหล่เฉพาะแล้วไม่เจอ) → ส่งต่อแอดมิน + notify (bell+Telegram) เหมือน no-match ฝั่งรูป. LINE `directNoMatchHandoff` OR เพิ่ม reason นี้; Messenger `shouldDirectNoMatchHandoff` early-return true (ครอบคำที่ไม่อยู่ใน `EXPLICIT_PRODUCT_NOUN_RE`). เปลี่ยนข้อความเป็นแบบระบุชื่ออะไหล่+รถ ผ่าน `buildJuneTextNoMatchHandoffReply(known)` ใหม่ ใน `lib/chat-core/ai-service.ts` (แทน generic `DIRECT_NO_MATCH_HANDOFF_MESSAGE` ที่ลบทิ้งทั้ง 2 processor); strip suffix "(English)" ของชื่อหมวด. tests: processor +1 (part ไม่มีรถ → handoff) → 66 ผ่าน, ปรับ assertion ข้อความ part-aware, messenger mock เพิ่ม export → 4 ผ่าน; tsc ผ่าน

  - [x] (20) AI Intent Classifier (hybrid routing — แทน regex จับกลุ่มคำที่เปราะ):
    - `lib/line-intent-groups.ts` (ใหม่): 12 กลุ่ม (`product`/`shop_info`/`general_faq`/`payment`/`shipping_address`/`order_status`/`price_negotiation`/`claim_or_return`/`purchase`/`greeting`/`social`/`other`) + `groupToRoute`/`intentToGroup`/`GUARD_GROUPS` (single source of truth)
    - `extractLineSearchIntent` → คืน `group` + product fields, ทำงาน **ทุกข้อความ** (รวมข้อความแรก, A=ข); ไม่มั่นใจ → `other`
    - processor: Layer-1 regex (guard payment/claim/price/purchase override + เมนู) → Layer-2 AI group → route ตามกลุ่ม (reuse forced-response/handoff/policy เดิม)
    - `general_faq`/`other` → ลอง FAQ ก่อน, ไม่เจอ → `buildJuneAskDetailsReply()` ถามต่อสไตล์จูน (ไม่ handoff/ไม่ dead-end)
    - `social` → `handleSocialTurn`: ack สั้น หรือนิ่งถ้าเป็น ack ปิดท้าย (กันวนซ้ำ)
    - safety: **ไม่มี flag เปิดใช้เลย**; classify ล่ม → fallback regex routing; รูปภาพไปทาง image path เดิม (text classifier เฉพาะข้อความ); เคารพ paused/waiting_admin (ไม่เด้งแทรกตอนแอดมินรับช่วง); audit `INTENT_CLASSIFIED {group, source, routedIntent}` + `SOCIAL_HANDLED`; classify timeout 15s
    - unit tests: groups mapping 6 + parse group 7 + processor (social/shop_info/paused/non-product/FAQ) + เดิมทั้งหมด

### 6. Image Egress Reduction — Vercel CDN Proxy (2026-06-11)
- บริบท: Supabase Free ใกล้ชน limit Egress (3.675/5GB) + Cached Egress (3.662/5GB, เข้า grace period ถึง 15 มิ.ย.) ขณะที่ Vercel Pro มี headroom เหลือมาก ($5.43/$20) → ย้ายการ serve รูปสินค้าไปผ่าน Vercel CDN
- หลักการ: client / crawler / next-image optimizer → `/img/<objectPath>` (Vercel CDN cache, immutable 1 ปี) → Supabase Storage (ดึงครั้งเดียวต่อ cache window) ไม่แตะข้อมูลใน DB
- [x] เพิ่ม `lib/product-image-url.ts` — pure URL helper (no `@supabase/supabase-js`, client-safe) + `toProductImageCdnPath()` (idempotent, no-op กับ url ที่ไม่ใช่ product bucket); `product-image-storage.ts` re-export ให้ importer เดิมไม่ต้องแก้
- [x] เพิ่ม route handler `app/img/[...path]/route.ts` — proxy public object จาก Supabase + `Cache-Control: public, max-age/s-maxage=31536000, immutable`; จำกัดเฉพาะ object path ใน bucket `products/` (กัน open proxy)
- [x] ยกเว้น `/img` จาก middleware matcher (`proxy.ts`) — ไม่ให้ auth()/bot-check รันต่อ request รูป
- [x] ชี้ `next/image` src ผ่าน `/img` ทุกจุดที่ render รูปสินค้า: `ProductCard`, `ProductImageGallery`, `ProductImageZoomLightbox`, `ProductAutocomplete`, admin `ProductImagePreview` / `ProductForm` (display เท่านั้น, formData/DB คงเป็น URL Supabase เดิม) / `products/search`
- [x] JSON-LD/ItemList ใช้ absolute CDN URL: `ProductJsonLd` (product detail), `products/page.tsx`, `products/[categorySlug]/page.tsx` → ตัด bot ดูดรูปจาก Supabase ตรง
- [x] 2026-06-12: ขยาย `/img` เป็น allowlisted public-storage proxy สำหรับ `products/settings/*`, `products/users/signatures/*`, `products/delivery-proofs/*`, และ bucket `line-chat/*` โดยยัง block path อื่นและ `payment-slips/*`
- [x] 2026-06-12: ชี้โลโก้ร้าน, LINE QR, ลายเซ็น, หลักฐานจัดส่ง, LINE chat image, icon/JSON-LD, และ LINE Flex placeholder ผ่าน `/img` แบบ render-time; ค่า URL ใน DB ยังเป็นค่าเดิม ไม่ต้อง migrate
- [x] 2026-06-12: payment slips ยังเป็น private signed URL และ render ด้วย native `<img>` แทน `next/image` เพื่อไม่นำข้อมูลส่วนตัวเข้า shared image optimizer/CDN cache
- [x] 2026-06-12: ลบ Supabase `images.remotePatterns` ออกจาก `next.config.ts` เพื่อบังคับไม่ให้ `next/image` ดึง Supabase ตรง; ยังเก็บ CSP `img-src` Supabase ไว้สำหรับ signed/private image และ fallback ที่ตั้งใจไว้
- [ ] (Ops) หลัง deploy: เฝ้าดู Supabase Egress/Cached Egress ควรลดลงชัดเจน + ตรวจ Vercel Image Optimization usage ไม่พุ่งเกินงบ
- [ ] (ทางเลือก, ยังไม่ทำ) migrate `Product.imageUrl`/`ProductImage.url` ใน DB ให้เป็น CDN path — ชะลอไว้เพราะกระทบ logic ownership ใน `product-image-storage.ts` และไม่จำเป็นเมื่อใช้ render-time helper แล้ว

### 7. Image Storage Migration — Supabase Storage → Vercel Blob (2026-06-23)
- บริบท/สาเหตุ: Supabase Free โดน restrict (Egress 7.16/5GB = 143%, Cached Egress 5.05/5GB = 101%) ขณะที่ Storage จริงมีแค่ 224MB → รูปถูกดึงซ้ำ ~22 เท่า. วินิจฉัยว่า `/img` route-handler cache เป็น per-deployment → deploy ถี่ (เกือบทุกวัน) + bot crawl ทำให้ Vercel CDN miss แล้ว re-pull จาก Supabase ซ้ำ. แก้ถาวรด้วยการย้าย object ไป Vercel Blob (public CDN อยู่ข้าม deploy, included ในโควต้า Pro)
- ขอบเขตที่ยืนยันแล้ว: ย้าย bucket `products` (public), `line-chat` (public), `payment-slips` (private/PII) — **ไม่ย้าย** `purchase-ocr` (ชั่วคราว, ไม่กระทบ egress, คงไว้บน Supabase → ยังลบ `@supabase/supabase-js` ทั้งหมดไม่ได้). ลำดับ: public ก่อน (หยุดดูผล egress) แล้วค่อย payment-slips
- การตัดสินใจที่อนุมัติแล้ว: payment-slips (private) เปลี่ยนจาก Supabase signed-URL → route สตรีมที่ตรวจ session admin (รออนุมัติดีไซน์ละเอียดก่อน Phase 3)
- [x] **Phase 0 — เตรียม (additive, ไม่เปลี่ยนพฤติกรรม)**: เพิ่ม dep `@vercel/blob@2.4.1`; เพิ่ม env `BLOB_READ_WRITE_TOKEN` + flag ต่อ bucket `IMAGE_STORAGE_{PRODUCTS,LINE_CHAT,PAYMENT_SLIPS}` ใน `.env.example`; เพิ่ม `**.public.blob.vercel-storage.com` ใน `next.config.ts` (`images.remotePatterns` + CSP `img-src`); เพิ่ม `lib/storage-backend.ts` (`getStorageBackend`/`isBlobBackend`, default `supabase`)
  - [x] (งานเจ้าของร้าน) สร้าง Blob store ใน Vercel Dashboard + ตั้ง `BLOB_READ_WRITE_TOKEN` ใน Vercel env — เสร็จแล้ว
- [x] **Phase 1 — Public dual-write** (2026-06-23): เพิ่ม facade `lib/products-bucket-storage.ts` (`uploadProductsBucketObject`, เลือก backend ตาม `IMAGE_STORAGE_PRODUCTS`); wire 4 จุดอัปโหลด bucket `products` → product image (`products/actions.ts`), โลโก้ (`settings/company/actions.ts`), ลายเซ็น (`users/actions.ts`), delivery-proof (`sales/actions.ts`); เพิ่ม Blob branch ใน `line-chat-image-storage.ts` (flag `IMAGE_STORAGE_LINE_CHAT`). รูปเก่ายังโหลดผ่าน `/img` ได้ (render helper ปล่อย Blob URL ผ่านอยู่แล้ว + remotePatterns รับ host). Default flag = supabase → พฤติกรรมเดิมไม่เปลี่ยนจนกว่าจะ flip. tsc/lint ผ่าน
  - ข้อจำกัดที่ยอมรับไว้ (จัดการใน Phase 4): เมื่อ flag=blob, copy-to-code-folder เดิม (Supabase) จะ no-op กับ Blob URL (รูปค้างใต้ folder uploadCode เดิม ใช้งานได้ปกติ) และรูปสินค้า Blob ที่ถูกแทนที่จะค้างเป็น orphan รอ sweep ใน Phase 4
- [x] **Phase 2 — Backfill public — DONE 2026-06-25** (รัน apply หลัง egress reset: migrated 2,375 + เก็บตก stragglers 3 = 2,378, เหลือ 0; flip `IMAGE_STORAGE_PRODUCTS`+`IMAGE_STORAGE_LINE_CHAT=blob` ใน Vercel แล้ว → รูปสาธารณะ serve จาก Blob หมด): `prisma/scripts/backfill-public-images-to-blob.ts` (`npm run backfill:public-images-blob [-- --apply]`) — download จาก Supabase → `put` ขึ้น Blob path เดิม → rewrite URL ใน DB (Product.imageUrl, ProductImage.url, SiteContent shop_logo_url, User.signatureUrl, DeliveryProof.signature/photo, LineMessage.imageUrl); idempotent (ข้าม Blob URL แล้ว), ไม่ลบ Supabase, dump rollback JSON (gitignored), เขียน AuditLog UPDATE/ImageStorageMigration. **dry-run พบ 2,375 แถว (products 2,373 + line-chat 2)**
  - download ใช้ `fetch(publicUrl)` ตรง (bucket เป็น public) → **ไม่ต้องใช้ `SUPABASE_SERVICE_ROLE_KEY`**, ต้องการแค่ `BLOB_READ_WRITE_TOKEN` (มีใน `.env.local` แล้ว)
  - [x] รันสำเร็จ 2026-06-25 หลัง egress reset (download `fetch(publicUrl)` ไม่ต้องใช้ service key) → เฝ้าดู egress ตก
- [x] **Phase 3 — Payment slips (private/PII)** — **เสร็จและเปิดใช้งานจริงแล้ว** (code + backfill 9 สลิป + ตั้ง token + flip `IMAGE_STORAGE_PAYMENT_SLIPS=blob`):
  - **บทเรียน: Blob store มี 2 ชนิด public/private ระดับ store** — public store (รูปสินค้า) เก็บ private blob ไม่ได้ → ต้องสร้าง **private store แยก** + token `BLOB_SLIPS_READ_WRITE_TOKEN` (wire เข้า put/get/del + backfill ผ่าน option `token`). รูปสินค้ายังใช้ default `BLOB_READ_WRITE_TOKEN` (public store)
  - backfill `backfill-payment-slips-to-blob.ts` รัน apply → migrated 9/9, idempotent (รันซ้ำ skip 9). ไม่ rewrite DB (imageUrl เก็บ path เดิม)
  - [x] (เจ้าของ) ตั้ง `BLOB_SLIPS_READ_WRITE_TOKEN` ใน Vercel env → push โค้ด slip-token → flip `IMAGE_STORAGE_PAYMENT_SLIPS=blob` — เสร็จแล้ว
  - storage `lib/line-payment-slip-storage.ts`: `storePaymentSlipImage` เพิ่ม Blob `put(access:'private')`; `deletePaymentSlipImage` ลบทั้ง Blob+Supabase (mixed-state); เพิ่ม `getPaymentSlipDisplayUrl` (route URL เมื่อ blob, signed URL เมื่อ supabase) + `readPaymentSlipImage` (Blob get → **fallback Supabase** ให้ slip ที่ยังไม่ backfill ใช้ได้หลัง flip)
  - route ใหม่ `app/api/admin/line-payment-slips/[id]/image/route.ts` (GET) — `requirePermission("line_payment_slips.view")` → stream, `Cache-Control: private, max-age=300`
  - display backend-aware: detail page `[id]/page.tsx` + gallery `line-payment-slip-gallery.ts` (blob → route URL, ข้าม signed-URL batch)
  - backfill `prisma/scripts/backfill-payment-slips-to-blob.ts` (`npm run backfill:payment-slips-blob`) — copy bytes Supabase→Blob private ที่ path เดิม, **ไม่ rewrite DB** (imageUrl เก็บ path), idempotent, AuditLog. dry-run พบ **9 slips**
  - security review: auth = view permission เดิม / by-id ไม่มี path injection / IDOR เท่าหน้าเดิม / Blob private + `private` cache (ไม่เข้า shared CDN) / token server-only / same-origin (CSP 'self')
  - [x] รัน apply backfill + flip flag เสร็จแล้ว: `npm run backfill:payment-slips-blob -- --apply` → `IMAGE_STORAGE_PAYMENT_SLIPS=blob`
- [x] **Phase 4 — Cleanup — DONE 2026-07-14**:
  - [x] ลบโค้ด Supabase storage เฉพาะ 3 bucket + เอา flag `IMAGE_STORAGE_*` / `lib/storage-backend.ts` / route `/img` ออก — storage libs เป็น Blob-only แล้ว; ผู้ใช้ Supabase Storage รายเดียวที่เหลือคือ `lib/purchase-invoice-storage.ts` (bucket `purchase-ocr-temp` — คงไว้ตามแผน)
  - [x] pre-fix ก่อนลบ (2026-07-14): preflight gate พบ backfill 25 มิ.ย. ไม่ได้ rewrite 3 คอลัมน์ — `Sale.signerSignatureUrl` (30 แถว) + `Receipt.signerSignatureUrl` (18 แถว) ชี้ไฟล์ลายเซ็นเดียวกันซึ่งมีบน Blob แล้ว → สลับ URL ด้วย `prisma/scripts/fix-dangling-supabase-refs.ts` (HEAD 200 gate + AuditLog); `PaymentSlip.imageSignedUrl` cache signed URL หมดอายุ 3 แถว (โค้ดเลิกใช้คอลัมน์นี้แล้ว) → NULL
  - [x] ลบ objects เก่า 3 bucket (2026-07-14): **2,515 ไฟล์ / 417.1MB** (products 2,502 / line-chat 4 / payment-slips 9) ด้วย `prisma/scripts/delete-supabase-storage-objects.ts` (`npm run cleanup:supabase-storage`) — dry-run default, preflight gate ในตัว (สแกนทุกคอลัมน์ text หา Supabase URL + เช็ค slip ทุกใบบน private Blob), manifest JSON gitignored, batch ละ 100, AuditLog, guard ห้ามแตะ `purchase-ocr`; **ตัว bucket คงไว้ ลบเฉพาะ objects**
  - cross-check ก่อนลบ: ทุก object เข้าเงื่อนไข "มีบน Blob แล้ว (1,839)" หรือ "orphan ก่อน migration ที่ไม่มี DB row อ้างถึง (676)" — referenced-but-missing-from-Blob = **0**; post-verify: `storage.objects` เหลือ 0 ใน 3 bucket, `purchase-ocr-temp` ไม่ถูกแตะ (ว่างเองอยู่แล้ว), spot-check HEAD รูปสินค้า+ลายเซ็น = 200

### 8. Database Egress Reduction — Supabase Pro (2026-07-14)
- บริบท: egress รอบบิล 6.44GB (~320MB/วัน ≈ 10GB/เดือน) เกินธงภายใน 5GB/เดือน — วินิจฉัยจาก `pg_stat_statements` + `pg_stat_activity` + `storage.objects`: ไม่มี replication/backup รั่ว, ตัวการคือ query ที่ดึง "ทั้ง catalog / ทั้งตาราง" ซ้ำบ่อย (Product ~64k แถว/วัน ที่แถวกว้าง ~4.9KB, ProductAlias ~157k แถว/วัน, StockCard ~87k แถว/วัน)
- [x] `lib/transaction-options.ts` — ลด egress ของ product-option superset: truncate `description` เหลือ 300 ตัวอักษรแรก (ใช้ค้นใน dropdown เท่านั้น ไม่เคยถูกบันทึก/แสดง), aliasSearchText เปลี่ยนเป็น `string_agg(DISTINCT lower(alias))` ฝั่ง SQL (902 แถวแทน 36k แถว — ยืนยัน membership ตรงกับ logic JS เดิม 100%), revalidate safety-net 300s → 86,400s (tag invalidation ยังเป็น path หลัก), bump cache key เป็น `-v3`; **เงื่อนไขแสดงสินค้าทุกตัว active+inactive คงเดิม**
- [x] `lib/stock-card-latest-balance.ts` (ใหม่) — `getLatestStockBalances()` ใช้ SQL `DISTINCT ON ("productId") … ORDER BY "productId","docDate" DESC,sorder DESC` แทน Prisma `distinct` (ซึ่ง dedupe ฝั่งแอป = ลาก StockCard ทั้งประวัติมาทุกครั้ง); ใช้ค่า 4 ตำแหน่งจาก ledger เหมือนเดิมทุกไบต์ (ยืนยันเทียบ 887 สินค้า mismatch=0) — เปลี่ยน call site ใน `lib/product-report-queries.ts` + `lib/ar-ap-stock-report-queries.ts`
- [x] sitemap revalidate 900s → 21,600s (6 ชม.) ทั้ง `app/sitemap.ts` + `lib/storefront-sitemap.ts` (bot crawl ทำให้ refetch สินค้าทุกตัวได้ ~96 รอบ/วัน)
- [ ] (Ops) เฝ้าดูกราฟ egress 3–5 วันหลัง deploy — คาดลดจาก ~320MB/วัน เหลือระดับ ~150MB/วัน (<5GB/เดือน)
- หมายเหตุที่พบระหว่างตรวจ: bucket `products` เก่าบน Supabase ยังมี 2,502 ไฟล์ / 417MB (public) แม้ DB ชี้ Blob ครบแล้ว — เป็นงาน Phase 4 Cleanup ของ ข้อ 7 อยู่แล้ว
- [x] (follow-up 2026-07-14) พบ+แก้ภาษาไทยเพี้ยน (cp1252 double-encode) 5 ไฟล์: `ar-ap-stock-report-queries.ts` (หัวตาราง export AR/AP เพี้ยนตั้งแต่ 16 เม.ย.), cash-bank report 2 หน้า, `CommandPalette.tsx`, `AGENTS.md` — กู้คืนด้วย decode ย้อน ยืนยันตรงกับ git ก่อนเพี้ยน + เพิ่ม guard กันเกิดซ้ำ: `scripts/check-mojibake.mjs` + pre-commit hook (`.githooks/`, เปิดใช้อัตโนมัติผ่าน postinstall → `scripts/setup-git-hooks.mjs`), สแกนมือได้ด้วย `npm run check:mojibake`

### 9. Facebook Messenger AI Agent (2026-07-04)
- สถานะ: **Phase B–E เสร็จครบ** (B: สมองกลาง `lib/chat-core/`; C: 4 model; D: webhook + Send API + processor เต็ม text/รูป/สลิป/coalescing/24h/cron; E: admin inbox + permission 5 ขั้น + audit กลาง + notification bell+Telegram + handoff escalation — reuse chat-core ไม่แตะ LINE) · เหลือ **Phase A (Meta App Review — งานเจ้าของร้าน)** + **Phase F (rollout/QA)** — checklist เต็มดู [PLAN-MESSENGER.md](/D:/autoparts/PLAN-MESSENGER.md)
- เป้าหมาย: AI ตอบ DM Facebook ใช้สมอง + logic ค้นหาชุดเดียวกับ LINE OA (แก้ที่เดียว) — ตารางแยกใหม่ `MessengerConversation`, parity เต็มเท่า LINE, webhook `www.sriwanparts.com/api/messenger/webhook`
- หลักการ: extract `lib/chat-core/` (channel-agnostic) แบบ clean repo-wide ไม่ใช้ shim → LINE ต้องทำงานเหมือนเดิม 100% (build เขียว + test เดิมผ่านทุกตัว)

## Source Of Truth Map
### Product and Inventory
- Stock movement + MAVG: `lib/stock-card.ts`
- Document numbers: `lib/doc-number.ts`
- VAT calculations: `lib/vat.ts`
- Prisma schema: `prisma/schema.prisma`
- Transaction item display snapshots: [docs/decisions/document-item-display-snapshot.md](/D:/autoparts/docs/decisions/document-item-display-snapshot.md)

### Admin Rules
- Cross-module delivery/print/theme/search rules: [AGENTS.md](/D:/autoparts/AGENTS.md)

### Planning Docs
- Architecture overview: [docs/architecture.md](/D:/autoparts/docs/architecture.md)
- Active roadmap: [docs/roadmap/active.md](/D:/autoparts/docs/roadmap/active.md)
- Completed roadmap: [docs/roadmap/completed.md](/D:/autoparts/docs/roadmap/completed.md)
- Decision log index: [docs/decisions/README.md](/D:/autoparts/docs/decisions/README.md)
- Spec index: [docs/specs/README.md](/D:/autoparts/docs/specs/README.md)

## How To Use This Repo As AI
1. อ่าน [AGENTS.md](/D:/autoparts/AGENTS.md) ก่อนเสมอ
2. อ่านไฟล์นี้เพื่อดู current focus และ source of truth
3. เปิด [docs/architecture.md](/D:/autoparts/docs/architecture.md) ถ้าต้องเข้าใจภาพรวมระบบ
4. ถ้างานยัง active ให้ดู [docs/roadmap/active.md](/D:/autoparts/docs/roadmap/active.md)
5. ถ้าต้องตัดสินใจเชิงธุรกิจหรือ implementation rule ให้ดู `docs/decisions/`
6. ถ้าต้องลงลึกเป็นโมดูล ให้ดู `docs/specs/`

## Archive
- Master plan เดิมแบบละเอียดมาก: [docs/archive/PLAN-legacy-2026-05-21.md](/D:/autoparts/docs/archive/PLAN-legacy-2026-05-21.md)
- ห้ามใช้ archive เป็น entrypoint หลัก ยกเว้นต้องตาม historical detail
